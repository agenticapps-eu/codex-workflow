# AGENTS.md

This file is intentionally minimal during Phase 0.

It is populated in Phase 6 when `codex-workflow` self-applies the
`agentic-apps-workflow` trigger skill to itself. At that point the setup
skill appends:

- Development workflow section
- Workflow enforcement hooks (gate-to-skill bindings)
- Skill routing rules
- Session handoff conventions

Until then, this file holds only the placeholder so the repo's
`AGENTS.md` exists at the expected root path for Codex's
root-down concat behavior.
