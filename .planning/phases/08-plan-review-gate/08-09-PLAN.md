---
phase: 08-plan-review-gate
plan: 09
type: execute
wave: 3
gap_closure: true
depends_on: ["08-07", "08-08"]
files_modified:
  - docs/decisions/0009-plan-review-gate.md
  - .planning/config.codex.json
  - skills/setup-codex-agenticapps-workflow/templates/config-hooks.json
autonomous: true
requirements:
  - "core spec §02 (plan-review gate) — the declarative binding must describe the condition that actually drives the block"
  - "core spec §09 (conformance) — the ADR is this host's conformance record for the gate"
user_setup: []

must_haves:
  truths:
    - "ADR-0009 records that decision 11's stated contract — 'the >=5-line fallback is reachable only when frontmatter is entirely absent' — is now actually enforced rather than aspirational, and names CR-01 as the defect that made it aspirational"
    - "ADR-0009 records the D-15 codex exclusion (WR-01), the exclusion-vs-allowlist reasoning, and the cross-host [codex, gemini] residual"
    - "ADR-0009 records WR-02's correction and that it was reproduced, upgrading the review's code-inspection-only status"
    - "ADR-0009 records WR-03 (the --file traversal guard covers lexical '..' only, not symlinked directory components) as an explicit, accepted, documented limitation — the way decision 11 documents the fallback's spoofability"
    - "The declarative fires_when text in BOTH .planning/config.codex.json and the fresh-install template names the REVIEWS.md-evidence condition that actually drives the block (IN-01)"
    - "The two config files stay byte-identical in the plan_review block, as test_migration_0008 and the phase's own verification both assert"
  artifacts:
    - path: "docs/decisions/0009-plan-review-gate.md"
      provides: "The gap-closure record: what CR-01/WR-01/WR-02 changed, and WR-03 accepted explicitly"
      contains: "D-15"
    - path: ".planning/config.codex.json"
      provides: "pre_execution.plan_review binding with an accurate fires_when"
      contains: "REVIEWS.md"
    - path: "skills/setup-codex-agenticapps-workflow/templates/config-hooks.json"
      provides: "Same binding, fresh-install path"
      contains: "REVIEWS.md"
  key_links:
    - from: ".planning/config.codex.json"
      to: "skills/setup-codex-agenticapps-workflow/templates/config-hooks.json"
      via: "identical plan_review block — migrated == fresh"
      pattern: "plan_review"
---

<objective>
Record in ADR-0009 what the gap-closure fixes changed, and make the two explicit
calls the gap brief asks for on WR-03 and IN-01.

Purpose: 08-VERIFICATION.md's human-verification item asks for "a deliberate
decision recorded in the codebase (either a code fix + tests, or an ADR
addendum) — not silence, since silence is what let CR-01 sit unaddressed between
the code review and this verification pass." Plans 08-07 and 08-08 supply the
code fixes; this plan supplies the record, and closes the loop on the two
findings that are NOT getting code fixes.

Output: an ADR whose decision 11 describes enforced behavior rather than
aspirational behavior, and a declarative binding whose description matches what
the verifier does.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-plan-review-gate/08-VERIFICATION.md
@.planning/phases/08-plan-review-gate/08-REVIEW.md
@docs/decisions/0009-plan-review-gate.md
@.planning/phases/08-plan-review-gate/08-07-SUMMARY.md
@.planning/phases/08-plan-review-gate/08-08-SUMMARY.md

<interfaces>
<!-- Extracted during planning. -->

`docs/decisions/0009-plan-review-gate.md` structure:
- `## Decision` — 11 numbered decisions. Decision 4 covers REVIEWS.md schema +
  verifier strictness (D-12/D-13/D-14). Decision 11 (lines 276-285) states the
  fallback contract this gap-closure makes true.
- `## Consequences`, `## Verification`, `## Open follow-ups`.
- Decision 11's load-bearing sentence, verbatim: "It is reachable only when
  frontmatter is entirely absent: malformed frontmatter blocks instead, and a
  present, well-formed frontmatter is authoritative."
- Status line reads `**Status**: Accepted  **Date**: 2026-07-15`.

The `fires_when` text to correct (IN-01), currently identical in both files at
line 10:
`"fires_when": "phase has >=1 *-PLAN.md AND no *-SUMMARY.md exists"`
The review's suggested replacement names the additional REVIEWS.md condition.
Both files MUST stay identical in the `plan_review` block — the phase's own
verification asserts they match "byte-for-byte in the fields that matter", and
`test_migration_0008` compares template-extracted content.

`.planning/config.codex.json`'s own `implements_spec` reads "0.1.0". That is
PRE-EXISTING drift, explicitly out of scope (D-17, 08-CONTEXT.md canonical
refs). Do NOT touch it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the gap-closure fixes and the WR-03 acceptance in ADR-0009</name>
  <files>docs/decisions/0009-plan-review-gate.md</files>
  <action>
    Read `08-07-SUMMARY.md` and `08-08-SUMMARY.md` first and record what was
    ACTUALLY done, not what this plan predicted. If an executor diverged, the
    ADR follows the code.

    Amend **decision 11** so it states that its own contract is now enforced
    rather than aspirational. The honest framing: the sentence "reachable only
    when frontmatter is entirely absent... a present, well-formed frontmatter is
    authoritative" was written as a description of intended behavior, but the
    byte-exact `---` comparison at `check-plan-review.sh:539` meant a REVIEWS.md
    with a trailing space or CRLF on line 1 had present, well-formed frontmatter
    and was NOT treated as authoritative — it silently collapsed to the fallback,
    which has no reviewer check at all. Found by this phase's own code review
    (CR-01), independently re-reproduced by verification, and reproduced a third
    time during gap-closure planning. Fixed in plan 08-07 by normalizing the
    opening delimiter and mirroring that normalization onto the closing-delimiter
    search. Say plainly that the fallback ITSELF is unchanged and still
    deliberately spoofable — narrowing when it is reachable was the fix; D-13
    keeps it for the hand-written cross-host case.

    Also record under decision 11 (or its own sub-point) the closing-delimiter
    behavior change 08-07 introduced: a closing `--- ` with a trailing space was
    previously caught as MALFORMED and blocked; it is now accepted and the file
    is parsed strictly. That is the intended consequence of making open and
    close agree.

    Amend **decision 4** (verifier strictness) to record the D-15 exclusion:
    the strict path enforced COUNT but not IDENTITY, so `reviewers: [codex,
    codex-self]` — two distinct strings, zero genuine external reviewers, and
    the exact self-review D-15 names as forbidden — passed at exit 0 (WR-01,
    reproduced live twice). Codex-derived entries are now excluded from the
    count. Record the reasoning for exclusion over a hard-coded allowlist:
    an allowlist silently false-blocks a legitimate future vendor and a
    cross-host REVIEWS.md naming an unfamiliar reviewer (the ADR-0007 point 5
    case), which is what D-13 warns against; and it buys nothing against a
    determined spoofer, who has `touch multi-ai-review-skipped` available and
    already accepted under decisions 10/11. Exclusion closes the honest mistake
    D-15 actually names — counting the implementing host as an external
    reviewer — and nothing more.

    Record the **residual** 08-07 introduces, without hedging: a REVIEWS.md
    produced on another host that legitimately used codex as an external
    reviewer (e.g. `[codex, gemini]` written by claude-workflow in a shared
    tree) now blocks on this host where it previously passed. This is D-15
    applied as written — from the codex host's vantage, codex is always
    self-review — and both escape hatches remain. Note that this repo's own
    `08-REVIEWS.md` (`[gemini, codex, opencode]`) survives with exactly 2
    external reviewers, zero margin, and that `test_check_plan_review_contract`
    pins it.

    Record **WR-02** (migration 0008 Step 3's unscoped table edit, fixed in plan
    08-08) under decision 5, the existing-install migration story. Note it was
    code-inspection-only in 08-REVIEW.md and was REPRODUCED during gap-closure
    planning, so it is a confirmed defect rather than a suspicion. Note the
    self-sealing property that made it worth fixing rather than accepting: the
    Step 3 idempotency check would find the misplaced row and mark the step
    applied forever, permanently masking a bindings table that never received
    the plan-review row.

    Record **WR-03** as an explicit, accepted, documented limitation — this is
    the call this plan makes and must not leave silent. The `--file` bypass's
    traversal guard rejects lexical `..` components but not symlinked directory
    components, so a pre-existing symlink inside `.planning/phases/<phase>/`
    pointing outside the tree lets a textually-legitimate-looking `--file` value
    fire the bypass. Deferred rather than fixed, and say why: the whole gate is
    agent-mediated advisory text (decision 9), so an agent able to pass a
    crafted `--file` is already able to skip the verifier entirely — the guard
    is a hygiene check against accidental over-broad bypass, not a security
    boundary against a hostile caller. Canonicalize-and-contain is not available
    here because `--file` may name a file that does not exist yet (that is why
    `_canon_dir` is deliberately unused at that site). Mirror decision 11's
    treatment: a limitation recorded is a limitation a later reader can act on;
    a limitation only the planner knew about is a trap. Note that the script's
    own comment block already scopes its claim to `..` — the ADR now agrees with
    the code. Add it to `## Open follow-ups` as the concrete fix a future phase
    can pick up: reject any `--file` value with a symlinked existing prefix
    component.

    Update `## Verification` to note the enforcement suite now covers delimiter
    tolerance and the D-15 exclusion, and that `test_migration_0008` covers the
    unrelated-preceding-table case.

    Keep the bootstrap paradox (decision 8) intact and do NOT weaken it — no
    dogfood was manufactured; the fixtures remain the only real coverage.
    Do not touch decision 6 (`implements_spec` stays 0.4.0).
  </action>
  <verify>
    <automated>grep -c "D-15\|WR-03\|CR-01" docs/decisions/0009-plan-review-gate.md</automated>
  </verify>
  <done>
    ADR-0009 records: CR-01's fix under decision 11 with the fallback explicitly
    unchanged; WR-01's fix and the exclusion-vs-allowlist reasoning and the
    cross-host residual under decision 4; WR-02 under decision 5 as reproduced;
    WR-03 as an accepted documented limitation plus an Open follow-up entry.
    Decisions 6 and 8 are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: IN-01 — make fires_when describe the condition that actually blocks</name>
  <files>.planning/config.codex.json, skills/setup-codex-agenticapps-workflow/templates/config-hooks.json</files>
  <action>
    In BOTH files, extend the `pre_execution.plan_review.fires_when` string so it
    names the REVIEWS.md-evidence condition. The current text describes when the
    verifier REACHES the evidence check, not when it blocks — a reader scanning
    the declarative config without reading the verifier would conclude the gate
    fires on every plans-without-summary phase, when a phase with a valid
    REVIEWS.md passes silently. Follow 08-REVIEW.md IN-01's suggested wording:
    the existing condition AND no valid `<NN>-REVIEWS.md` (>=2 distinct
    reviewers, full plans_reviewed coverage) exists.

    Given 08-07's D-15 change, say "distinct EXTERNAL reviewers" (or equivalent
    wording that reflects the codex exclusion) rather than restating the old
    count-only rule — the description should match the shipped verifier, which
    is the entire point of the finding.

    The two `plan_review` blocks MUST remain identical. Edit both in the same
    task and diff them afterward; the phase's verification asserts they match,
    and migrated-must-equal-fresh is the D-19 invariant.

    Do NOT touch `implements_spec` in `.planning/config.codex.json` — its
    "0.1.0" value is pre-existing drift, explicitly out of scope (D-17).
  </action>
  <verify>
    <automated>diff <(jq -S '.hooks.pre_execution.plan_review' .planning/config.codex.json) <(jq -S '.hooks.pre_execution.plan_review' skills/setup-codex-agenticapps-workflow/templates/config-hooks.json) && echo "IDENTICAL"</automated>
  </verify>
  <done>
    Both files' `fires_when` names the REVIEWS.md-evidence condition; `jq`
    parses both; the two `plan_review` blocks are identical; `implements_spec`
    is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR / declarative config → a later reader (human or agent) | Documentation is the only surface describing what the gate enforces; an inaccurate description is acted on as if true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-46 | Repudiation | ADR-0009 decisions 4 + 11 | mitigate | Record CR-01/WR-01/WR-02 outcomes so a later reader is not misled into believing a contract holds that once did not — the exact "silence is what let CR-01 sit unaddressed" failure 08-VERIFICATION.md names. |
| T-08-47 | Information Disclosure | `fires_when` description | mitigate | Correct the text so an agent reading only the declarative binding does not conclude the gate blocks unconditionally on plans-without-summary (IN-01). |
| T-08-48 | Elevation of Privilege | `--file` symlinked-component bypass (WR-03) | accept | Documented explicitly in ADR-0009 + carried as an Open follow-up. Bounded: the gate is agent-mediated (decision 9), so a caller able to craft `--file` can already skip the verifier entirely; the guard is hygiene, not a boundary against a hostile caller. |
</threat_model>

<verification>
- `jq` parses both config files; their `plan_review` blocks are identical.
- ADR-0009 names CR-01, WR-01 (with the exclusion-vs-allowlist reasoning and
  the cross-host residual), WR-02 (as reproduced), and WR-03 (as accepted, with
  an Open follow-up).
- Decision 8 (bootstrap paradox) and decision 6 (`implements_spec` 0.4.0) are
  unchanged.
- `bash migrations/run-tests.sh` still 0 FAIL (config drift tests read these
  files).
</verification>

<success_criteria>
Every finding from 08-REVIEW.md has an explicit disposition recorded in the
codebase: CR-01, WR-01, WR-02 fixed and recorded; WR-03 accepted and documented;
IN-01 corrected. No finding is left silent — silence is the failure
08-VERIFICATION.md called out by name.
</success_criteria>

<output>
Create `.planning/phases/08-plan-review-gate/08-09-SUMMARY.md` when done.
</output>
</content>
