---
phase: 08-plan-review-gate
plan: 05
type: tdd
wave: 4
depends_on: ["08-02", "08-04"]
files_modified:
  - migrations/0008-plan-review-gate.md
  - migrations/run-tests.sh
  - skills/agentic-apps-workflow/SKILL.md
  - .codex/workflow-version.txt
  - CHANGELOG.md
autonomous: true
requirements:
  - "core spec §02 (plan-review gate) — existing-install upgrade path"
  - "core spec §09 (conformance) — migrated installs reach the same bound state as fresh ones"
user_setup: []

must_haves:
  truths:
    - "migrations/run-tests.sh passes, including a test_migration_0008 that is a no-op on second run (D-19)"
    - "An existing install at 0.5.0 gains the pre_execution binding and the ritual section without losing existing config keys — a jq merge that skips keys already present (D-19)"
    - "The AGENTS.md section is extracted from the template, never heredoc'd (macOS/BSD awk constraint) (D-19)"
    - "implements_spec stays 0.4.0 while the scaffolder version moves 0.5.0 -> 0.6.0 (D-17)"
  artifacts:
    - path: "migrations/0008-plan-review-gate.md"
      provides: "Idempotent upgrade path for existing installs; 0.5.0 -> 0.6.0"
      contains: "from_version: 0.5.0"
    - path: "migrations/run-tests.sh"
      provides: "test_migration_0008 + repo-layout guards for this phase's new artifacts"
      contains: "test_migration_0008"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry recording the gate, the 16-distinct-gate bindings table (D-20), and the implements_spec non-bump rationale (D-17)"
      contains: "plan-review"
  key_links:
    - from: "migrations/0008-plan-review-gate.md"
      to: "skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md"
      via: "awk section extraction at the installed template path"
      pattern: "agents-md-additions.md"
    - from: "migrations/0008-plan-review-gate.md"
      to: ".planning/config.codex.json"
      via: "jq merge into .hooks, preserving existing keys"
      pattern: "config.codex.json"
    - from: "skills/agentic-apps-workflow/SKILL.md"
      to: "migrations/0008-plan-review-gate.md"
      via: "drift coupling — version must equal latest migration to_version"
      pattern: "version: 0.6.0"
---

<objective>
Ship the upgrade path for existing installs, and close the phase.

Plan `08-04` made **fresh** installs conformant by construction — the template
carries the ritual section and the `pre_execution` block. An install already at
0.5.0 gets neither. Migration 0008 is what reaches the fleet via
`$update-codex-agenticapps-workflow`.

Purpose: `run-tests.sh` green with a `test_migration_0008` that is a no-op on
second run is ROADMAP success criterion #7 — the phase's own definition of done.

Output: migration 0008, its test, layout guards for every artifact this phase
added, the version bump in lockstep, and the CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-plan-review-gate/08-CONTEXT.md
@.planning/phases/08-plan-review-gate/08-PATTERNS.md
@.planning/phases/08-plan-review-gate/08-02-SUMMARY.md
@.planning/phases/08-plan-review-gate/08-03-SUMMARY.md
@.planning/phases/08-plan-review-gate/08-04-SUMMARY.md
@migrations/0007-knowledge-capture.md
@migrations/run-tests.sh
@CHANGELOG.md
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring. -->

**Version arithmetic — verified, do not re-derive:**

  skills/agentic-apps-workflow/SKILL.md   version: 0.5.0    implements_spec: 0.4.0
  .codex/workflow-version.txt             0.5.0
  migrations/0007-knowledge-capture.md    from 0.4.0 -> to 0.5.0   (latest today)

So 0008 is `from_version: 0.5.0`, `to_version: 0.6.0`. The drift test
(`run-tests.sh drift`, policy: mismatch is a hard FAIL) requires
`SKILL.md version` == latest migration `to_version` — so SKILL.md must move to
0.6.0 **in this plan**, in lockstep. Plan `08-04` deliberately left it at 0.5.0
so the drift test stayed green in its own wave.

`implements_spec` stays `0.4.0` (D-17). 0007 carries the same explicit note:
"implements_spec is unchanged — do NOT touch it."

**Migration 0008 frontmatter shape** — mirror 0007's (its lines 1-14):

  ---
  id: 0008
  slug: plan-review-gate
  title: Bind the plan-review pre-execution gate — spec §02 (v0.5.0 -> 0.6.0)
  from_version: 0.5.0
  to_version: 0.6.0
  applies_to:
    - .planning/config.codex.json
    - AGENTS.md
    - skills/agentic-apps-workflow/SKILL.md
    - .codex/workflow-version.txt
  requires: []
  optional_for: []
  ---

**Config target differs from 0007's.** 0007 wrote the host-neutral
`.planning/config.json` because `knowledge_capture` must be the same block both
hosts read. `pre_execution` is the opposite: it is **host-scoped like the other
15 gates**, so it targets `.planning/config.codex.json`'s `.hooks` object
(D-01/D-19, ADR-0007 point 5's codex-namespacing precedent). Do not copy 0007's
destination.

**jq merge idempotency shape** — adapt 0007's Step 1 (its lines 99-107) to the
nested target:

  idempotency check: jq -e '.hooks.pre_execution' .planning/config.codex.json >/dev/null
  apply:             jq '.hooks += {pre_execution: $pe}'  (--argjson pe from the template)

`+=` on `.hooks` preserves the four existing groups. Source `$pe` from the
installed template
(`$CODEX/skills/setup-codex-agenticapps-workflow/templates/config-hooks.json`,
`.hooks.pre_execution`) — never a heredoc'd literal, same single-source rule as
the prose.

**AGENTS.md insert: template extraction, NOT heredoc (D-19).** 0007's Step 2
(its lines 124-147) is the exact mechanism. Its portability comment is
load-bearing, not style:

  "getline-from-file is portable (BSD/macOS awk rejects a multi-line -v assignment)"

A multi-line section CANNOT be passed via `awk -v var="$(cat file)"` on
macOS/BSD awk. The two-pass extract-to-tempfile-then-getline pattern exists
specifically to work around that. Copy the mechanism; change only the heading
regex to the new section's exact heading — read it from `08-04-SUMMARY.md`,
which records the shipped text. A heading-regex mismatch makes the extraction
produce an empty file and the migration a **silent no-op**.

**Harness primitives** — see plan `08-01`'s `<interfaces>`. `assert_check`'s
two-call idiom (pre-condition "not-applied", post-condition "applied") is the
repo's idempotency convention; `test_migration_0007` lines 620-630 show it.

**test_repo_layout** (`run-tests.sh:776`) is a flat `for f in ... ; do` list of
paths asserted to exist, plus symlink regression guards at the end.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): test_migration_0008 + repo-layout guards</name>

  <read_first>
    - `migrations/run-tests.sh` lines 573-750 (`test_migration_0007` in full — the skeleton, the `assert_check` two-call idiom, the inline heredoc fixtures with a deliberate foreign-host key, the direct-assertion pattern for merge-preserving-keys at its lines 642-649) and lines 756-830 (`test_drift`, `test_repo_layout`) and the dispatcher tail
    - `migrations/0007-knowledge-capture.md` in full — the migration this one mirrors
    - `.planning/phases/08-plan-review-gate/08-04-SUMMARY.md` — the shipped ritual-section heading text and the `pre_execution` JSON as landed
    - `.planning/phases/08-plan-review-gate/08-CONTEXT.md` D-17, D-19
    - `.planning/phases/08-plan-review-gate/08-PATTERNS.md` §"`migrations/run-tests.sh`"
    - The `<interfaces>` section of this plan
  </read_first>

  <behavior>
    Add `test_migration_0008()` to `migrations/run-tests.sh`, mirroring
    `test_migration_0007`'s skeleton exactly: `jq` availability guard with `SKIP`,
    an isolated `mktemp -d` sandbox with `trap 'rm -rf "$tmp"' RETURN`,
    `mkdir -p "$tmp/.planning"`.

    Templates ship:
    - `templates/config-hooks.json` carries `.hooks.pre_execution.plan_review`
    - `templates/agents-md-additions.md` carries the ritual section heading
    - the section heading in the template matches the migration's awk regex
      **exactly** (a mismatch makes the migration a silent no-op — assert the
      extraction against the real template produces a NON-EMPTY file)

    Step 1 — config merge into `.planning/config.codex.json`:
    - idempotency pre: fixture config without `.hooks.pre_execution` → "not-applied"
    - after merge: `.hooks.pre_execution.plan_review.min_reviewers == 2` → "applied"
    - **merge preserves existing keys**: build the fixture with a pre-existing gate
      under `.hooks` (e.g. `post_phase.spec_review`) AND a foreign top-level key,
      mirroring 0007's `"host": "claude"` trick; assert both survive
    - second run is a no-op: re-running the merge leaves the file byte-identical
      and does not duplicate or nest `pre_execution`
    - the merged block equals the template's block

    Step 2 — AGENTS.md section insert:
    - idempotency pre: fixture AGENTS.md with the marker pair but no section →
      "not-applied"
    - extraction from the REAL template yields a non-empty section file
    - after insert: the heading is present → "applied"
    - the section landed INSIDE the marker block (heading line number < END marker
      line number), mirroring 0007's awk assertion
    - second run is a no-op: the idempotency check is positive and the section
      appears exactly once
    - the inserted text is byte-identical to the template's section

    Step 3 — version bump on a synthetic SKILL.md copy:
    - `version: 0.5.0` → `version: 0.6.0`
    - `implements_spec: 0.4.0` is **untouched** (D-17) — assert it explicitly, as
      `test_migration_0007` does

    Step 4 — `.codex/workflow-version.txt` records `0.6.0`

    Ship guards:
    - `migrations/0008-plan-review-gate.md` exists
    - `docs/decisions/0009-plan-review-gate.md` exists (mirrors 0007's ADR-0008 check)

    Then extend `test_repo_layout`'s path list with every artifact this phase adds:
    - `skills/agentic-apps-workflow/scripts/check-plan-review.sh`
    - `skills/codex-plan-review/SKILL.md`
    - `migrations/0008-plan-review-gate.md`
    - `docs/decisions/0009-plan-review-gate.md`
  </behavior>

  <action>
    Append `test_migration_0008()` after `test_migration_0007` and before
    `test_drift`. Wire a `0008` FILTER block into the dispatcher tail beside the
    existing `0007` block.

    Build fixtures inline with quoted heredocs, per the harness convention. The
    config fixture must carry a pre-existing `.hooks` gate and a foreign top-level
    key so the merge-preservation assertion is real rather than vacuous — this is
    exactly what 0007's `"host": "claude"` fixture proves, and it is the assertion
    that catches a `.hooks = {...}` typo where `.hooks += {...}` was meant.

    Extract the AGENTS.md section from the REAL template
    (`$REPO_ROOT/skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md`),
    not a fixture copy. That is the whole point of D-19's single-source rule: the
    test must fail if the template's heading and the migration's regex ever drift
    apart. Assert the extracted file is non-empty BEFORE asserting the insert, so a
    regex drift reports as "extraction empty" rather than as a confusing
    downstream insert failure.

    For every "second run is a no-op" assertion, capture a checksum before the
    re-run and compare after — do not merely re-check the idempotency predicate,
    which would pass even if the second run appended a duplicate section.

    Extend `test_repo_layout`'s `for f in ...` list with the four paths in
    `<behavior>`.

    Expect RED: `migrations/0008-plan-review-gate.md` does not exist, and the
    version-arithmetic assertions target 0.6.0 while SKILL.md is at 0.5.0. Note
    `run-tests.sh drift` will also go RED once 0008 exists — that is Task 2's
    problem, and it is why the version bump and the migration must land together.

    Commit as `test(RED): migration 0008 idempotency + repo-layout guards`.
  </action>

  <verify>
    <automated>bash migrations/run-tests.sh 0008; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'test_migration_0008' migrations/run-tests.sh` returns >= 2 (definition + dispatcher call)
    - `bash migrations/run-tests.sh 0008` exits 1 with at least one `FAIL` line
    - `bash migrations/run-tests.sh layout` exits 1 (the four new paths are asserted; `0008-plan-review-gate.md` is absent)
    - The four new layout paths are listed: `grep -c 'check-plan-review.sh\|codex-plan-review/SKILL.md\|0008-plan-review-gate.md\|0009-plan-review-gate.md' migrations/run-tests.sh` returns >= 4
    - `bash -n migrations/run-tests.sh` exits 0
    - `bash migrations/run-tests.sh 0007` still exits 0
    - `bash migrations/run-tests.sh check-plan-review` still exits 0 (plans `08-01`/`08-02` suites unaffected)
    - Commit exists matching `^test(RED):`
  </acceptance_criteria>

  <done>
    `run-tests.sh` carries a `test_migration_0008` covering all four migration
    steps, merge-preservation against a fixture with pre-existing keys, byte-level
    second-run no-op assertions, the template-extraction-non-empty guard, and the
    `implements_spec`-untouched assertion. `test_repo_layout` asserts this phase's
    four new artifacts. Both suites are RED; every pre-existing suite is green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Write migration 0008 and bump the version in lockstep</name>

  <read_first>
    - `migrations/0007-knowledge-capture.md` in full — the document to mirror wholesale: frontmatter (1-14), the framing prose (16-47), `## Pre-flight` (49-73), the four Steps with their Idempotency check / Pre-condition / Apply / Rollback sub-shape (75-171), `## Post-checks` (173-186), `## Skip cases` (190-201), `## Compatibility` (203-216), `## Notes` (218-228), `## References` (230-237)
    - `.planning/phases/08-plan-review-gate/08-04-SUMMARY.md` — the shipped ritual-section heading text (the awk regex must match it exactly) and the `pre_execution` JSON as landed
    - `.planning/phases/08-plan-review-gate/08-02-SUMMARY.md` and `08-03-SUMMARY.md` — the verifier path and producer skill name the migration's pre-flight checks for
    - `.planning/phases/08-plan-review-gate/08-CONTEXT.md` D-17, D-19
    - `.planning/phases/08-plan-review-gate/08-PATTERNS.md` §"`migrations/0008-*.md`"
    - `migrations/run-tests.sh` `test_migration_0008` (Task 1 — the contract)
    - The `<interfaces>` section of this plan
  </read_first>

  <action>
    Create `migrations/0008-plan-review-gate.md`, mirroring 0007's document
    structure wholesale. Frontmatter per `<interfaces>`.

    Framing prose, per 0007's: what §02's plan-review gate is, that this migration
    teaches an **existing install** the config block and the ritual section while
    fresh installs get both by construction (the template carries the section; the
    config template carries the block). Include 0007's "why a 0.x minor bump"
    paragraph adapted: the update engine applies a migration only when
    `installed >= from_version AND installed < to_version`; every live project is at
    0.5.0 after 0007, so `0.5.0 -> 0.6.0` is the shape that reaches the fleet via
    `$update-codex-agenticapps-workflow`. Supported upgrade floor: 0.5.0 -> 0.6.0.

    **Explicitly state the config-destination divergence from 0007** and why:
    `knowledge_capture` went to the host-neutral `.planning/config.json` because both
    hosts must read the same block; `pre_execution` is host-scoped like the other 15
    gates and goes to `.planning/config.codex.json` (ADR-0007 point 5). A reader
    coming from 0007 will otherwise assume the same destination.

    **`## Pre-flight`** — mirror 0007's shape (its lines 49-73):
    - git repo check
    - `jq` availability
    - version floor: `grep -qE '^version: 0\.(5|6)\.0$'` on the scaffolder SKILL.md
      (the `(5|6)` alternation is 0007's re-apply tolerance — keep it)
    - required templates present at the **installed** `$CODEX/skills/...` path, not
      repo-relative: `config-hooks.json` and `agents-md-additions.md`
    - **the verifier itself must ship before the migration that wires it**: check
      `$CODEX/skills/agentic-apps-workflow/scripts/check-plan-review.sh` exists. A
      migration that binds a `verifier` path to a script that is not installed
      produces a config pointing at nothing — a gate that silently never fires.

    **Step 1 — merge `pre_execution` into `.planning/config.codex.json`.**
    Idempotency check, pre-condition, apply, rollback — per `<interfaces>`'s jq
    shape. Source the block from the installed template via `--argjson`, never a
    heredoc'd literal. `+=` on `.hooks`, preserving the four existing groups.
    Rollback: `jq 'del(.hooks.pre_execution)'`.

    **Step 2 — insert the ritual section into `AGENTS.md`.** Copy 0007's Step 2
    mechanism **exactly** (extract-to-tempfile with awk, insert with
    getline-from-file), changing only the heading regex to the shipped heading from
    `08-04-SUMMARY.md`. **Carry 0007's portability comment forward verbatim** —
    "getline-from-file is portable (BSD/macOS awk rejects a multi-line -v
    assignment)". It is the reason the pattern exists; without it a later editor
    will "simplify" it to `awk -v` and break every macOS install. Escape the `§` and
    parens in the regex as 0007 does. Rollback: `git checkout -- AGENTS.md` plus a
    manual anchor.

    **Step 3 — bump the scaffolder version 0.5.0 -> 0.6.0.** Copy 0007's sed
    round-trip (its lines 155-164). **Carry its parenthetical forward:
    `implements_spec` is unchanged — do NOT touch it** (D-17). §02 wiring is real
    conformance either way; `implements_spec` tracks the last full audit.

    **Step 4 — record `0.6.0` in `.codex/workflow-version.txt`.**

    **`## Post-checks`** — mirror 0007's: jq assertion that
    `.hooks.pre_execution.plan_review.min_reviewers == 2`; assertion that the four
    pre-existing groups survive; `grep -q` for the ritual heading; version-bump greps
    on both SKILL.md and workflow-version.txt; the drift-test note.

    **`## Skip cases` / `## Compatibility` / `## Notes` / `## References`** — copy
    the document structure wholesale. Notes must carry 0007's "Testable
    non-interactively via `test_migration_0008`" convention. References: core spec
    `02-hook-taxonomy.md`, this repo's `docs/decisions/0009-plan-review-gate.md`,
    and the sibling precedent — claude-workflow's ADR-0025 / migration-0016.

    **Then bump for real, in lockstep:** `skills/agentic-apps-workflow/SKILL.md`
    `version: 0.5.0` → `0.6.0`, and `.codex/workflow-version.txt` → `0.6.0`. Do NOT
    touch `implements_spec: 0.4.0`. The drift test goes RED the moment 0008 exists
    and green only once SKILL.md matches its `to_version` — they must land in the
    same commit.

    Commit as `feat(GREEN): migration 0008 — bind plan-review gate (0.5.0 -> 0.6.0)`.
  </action>

  <verify>
    <automated>bash migrations/run-tests.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash migrations/run-tests.sh 0008` exits 0
    - `bash migrations/run-tests.sh drift` exits 0 (SKILL.md 0.6.0 == 0008 to_version 0.6.0)
    - `bash migrations/run-tests.sh layout` exits 0 (all four new paths ship)
    - `bash migrations/run-tests.sh` (FULL suite) exits 0 and reports `FAIL: 0` — ROADMAP success criterion #7
    - `grep -q '^from_version: 0.5.0$' migrations/0008-plan-review-gate.md` and `grep -q '^to_version: 0.6.0$' migrations/0008-plan-review-gate.md` both succeed
    - `grep -q '^version: 0.6.0$' skills/agentic-apps-workflow/SKILL.md` succeeds
    - `grep -q '^implements_spec: 0.4.0$' skills/agentic-apps-workflow/SKILL.md` succeeds (D-17 — the whole point)
    - `grep -q '^0.6.0$' .codex/workflow-version.txt` succeeds
    - D-19 heredoc prohibition held: `grep -c 'agents-md-additions.md' migrations/0008-plan-review-gate.md` returns >= 1, and the migration contains no inline copy of the ritual section (`grep -c '^## Pre-execution Gate — Plan Review' migrations/0008-plan-review-gate.md` returns 0)
    - The portability comment survives: `grep -ci 'BSD/macOS awk\|getline-from-file' migrations/0008-plan-review-gate.md` returns >= 1
    - The config target is the host-scoped file: `grep -c 'config.codex.json' migrations/0008-plan-review-gate.md` returns >= 1
    - The migration targets `.hooks` additively: `grep -c 'hooks +=\|hooks.pre_execution' migrations/0008-plan-review-gate.md` returns >= 1
    - The chain stays contiguous: `ls migrations/000*.md | wc -l` returns 9 (0000-0008)
    - Two commits exist for this plan matching `^test(RED):` then `^feat(GREEN):`, in that order
  </acceptance_criteria>

  <done>
    `migrations/0008-plan-review-gate.md` mirrors 0007's structure: pre-flight
    guards (including the verifier-must-ship check), four idempotent steps with
    rollbacks, post-checks, skip cases, compatibility, notes, references. The config
    merge targets `.planning/config.codex.json`'s `.hooks` additively; the AGENTS.md
    section is extracted from the template with the getline-from-file mechanism and
    its portability comment intact; `implements_spec` is untouched at 0.4.0 while
    the scaffolder moves to 0.6.0 in lockstep. The FULL harness is green with
    `FAIL: 0`.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG entry</name>

  <read_first>
    - `CHANGELOG.md` lines 1-40 — the `## [Unreleased]` section and the two `### Fixed` migration-discovery entries whose shape this mirrors (bold one-line summary; what was broken; why unnoticed; the fix; what was verified; whether a version bump applied and why; what test coverage was added)
    - `CHANGELOG.md` lines 80-95 — the existing `implements_spec` language 08-CONTEXT.md cites (it tracks a full conformance audit, not one gate)
    - `.planning/phases/08-plan-review-gate/08-PATTERNS.md` §"`CHANGELOG.md`"
    - `.planning/phases/08-plan-review-gate/08-CONTEXT.md` D-17
    - `.planning/ROADMAP.md` — the 7 success criteria, for the "what was verified" clause
  </read_first>

  <action>
    Add an entry under `## [Unreleased]`. It belongs under **`### Added`**, not
    `### Fixed` — create that subsection if absent. Unlike the two existing
    Unreleased entries (which explicitly note "no version bump"), this one IS a
    version-bumping migration, so say so.

    Follow the established entry shape: a bold one-line summary, then the narrative.

    Cover:
    - **What shipped:** the `plan-review` pre-execution gate bound on Codex —
      declarative binding in `.planning/config.codex.json` plus a programmatic
      verifier (the hybrid, ADR-0009), the `codex-plan-review` producer, ritual
      wiring in AGENTS.md + the trigger skill, migration 0008.
    - **Why:** core spec v0.5.0 added the gate and `spec/02:105-109` names this repo
      as an outstanding follow-up. The gate closes core ADR-0018's observed failure:
      cparx phases 04.9 → 05 silently dropped multi-AI plan review for 8
      consecutive phases.
    - **Why it went unnoticed here:** zero matches for `plan-review` repo-wide; the
      bindings table covered exactly the 15 pre-0.5.0 gates and jumped from
      `design-critique` straight to `tdd`. It was not recorded as a Spec Delta
      either, and the trigger plainly could occur.
    - **What was verified:** name `run-tests.sh`'s `test_check_plan_review_resolver`,
      `test_check_plan_review_enforcement`, and `test_migration_0008`, and the
      ROADMAP's 7 success criteria.
    - **Version bump:** scaffolder 0.5.0 → 0.6.0 via migration 0008.
    - **`implements_spec` non-bump, stated inline** (D-17): it stays 0.4.0 because it
      tracks the last full conformance audit, not one gate — mirror the existing
      CHANGELOG:88-91 language rather than inventing new phrasing. This phase
      delivers the §02 *content* required for a future 0.5.0 claim; it does not make
      that claim.
    - **D-20:** the bindings table now reads 16 distinct gates, matching `spec/02`
      (the duplicate `tdd` row is collapsed).

    Worth recording, since it is a finding rather than a restatement: the reference
    implementation's resolver carries three defects this port corrects — the dead
    `## Current Phase` heading, the absent `gsd-tools` analogue, and the colon-blocked
    line regex that silently matches incidental prose. Keep it to a sentence and
    point at ADR-0009.

    Do NOT claim `implements_spec: 0.5.0`. Do NOT describe the deferred items
    (native hooks.json, §14, the 00-07 layout migration) as shipped.
  </action>

  <verify>
    <automated>grep -q '^## \[Unreleased\]' CHANGELOG.md && grep -q 'plan-review' CHANGELOG.md && grep -q '0.4.0' CHANGELOG.md</automated>
  </verify>

  <acceptance_criteria>
    - `awk '/^## \[Unreleased\]/{f=1} /^## \[0/{f=0} f' CHANGELOG.md | grep -c 'plan-review'` returns >= 1 (the entry is under Unreleased)
    - `awk '/^## \[Unreleased\]/{f=1} /^## \[0/{f=0} f' CHANGELOG.md | grep -c '^### Added'` returns 1
    - The entry names the migration: `grep -c '0008' CHANGELOG.md` returns >= 1
    - The version bump is stated: `awk '/^## \[Unreleased\]/{f=1} /^## \[0/{f=0} f' CHANGELOG.md | grep -c '0.6.0'` returns >= 1
    - The `implements_spec` non-bump rationale is inline: `awk '/^## \[Unreleased\]/{f=1} /^## \[0/{f=0} f' CHANGELOG.md | grep -c 'implements_spec'` returns >= 1
    - No false conformance claim: `grep -c 'implements_spec: 0.5.0\|implements_spec.*bumped to 0.5.0' CHANGELOG.md` returns 0
    - The two pre-existing Unreleased `### Fixed` entries are intact: `grep -c 'Wire update-path migration discovery\|Wire setup-path migration discovery' CHANGELOG.md` returns 2
    - Test coverage is named: `awk '/^## \[Unreleased\]/{f=1} /^## \[0/{f=0} f' CHANGELOG.md | grep -c 'test_migration_0008\|run-tests'` returns >= 1
  </acceptance_criteria>

  <done>
    `CHANGELOG.md`'s Unreleased section carries an `### Added` entry in the
    established shape recording what shipped, why, why the gap went unnoticed, what
    was verified, the 0.5.0 → 0.6.0 bump, the `implements_spec: 0.4.0` non-bump with
    its rationale, the 16-gate table correction, and the three corrected reference
    defects. The two pre-existing Fixed entries are untouched. No 0.5.0 conformance
    claim appears.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → every downstream install | Migration 0008 executes against other people's repos via `$update-codex-agenticapps-workflow`, editing their `AGENTS.md` and config. It is a supply-chain write surface. |
| installed template → migration | The migration reads `$CODEX/skills/.../templates/*` and writes their content into a target repo. |
| target repo config → migration | The migration's `jq` merge reads and rewrites a config file it did not author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-22 | Tampering | `jq` merge into a target repo's `.planning/config.codex.json` | mitigate | A `.hooks = {...}` where `.hooks += {...}` was meant would silently delete all four existing gate groups — unbinding 15 gates while appearing to add one. Controls: `+=` merge only; an idempotency guard so a re-run is a no-op; and Task 1's fixture carries a pre-existing `.hooks` gate plus a foreign top-level key, with assertions that both survive. This is 0007's proven `"host": "claude"` trick, which exists for exactly this failure. |
| T-08-23 | Tampering | AGENTS.md awk insert in a target repo | mitigate | A heading-regex mismatch between the migration and the template makes the extraction produce an empty file, the insert a **silent no-op**, and the install non-conformant while the migration reports success. Controls: extract from the REAL installed template (never a heredoc — D-19), take the heading from `08-04-SUMMARY.md` rather than retyping it, and Task 1 asserts the extraction is non-empty *before* asserting the insert, so drift reports as itself. |
| T-08-24 | Denial of service | `awk -v` with a multi-line value on macOS/BSD | mitigate | BSD/macOS awk rejects a multi-line `-v` assignment — a "simplification" to `awk -v var="$(cat file)"` breaks the migration on every macOS install, which is this repo's primary platform. The getline-from-file mechanism exists solely to avoid it. Port 0007's portability comment verbatim so the constraint travels with the code; acceptance criteria assert the comment survives. |
| T-08-25 | Elevation of privilege | migration binds a `verifier` path | mitigate | If 0008 writes a config naming a verifier that is not installed, the target repo gets a gate that silently never fires — worse than no gate, since the config asserts coverage that does not exist. Pre-flight therefore requires `$CODEX/skills/agentic-apps-workflow/scripts/check-plan-review.sh` to exist before any step runs, and aborts with a distinct exit code otherwise (0007's pre-flight abort-code convention). |
| T-08-26 | Tampering | `mktemp` tempfile for the section extract | accept | The extract-to-tempfile step uses `mktemp` (which creates with a safe mode and an unpredictable name) and `rm -f`s it after. A local symlink race is theoretically possible but requires a local attacker already able to write the operator's `$TMPDIR`, who could edit the repo directly. Matches the 0007 precedent; no deviation warranted. |
| T-08-27 | Repudiation | false conformance claim in CHANGELOG | mitigate | Claiming `implements_spec: 0.5.0` on the strength of one gate would misrepresent conformance to every downstream consumer reading that field, since it denotes a full audit (CHANGELOG:88-91). D-17 forbids the bump; the acceptance criteria assert `implements_spec: 0.4.0` survives in SKILL.md and that no 0.5.0 claim appears in CHANGELOG.md. |
| T-08-SC | Tampering | supply chain / package installs | accept | This plan installs zero packages — one markdown migration, one bash harness edit, two version-string bumps, one changelog entry. No `npm`/`pip`/`cargo` surface exists, so the Package Legitimacy Gate does not apply. The migration itself is a supply-chain *write* surface, covered by T-08-22 through T-08-25. |
</threat_model>

<verification>
1. `bash migrations/run-tests.sh` → FULL suite exits 0, `FAIL: 0` (ROADMAP criterion #7)
2. `bash migrations/run-tests.sh 0008` → exits 0, including the second-run byte-identical no-op assertions
3. `bash migrations/run-tests.sh drift` → exits 0 (SKILL.md 0.6.0 == 0008 to_version)
4. `bash migrations/run-tests.sh layout` → exits 0 (all four new artifacts ship)
5. D-17 held: `grep -q '^implements_spec: 0.4.0$' skills/agentic-apps-workflow/SKILL.md` succeeds; no 0.5.0 claim in CHANGELOG.md
6. D-19 held: the migration contains no inline copy of the ritual section, and its portability comment survives
7. Git history: RED commit strictly before GREEN commit; the version bump is in the GREEN commit with the migration

**Phase close.** With this plan green, all 7 ROADMAP success criteria are met:
1, 5 (plan `08-02`), 2, 3, 6 (plan `08-01`), 4 (plans `08-02` + `08-03`), 7 (here).

**Gate coverage — every criterion above is met by `run-tests.sh`, not by a live
dogfood.** Phase 08 is grandfathered against its own gate: this fleet writes one
`*-SUMMARY.md` per plan, and the grandfather guard
(`multi-ai-review-gate.sh:141-142`) fires on any summary **before** the REVIEWS.md
check at line 145 — so from wave 1 onward the verifier exits 0 at this repo's
root without ever consulting `08-REVIEWS.md`. This is the bootstrap paradox
(a gate cannot gate the phase that builds it), it is expected and accepted, and
**no task in this phase manufactures a passing dogfood**. When closing the phase,
do NOT cite a grandfathered exit 0 as evidence the gate works — cite the suites
(`test_check_plan_review_resolver`, `test_check_plan_review_enforcement`,
`test_migration_0008`), which exercise the gate against synthetic fixtures. The
gate becomes genuinely live for phase 09 onward. Real adversarial review of these
plans happens out-of-band via the operator's `/gsd-review 8` run, which produces
`08-REVIEWS.md`; that is real, but it is not *caused by* the live gate. ADR-0009
records the paradox and the upstream design issue it exposes.
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 8 success criterion **7**:

- (7) `migrations/run-tests.sh` passes, including a `test_migration_0008` that is a
  no-op on second run.

This is the phase's terminal criterion — the full harness green with `FAIL: 0`
across every suite this phase added (`test_check_plan_review_resolver`,
`test_check_plan_review_enforcement`, `test_migration_0008`) plus every
pre-existing one, with drift and layout both green.
</success_criteria>

<output>
Create `.planning/phases/08-plan-review-gate/08-05-SUMMARY.md` when done.

Record in it: the final harness counts (PASS/FAIL/SKIP), the version state as
shipped (SKILL.md, workflow-version.txt, both configs' `implements_spec`), and any
deferred item that surfaced during execution — the deferred list in 08-CONTEXT.md
gains the third resolver defect for the upstream `claude-workflow` bug report.
</output>
