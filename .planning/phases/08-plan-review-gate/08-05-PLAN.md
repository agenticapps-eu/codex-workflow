---
phase: 08-plan-review-gate
plan: 05
type: tdd
wave: 4
depends_on: ["08-02", "08-04"]
files_modified:
  - migrations/0008-plan-review-gate.md
  - migrations/run-tests.sh
  - skills/agentic-apps-workflow/SKILL.md
  - .codex/workflow-version.txt
autonomous: true
requirements:
  - "core spec §02 (plan-review gate) — existing-install upgrade path"
  - "core spec §09 (conformance) — migrated installs reach the same bound state as fresh ones"
user_setup: []

must_haves:
  truths:
    - "An existing install at 0.5.0 gains the pre_execution.plan_review binding and the ritual section without losing existing config keys — a leaf-level deep merge that skips when plan_review is already present (D-19)"
    - "A pre-existing sibling gate under .hooks.pre_execution survives BOTH apply and rollback — the merge preserves existing keys, which is D-19's stated intent (D-19)"
    - "Rollback removes only .hooks.pre_execution.plan_review, dropping the parent object only if it becomes empty (D-19)"
    - "The AGENTS.md ritual section is extracted from the template, never heredoc'd (macOS/BSD awk constraint) (D-19)"
    - "implements_spec stays 0.4.0 while the scaffolder version moves 0.5.0 -> 0.6.0 (D-17)"
    - "Migration 0008 touches ONLY files a real target project actually has — .planning/, AGENTS.md, .codex/ — and never skills/agentic-apps-workflow/SKILL.md, which exists in this repo but in no downstream install (08-REVIEWS.md round 2, Codex HIGH)"
    - "A target project with NO local skills/ tree migrates successfully: the migration's version floor reads .codex/workflow-version.txt (the durable per-project record), not a scaffolder file that is not there"
    - "The harness is green at this plan's boundary: migration 0008 and this repo's scaffolder bump land in the SAME commit, so run_drift_test never sees a 0.6.0 to_version against a 0.5.0 SKILL.md — the bump is a direct edit to THIS repo, not a migration step shipped to targets (D-17)"
    - "test_migration_0008 is a no-op on second run for every step this plan owns (D-19)"
  artifacts:
    - path: "migrations/0008-plan-review-gate.md"
      provides: "Idempotent upgrade path for existing installs; 0.5.0 -> 0.6.0; config leaf-merge, ritual insert, project version record. Touches only files a real target project has. Plan 08-06 inserts the bindings-table step into this same document."
      contains: "from_version: 0.5.0"
    - path: "migrations/run-tests.sh"
      provides: "test_migration_0008 (steps 1-3) + the no-scaffolder-tree fixture + repo-layout guards for this phase's new artifacts"
      contains: "test_migration_0008"
  key_links:
    - from: "migrations/0008-plan-review-gate.md"
      to: "skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md"
      via: "awk section extraction at the installed template path — one source for prose (D-19)"
      pattern: "agents-md-additions.md"
    - from: "migrations/0008-plan-review-gate.md"
      to: ".planning/config.codex.json"
      via: "jq leaf-level deep merge into .hooks.pre_execution, preserving sibling gates"
      pattern: "config.codex.json"
    - from: "skills/agentic-apps-workflow/SKILL.md"
      to: "migrations/0008-plan-review-gate.md"
      via: "drift coupling — THIS repo's scaffolder version must equal the latest migration's to_version, enforced by a direct edit in the same commit. NOT a migration step: no target project has this file (T-08-38)."
      pattern: "version: 0.6.0"
---

<objective>
Ship the core of the upgrade path for existing installs.

Plan `08-04` made **fresh** installs conformant by construction — the template
carries the ritual section, the corrected bindings table, and the `pre_execution`
block. An install already at 0.5.0 gets none of it. Migration 0008 is what
reaches the fleet via `$update-codex-agenticapps-workflow`.

This plan writes migration 0008's **config merge**, its **ritual insert**, and
its **project version record** — the parts that mirror migration 0007
move-for-move, minus the one place 0007 is wrong (`<target_project_surface>`). Plan
`08-06` adds the bindings-table step (the genuinely novel work, with its own
header-shape guard) and the CHANGELOG, and is the plan that closes ROADMAP
success criterion #7.

Purpose: this is the half of 0008 with a direct precedent. 0007 did exactly this
shape — a config step, a ritual step, a version bump — so the risk here is not
novelty but **fidelity**: four merge-safety corrections that cross-AI review
forced, each of which fails silently when wrong.

Output: migration 0008 (steps 1-3), `test_migration_0008` including the
no-scaffolder-tree fixture, layout guards for every artifact this phase added, and
this repo's scaffolder bump in lockstep.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-plan-review-gate/08-CONTEXT.md
@.planning/phases/08-plan-review-gate/08-PATTERNS.md
@.planning/phases/08-plan-review-gate/08-REVIEWS.md
@.planning/phases/08-plan-review-gate/08-02-SUMMARY.md
@.planning/phases/08-plan-review-gate/08-03-SUMMARY.md
@.planning/phases/08-plan-review-gate/08-04-SUMMARY.md
@migrations/0007-knowledge-capture.md
@migrations/run-tests.sh
</context>

<plan_split_note>
## This plan was split — read this before wondering where the table step went

An earlier revision of this plan carried all five migration steps plus the
CHANGELOG. The plan-checker found it materially over the ~50% context target
(Task 1 encoded fixtures for five steps including four distinct merge
corrections and a header-shape skip case; Task 2 mirrored 0007 wholesale while
implementing all of it), and the operator approved a split.

**This plan owns:** the config leaf-merge, the ritual insert, the project version
record, and this repo's scaffolder bump.
**Plan `08-06` owns:** the bindings-table step, the CHANGELOG, and criterion #7.

**Why the version bump stayed here rather than moving to `08-06` with the table
(a deliberate deviation from the checker's proposed split shape, verified before
adopting):** `run_drift_test`
(`vendor/agenticapps-shared/migrations/lib/drift-test.sh`) resolves the latest
migration with `ls migrations/[0-9][0-9][0-9][0-9]-*.md | sort | tail -1` and
compares **that file's** `to_version` against `SKILL.md`'s `version`. The moment
`0008-plan-review-gate.md` exists carrying `to_version: 0.6.0`, a 0.5.0 SKILL.md
is a drift mismatch — and this repo's policy (ADR-0035, `run-tests.sh:756-768`)
makes a mismatch a **hard FAIL**, so the FULL harness goes red. Confirmed by
executing `run_drift_test` against a synthetic fixture during planning: 0007-only
+ SKILL 0.5.0 → green; add 0008 (`to_version: 0.6.0`) with SKILL still 0.5.0 →
`drift mismatch — skill_version=0.5.0 migration_to_version=0.6.0`; bump SKILL to
0.6.0 → green again.

Had the bump moved to `08-06`, this plan's GREEN commit would knowingly break the
conformance harness and leave it broken across the wave 4 → wave 5 boundary — a
"GREEN" commit that is red, and a repo that is broken if the phase is interrupted
there. Keeping the bump with the migration costs this plan only a sed round-trip
copied from 0007 and one file write (~3-5% context), and keeps **every plan
boundary in this phase green**. The split's purpose was context relief; the
expensive, novel work is the table step, and that is what moved.

**Step numbering.** This plan writes steps **1 (config), 2 (ritual), 3
(`.codex/workflow-version.txt`)** — **three** steps. Plan `08-06` inserts the
table step as **Step 3** and renumbers this plan's 3 to **4**, reaching the final
**4**-step shape.

**It is three, not four, because there is no target-project SKILL.md step** —
round 2 removed it (`<target_project_surface>`). The repo's own scaffolder bump
still happens in this plan's GREEN commit, as a direct edit for the
`run_drift_test` coupling; it is simply not a migration step, because no target
project has that file. Do not add a fourth step to "match 0007" — 0007's fourth
step is the bug.

That renumber is `08-06`'s explicit, asserted work — do not leave a gap or a
placeholder step here to accommodate it. A placeholder would be a scope reduction
wearing a numbering costume; write three complete steps.
</plan_split_note>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring. -->

**Version arithmetic — verified, do not re-derive:**

  skills/agentic-apps-workflow/SKILL.md   version: 0.5.0    implements_spec: 0.4.0
  .codex/workflow-version.txt             0.5.0
  migrations/0007-knowledge-capture.md    from 0.4.0 -> to 0.5.0   (latest today)

So 0008 is `from_version: 0.5.0`, `to_version: 0.6.0`.

**Two different things both called "the version bump" — keep them apart. This is
the distinction round 2's HIGH turns on** (`<target_project_surface>`):

1. **The repo's scaffolder bump** — `skills/agentic-apps-workflow/SKILL.md`
   `version: 0.5.0` → `0.6.0`. This is a **release-time edit in THIS repo**, made
   directly by this plan's GREEN commit. It is what `install.sh` then publishes to
   `${CODEX_HOME}`, and what the update skill reads as the *target* version. It is
   **not** a migration step and does not run in anyone's project.
2. **The project's version record** — `.codex/workflow-version.txt` → `0.6.0`.
   This IS a migration step, because every target project has this file. It is the
   durable per-project record the update skill reads to decide what is pending.

The drift test (`run-tests.sh drift`, policy: mismatch is a hard FAIL) requires
`SKILL.md version` == latest migration `to_version` — so the **repo's** SKILL.md
must move to 0.6.0 **in this plan, in the same commit as the migration** (see
`<plan_split_note>`). Plan `08-04` deliberately left it at 0.5.0 so the drift test
stayed green in its own wave. That coupling is real and it is why the bump lives
here — but it is satisfied by editing this repo's file directly. It does **not**
require, and must not produce, a migration step that sed-edits
`skills/agentic-apps-workflow/SKILL.md` inside a target project.

`implements_spec` stays `0.4.0` (D-17). 0007 carries the same explicit note:
"implements_spec is unchanged — do NOT touch it."

**Migration 0008 frontmatter shape** — mirror 0007's (its lines 1-14):

  ---
  id: 0008
  slug: plan-review-gate
  title: Bind the plan-review pre-execution gate — spec §02 (v0.5.0 -> 0.6.0)
  from_version: 0.5.0
  to_version: 0.6.0
  applies_to:
    - .planning/config.codex.json
    - AGENTS.md
    - .codex/workflow-version.txt
  requires: []
  optional_for: []
  ---

**`skills/agentic-apps-workflow/SKILL.md` is deliberately NOT in `applies_to`,
and 0007's presence of it is a bug this migration does not copy.** Read
`<target_project_surface>` before you "restore" it to match 0007.

**Config target differs from 0007's.** 0007 wrote the host-neutral
`.planning/config.json` because `knowledge_capture` must be the same block both
hosts read. `pre_execution` is the opposite: it is **host-scoped like the other
15 gates**, so it targets `.planning/config.codex.json`'s `.hooks` object
(D-01/D-19, ADR-0007 point 5's codex-namespacing precedent). Do not copy 0007's
destination.

**jq merge idempotency shape — CORRECTED after review. Use this, not 0007's
shallow form.** See `<merge_safety>` for the full reasoning; this is the
contract:

  idempotency check (LEAF, not the group):
    jq -e '.hooks.pre_execution.plan_review' .planning/config.codex.json >/dev/null

  apply (leaf-level deep merge — never replaces the pre_execution object):
    jq --argjson pe "$PE" '.hooks.pre_execution = ((.hooks.pre_execution // {}) + $pe)'

  rollback (removes only our leaf; drops the parent only if it is then empty):
    jq 'del(.hooks.pre_execution.plan_review)
        | if (.hooks.pre_execution // {}) == {} then del(.hooks.pre_execution) else . end'

`$PE` is the `pre_execution` **object's contents** — i.e.
`{"plan_review": {...}}` — sourced from the installed template
(`$CODEX/skills/setup-codex-agenticapps-workflow/templates/config-hooks.json`,
`.hooks.pre_execution`), never a heredoc'd literal, same single-source rule as
the prose.

**AGENTS.md insert: template extraction, NOT heredoc (D-19).** 0007's Step 2
(its lines 124-147) is the exact mechanism. Its portability comment is
load-bearing, not style:

  "getline-from-file is portable (BSD/macOS awk rejects a multi-line -v assignment)"

A multi-line section CANNOT be passed via `awk -v var="$(cat file)"` on
macOS/BSD awk. The two-pass extract-to-tempfile-then-getline pattern exists
specifically to work around that. Copy the mechanism; change only the heading
regex to the new section's exact heading — read it from `08-04-SUMMARY.md`,
which records the shipped text. A heading-regex mismatch makes the extraction
produce an empty file and the migration a **silent no-op**.

**Checksum command: use `cksum`.** Review flagged the checksum as unspecified
(08-REVIEWS.md, Codex, LOW). Pin `cksum` — POSIX, present on macOS and Linux
alike, unlike `sha256sum` (absent on macOS, where it is `shasum -a 256`) or
`md5`/`md5sum` (which differ by name across the two). Every "second run is a
no-op" assertion compares `cksum < file` before and after.

**Harness primitives** — see plan `08-01`'s `<interfaces>`. `assert_check`'s
two-call idiom (pre-condition "not-applied", post-condition "applied") is the
repo's idempotency convention; `test_migration_0007` lines 620-630 show it.

**test_repo_layout** (`run-tests.sh:776`) is a flat `for f in ... ; do` list of
paths asserted to exist, plus symlink regression guards at the end.
</interfaces>

<target_project_surface>
## What a target project actually contains — read this before writing any step

**Round 2's most consequential finding, and it holds** (08-REVIEWS.md round 2,
Codex, HIGH; uncorroborated, and correct anyway — the orchestrator verified it
against the real files before adopting it).

**The claim.** An earlier revision of this plan put
`skills/agentic-apps-workflow/SKILL.md` in migration 0008's `applies_to`, its
pre-flight version floor, and a runtime step. **Normal target projects do not have
that file.** A real downstream `update` would abort, while `test_migration_0008`
passed green against a synthetic `SKILL.md` the test itself created.

**The evidence, verified — do not re-derive, but do not doubt it either:**

1. **The setup skill defines the project-side surface, and `skills/` is not in
   it.** `skills/setup-codex-agenticapps-workflow/SKILL.md:8-9` enumerates the
   "required project-side artifacts" as: `AGENTS.md` sections,
   `.planning/config.codex.json`, `.codex/workflow-config.md`, `docs/decisions/`,
   `.codex/workflow-version.txt`. Its own post-checks (lines 103-109, 140-145)
   assert exactly those. **No local `skills/` tree is ever created in a target
   project.**
2. **The update skill reads the scaffolder from `${CODEX_HOME}`, not the
   project.** `skills/update-codex-agenticapps-workflow/SKILL.md:41-44`: "Read the
   trigger skill's `version:` frontmatter from
   `${CODEX_HOME:-$HOME/.codex}/skills/agentic-apps-workflow/SKILL.md`. This is the
   target version." The scaffolder lives in `${CODEX_HOME}`. The project has the
   `.codex/workflow-version.txt` record and nothing else version-shaped.
3. **Migration steps execute in the target project.** Same skill, Stage D/E: steps
   apply against the project, then `git add <files-touched-by-this-migration>` and
   commit **in the project**. So a step naming `skills/agentic-apps-workflow/SKILL.md`
   is naming a repo-relative path *in the target project*, where it does not exist.
4. **Migration 0007 has this exact bug today.** Its `applies_to` lists
   `skills/agentic-apps-workflow/SKILL.md`; its pre-flight runs
   `grep -qE '^version: 0\.(4|5)\.0$' skills/agentic-apps-workflow/SKILL.md ||
   { ... exit 3; }`; its Step 3 seds that path. In any project without a local
   `skills/` tree, the pre-flight grep fails and 0007 **aborts with exit 3** before
   any step runs.
5. **The harness cannot see it, by construction.** `run-tests.sh:729` does
   `printf 'version: 0.4.0\nimplements_spec: 0.4.0\n' > "$tmp/SKILL.md"` and seds
   *that*. The test manufactures the file whose absence is the bug. 89 PASS, and
   the product is broken.

**This is not a new failure mode in this repo — it is the third instance of one it
has already shipped twice.** Both `## [Unreleased]` CHANGELOG entries are this
same shape, and both say so in their own words: migration discovery was wired to
paths that "only resolved from the scaffolder checkout", latent because "the repo
dogfoods via direct edits and `run-tests.sh` synthetic fixtures, so it went
unnoticed". Same cause, same detection gap, same sentence. That is what makes this
finding worth acting on rather than deferring: the repo has a demonstrated blind
spot exactly here, and this is the plan that would have widened it a third time.

**The resolution — adopted, and narrower than it looks.**

- **0008 does NOT touch `skills/agentic-apps-workflow/SKILL.md` in any target
  project.** Not in `applies_to`, not in pre-flight, not in a step.
- **The version floor reads `.codex/workflow-version.txt` instead** — the durable
  per-project record the update skill itself uses (its Stage A step 1). Keep
  0007's re-apply tolerance: accept `0.5.0` or `0.6.0`.
- **The repo's own scaffolder bump stays in this plan's GREEN commit**, as a
  direct edit to this repo's file. The `run_drift_test` coupling is real and
  requires it (`<plan_split_note>`) — but that coupling is satisfied by editing
  this repo, not by shipping a step that edits everyone else's. Codex named this
  precisely: "keep the repository SKILL version bump in the implementation commit,
  but it should not be a target-project migration step."
- **The step count drops from 4 to 3**: config, ritual, `.codex/workflow-version.txt`.
  Plan `08-06` then inserts the table step as Step 3 and renumbers, reaching a
  final **4**-step shape (not 5 — `08-06`'s `<plan_split_note>` is corrected to
  match).
- **Task 1 adds the fixture that would have caught this**: a target sandbox with
  **no `skills/` directory at all**, asserting the migration completes. Codex asked
  for exactly this ("add an end-to-end fixture representing a real target repo with
  no local `skills/` directory"), and it is the assertion that makes the
  correction stick rather than being re-broken by the next person mirroring 0007.

**0007's identical bug is NOT fixed here.** It is a different migration, its own
scope, and fixing it means re-testing a shipped upgrade path. Record it as a
deferred item and in ADR-0009's follow-ups: *migration 0007 aborts in any target
project without a local `skills/` tree; same class as the two Unreleased
migration-discovery fixes; needs its own phase.* Do not widen this plan to repair
it, and do not copy it either. When a step here diverges from 0007, comment the
divergence and the reason at the call site — a reader coming from 0007 will
otherwise "fix" 0008 back toward the bug.

</target_project_surface>

<merge_safety>
## The config merge, corrected — read this before touching Step 1

Cross-AI review raised five HIGH findings against this plan's original merge
(08-REVIEWS.md, Codex). Gemini called this plan "a textbook execution" with no
concerns and OpenCode reviewed it only lightly, so these were adjudicated on
their merits rather than treated as outvoted. Four are adopted; the fifth is
deferred. **None of them overrides D-19 — three of them implement it correctly,
because the original shape contradicted D-19's own stated intent ("preserves
existing keys").**

**1. The idempotency check must test the LEAF.** Original:
`jq -e '.hooks.pre_execution'`. That is true as soon as *any* pre-execution gate
exists — so on an install that already has a sibling pre-execution gate but no
`plan_review`, the migration silently skips and the install never gets the gate,
while reporting success. Test `.hooks.pre_execution.plan_review`.

**2. The merge must be leaf-level, not shallow.** Original:
`.hooks += {pre_execution: $pe}`. `+=` on `.hooks` preserves `.hooks`'s *other
groups* (`pre_phase`, `per_task`, …) — which is what the original fixture proved
— but it **replaces the entire `pre_execution` object**, deleting any sibling
gate inside it. Today `pre_execution` has one member so nothing is lost; the
moment core spec adds a second pre-execution gate, this migration deletes it on
every install that has it. Use
`.hooks.pre_execution = ((.hooks.pre_execution // {}) + $pe)`, which merges at
the leaf and preserves siblings. The `// {}` handles the first-run case where
`pre_execution` does not exist at all.

**3. Rollback must remove only our leaf.** Original: `del(.hooks.pre_execution)`,
destructive to siblings for the same reason. Remove
`.hooks.pre_execution.plan_review`, then drop the now-empty parent only if it is
empty — so a rollback on an install with a sibling gate leaves that gate bound.

**4. The fixture must actually be able to catch 2 and 3.** The original fixture
carried a pre-existing gate under a *different top-level group*
(`post_phase.spec_review`) plus a foreign top-level key — mirroring 0007's
`"host": "claude"` trick. That proves `.hooks += {...}` preserves other groups,
which was never in doubt, and **cannot catch a replaced `pre_execution` object**.
Task 1's fixture must carry a pre-existing `.hooks.pre_execution.other_gate` and
assert it survives BOTH apply and rollback. Keep the original foreign keys too —
they still guard their own regression.

**6. Skips are STEP-LOCAL. The whole migration never skips.** (08-REVIEWS.md
round 2, Codex, HIGH.) An earlier revision's `## Skip cases` said "the whole
migration skips when `.hooks.pre_execution.plan_review` is already present". That
is wrong twice over:

  - **It contradicts 0007, which this plan otherwise mirrors move-for-move.**
    0007's own skip cases say it explicitly: "**Block already present** → Step 1
    idempotency is positive; the existing block is preserved verbatim and **Steps
    2–4 still run**" and "**Section already present** → Step 2 is a no-op; **Steps
    1, 3, 4 still run**." Per-step no-op, never a whole-migration skip. The
    framework is built for it — the update skill runs an idempotency check *per
    step* (Stage D: "Idempotency check — skip with log line if applied").
  - **It breaks recovery from a partial application.** The atomicity contract
    (`migrations/README.md:103-113`) lets an operator choose *skip-with-warning* on
    a failed step, which records the migration `partial` and continues. So a real
    install can legitimately have Step 1 applied and Step 2 not. Re-running to
    finish the job is the documented recovery path — and a whole-migration skip
    keyed on Step 1's artifact makes that recovery a no-op that reports success.
    The install stays half-migrated, permanently, and says it is fine. That is
    strictly worse than the drift the gate exists to stop.

**The rule: every step owns its own idempotency check and no step's check gates
another step.** Step 1 skips when `.hooks.pre_execution.plan_review` exists. Step
2 skips when the ritual heading is present. Step 3 skips when
`.codex/workflow-version.txt` already reads `0.6.0`. Each independently, each
logged, each continuing to the next. There is no migration-level skip predicate at
all — delete the idea, do not merely narrow it. Task 1's partial-application
fixture is the regression guard.

**5. DEFERRED — the update skill's multi-hop chain.** Review found that
`update-codex-agenticapps-workflow` selects pending migrations **once**, from the
initial project version: a project at 0.4.0 selects 0007 but not 0008, applies
0007, lands at 0.5.0, and then fails the final target-version check — so the
supported upgrade chain is untested and broken. **This is a real defect and it is
NOT this plan's work.** It lives in a different skill, and criterion 7 asks only
that `run-tests.sh` pass with an idempotent `test_migration_0008`. Do **not**
repair it here, do **not** add 0.4.0→0.5.0→0.6.0 end-to-end fixtures, and do
**not** widen this migration's `from_version` to paper over it. It is recorded in
08-CONTEXT.md's Deferred Ideas ("Multi-hop migration chain in update skill") and
in ADR-0009's follow-ups. This migration's supported floor stays 0.5.0 → 0.6.0,
which is where every live project already sits after 0007.
</merge_safety>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): test_migration_0008 (steps 1-3) + no-scaffolder-tree fixture + repo-layout guards</name>

  <read_first>
    - `migrations/run-tests.sh` lines 573-750 (`test_migration_0007` in full — the skeleton, the `assert_check` two-call idiom, the inline heredoc fixtures with a deliberate foreign-host key, the direct-assertion pattern for merge-preserving-keys at its lines 642-649) and lines 756-830 (`test_drift`, `test_repo_layout`) and the dispatcher tail
    - `migrations/0007-knowledge-capture.md` in full — the migration this one mirrors
    - `.planning/phases/08-plan-review-gate/08-04-SUMMARY.md` — the shipped ritual-section heading text and the `pre_execution` JSON as landed. (Its bindings-table column shapes are plan `08-06`'s concern, not this task's.)
    - `.planning/phases/08-plan-review-gate/08-REVIEWS.md` — Codex's 08-05 review; the five HIGH findings `<merge_safety>` answers
    - `.planning/phases/08-plan-review-gate/08-CONTEXT.md` D-17, D-19
    - `.planning/phases/08-plan-review-gate/08-PATTERNS.md` §"`migrations/run-tests.sh`"
    - The `<interfaces>`, `<merge_safety>`, and `<plan_split_note>` sections of this plan
  </read_first>

  <behavior>
    Add `test_migration_0008()` to `migrations/run-tests.sh`, mirroring
    `test_migration_0007`'s skeleton exactly: `jq` availability guard with `SKIP`,
    an isolated `mktemp -d` sandbox with `trap 'rm -rf "$tmp"' RETURN`,
    `mkdir -p "$tmp/.planning"`.

    Templates ship:
    - `templates/config-hooks.json` carries `.hooks.pre_execution.plan_review`
    - `templates/agents-md-additions.md` carries the ritual section heading
    - the section heading in the template matches the migration's awk regex
      **exactly** (a mismatch makes the migration a silent no-op — assert the
      extraction against the real template produces a NON-EMPTY file)

    Step 1 — config leaf-merge into `.planning/config.codex.json`
    (`<merge_safety>`):
    - idempotency pre: fixture config without `.hooks.pre_execution.plan_review` →
      "not-applied". **Assert against the LEAF, not `.hooks.pre_execution`**
      (finding 1).
    - **The skip-when-sibling-exists case (finding 1):** a fixture whose
      `.hooks.pre_execution` exists and holds ONLY `other_gate` → the idempotency
      check must read "not-applied", i.e. the migration must still run. A check on
      `.hooks.pre_execution` reports "applied" here and skips — that is the bug.
    - after merge: `.hooks.pre_execution.plan_review.min_reviewers == 2` → "applied"
    - **merge preserves a SIBLING pre-execution gate (finding 2 + 4):** fixture
      carries `.hooks.pre_execution.other_gate`; after the merge assert
      `.hooks.pre_execution.other_gate` still exists AND
      `.hooks.pre_execution.plan_review` exists. This is the assertion the original
      fixture could not make; without it findings 2 and 3 are invisible.
    - **merge preserves other top-level hook groups and foreign keys:** keep the
      original fixture's pre-existing `.hooks.post_phase.spec_review` and a foreign
      top-level key, mirroring 0007's `"host": "claude"` trick; assert both survive
    - **rollback removes only our leaf (finding 3):** from the merged state with a
      sibling present, run the rollback and assert `.hooks.pre_execution.plan_review`
      is gone AND `.hooks.pre_execution.other_gate` still exists
    - **rollback drops an empty parent (finding 3):** from a merged state with NO
      sibling, run the rollback and assert `.hooks.pre_execution` is absent
      entirely (not left as an empty object)
    - second run is a no-op: re-running the merge leaves the file byte-identical
      (`cksum` before/after) and does not duplicate or nest `pre_execution`
    - the merged block equals the template's block

    Step 2 — AGENTS.md ritual section insert:
    - idempotency pre: fixture AGENTS.md with the marker pair but no section →
      "not-applied"
    - extraction from the REAL template yields a non-empty section file
    - after insert: the heading is present → "applied"
    - the section landed INSIDE the marker block (heading line number < END marker
      line number), mirroring 0007's awk assertion
    - second run is a no-op: the idempotency check is positive and the section
      appears exactly once (assert by `cksum`, not only by the predicate — a
      predicate re-check passes even if the second run appended a duplicate)
    - the inserted text is byte-identical to the template's section

    Step 3 — `.codex/workflow-version.txt` records `0.6.0`:
    - idempotency pre: fixture reads `0.5.0` → "not-applied"
    - after: reads `0.6.0` → "applied"
    - second run is a no-op (`cksum` before/after)

    **There is NO synthetic-SKILL.md version-bump case, and its absence is the
    point** (`<target_project_surface>`). An earlier revision tested a Step 3 that
    sed-edited `skills/agentic-apps-workflow/SKILL.md` against a fixture the test
    itself created with
    `printf 'version: 0.5.0\nimplements_spec: 0.4.0\n' > "$tmp/SKILL.md"` —
    exactly what `test_migration_0007` does at `run-tests.sh:729`. That test
    manufactures the file whose absence is the bug, so it is green by construction
    and proves nothing about any real install. Do not port it. Do not "restore" it
    when you notice 0008 has one fewer step than 0007.

    **The no-scaffolder-tree fixture — the case that would have caught this**
    (Codex asked for it by name: "an end-to-end fixture representing a real target
    repo with no local `skills/` directory"). Build a sandbox shaped like a REAL
    target project, per the setup skill's own post-checks
    (`setup-codex-agenticapps-workflow/SKILL.md:103-109`): `.planning/config.codex.json`,
    `AGENTS.md` with the marker pair, `.codex/workflow-version.txt` reading
    `0.5.0`, `docs/decisions/`. **And NO `skills/` directory at all** — assert
    `test ! -e "$tmp/skills"` up front, so a later edit that quietly creates one
    fails here rather than silently restoring the blind spot. Then:
    - every step's idempotency check runs without error (none of them stats a
      path under `skills/`)
    - the pre-flight version floor passes, reading `.codex/workflow-version.txt`
    - all three steps apply and the migration completes
    This fixture is the whole regression guard for round 2's HIGH. Without it,
    nothing distinguishes the corrected migration from the broken one.

    **`implements_spec: 0.4.0` is asserted on the REAL repo file** (D-17), not on
    a synthetic copy — Task 2 bumps this repo's own
    `skills/agentic-apps-workflow/SKILL.md` directly, so that is where the
    assertion belongs and where it means something. Task 2's acceptance criteria
    carry it.

    **Step-local idempotency — the partial-application fixture** (`<merge_safety>`
    finding 6): a sandbox where Step 1 is ALREADY applied
    (`.hooks.pre_execution.plan_review` present) but Step 2 is NOT (no ritual
    heading) → **Steps 2 and 3 still run and complete**. This is the recovery path
    the atomicity contract's skip-with-warning creates, and a whole-migration skip
    keyed on Step 1's artifact turns it into a no-op that reports success. Mirror
    0007's own skip-case shape, which states exactly this behavior. Also the
    inverse: Step 2 applied, Step 1 not → Step 1 still runs.

    Ship guards:
    - `migrations/0008-plan-review-gate.md` exists
    - `docs/decisions/0009-plan-review-gate.md` exists (mirrors 0007's ADR-0008 check)

    Build the no-scaffolder-tree sandbox from the setup skill's post-check list,
    not from imagination — read
    `skills/setup-codex-agenticapps-workflow/SKILL.md` lines 103-109 and 140-145
    and mirror exactly the artifacts it asserts. The fixture's value is that it is
    shaped like a real install; a fixture shaped like this repo would reproduce the
    bug rather than catch it.

    **Do NOT create a `skills/` directory in any 0008 sandbox**, and do not sed a
    synthetic `SKILL.md`. `test_migration_0007` does both at `run-tests.sh:729`;
    that is the pattern under repair, not the pattern to mirror
    (`<target_project_surface>`). If a case seems to need a local `skills/` tree,
    the step under test is wrong, not the fixture.

    **Do NOT write table-step assertions here.** Plan `08-06` adds them alongside
    the step they test. A test for a step that does not exist yet is not RED, it is
    noise — and it would put `08-06`'s work in this plan's context budget, which is
    the thing the split exists to prevent.

    Then extend `test_repo_layout`'s path list with every artifact this phase adds:
    - `skills/agentic-apps-workflow/scripts/check-plan-review.sh`
    - `skills/codex-plan-review/SKILL.md`
    - `migrations/0008-plan-review-gate.md`
    - `docs/decisions/0009-plan-review-gate.md`
  </behavior>

  <action>
    Append `test_migration_0008()` after `test_migration_0007` and before
    `test_drift`. Wire a `0008` FILTER block into the dispatcher tail beside the
    existing `0007` block.

    Build fixtures inline with quoted heredocs, per the harness convention. The
    config fixture must carry **all three** kinds of neighbour so every preservation
    assertion is real rather than vacuous: a sibling gate under
    `.hooks.pre_execution.other_gate` (findings 2/3 — the one the original fixture
    lacked), a gate under a different group (`.hooks.post_phase.spec_review`), and a
    foreign top-level key (0007's `"host": "claude"` trick). Only the first can catch
    a replaced `pre_execution` object; keep all three.

    Extract the AGENTS.md section from the REAL template
    (`$REPO_ROOT/skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md`),
    not a fixture copy. That is the whole point of D-19's single-source rule: the
    test must fail if the template's heading and the migration's regex ever drift
    apart. Assert the extraction is non-empty BEFORE asserting the insert, so a
    regex drift reports as "extraction empty" rather than as a confusing downstream
    insert failure.

    For every "second run is a no-op" assertion, capture `cksum < file` before the
    re-run and compare after — do not merely re-check the idempotency predicate,
    which would pass even if the second run appended a duplicate section. Use
    `cksum`, not `sha256sum`/`md5sum`: it is POSIX and behaves identically on macOS
    and Linux (`<interfaces>`).

    Extend `test_repo_layout`'s `for f in ...` list with the four paths in
    `<behavior>`.

    **Do not add 0.4.0→0.5.0→0.6.0 chain fixtures or touch the update skill** — see
    `<merge_safety>` finding 5. That defect is deferred, not this plan's work.

    Expect RED: `migrations/0008-plan-review-gate.md` does not exist, so both the
    `0008` suite and the layout suite fail. Note that `run-tests.sh drift` is still
    **green** at this point — 0007 is still the latest migration and SKILL.md is
    still 0.5.0. It is Task 2 that moves both in the same commit and keeps it green.

    Commit as `test(RED): migration 0008 idempotency + repo-layout guards`.
  </action>

  <verify>
    <automated>bash migrations/run-tests.sh 0008; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'test_migration_0008' migrations/run-tests.sh` returns >= 2 (definition + dispatcher call)
    - `bash migrations/run-tests.sh 0008` exits 1 with at least one `FAIL` line
    - `bash migrations/run-tests.sh layout` exits 1 (the four new paths are asserted; `0008-plan-review-gate.md` is absent)
    - `test_repo_layout` is the RIGHT place for the scaffolder-file assertion: `skills/agentic-apps-workflow/scripts/check-plan-review.sh` is asserted there (it is a file THIS repo ships), while no 0008 migration step asserts anything under `skills/` in a target project. Keep that division — layout guards this repo; the migration guards other people's.
    - The four new layout paths are listed: `grep -c 'check-plan-review.sh\|codex-plan-review/SKILL.md\|0008-plan-review-gate.md\|0009-plan-review-gate.md' migrations/run-tests.sh` returns >= 4
    - **`run-tests.sh drift` still exits 0** — this task adds no migration file, so 0007 remains latest and SKILL.md remains 0.5.0. A red drift here means a migration file was created early; it belongs in Task 2's commit (`<plan_split_note>`).
    - **The sibling-gate fixture exists** (findings 2/3/4 — without it the merge bugs are invisible): `awk '/^test_migration_0008\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c 'other_gate'` returns >= 3 (fixture + apply-survival assertion + rollback-survival assertion)
    - **The idempotency check is on the leaf** (finding 1): `awk '/^test_migration_0008\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c "pre_execution.plan_review"` returns >= 1, AND the same stream contains no bare group-level idempotency predicate — piping it to `grep -cE "jq -e '\.hooks\.pre_execution'( |\")"` returns 0
    - **Rollback assertions exist**: the same stream returns >= 1 for `grep -ci 'rollback'`
    - **Checksums are pinned to `cksum`**: `awk '/^test_migration_0008\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c 'cksum'` returns >= 2, and the same stream, **with comment lines stripped** (`| grep -v '^[[:space:]]*#'`), returns 0 for `sha256sum` and 0 for `md5sum`
      (08-REVIEWS.md round 2, OpenCode, LOW: the unfiltered denylist fires on an explanatory comment like `# use cksum, not md5sum, because macOS names it md5` — a comment that makes the pin *more* durable, penalised by the assertion meant to enforce it. Strip comments; the pin is about what executes.)
    - **The no-scaffolder-tree fixture exists** — the regression guard for round 2's HIGH (`<target_project_surface>`): the same stream returns >= 1 for `grep -c 'test ! -e .*skills'`
    - **No 0008 sandbox manufactures a scaffolder SKILL.md**: the same stream returns 0 for `grep -c "SKILL.md"` — 0007's `printf 'version: ...' > "$tmp/SKILL.md"` shape (`run-tests.sh:729`) is the pattern under repair and must not be mirrored here
    - **The partial-application fixture exists** (`<merge_safety>` finding 6 — step-local, not whole-migration, skips): the same stream returns >= 1 for `grep -ci 'partial\|already applied'`
    - **The table step is NOT tested here** (it is plan `08-06`'s): the same stream returns 0 for `grep -ci 'Applies to scaffolder'`
    - **No multi-hop chain fixtures were added** (deferred, `<merge_safety>` finding 5): `grep -c '0\.4\.0' migrations/run-tests.sh` is unchanged from before this task (capture it first with `git show HEAD:migrations/run-tests.sh | grep -c '0\.4\.0'`)
    - `bash -n migrations/run-tests.sh` exits 0
    - `bash migrations/run-tests.sh 0007` still exits 0
    - `bash migrations/run-tests.sh check-plan-review` still exits 0 (plans `08-01`/`08-02` suites unaffected)
    - Commit exists matching `^test(RED):`
  </acceptance_criteria>

  <done>
    `run-tests.sh` carries a `test_migration_0008` covering this plan's three
    migration steps; leaf-level idempotency including the skip-when-a-sibling-exists
    case; step-local idempotency proven by a partial-application fixture; merge
    preservation against a fixture carrying a sibling `pre_execution` gate, a
    different-group gate, and a foreign top-level key; rollback that removes only
    `plan_review` and drops the parent only when empty; `cksum`-based second-run
    no-op assertions; and the template-extraction-non-empty guard. It carries the
    **no-scaffolder-tree fixture** — a sandbox shaped like a real target project,
    with no `skills/` directory — which is the regression guard for round 2's HIGH,
    and it manufactures no synthetic `SKILL.md` anywhere. `test_repo_layout` asserts
    this phase's four new artifacts. The `0008` and `layout` suites are RED; drift
    and every pre-existing suite are green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Write migration 0008 (steps 1-3) and bump this repo's scaffolder version in lockstep</name>

  <read_first>
    - `migrations/0007-knowledge-capture.md` in full — the document to mirror wholesale: frontmatter (1-14), the framing prose (16-47), `## Pre-flight` (49-73), the Steps with their Idempotency check / Pre-condition / Apply / Rollback sub-shape (75-171), `## Post-checks` (173-186), `## Skip cases` (190-201), `## Compatibility` (203-216), `## Notes` (218-228), `## References` (230-237)
    - `.planning/phases/08-plan-review-gate/08-04-SUMMARY.md` — the shipped ritual-section heading text (the awk regex must match it exactly) and the `pre_execution` JSON as landed
    - `.planning/phases/08-plan-review-gate/08-02-SUMMARY.md` and `08-03-SUMMARY.md` — the verifier path and producer skill name the migration's pre-flight checks for
    - `.planning/phases/08-plan-review-gate/08-CONTEXT.md` D-17, D-19
    - `.planning/phases/08-plan-review-gate/08-PATTERNS.md` §"`migrations/0008-*.md`"
    - `migrations/run-tests.sh` `test_migration_0008` (Task 1 — the contract)
    - The `<interfaces>`, `<merge_safety>`, and `<plan_split_note>` sections of this plan
  </read_first>

  <action>
    Create `migrations/0008-plan-review-gate.md`, mirroring 0007's document
    structure wholesale. Frontmatter per `<interfaces>`.

    Framing prose, per 0007's: what §02's plan-review gate is, and that this
    migration teaches an **existing install** the config block and the ritual
    section while fresh installs get them by construction. Include 0007's "why a
    0.x minor bump" paragraph adapted: the update engine applies a migration only
    when `installed >= from_version AND installed < to_version`; every live project
    is at 0.5.0 after 0007, so `0.5.0 -> 0.6.0` is the shape that reaches the fleet
    via `$update-codex-agenticapps-workflow`. **Supported upgrade floor: 0.5.0 ->
    0.6.0.** State it plainly — a project below 0.5.0 must reach 0.5.0 first (see
    `<merge_safety>` finding 5; do not widen the floor to work around the update
    skill's chain defect, which is deferred).

    **Explicitly state the config-destination divergence from 0007** and why:
    `knowledge_capture` went to the host-neutral `.planning/config.json` because both
    hosts must read the same block; `pre_execution` is host-scoped like the other 15
    gates and goes to `.planning/config.codex.json` (ADR-0007 point 5). A reader
    coming from 0007 will otherwise assume the same destination.

    **`## Pre-flight`** — mirror 0007's shape (its lines 49-73), with **one
    deliberate divergence: the version floor**.
    - git repo check
    - `jq` availability
    - **version floor: `grep -qE '^0\.(5|6)\.0$' .codex/workflow-version.txt`** —
      NOT 0007's `grep -qE '^version: 0\.(4|5)\.0$' skills/agentic-apps-workflow/SKILL.md`.
      Keep the `(5|6)` alternation; that is 0007's re-apply tolerance and it stays.
      **Comment the divergence and why, at the check** (`<target_project_surface>`):
      a target project has no local `skills/` tree — the setup skill never creates
      one — so 0007's floor grep hits a non-existent path and aborts with exit 3 on
      every real install. `.codex/workflow-version.txt` is the durable per-project
      version record and is what the update skill itself reads (its Stage A step 1).
      A reader coming from 0007 will otherwise "fix" this back to the SKILL.md form;
      the comment is what stops them. Keep 0007's abort-code convention and its
      message shape, naming the supported floor.
    - required templates present at the **installed** `$CODEX/skills/...` path, not
      repo-relative: `config-hooks.json` and `agents-md-additions.md`
    - **the verifier itself must ship before the migration that wires it**: check
      `$CODEX/skills/agentic-apps-workflow/scripts/check-plan-review.sh` exists. A
      migration that binds a `verifier` path to a script that is not installed
      produces a config pointing at nothing — a gate that silently never fires.

    **Step 1 — merge `pre_execution.plan_review` into `.planning/config.codex.json`.**
    Idempotency check, pre-condition, apply, rollback — use the **corrected jq shapes
    in `<interfaces>` verbatim**, and read `<merge_safety>` before writing this step.
    Three things the shape must get right, each with a comment naming why, because
    all three are silent when wrong:
    - the idempotency check tests `.hooks.pre_execution.plan_review` (the **leaf**),
      not `.hooks.pre_execution` — a group-level check skips the migration on an
      install that has a sibling pre-execution gate but not ours
    - the apply is `.hooks.pre_execution = ((.hooks.pre_execution // {}) + $pe)` — a
      **leaf-level deep merge**. Do NOT write `.hooks += {pre_execution: $pe}`: that
      preserves `.hooks`'s other groups but **replaces the whole `pre_execution`
      object**, deleting any sibling gate inside it. Today there is no sibling; the
      day core spec adds one, the shallow form deletes it on every install.
    - the rollback is `del(.hooks.pre_execution.plan_review)` followed by dropping
      the parent only if it is then empty. `del(.hooks.pre_execution)` is destructive
      to siblings for the same reason.
    Source `$pe` from the installed template via `--argjson`, never a heredoc'd
    literal.

    **Step 2 — insert the ritual section into `AGENTS.md`.** Copy 0007's Step 2
    mechanism **exactly** (extract-to-tempfile with awk, insert with
    getline-from-file), changing only the heading regex to the shipped heading from
    `08-04-SUMMARY.md`. **Carry 0007's portability comment forward verbatim** —
    "getline-from-file is portable (BSD/macOS awk rejects a multi-line -v
    assignment)". It is the reason the pattern exists; without it a later editor
    will "simplify" it to `awk -v` and break every macOS install. Escape the `§` and
    parens in the regex as 0007 does. Rollback: `git checkout -- AGENTS.md` plus a
    manual anchor.

    **Step 3 — record `0.6.0` in `.codex/workflow-version.txt`.** Idempotency
    check: the file already reads `0.6.0`. This is the project's durable version
    record and the last step, per 0007's content-steps-then-version-seal
    convention.

    **There is NO step that bumps `skills/agentic-apps-workflow/SKILL.md`, and you
    must not add one** (`<target_project_surface>`). 0007's Step 3 does exactly
    that and it is a bug: no target project has a local `skills/` tree, so that
    step — and 0007's pre-flight grep on the same path — abort on every real
    install. This repo's own scaffolder bump still happens, below, as a **direct
    edit to this repo's file in this commit**, which is what the `run_drift_test`
    coupling actually requires. Do not confuse the two — `<interfaces>` splits them
    explicitly. **Add a `## Notes` entry recording the divergence from 0007 and its
    reason**, so the next migration author mirrors the corrected shape rather than
    the bug.

    Number the steps 1-3 as written here, and **use 0007's exact heading shape**:
    a `## Steps` (h2) wrapper containing `### Step N: Title` (h3) headings. That shape
    is load-bearing, not style — plan `08-06`'s renumber and this plan's own step-count
    criterion both key on `^### Step [0-9]`. Plan `08-06` inserts the bindings-table
    step as Step 3 and renumbers this plan's Step 3 to Step 4, reaching the final
    4-step shape (`<plan_split_note>`). Do not leave a gap, a placeholder, or a
    "TODO: table step" marker for it.

    **`## Post-checks`** — mirror 0007's: jq assertion that
    `.hooks.pre_execution.plan_review.min_reviewers == 2`; assertion that the four
    pre-existing groups survive; assertion that any pre-existing sibling under
    `.hooks.pre_execution` survives; `grep -q` for the ritual heading; a
    version-record grep on `.codex/workflow-version.txt` **only** — there is no
    target-project SKILL.md to grep (`<target_project_surface>`), which is a
    deliberate divergence from 0007's post-check; the drift-test note.

    **`## Skip cases`** — 0007's shape, and **every skip is STEP-LOCAL**
    (`<merge_safety>` finding 6). Mirror 0007's own wording, which states this
    explicitly for both of its content steps:
    - Step 1 skips when `.hooks.pre_execution.plan_review` is already present; the
      existing block is preserved verbatim and **Steps 2 and 3 still run**.
    - Step 2 skips when the ritual heading is already present (a fresh install got
      it from the template); **Steps 1 and 3 still run**.
    - Step 3 skips when `.codex/workflow-version.txt` already reads `0.6.0`.
    **Do NOT write a whole-migration skip.** An earlier revision did — "the whole
    migration skips when `.hooks.pre_execution.plan_review` is already present" —
    and it breaks the atomicity contract's own recovery path
    (`migrations/README.md:103-113`): an operator who chose *skip-with-warning* on
    a failed Step 2 has a `partial` install whose Step 1 IS applied, and re-running
    to finish is the documented remedy. A whole-migration skip keyed on Step 1's
    artifact makes that re-run a no-op that reports success, stranding the install
    half-migrated while claiming it is current. There is no migration-level skip
    predicate at all — delete the idea, do not narrow it.

    **`## Compatibility` / `## Notes` / `## References`** — copy the document
    structure wholesale. Compatibility must state the 0.5.0 floor. Notes must carry
    0007's "Testable non-interactively via `test_migration_0008`" convention.
    References: core spec `02-hook-taxonomy.md`, this repo's
    `docs/decisions/0009-plan-review-gate.md`, and the sibling precedent —
    claude-workflow's ADR-0025 / migration-0016.

    **Then bump THIS REPO for real, in lockstep, in THIS commit — as direct file
    edits, not as migration steps** (`<interfaces>` and `<target_project_surface>`
    explain the distinction; it is the crux of round 2's HIGH):
    `skills/agentic-apps-workflow/SKILL.md` `version: 0.5.0` → `0.6.0` (this repo's
    scaffolder, which `install.sh` publishes to `${CODEX_HOME}` and which the update
    skill reads as the target version), and `.codex/workflow-version.txt` → `0.6.0`
    (this repo's own project record, which Step 3 maintains for everyone else).
    Do NOT touch `implements_spec: 0.4.0`.
    The drift test goes RED the moment 0008 exists and green only once SKILL.md
    matches its `to_version` — verified against the real `run_drift_test` during
    planning (`<plan_split_note>`). They must land in the same commit or this
    plan ships a red harness.

    Commit as `feat(GREEN): migration 0008 — bind plan-review gate (0.5.0 -> 0.6.0)`.
  </action>

  <verify>
    <automated>bash migrations/run-tests.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash migrations/run-tests.sh 0008` exits 0
    - `bash migrations/run-tests.sh drift` exits 0 (SKILL.md 0.6.0 == 0008 to_version 0.6.0) — the lockstep assertion
    - `bash migrations/run-tests.sh layout` exits 0 (all four new paths ship)
    - **`bash migrations/run-tests.sh` (FULL suite) exits 0 and reports `FAIL: 0`.** This plan does not close ROADMAP criterion #7 (plan `08-06` does, once the table step is covered), but it must not ship a red harness either — every plan boundary in this phase is green.
    - `grep -q '^from_version: 0.5.0$' migrations/0008-plan-review-gate.md` and `grep -q '^to_version: 0.6.0$' migrations/0008-plan-review-gate.md` both succeed
    - **This repo's scaffolder was bumped by a direct edit** (not by a migration step): `grep -q '^version: 0.6.0$' skills/agentic-apps-workflow/SKILL.md` succeeds
    - `grep -q '^implements_spec: 0.4.0$' skills/agentic-apps-workflow/SKILL.md` succeeds (D-17 — asserted on the REAL repo file, which is where the bump happens; there is no synthetic copy to assert against any more)
    - `grep -q '^0.6.0$' .codex/workflow-version.txt` succeeds
    - **The migration and the bump are in ONE commit** (the drift coupling): `git show --stat --name-only HEAD | grep -c '0008-plan-review-gate.md'` returns 1 AND the same stream returns 1 for `skills/agentic-apps-workflow/SKILL.md`
    - **The merge is leaf-level, not shallow** (`<merge_safety>` finding 2): `grep -c 'hooks.pre_execution = ' migrations/0008-plan-review-gate.md` returns >= 1 AND `grep -c 'hooks += ' migrations/0008-plan-review-gate.md` returns 0
    - **The idempotency check is on the leaf** (finding 1): `grep -cE "jq -e '\.hooks\.pre_execution\.plan_review'" migrations/0008-plan-review-gate.md` returns >= 1
    - **The rollback is not destructive to siblings, asserted STRUCTURALLY** (finding 3; 08-REVIEWS.md round 2, OpenCode, MEDIUM). The round-1 criterion ended "verify by reading the rollback block" — a human-read check, which is not an acceptance criterion at all: `grep -cE "del\(\.hooks\.pre_execution\)[^.]"` catches only the non-leaf form, and if a future editor moved the bare `del(.hooks.pre_execution)` above the conditional (exactly the kind of thing `08-06`'s renumber touches), the criteria would still pass mechanically. Make it structural — the bare form must appear ONLY when immediately preceded by the empty-parent conditional:
      - `grep -c 'del(.hooks.pre_execution.plan_review)' migrations/0008-plan-review-gate.md` returns >= 1 (the leaf form is present)
      - Every bare occurrence is guarded, checked with awk rather than by eye:
        `awk '/del\(\.hooks\.pre_execution\)/{ if (prev !~ /== *\{\}/ && prev !~ /if .*empty/ && $0 !~ /== *\{\}/) bad++ } {prev=$0} END{print bad+0}' migrations/0008-plan-review-gate.md` prints `0` — i.e. no bare `del(.hooks.pre_execution)` appears except on, or immediately after, the `if (.hooks.pre_execution // {}) == {} then` guard line
      - The guard itself is present: `grep -cE 'if \(\.hooks\.pre_execution // \{\}\) == \{\}' migrations/0008-plan-review-gate.md` returns >= 1
    - **The step count is 3, numbered 1-3** (plan `08-06` makes it 4). Note 0007's real heading shape is `### Step N: Title` (h3) under a `## Steps` (h2) wrapper — mirror it, and key the check on it: `grep -cE '^### Step [0-9]' migrations/0008-plan-review-gate.md` returns 3, and `grep -c '^### Step 4' migrations/0008-plan-review-gate.md` returns 0
      (It is 3 rather than 0007's 4 because there is no target-project SKILL.md step — `<target_project_surface>`. If you find yourself adding a fourth to match 0007, you are re-introducing the bug.)
    - **The migration never touches a target project's `skills/` tree** — round 2's HIGH, and the single most important criterion in this plan (`<target_project_surface>`): `grep -c 'skills/agentic-apps-workflow' migrations/0008-plan-review-gate.md` returns 0 **outside the `## Notes` divergence record and the `$CODEX/`-rooted pre-flight template checks**. Verify precisely: `awk '/^## Notes/{f=1} /^## References/{f=0} !f' migrations/0008-plan-review-gate.md | grep -v 'CODEX' | grep -c 'skills/agentic-apps-workflow'` returns 0
    - **The version floor reads the project's record, not a scaffolder file**: `awk '/^## Pre-flight/{f=1} /^## Steps/{f=0} f' migrations/0008-plan-review-gate.md | grep -c 'workflow-version.txt'` returns >= 1, AND the same stream returns 0 for `grep -c "grep -qE '\^version: "` (0007's SKILL.md floor form, which aborts on every real install)
    - **The divergence from 0007 is recorded for the next author**: `awk '/^## Notes/{f=1} /^## References/{f=0} f' migrations/0008-plan-review-gate.md | grep -ci '0007'` returns >= 1 and the same stream returns >= 1 for `grep -ci 'skills/\|scaffolder'`
    - **Skips are step-local; no whole-migration skip predicate exists** (`<merge_safety>` finding 6): `awk '/^## Skip cases/{f=1} /^## /{f=0} f' migrations/0008-plan-review-gate.md | grep -ci 'whole migration\|entire migration'` returns 0, AND the same stream names each step's own skip — `grep -cE 'Step [123]'` returns >= 3
    - **No placeholder was left for the table step** (`<plan_split_note>`): `grep -ci 'TODO\|placeholder\|to be added\|coming in' migrations/0008-plan-review-gate.md` returns 0
    - D-19 heredoc prohibition held: `grep -c 'agents-md-additions.md' migrations/0008-plan-review-gate.md` returns >= 1, and the migration contains no inline copy of the ritual section (`grep -c '^## Pre-execution Gate — Plan Review' migrations/0008-plan-review-gate.md` returns 0)
    - The portability comment survives: `grep -ci 'BSD/macOS awk\|getline-from-file' migrations/0008-plan-review-gate.md` returns >= 1
    - The config target is the host-scoped file: `grep -c 'config.codex.json' migrations/0008-plan-review-gate.md` returns >= 1
    - The supported floor is stated: `awk '/^## Compatibility/{f=1;next} /^## /{f=0} f' migrations/0008-plan-review-gate.md | grep -c '0.5.0'` returns >= 1
    - **The chain is contiguous and unique** (08-REVIEWS.md, Codex, LOW — an exact count is brittle and fails on an unrelated valid migration without indicating a defect; replaces the old `wc -l == 9`):
      `ls migrations/[0-9][0-9][0-9][0-9]-*.md | sed -E 's#.*/([0-9]{4})-.*#\1#' | sort` produces a list with no duplicates (`| uniq -d` is empty) and no gaps (each entry equals its predecessor plus one), and `0008` appears in it
    - Two commits exist for this plan matching `^test(RED):` then `^feat(GREEN):`, in that order
  </acceptance_criteria>

  <done>
    `migrations/0008-plan-review-gate.md` mirrors 0007's structure — pre-flight
    guards (including the verifier-must-ship check), idempotent steps with
    rollbacks, post-checks, skip cases, compatibility, notes, references — with
    **three** steps rather than 0007's four, and two deliberate, commented
    divergences from it: the version floor reads `.codex/workflow-version.txt`
    rather than a scaffolder file no target project has, and there is no
    target-project SKILL.md step at all. The config merge targets
    `.planning/config.codex.json` at the **leaf**
    (`.hooks.pre_execution.plan_review`) with a deep merge that preserves sibling
    gates and a rollback that removes only our leaf; the ritual section is extracted
    from the template, never heredoc'd; every skip is step-local, with no
    migration-level skip predicate. This repo's own scaffolder moved to 0.6.0 by a
    direct edit in the same commit as the migration, keeping `run_drift_test` green,
    while `implements_spec` is untouched at 0.4.0. The FULL harness is green with
    `FAIL: 0`. The bindings-table step and the CHANGELOG are plan `08-06`'s.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → every downstream install | Migration 0008 executes against other people's repos via `$update-codex-agenticapps-workflow`, editing their `AGENTS.md` and config. It is a supply-chain write surface. |
| installed template → migration | The migration reads `$CODEX/skills/.../templates/*` and writes their content into a target repo. |
| target repo config → migration | The migration's `jq` merge reads and rewrites a config file it did not author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-22 | Tampering | `jq` merge into a target repo's `.planning/config.codex.json` | mitigate | **Revised after review (08-REVIEWS.md, Codex, 3× HIGH — see `<merge_safety>`).** Three distinct destructive failures, all silent: (1) `.hooks = {...}` where `.hooks += {...}` was meant deletes all four gate groups — the original threat, still guarded by the different-group fixture and the foreign top-level key (0007's `"host": "claude"` trick). (2) `.hooks += {pre_execution: $pe}` preserves other *groups* but **replaces the whole `pre_execution` object**, deleting sibling gates inside it — invisible today (one member) and destructive the day core spec adds a second pre-execution gate. Mitigation: leaf-level deep merge, `.hooks.pre_execution = ((.hooks.pre_execution // {}) + $pe)`. (3) `del(.hooks.pre_execution)` on rollback is destructive for the same reason. Mitigation: `del(.hooks.pre_execution.plan_review)`, dropping the parent only if it is then empty. The controls that make (2) and (3) *catchable*: Task 1's fixture carries `.hooks.pre_execution.other_gate` and asserts it survives BOTH apply and rollback — the original fixture guarded only other top-level groups and could not have caught either. The idempotency guard also moves to the **leaf**: a group-level check skips the migration entirely on an install that has a sibling gate but not ours, leaving it unbound while reporting success. |
| T-08-23 | Tampering | AGENTS.md awk insert in a target repo | mitigate | A heading-regex mismatch between the migration and the template makes the extraction produce an empty file, the insert a **silent no-op**, and the install non-conformant while the migration reports success. Controls: extract from the REAL installed template (never a heredoc — D-19), take the heading from `08-04-SUMMARY.md` rather than retyping it, and Task 1 asserts the extraction is non-empty *before* asserting the insert, so drift reports as itself. (Plan `08-06` carries the same class of control for the table step's header regex, under T-08-34.) |
| T-08-24 | Denial of service | `awk -v` with a multi-line value on macOS/BSD | mitigate | BSD/macOS awk rejects a multi-line `-v` assignment — a "simplification" to `awk -v var="$(cat file)"` breaks the migration on every macOS install, which is this repo's primary platform. The getline-from-file mechanism exists solely to avoid it. Port 0007's portability comment verbatim so the constraint travels with the code; acceptance criteria assert the comment survives. Same class: the checksum command is pinned to `cksum` (POSIX, identical on macOS and Linux) rather than `sha256sum` (absent on macOS) or `md5sum` (named `md5` there) — 08-REVIEWS.md, Codex, LOW. |
| T-08-25 | Elevation of privilege | migration binds a `verifier` path | mitigate | If 0008 writes a config naming a verifier that is not installed, the target repo gets a gate that silently never fires — worse than no gate, since the config asserts coverage that does not exist. Pre-flight therefore requires `$CODEX/skills/agentic-apps-workflow/scripts/check-plan-review.sh` to exist before any step runs, and aborts with a distinct exit code otherwise (0007's pre-flight abort-code convention). |
| T-08-26 | Tampering | `mktemp` tempfile for the section extract | accept | The extract-to-tempfile step uses `mktemp` (which creates with a safe mode and an unpredictable name) and `rm -f`s it after. A local symlink race is theoretically possible but requires a local attacker already able to write the operator's `$TMPDIR`, who could edit the repo directly. Matches the 0007 precedent; no deviation warranted. |
| T-08-27 | Repudiation | false conformance claim via the version bump | mitigate | The version bump is exactly where a `implements_spec` bump gets made by reflex — the two fields sit adjacent in SKILL.md's frontmatter and both look like "the version". Claiming `implements_spec: 0.5.0` on the strength of one gate would misrepresent conformance to every downstream consumer reading that field, since it denotes a full audit (CHANGELOG:88-91). D-17 forbids the bump. **Round 2 moved where this is asserted**: there is no synthetic `SKILL.md` copy any more (T-08-38 — the synthetic fixture was the blind spot, not a control), so Task 2 asserts `implements_spec: 0.4.0` survives in the **real repo file**, which is the only place the bump now happens. (The CHANGELOG's release-altitude claims are plan `08-06`'s T-08-35.) |
| T-08-38 | Denial of service | migration steps naming a path no target project has | mitigate | **Added after round 2 (08-REVIEWS.md, Codex, HIGH), and the most consequential threat in this plan.** An earlier revision put `skills/agentic-apps-workflow/SKILL.md` in 0008's `applies_to`, its pre-flight version floor, and a runtime step. **No target project has that file** — the setup skill's project-side surface is `AGENTS.md`, `.planning/`, `.codex/`, and `docs/decisions/` (`setup-codex-agenticapps-workflow/SKILL.md:8-9`, post-checks at 103-109), and the update skill reads the scaffolder from `${CODEX_HOME}` (`update-.../SKILL.md:41-44`) while executing steps in the project (Stage D/E). So the pre-flight grep hits nothing and the migration aborts with exit 3 before any step runs: **every downstream install fails to upgrade, and the gate this phase exists to ship never reaches the fleet.** The harness cannot see it — `test_migration_0008` would create a synthetic `SKILL.md` and sed *that*, exactly as `test_migration_0007` does at `run-tests.sh:729`, manufacturing the file whose absence is the bug. Green harness, broken product. **This repo has already shipped this exact failure twice**: both `## [Unreleased]` CHANGELOG entries are the same shape and both say so — paths that "only resolved from the scaffolder checkout", latent because "the repo dogfoods via direct edits and `run-tests.sh` synthetic fixtures, so it went unnoticed". Mitigation: 0008 names no path under `skills/` in any target project; the version floor reads `.codex/workflow-version.txt`, the durable per-project record the update skill itself uses; this repo's scaffolder bump is a direct edit in the GREEN commit (satisfying the real `run_drift_test` coupling) rather than a step shipped to others; and Task 1 carries a **no-scaffolder-tree fixture** — a sandbox shaped like a real install with no `skills/` directory — which is the assertion that makes the correction durable. Migration 0007 carries the identical bug and is **not** fixed here: different migration, own scope, recorded as deferred and in ADR-0009's follow-ups. |
| T-08-39 | Repudiation | whole-migration skip marking a partial install current | mitigate | **Added after round 2 (08-REVIEWS.md, Codex, HIGH).** An earlier revision's `## Skip cases` skipped the **whole migration** when `.hooks.pre_execution.plan_review` was present. The atomicity contract (`migrations/README.md:103-113`) lets an operator choose *skip-with-warning* on a failed step, recording the migration `partial` and continuing — so an install can legitimately have Step 1 applied and Step 2 not, and re-running is the documented recovery. A whole-migration skip keyed on Step 1's artifact turns that recovery into a no-op **that reports success**: the install stays half-migrated — config bound, ritual absent — and records itself current. A gate that is bound in config but has no ritual instructing anyone to run it is precisely the silent non-enforcement of ADR-0018, arrived at through the repair path. It also contradicts 0007, which this plan mirrors and which correctly makes only the *corresponding step* a no-op. Mitigation: every step owns its own idempotency check, no step's check gates another, and no migration-level skip predicate exists at all. Task 1's partial-application fixture (Step 1 applied, Step 2 not → Steps 2 and 3 still run) is the regression guard, and the acceptance criteria assert the `## Skip cases` section names each step's skip rather than a whole-migration one. |
| T-08-SC | Tampering | supply chain / package installs | accept | This plan installs zero packages — one markdown migration, one bash harness edit, two version-string bumps. No `npm`/`pip`/`cargo` surface exists, so the Package Legitimacy Gate does not apply. The migration itself is a supply-chain *write* surface, covered by T-08-22 through T-08-25. |
</threat_model>

<verification>
1. `bash migrations/run-tests.sh` → FULL suite exits 0, `FAIL: 0`. Not criterion #7 (that needs `08-06`'s table coverage), but a green boundary.
2. `bash migrations/run-tests.sh 0008` → exits 0, including the `cksum`-based second-run no-op assertions and the sibling-gate survival assertions (apply and rollback)
3. `bash migrations/run-tests.sh drift` → exits 0 (SKILL.md 0.6.0 == 0008 to_version) — the lockstep held
4. `bash migrations/run-tests.sh layout` → exits 0 (all four new artifacts ship)
5. D-17 held: `grep -q '^implements_spec: 0.4.0$' skills/agentic-apps-workflow/SKILL.md` succeeds
6. D-19 held, and held *correctly*: the migration contains no inline copy of the ritual section, its portability comment survives, and its merge preserves existing keys at the leaf — which is what D-19 asked for and the original shallow form did not deliver (`<merge_safety>`)
7. The migration chain is contiguous and unique (no exact-count assertion — see Task 2's criteria)
8. Git history: RED commit strictly before GREEN commit; the version bump is in the GREEN commit with the migration
9. The deferred item stayed deferred: no chain fixtures, no edit to `skills/update-codex-agenticapps-workflow/` — `git diff --name-only | grep -c 'update-codex-agenticapps-workflow'` returns 0
10. The migration is 3 steps, and no placeholder was left for `08-06`'s table step
11. **T-08-38 held — the migration never names a target-project `skills/` path**: it is absent from `applies_to`, from the pre-flight floor (which reads `.codex/workflow-version.txt`), and from every step. The no-scaffolder-tree fixture passes: a sandbox with no `skills/` directory migrates end to end.
12. T-08-39 held — every skip is step-local; `## Skip cases` names no whole-migration skip, and the partial-application fixture (Step 1 applied, Step 2 not) shows Steps 2 and 3 still run
13. Migration 0007's identical scaffolder-path bug stayed **deferred**, not fixed: `git diff --name-only | grep -c '0007-knowledge-capture.md'` returns 0

**This plan does not close the phase.** Plan `08-06` adds the bindings-table step
and the CHANGELOG and owns ROADMAP success criterion #7. Do not write a phase-close
summary here.
</verification>

<success_criteria>
Contributes to ROADMAP.md Phase 8 success criterion **7**, which plan `08-06`
closes:

- (7) `migrations/run-tests.sh` passes, including a `test_migration_0008` that is a
  no-op on second run.

This plan delivers `test_migration_0008` and its steps 1-3 coverage with the full
harness green (`FAIL: 0`), including drift and layout. Criterion 7 is not claimed
complete until `08-06` extends the same test with the bindings-table step's
coverage — a migrated install that gets the ritual but not the table has not
reached the same bound state as a fresh one (D-19/D-20, T-08-34).
</success_criteria>

<output>
Create `.planning/phases/08-plan-review-gate/08-05-SUMMARY.md` when done.

Record in it: the final harness counts (PASS/FAIL/SKIP), the version state as
shipped (this repo's SKILL.md, workflow-version.txt, `implements_spec`), the jq
merge and rollback expressions as landed, the exact ritual heading regex used (plan
`08-06` needs the same discipline for the table header), the step numbering as
written (**1-3**, for `08-06` to renumber to 1-4), the pre-flight version-floor
check as landed, and any deferred item that surfaced during execution.

**Record the two deferred items this plan's round-2 corrections surfaced** — they
belong in 08-CONTEXT.md's Deferred Ideas and in ADR-0009's follow-ups, and this
summary is where they are handed off:

1. **Migration 0007 aborts in any target project without a local `skills/` tree.**
   Its `applies_to`, its pre-flight floor grep, and its Step 3 all name
   `skills/agentic-apps-workflow/SKILL.md`, which the setup skill never creates in
   a project. Same class as the two Unreleased migration-discovery fixes — and
   invisible for the same reason: `test_migration_0007` manufactures a synthetic
   `SKILL.md` at `run-tests.sh:729` and seds that. Found while planning 0008
   (08-REVIEWS.md round 2, Codex, HIGH, against this plan). **Real defect, different
   migration, own scope** — fixing it means re-testing a shipped upgrade path and
   deciding whether 0007 is re-run or its floor is rewritten in place. 0008 does not
   replicate it (`<target_project_surface>`); 0008 does not repair it either.
2. **`test_migration_0007`'s synthetic-`SKILL.md` fixture hides item 1** and should
   be replaced by a no-scaffolder-tree fixture of the shape this plan's Task 1
   introduces, whenever item 1 is taken up.
</output>
</content>
