---
phase: 12-path-safety-review-debt
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - migrations/validate-0009-anchor.sh
  - migrations/run-tests.sh
autonomous: true
requirements: [REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "A full-script grep of validate-0009-anchor.sh stdout finds zero mirror-derived values (no `wc -l < $MIRROR` banner count, no mirror-derived line numbers in CASE 2 PASS text)"
    - "A re-vendor of the mirror (line count change) does not alter validate-0009-anchor.sh's passing-run stdout"
    - "extract_step_block extracts `### Step 1` content without capturing `### Step 10`+ content, verified against a synthetic 10+-step document"
    - "CASE 1 asserts the strip produced strictly fewer lines than its input (no hardcoded line number) between strip and insert"
    - "Each of the three fixes is independently mutation-proven: break it -> RED, restore -> GREEN"
  artifacts:
    - path: "migrations/validate-0009-anchor.sh"
      provides: "Deterministic stdout (mirror-derived values removed) + CASE 1 strictly-smaller-count assertion"
      contains: "wc -l"
    - path: "migrations/run-tests.sh"
      provides: "REV-01 determinism grep test + REV-02 extract_step_block delimiter fix + synthetic 10+-step fixture"
      contains: "extract_step_block"
  key_links:
    - from: "run-tests.sh REV-01 determinism test"
      to: "migrations/validate-0009-anchor.sh stdout"
      via: "run the validator, grep full stdout for mirror-derived values"
      pattern: "validate-0009-anchor"
    - from: "extract_step_block"
      to: "### Step N delimiter"
      via: "delimiter-aware prefix match (colon / space / EOL after the digit)"
      pattern: "extract_step_block"
---

<objective>
Close three independently-scoped 09-REVIEW.md defects, each with its own
mutation-proven assertion — never batched into one undifferentiated cleanup:

- REV-01 (WR-05): make `validate-0009-anchor.sh`'s stdout genuinely deterministic
  by REMOVING mirror-derived values (not rewording the claim), proven by a
  full-script grep test in run-tests.sh.
- REV-02 (IN-01): fix `extract_step_block` so `### Step 1` no longer prefix-matches
  `### Step 10`+, proven by a synthetic 10+-step fixture.
- REV-03 (IN-02): add a strictly-smaller-count assertion to CASE 1's line drop
  (no hardcoded number), mutation-proven.

Purpose: these are the review-debt defects the milestone's "observed failing"
standard demands each be proven against — the verifier re-runs the RED→GREEN
cycle per fix, not trusting the executor's claim.

Output: deterministic validator stdout + delimiter-safe extractor + asserted
line drop, each with its own independent proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-path-safety-review-debt/12-CONTEXT.md
@.planning/phases/12-path-safety-review-debt/12-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md

<interfaces>
<!-- extract_step_block (run-tests.sh:123) uses index($0, stepp)==1 where stepp="### Step N"
     — a bare-prefix match that also matches "### Step 10"+. Fix: require the char after the
     digit to be a delimiter (:, space, or EOL) via substr/literal compare, NOT a compiled
     regex (preserve the no-escaping property justified at :80-91). -->
<!-- validate-0009-anchor.sh: pass()/fail() helpers at :52-53; banner at :241;
     CASE 1 strip:253 / insert:254 (insert assertion BETWEEN them); CASE 2 PASS text at :292. -->
<!-- No existing validate-0009-anchor.sh invocation in run-tests.sh — REV-01's determinism
     test is a NEW test function (test_validate_0009_anchor_determinism), following the
     test_migration_NNNN naming convention, registered near test_migration_0009 (:5091). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REV-01 — remove mirror-derived stdout from validate-0009-anchor.sh + add mutation-proven determinism grep test</name>
  <files>migrations/validate-0009-anchor.sh, migrations/run-tests.sh</files>
  <read_first>
    - migrations/validate-0009-anchor.sh:231-241 (the deterministic-banner comment + the `$(wc -l < "$MIRROR")` banner count at :241)
    - migrations/validate-0009-anchor.sh:275-292 (CASE 2 computes c2_start/c2_prov and prints them in the PASS text at :292 — mirror-derived line numbers)
    - migrations/validate-0009-anchor.sh:52-53 (pass/fail helpers)
    - migrations/run-tests.sh:3596 (test_migration_0009) and :5091 (runner registration list)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-11
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md REV-01 guidance (:172-197, :417-426)
  </read_first>
  <action>
    In `validate-0009-anchor.sh`: REMOVE the mirror-derived values from stdout (D-11 fix-option
    (a), not (b) reword). Strip the `$(wc -l < "$MIRROR" | tr -d ' ')` clause (and its "(N lines)"
    text) from the banner at `:241`. In CASE 2's PASS message (`:292`), remove the derived line-
    number references (`at line $c2_prov`, `at line $c2_start`) from the emitted text while KEEPING
    the relational assertions that compare those numbers (`[ "$c2_prov" -ge "$c2_start" ]` etc. at
    `:285-288` compare, not print — leave them). The passing-run stdout must carry no value tied to
    the mirror's line count. Also reconcile the "deliberately DETERMINISTIC" comment (`:231-240`)
    so it no longer over-claims about content that no longer prints.

    In `run-tests.sh`: add a new `test_validate_0009_anchor_determinism` function (register it in
    the runner list next to `test_migration_0009`, ~:5091) that runs
    `bash migrations/validate-0009-anchor.sh`, captures full stdout, and asserts (grep) the ABSENCE
    of every mirror-derived value — no `wc -l`-derived count, no mirror-length-derived line number
    in any PASS line. Make it mutation-proven: the test must be demonstrated RED when a
    `$(wc -l < "$MIRROR")`-style value is temporarily reintroduced into stdout, GREEN after removal.
    Use the file's existing PASS/FAIL counter idiom.
  </action>
  <verify>
    <automated>bash migrations/validate-0009-anchor.sh 2>/dev/null | grep -Eq "\([0-9]+ lines\)|at line [0-9]+" && exit 1; bash migrations/run-tests.sh 2>&1 | grep -i "determinism" | grep -iq pass</automated>
  </verify>
  <acceptance_criteria>
    - `bash migrations/validate-0009-anchor.sh | grep -Ec "\([0-9]+ lines\)|gitnexus:start at line [0-9]+|at line [0-9]+"` == 0 (no mirror-derived value survives in stdout).
    - `grep -c 'wc -l < "\$MIRROR"' migrations/validate-0009-anchor.sh` == 0 in stdout-emitting (`echo`/`printf`/`pass`) lines.
    - The relational assertions (`[ "$c2_prov" -ge "$c2_start" ]` and siblings) remain present — comparisons kept, only emitted numbers removed.
    - `test_validate_0009_anchor_determinism` is registered and PASSES; it is observed RED when a `wc -l`-derived value is temporarily reintroduced to stdout, GREEN after removal (verifier re-runs).
  </acceptance_criteria>
  <done>All mirror-derived values removed from validate-0009-anchor.sh stdout (banner count + CASE 2 PASS line numbers); relational comparisons retained; a new mutation-proven determinism grep test in run-tests.sh passes and is proven to catch a reintroduced non-deterministic value.</done>
</task>

<task type="auto">
  <name>Task 2: REV-02 — delimiter-aware extract_step_block fix + synthetic 10+-step proof fixture</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh:100-146 (extract_step_block def + the :80-91 header comment stating the LITERAL-PREFIX no-escaping invariant to preserve)
    - migrations/run-tests.sh:1922, :3621-3622, :4871-4873 (existing extract_step_block callers — must not break their Apply-block extraction)
    - migrations/validate-0009-anchor.sh:196-218 (synth_region_led — the printf-into-$tmp fixture idiom to follow)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-12
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md REV-02 guidance (:374-415)
  </read_first>
  <action>
    Fix `extract_step_block` (`:123`) so the `index($0, stepp) == 1` prefix test no longer matches
    a bare numeric continuation: require the character immediately AFTER `stepp` (`"### Step N"`)
    to be one of the valid delimiters — `:` (this repo's `### Step 1:`), ` ` (space, covering the
    ` —`/` -` upstream dash form and trailing-space/EOL), or end-of-line. Do this via `substr`/
    literal character comparison on the matched line, NOT by building a compiled regex from
    interpolated input — the header comment (`:80-91`) locks the "literal prefix, never an
    interpolated regex" no-escaping property; preserve it. Apply the same delimiter guard to the
    `nextp` (`"### Step N+1"`) boundary test so the end-of-block detection is equally precise.

    Add a synthetic proof fixture (new content — no existing 10-step fixture) using the
    printf-into-`$tmp` idiom (no static fixture files — D-34 locks this): write `### Step 1:` …
    `### Step 10:` … blocks (each with an `**Apply:**` label body) in one printf/heredoc sequence,
    then assert `extract_step_block(doc, 1, Apply)` returns Step 1's body and does NOT contain
    Step 10's body. Mutation-proof: this fixture is RED under the old bare-prefix match (`### Step 1`
    swallows `### Step 10`'s header), GREEN under the delimiter fix.
  </action>
  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | grep -Ei "step 10|step_block|10.?step|delimiter" | grep -iv fail | grep -qi pass</automated>
  </verify>
  <acceptance_criteria>
    - The synthetic 10+-step fixture asserts `extract_step_block(doc,1,Apply)` yields Step 1's body with zero bytes of Step 10's body; it is observed RED against the pre-fix `extract_step_block` and GREEN after (verifier re-runs).
    - `grep -c "index(\$0, stepp)" migrations/run-tests.sh` still present but now paired with a delimiter/`substr` guard; no compiled-regex-from-interpolation construct (`"/^" stepp`) is introduced (no-escaping property preserved).
    - The pre-existing `extract_step_block` callers (run-tests.sh:1922, :3621-3622, :4871-4873) still extract their Apply blocks — full `bash migrations/run-tests.sh` reports 0 FAIL in migration_0008/0009/0010 suites.
  </acceptance_criteria>
  <done>extract_step_block requires a delimiter (`:`, space, or EOL) after the step digit on both `stepp` and `nextp`, preserving the literal-prefix no-escaping property; a synthetic 10+-step fixture proves `### Step 1` no longer matches `### Step 10`, mutation-proven RED→GREEN; all existing callers still pass.</done>
</task>

<task type="auto">
  <name>Task 3: REV-03 — strictly-smaller-count line-drop assertion in CASE 1</name>
  <files>migrations/validate-0009-anchor.sh</files>
  <read_first>
    - migrations/validate-0009-anchor.sh:249-264 (CASE 1: strip at :253, insert at :254 — insert the assertion BETWEEN them)
    - migrations/validate-0009-anchor.sh:52-53 (pass/fail helpers — use the existing `[ ... ] && pass ... || fail ...` idiom)
    - migrations/validate-0009-anchor.sh:281-293, :310-318 (existing pass/fail assertion style to mirror)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-13
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md REV-03 guidance (:199-252)
  </read_first>
  <action>
    Between `candidate_strip "$tmp/case1-input.md" > "$tmp/case1.strip"` (`:253`) and
    `candidate_insert "$tmp/case1.strip" > "$tmp/case1.out"` (`:254`), add a strictly-smaller-count
    assertion comparing `wc -l` of `$tmp/case1.strip` against `$tmp/case1-input.md` with the `-lt`
    integer test, gated through the file's own pass/fail helpers (D-13). NO hardcoded line number —
    not the ADR's `313 → 232`; the assertion is relational so a re-vendored AGENTS.md cannot
    invalidate it. Use the same `[ ... ] && pass "CASE 1 LINE DROP — strip removed lines (strictly
    fewer than input)" || fail "CASE 1 LINE DROP — strip did not reduce line count"` idiom already
    used across CASE 2 / COUNTER-CASE B. This is a validate-0009-anchor.sh-internal assertion (the
    script is a standalone harness re-run manually), NOT a new run-tests.sh counter-case. Mutation-
    proof (verification-process step): break the drop (comment the assertion / substitute a
    tautology, or make candidate_strip a no-op) → confirm RED; restore → confirm GREEN.
  </action>
  <verify>
    <automated>grep -q 'case1.strip' migrations/validate-0009-anchor.sh && grep -Eq '\-lt' migrations/validate-0009-anchor.sh && bash migrations/validate-0009-anchor.sh >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - A strictly-smaller-count assertion exists between CASE 1's strip (`:253`) and insert (`:254`): `grep -c 'wc -l < "\$tmp/case1.strip"' migrations/validate-0009-anchor.sh` >= 1 and it is compared with `-lt` against the input's `wc -l`.
    - NO hardcoded line number appears in the assertion (`grep -E "313|232" migrations/validate-0009-anchor.sh` shows no such literal in the CASE 1 line-drop assertion region).
    - The assertion is routed through the file's `pass`/`fail` helpers (emits a CASE 1 LINE DROP pass/fail line).
    - Mutation-proven: breaking the drop (no-op strip / tautology) is observed to flip the assertion to FAIL (RED); restoring it returns PASS (GREEN) — verifier re-runs the cycle independently.
  </acceptance_criteria>
  <done>CASE 1 carries a strictly-smaller-count `-lt` assertion between strip and insert with no hardcoded line number, routed through pass/fail, mutation-proven RED→GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → recorded evidence | validate-0009-anchor.sh's stdout is re-run-and-diffed evidence; non-deterministic content lets a stale evidence file falsely verify (integrity, not attack surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Repudiation / false verification | validate-0009-anchor.sh stdout determinism (REV-01) | mitigate | Remove mirror-derived values from stdout so a re-vendor cannot silently invalidate recorded evidence; mutation-proven grep test in CI-gated run-tests.sh. No new attack surface — strengthens evidence integrity. |
| T-12-07 | Tampering (silent extraction error) | extract_step_block prefix collision (REV-02) | mitigate | Delimiter-aware match prevents a 10+-step document from extracting the wrong step's Apply block into a migration test; preserves the no-escaping property (no regex-from-interpolation). Low direct threat surface. |
| T-12-08 | Repudiation (vacuous assertion) | CASE 1 unasserted line drop (REV-03) | mitigate | Strictly-smaller-count assertion prevents a false-positive PASS on a no-op strip. Evidence-integrity hardening; no new attack surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this phase (pure shell edits). No supply-chain surface added. |

No new external input crosses a trust boundary in this plan; all three fixes strengthen
evidence integrity / prevent false-positive verification.
</threat_model>

<verification>
- `bash migrations/run-tests.sh` passes with 0 FAIL, including the new REV-01 determinism test, the REV-02 synthetic 10+-step fixture, and all pre-existing extract_step_block-dependent migration suites.
- `bash migrations/validate-0009-anchor.sh` passes and its stdout carries zero mirror-derived values; CASE 1 emits the new strictly-smaller-count line-drop assertion.
- Each of the three fixes is independently observed RED (mutation) then GREEN (restore) — the verifier re-runs each cycle, not trusting the executor's claim.
</verification>

<success_criteria>
Roadmap Phase 12 SC#2 (REV-01), SC#3 (REV-02), SC#4 (REV-03): each defect closed
with its own mutation-proven assertion — validator stdout genuinely deterministic;
`### Step 1` no longer matches `### Step 10`+; CASE 1's line drop caught by a
strictly-smaller-count check with no hardcoded line number.
</success_criteria>

<output>
Create `.planning/phases/12-path-safety-review-debt/12-02-SUMMARY.md` when done.
Record each REV fix's RED→GREEN mutation-proof observation separately.
</output>
