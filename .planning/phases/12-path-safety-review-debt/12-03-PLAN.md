---
phase: 12-path-safety-review-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/README.md
autonomous: true
requirements: [REV-04]
must_haves:
  truths:
    - "docs/decisions/README.md states ADR-NNNN and migration-NNNN are independent numbering sequences"
    - "It prescribes always qualifying a number as `ADR-NNNN` or `migration NNNN`, never a bare `NNNN`"
    - "It carries the live worked example: migration 0009 is documented by ADR-0010, and ADR-0009 is a different subject (the plan-review gate)"
  artifacts:
    - path: "docs/decisions/README.md"
      provides: "Normative numbering-convention subsection disambiguating ADR vs migration numbers"
      contains: "independent"
  key_links:
    - from: "docs/decisions/README.md numbering subsection"
      to: "the repo's two live numbered series (ADR-NNNN, migrations/NNNN)"
      via: "worked example ADR-0010 documents migration 0009"
      pattern: "migration 0009|ADR-0010"
---

<objective>
Correct `docs/decisions/README.md` so an ADR number and a migration number can no
longer be conflated (REV-04 / IN-03).

Purpose: `ADR-0009` (plan-review gate) and `migration 0009` (region-aware §11
placement, documented by `ADR-0010`) are numbered independently and now collide
at adjacent numbers. Bare `0009` is ambiguous in prose. REV-04 is explicitly the
constraint the roadmapper honors when assigning Phase 13/14's new migration
numbers, so a bare disambiguation sentence is too thin (D-10) — this adds
actionable forward guidance.

Output: a normative numbering-convention subsection with the live worked example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-path-safety-review-debt/12-CONTEXT.md
@.planning/phases/12-path-safety-review-debt/12-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a normative ADR/migration numbering-convention subsection to docs/decisions/README.md</name>
  <files>docs/decisions/README.md</files>
  <read_first>
    - docs/decisions/README.md:1-27 (full file — intro at :3 "Numbered sequentially", the Index table :14-27, and the worked example rows: ADR-0009 plan-review gate, ADR-0010 region-aware placement)
    - migrations/ (confirm via `ls migrations/*.md` that migration 0009 is `0009-spec-11-region-aware-placement.md`, documented by ADR-0010 — the live worked example)
    - .planning/phases/09-region-aware-11-placement/09-REVIEW.md:418-427 (IN-03 finding + suggested fix)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-10
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md README guidance (:500-538)
  </read_first>
  <action>
    Add a short normative subsection (e.g. `## Numbering convention`) after the intro paragraphs,
    before or after `## Index`. It MUST state three things (D-10): (1) the `ADR-NNNN` and
    `migration NNNN` series are INDEPENDENT sequences, numbered separately; (2) always qualify a
    number as `ADR-NNNN` or `migration NNNN` in prose — never a bare `NNNN`; (3) the live worked
    example proving the current off-by-one hazard — migration `0009`
    (`0009-spec-11-region-aware-placement.md`) is documented by **ADR-0010**, while **ADR-0009**
    is a different subject (`0009-plan-review-gate.md`, the plan-review gate). Confirm the example
    against the actual repo (`ls migrations/*.md` and the Index table); do not invent an
    illustration — the collision is already present in this repo's two series. Keep it concise and
    actionable; this is the guidance the roadmapper cites when assigning Phase 13/14 migration
    numbers.
  </action>
  <verify>
    <automated>grep -qi "independent" docs/decisions/README.md && grep -q "migration 0009" docs/decisions/README.md && grep -q "ADR-0010" docs/decisions/README.md</automated>
  </verify>
  <acceptance_criteria>
    - A dedicated numbering-convention subsection exists (`grep -ci "numbering" docs/decisions/README.md` >= 1) stating the ADR and migration series are independent sequences (`grep -qi "independent"`).
    - It prescribes `ADR-NNNN` / `migration NNNN` qualification and forbids a bare `NNNN` (`grep -Eqi "never|bare|always (write|qualify)" docs/decisions/README.md`).
    - It carries the live worked example: `grep -q "migration 0009" docs/decisions/README.md` AND `grep -q "ADR-0010" docs/decisions/README.md` AND references ADR-0009 as a distinct subject — the example is factually true against `ls migrations/*.md` and the Index table (not invented).
  </acceptance_criteria>
  <done>docs/decisions/README.md carries a normative subsection: ADR-NNNN and migration-NNNN are independent sequences, always qualified, with the true ADR-0010-documents-migration-0009 worked example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary is crossed by this plan — it is a documentation-only edit to a
decision-records index.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Repudiation / confusion | ADR/migration numbering collision (REV-04) | mitigate | Normative convention prevents future ADR/migration number conflation when assigning new migration IDs. Documentation-only; no new attack surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this phase. No supply-chain surface added. |

No new attack surface; strengthens decision-record integrity by removing a
numbering-conflation hazard.
</threat_model>

<verification>
- `docs/decisions/README.md` carries the numbering-convention subsection with the
  independent-sequences statement, the qualification rule, and the true worked example.
- The worked example is factually verified against `ls migrations/*.md` and the Index table.
</verification>

<success_criteria>
Roadmap Phase 12 SC#5: `docs/decisions/README.md` is corrected so an ADR number and
a migration number can no longer be conflated — the exact hazard the roadmapper was
told to honor when assigning Phase 11/13/14's new migration numbers.
</success_criteria>

<output>
Create `.planning/phases/12-path-safety-review-debt/12-03-SUMMARY.md` when done.
</output>
