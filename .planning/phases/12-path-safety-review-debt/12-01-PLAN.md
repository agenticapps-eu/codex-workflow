---
phase: 12-path-safety-review-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/agentic-apps-workflow/scripts/check-plan-review.sh
  - migrations/run-tests.sh
  - docs/decisions/0009-plan-review-gate.md
autonomous: true
requirements: [WR-03]
must_haves:
  truths:
    - "A `--file` value whose parent directory is a symlink that resolves OUTSIDE `$REPO_ROOT/.planning` is rejected by the bypass (exit 2 / no exit 0), where the old lexical-`..`-only check let it through"
    - "A `--file` value whose parent directory is a symlink that resolves INSIDE `$REPO_ROOT/.planning` is still accepted (exit 0) — resolve-then-contain, not reject-any-symlink"
    - "A sibling-prefix-collision path (`vendor/foo/.planning/X-PLAN.md`-shaped) that satisfied the old `*/.planning/*` lexical arm no longer bypasses — it falls to the gate (exit 2)"
    - "A `--file` naming a not-yet-created file in a not-yet-created dir still falls through (never exit-2-blocks) — fail-safe, never fail-open"
    - "ADR-0009 decision 12 carries a Reversed marker describing the shipped mechanism (parent-dir canonicalization + `_is_contained`), and the matching Open-follow-up is marked resolved"
  artifacts:
    - path: "skills/agentic-apps-workflow/scripts/check-plan-review.sh"
      provides: "Augmented --file guard: lexical-.. floor + parent-dir canonicalize-and-contain against $REPO_ROOT/.planning, with repo-root block hoisted above the bypass"
      contains: "_canon_dir"
    - path: "migrations/run-tests.sh"
      provides: "WR-03 fixtures: symlinked-parent-escape (reject), symlinked-parent-inside (accept), sibling-prefix-collision (reject) — each RED-before-GREEN"
      contains: "_cpr_case"
    - path: "docs/decisions/0009-plan-review-gate.md"
      provides: "In-place Reversed (Phase 12, WR-03) marker at decision 12 + Open-follow-up resolved note"
      contains: "Reversed (Phase 12"
  key_links:
    - from: "check-plan-review.sh --file guard"
      to: "_canon_dir / _is_contained helpers"
      via: "reuse (not reinvent) against $REPO_ROOT/.planning"
      pattern: "_canon_dir|_is_contained"
    - from: "migrations/run-tests.sh WR-03 fixtures"
      to: "skills/agentic-apps-workflow/scripts/check-plan-review.sh"
      via: "_cpr_case sandbox invocation, exit-code assertion"
      pattern: "_cpr_case"
---

<objective>
Turn the `--file` bypass in `check-plan-review.sh` from a lexical-`..`-only check
into a real symlink-resolution boundary check (WR-03), and record the reversal of
ADR-0009 decision 12 in place.

Purpose: The current guard passes a symlink whose parent directory resolves
outside `.planning` — a path-traversal / symlink-escape hole ADR-0009 d.12
recorded as an accepted limitation. WR-03 closes it by reusing the existing
`_canon_dir`/`_is_contained` helpers (SC#1: reuse, not reinvent), keeping the
lexical `..` reject as a defensive floor (D-01), and never failing open (D-02).

Output: augmented guard + three mutation-proven fixtures (RED under old check,
GREEN under new) + minimal in-place ADR-0009 marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-path-safety-review-debt/12-CONTEXT.md
@.planning/phases/12-path-safety-review-debt/12-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Helpers WR-03 MUST reuse, not reinvent (check-plan-review.sh:129-146). -->
_canon_dir() { ( cd "${1:-}" 2>/dev/null && pwd -P ); }   # empty output on non-existent path
_is_contained() { ... }   # separator-aware: ".planning/phases-evil" is NOT a child of ".planning/phases"

<!-- The exact reuse idiom to replicate (current-phase resolver, ~:258-277):
     _canon_dir the candidate dir, _canon_dir the allowed root, gate on _is_contained. -->

<!-- Fixture/assertion helpers already at file scope in run-tests.sh — reuse verbatim:
     _cpr_case (:1991), _cpr_case_and_absent (:2052), _cpr_enf_phase (:2483). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist repo-root block and augment the --file guard with parent-dir canonicalize-and-contain</name>
  <files>skills/agentic-apps-workflow/scripts/check-plan-review.sh</files>
  <read_first>
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:65-68 (GSD_SKIP_REVIEWS hatch — MUST stay first)
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:84-118 (the --file bypass: lexical-`..` loop + `.planning/*|*/.planning/*` case + basename case — the block being AUGMENTED, not replaced)
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:129-146 (_canon_dir + _is_contained defs — reuse these)
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:167-189 (repo-root self-location block being HOISTED)
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:258-277 (current-phase resolver — the exact _canon_dir/_is_contained reuse idiom to mirror)
    - skills/agentic-apps-workflow/scripts/check-plan-review.sh:459-472 (REVIEWS.md reject-any-symlink guard — the deliberately-asymmetric rule NOT to copy, D-03)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-01..D-05
  </read_first>
  <action>
    Move the repo-root self-location block (`REPO_ROOT` resolution via `git rev-parse` /
    `.planning`-ancestor walk, the `exit 0` on unresolved, and the `cd "$REPO_ROOT"`) to
    immediately after the `GSD_SKIP_REVIEWS` hatch and BEFORE the `--file` bypass block, so
    `$REPO_ROOT` exists and cwd is normalized before the bypass runs (D-04). Keep the
    `GSD_SKIP_REVIEWS` hatch as the first executable gate. Also move the `_canon_dir` /
    `_is_contained` / `_debug` / `_mtime` helper definitions above the bypass if they are
    not already reachable there — the bypass now calls `_canon_dir`/`_is_contained`.

    Inside the `--file` bypass block, KEEP the existing lexical-`..` loop and the
    `_cpr_has_dotdot -eq 0` guard intact as the defensive floor (D-01 — do not strip the
    `..` loop; it protects the not-yet-created-parent case and preserves T-08-37). AUGMENT
    it: after the `..`-clear guard passes and inside/alongside the
    `.planning/*|*/.planning/*` prefix `case`, add a parent-directory containment gate.
    Compute the candidate parent via `dirname "$CPR_FILE"`, canonicalize it with
    `_canon_dir` (the leaf file may not exist yet — canonicalize the PARENT, per D-02), and
    canonicalize the allowed root as `_canon_dir "$REPO_ROOT/.planning"` (D-05 — this repo's
    `.planning` tree ONLY). Require `_is_contained "$canon_parent" "$canon_root"` to be true
    for `exit 0` to fire. TIGHTEN the prefix arm per D-05: the containment root is
    `$REPO_ROOT/.planning` only — a `vendor/foo/.planning/X-PLAN.md` that used to satisfy the
    old `*/.planning/*` lexical arm no longer bypasses. Per D-02/D-03 fail-mode: when
    `_canon_dir` returns empty (parent does not exist / cannot be canonicalized), the bypass
    MUST fall through to normal resolution — never `exit 2`, never fail open. Resolve-then-
    contain (D-03): a symlinked parent that resolves INSIDE the tree is accepted; only one
    that escapes is rejected. Do NOT import the REVIEWS.md reject-any-symlink rule (`:459-472`)
    — that asymmetry is deliberate for evidence, wrong for a `--file` edit target.
  </action>
  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | grep -E "T-08-01|T-08-37|T-08-08|T-08-28|check_plan_review" | grep -i fail; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GSD_SKIP_REVIEWS" skills/agentic-apps-workflow/scripts/check-plan-review.sh` line number is LESS than the first `git rev-parse --show-toplevel` line, which is LESS than the `if [ -n "$CPR_FILE" ]` bypass line (skip-hatch first, repo-root hoisted above bypass — D-04).
    - `grep -c "_canon_dir" skills/agentic-apps-workflow/scripts/check-plan-review.sh` shows the `--file` bypass region now calls `_canon_dir` (reuse, not reinvent — SC#1); no new `pwd -P` subshell helper is defined besides the existing `_canon_dir`.
    - The lexical `..` loop (`_cpr_has_dotdot`) is still present in the bypass block (`grep -c '_cpr_has_dotdot' ...` >= 2) — floor retained per D-01, T-08-37 not regressed.
    - Running `bash migrations/run-tests.sh` yields zero FAIL lines in the existing T-08-* check-plan-review suites (ordering/self-location assertions still hold after the reorder).
  </acceptance_criteria>
  <done>Repo-root block hoisted above the bypass (skip-hatch still first); `--file` bypass augmented with parent-dir canonicalize-and-contain against `$REPO_ROOT/.planning`; lexical `..` floor retained; fall-through-on-empty preserved; all existing T-08-* assertions still PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Add three RED-before-GREEN WR-03 fixtures to run-tests.sh</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh:1991-2066 (_cpr_case, _cpr_case_and_absent — reuse verbatim)
    - migrations/run-tests.sh:2446-2465 (existing T-08-01 symlink-escape fixtures — direct template for the symlinked-parent case)
    - migrations/run-tests.sh:2483-2497 (_cpr_enf_phase sandbox builder — reuse for both new fixtures)
    - migrations/run-tests.sh:3022-3043 (T-08-08/T-08-37 --file bypass-list fixtures — template for the sibling-prefix-collision case; place new fixture immediately after)
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md "sibling-prefix-collision" + "symlinked-parent" guidance (:304-353)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-03, D-05
  </read_first>
  <action>
    In `test_check_plan_review_enforcement` (near the T-08-08/T-08-37 `--file bypass list`
    block, `:3022`), add three fixtures using `_cpr_case` and `_cpr_enf_phase` verbatim (do
    NOT write a second sandbox or exit-code helper — the file's own header forbids it):
    (a) SYMLINKED-PARENT-ESCAPE — build a sandbox with `.planning/phases/<name>/`, create an
    outside dir under the sandbox root (or mktemp-derived, mirroring the T-08-01 escape-tmp
    idiom), symlink `.planning/phases/<name>/evil-link -> <outside>`, then
    `_cpr_case "... --file .planning/phases/<name>/evil-link/some-PLAN.md -> exit 2 (symlinked
    parent resolves outside .planning)" "$s" 2 --file ".planning/phases/<name>/evil-link/some-PLAN.md"`.
    (b) SYMLINKED-PARENT-INSIDE — same shape but the symlink target resolves to a real dir
    INSIDE `.planning/phases/`, asserting `exit 0` (resolve-then-contain accepts an in-tree
    symlink — D-03); label it so the accept-on-inside contract is explicit.
    (c) SIBLING-PREFIX-COLLISION — a `vendor/foo/.planning/X-PLAN.md`-shaped path that the old
    `*/.planning/*` lexical arm accepted; assert `exit 2` under the new `$REPO_ROOT/.planning`-
    only containment (D-05 disclosed tightening). Label it stating WHY, mirroring the existing
    `"(basename matches but NOT .planning/-rooted)"` label style.
    Each fixture is mutation-proven by the RED-before-GREEN convention: confirm the reject/accept
    fixtures go RED against a stashed pre-fix copy of `check-plan-review.sh` (or note the
    observed old-behavior exit code in a fixture comment), then GREEN against Task 1's fix. Add
    a short before/after note in the SUMMARY documenting the disclosed `*/.planning/*` behavior
    change (D-05).
  </action>
  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | grep -Ei "symlinked-parent|sibling-prefix|evil-link" | grep -iv fail | grep -qi pass</automated>
  </verify>
  <acceptance_criteria>
    - Fixture (a) symlinked-parent-escape asserts `exit 2` and is observed RED against a pre-Task-1 copy of `check-plan-review.sh` (old lexical check returned `exit 0`), GREEN after.
    - Fixture (b) symlinked-parent-inside asserts `exit 0` — proving resolve-then-contain, not reject-any-symlink (D-03).
    - Fixture (c) sibling-prefix-collision asserts `exit 2` and is observed RED against pre-Task-1 code (old `*/.planning/*` arm returned `exit 0`), GREEN after — the disclosed D-05 tightening.
    - `grep -c "_cpr_case\b\|_cpr_enf_phase" migrations/run-tests.sh` increases (helpers reused, none reinvented); no new sandbox-builder or exit-code-check function is defined.
    - Full `bash migrations/run-tests.sh` run reports 0 FAIL for the three new fixtures.
  </acceptance_criteria>
  <done>Three fixtures (symlinked-parent-escape reject, symlinked-parent-inside accept, sibling-prefix-collision reject) land in run-tests.sh, reuse `_cpr_case`/`_cpr_enf_phase` verbatim, each mutation-proven RED-before-GREEN, all GREEN under Task 1's guard.</done>
</task>

<task type="auto">
  <name>Task 3: Add in-place ADR-0009 Reversed marker at decision 12 and resolve the Open-follow-up</name>
  <files>docs/decisions/0009-plan-review-gate.md</files>
  <read_first>
    - docs/decisions/0009-plan-review-gate.md:366-398 (decision 12 body — the accepted lexical-only limitation being reversed)
    - docs/decisions/0009-plan-review-gate.md:428-442 (Open follow-ups section + the WR-03 symlinked-prefix-component entry to mark resolved)
    - .planning/phases/12-path-safety-review-debt/12-CONTEXT.md D-08, D-09
    - .planning/phases/12-path-safety-review-debt/12-PATTERNS.md ADR-0009 marker guidance (:430-496)
  </read_first>
  <action>
    Append a `**Reversed (Phase 12, WR-03):** <date>.` marker in place at decision 12 (after
    its body text). Per D-09 the marker MUST describe the mechanism ACTUALLY shipped: the guard
    now canonicalizes the `--file` value's parent directory via `_canon_dir` (the `cd … && pwd -P`
    idiom) and rejects a symlink-resolved escape via `_is_contained` against `$REPO_ROOT/.planning`
    — reusing the same helpers this decision's text called "not available at this call site." State
    explicitly this is NOT the walk-each-prefix-component fix speculated in the Open follow-up
    (parent canonicalization resolves symlinks anywhere in the parent chain in one shot); that the
    lexical `..` check is retained as a defensive floor for the not-yet-created-parent case; and
    that it tightens the `*/.planning/*` bypass arm to `$REPO_ROOT/.planning` only (disclosed
    behavior change — a vendored `vendor/foo/.planning/X-PLAN.md` no longer bypasses). Note the
    dated Correction section (d.9 superseded + this reversal + global-vs-per-project fix) lands in
    Phase 13 (DOC-03) — do NOT author that Correction section here (D-08). Then mark the Open-
    follow-up entry (`:435-442`) `**Resolved (Phase 12):**` in place (do not delete it), with a
    short trailing note that it shipped as parent-directory canonicalization, not the walk-each-
    prefix approach speculated there.
  </action>
  <verify>
    <automated>grep -q "Reversed (Phase 12, WR-03)" docs/decisions/0009-plan-review-gate.md && grep -q "Resolved (Phase 12)" docs/decisions/0009-plan-review-gate.md && ! grep -qi "^## Correction" docs/decisions/0009-plan-review-gate.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Reversed (Phase 12, WR-03)" docs/decisions/0009-plan-review-gate.md` == 1, located within decision 12's region (line number between the decision-12 header ~366 and the Open follow-ups header ~428).
    - The marker names `_canon_dir` and `_is_contained` and the phrase describing parent-directory canonicalization (the shipped mechanism), and explicitly disclaims the walk-each-prefix-component approach (D-09).
    - The marker discloses the `*/.planning/*` -> `$REPO_ROOT/.planning` tightening (D-05).
    - `grep -c "Resolved (Phase 12)" docs/decisions/0009-plan-review-gate.md` >= 1 on the Open-follow-up entry, which is still present (not deleted).
    - `grep -ci "^## Correction" docs/decisions/0009-plan-review-gate.md` == 0 — no dated Correction section opened here (that is Phase 13 / DOC-03).
  </acceptance_criteria>
  <done>Decision 12 carries an in-place Reversed marker describing the shipped parent-canonicalization mechanism and the disclosed tightening; the Open-follow-up is marked Resolved in place; no Correction section is opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent/tool → `check-plan-review.sh --file <path>` | An agent-supplied `--file` argument crosses into the bypass logic; a crafted path (symlinked parent) is untrusted input that must not escape `$REPO_ROOT/.planning` |
| filesystem symlink → canonicalized path | A symlink in the `--file` parent chain can redirect the effective target outside the allowed tree at resolution time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege / Tampering | `--file` bypass in check-plan-review.sh | mitigate | Canonicalize the `--file` parent dir via `_canon_dir` (`pwd -P`, resolves symlinks) and require `_is_contained` against `$REPO_ROOT/.planning`; a symlink-resolved escape is rejected (fixture a). Closes the primary WR-03 hole. |
| T-12-02 | Elevation of Privilege | Fail-open regression on un-canonicalizable parent | mitigate | Per D-02 the guard FALLS THROUGH (never `exit 2`-blocks, never bypass-approves) when `_canon_dir` returns empty — asserted by the not-yet-created-dir case; the guard is proven to never fail open. Failing open is the milestone's nemesis. |
| T-12-03 | Tampering (disclosed behavior change) | `*/.planning/*` lexical arm tightened to `$REPO_ROOT/.planning` | accept (deliberate) | D-05 removes the vendored-sub-project bypass on purpose: a `vendor/foo/.planning/X-PLAN.md` no longer bypasses this repo's gate. Disclosed in the ADR marker and SUMMARY, not silent. Fixture (c) proves it. |
| T-12-04 | Tampering (over-rejection of legitimate symlink) | resolve-then-contain policy | mitigate | D-03: an in-tree symlinked parent is accepted (fixture b) — the guard does not adopt REVIEWS.md's reject-any-symlink asymmetry, which would false-block legitimate worktree symlinks. |
| T-12-05 | Time-of-check/Time-of-use race | canonicalize-then-use of `--file` parent | accept (deferred, locked upstream) | TOCTOU is explicitly OUT OF SCOPE (REQUIREMENTS §Out of Scope). The guard canonicalizes-and-contains; it does not attempt to defeat time-of-check/time-of-use. Accepted risk: the gate is agent-mediated, not a boundary against a hostile concurrent caller. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this phase (pure shell/doc edits). No supply-chain surface added. |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` passes with 0 FAIL, including the three new WR-03 fixtures and all pre-existing T-08-* check-plan-review assertions.
- The three fixtures are each independently observed RED against a stashed pre-fix `check-plan-review.sh` and GREEN against the fix (verifier re-runs the cycle, does not trust the executor's claim).
- ADR-0009 carries the in-place Reversed marker and Resolved Open-follow-up; no Correction section opened.
</verification>

<success_criteria>
Roadmap Phase 12 SC#1: `--file`'s guard rejects a symlink-resolved parent-directory
escape and a sibling-prefix collision — both previously passed by the lexical-`..`-only
check — via the reused `_canon_dir`/`_is_contained` helpers; each fixture observed
failing under the old check and passing under the new.
</success_criteria>

<output>
Create `.planning/phases/12-path-safety-review-debt/12-01-SUMMARY.md` when done.
MUST record the disclosed `*/.planning/*` → `$REPO_ROOT/.planning` behavior change (D-05).
</output>
