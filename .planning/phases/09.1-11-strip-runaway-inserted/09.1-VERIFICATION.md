---
phase: 09.1-11-strip-runaway-inserted
verified: 2026-07-15T22:15:00Z
status: passed
score: 11/11 roadmap success criteria verified; 10/10 requirement IDs accounted for (9 fully tested, 1 correct-but-execution-untested)
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 9.1: §11 Strip Runaway — Verification Report

**Phase Goal:** Close the data-loss paths `09-REVIEW.md` reproduced in the shipped migration
0009, so the migration cannot destroy user content in the states it does not abort on — and
make the suite capable of catching it if it regresses.

**Verified:** 2026-07-15T22:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Verification method

This report does not trust SUMMARY.md prose. Every claim below was independently
re-derived from the codebase: `bash migrations/run-tests.sh` and
`bash migrations/run-tests.sh 0009` were run live against HEAD (unmodified); the shipped
`migrations/0009-spec-11-region-aware-placement.md` was read in full; `docs/decisions/0010`
and the upstream GitHub issue were read/fetched directly (`gh issue view 90 --repo
agenticapps-eu/claude-workflow`); and — because this phase's own subject matter is
"assertions that read as coverage but cannot fail" — two of the load-bearing regression
claims (criteria 5 and 6) were independently re-proven by **mutating a scratch git clone**
(not a tracked file) and re-running the suite against the mutation, rather than accepting
the ADR's or SUMMARY's account of what a deletion mutation showed.

## Full-suite / 0009-filter results (ground truth)

| Check | Command | Result | Status |
|---|---|---|---|
| Full fixture suite | `bash migrations/run-tests.sh` | PASS: 345, FAIL: 0, SKIP: 1 | VERIFIED — matches expected state exactly |
| 0009 filter | `bash migrations/run-tests.sh 0009` | PASS: 63, FAIL: 0 (includes state A/B/C/D, 01–15, `no-scaffolder-tree`) | VERIFIED |

## Independent mutation re-proofs (not taken on faith from ADR-0010 / SUMMARY)

| Mutation | Method | Result | Proves |
|---|---|---|---|
| Narrow the strip terminator to `/^## /` only (drop the `gitnexus:start` alternation) | Cloned repo to a scratch dir (`git clone --local`), edited the clone's `migrations/0009-...md` line 304, ran `bash migrations/run-tests.sh 0009` there | 2 FAIL: `12-idempotent-rerun: … BYTE-IDENTICAL …` and `… region markers still paired … (start=0 end=1)` | **Criterion 6 / ANCHOR-05 is real, live suite coverage** — the fixture the phase built genuinely fails when the invariant it guards is broken. |
| Delete the pre-flight `test -s "$MIRROR"` guard | Same scratch-clone method, guard block replaced with a no-op comment | 2 FAIL: `D-28.1 layer 1` and `10-corrupt-mirror-refused (a) … refused, but not by test -s (wrong layer)` | **Criterion 5 is real, live suite coverage** — deleting the guard is caught, not silently absorbed by another layer. |

Both mutations were done in throwaway `git clone --local` scratch directories under the
session scratchpad; no tracked file in the working tree was touched.

## Goal Achievement — ROADMAP Success Criteria (0–10)

| # | Criterion (paraphrased) | Status | Evidence |
|---|---|---|---|
| 0 | 0009 actually runs on a real target project (BLOCKER from Phase 9's V-01) | VERIFIED | `0009:98` now greps `.codex/workflow-version.txt`, not a project-relative `skills/` path. `_m0009_mk_project` (`run-tests.sh:3411-3416`) no longer manufactures a synthetic `SKILL.md` — confirmed by reading the function body directly. `no-scaffolder-tree` fixture (4 assertions) PASSES live. |
| 1 | Runaway reproduced as a FAILING fixture before any awk change | VERIFIED | Commit graph: `8c4adb3 test(09.1-04): add fixtures 13/14/15 — observed RED against unfixed 0009 (CR-01)` precedes the fix commits `9927acd`/`21aeeaa`/`c36deab` (09.1-05). Chronological RED-before-GREEN is auditable, not asserted. |
| 2 | Provenance present + exact H2 absent ⇒ refuse, `exit 3`, byte-identical | VERIFIED | `0009:232-255` refuse-gate `elif`; `0009:288-324` awk `unresolved`/`END{exit 4}`. Fixtures `13-runaway-drifted-h2`, `14-runaway-orphan-provenance`, `15-mixed-provenance-unresolved` all PASS live, each asserting both exit code and byte-identity. |
| 3 | `PROV_RE` anchored, ported from upstream `f9354cc`, with `11-prose-mention-provenance` | VERIFIED | `0009:207` `PROV_RE='^<!-- spec-source: ... -->$'`, applied at all 4 sites per the document's own `## Notes`. Fixture `11-prose-mention-provenance` (4 assertions) PASSES live; `@[^[:space:]]+` any-version class preserved (confirmed by reading `:207`, `:290`). |
| 4 | Post-strip integrity check reachable-or-removed, with a mutation-based determination recorded | VERIFIED | h2-count guard kept (`0009:358-367`, `h2_out` appears exactly 3× in the document, matching ADR's claim). ADR-0010 §"Also recorded" records the specific mutation (deleting the END guard alone still left fixture 15 refusing via the h2-count guard's own diagnostic) — internally consistent with the guard's independent code path. |
| 5 | `test -s` guard is live (deleting it fails the suite) | **VERIFIED — independently reproduced**, see mutation table above |
| 6 | `12-idempotent-rerun` exists; narrowing the terminator fails the suite | **VERIFIED — independently reproduced**, see mutation table above. Fixture is genuinely non-trivial: it builds its BEFORE state by running Apply once against a region-led file (not hand-authored), then re-runs Apply and asserts byte-identity + paired markers + surviving region body. |
| 7 | MIGR-07's `state-a` fixture is genuinely off-anchor (closes V-02) | VERIFIED | `run-tests.sh:3566-3581`: block is placed BELOW `## Project Overview`, with an explicit self-guard (`:3587-3588`) asserting the generated file contains no `gitnexus` string at all — position is provably the only variable, unlike Phase 9's on-anchor fixture that this replaces. |
| 8 | `.codex/workflow-version.txt` and `SKILL.md` agree; drift test reads the version file | VERIFIED | Both files read `0.7.0` (checked directly: `cat .codex/workflow-version.txt` → `0.7.0`; `skills/agentic-apps-workflow/SKILL.md:3` → `version: 0.7.0`). `run-tests.sh:3241-3246` adds the version-file-vs-SKILL.md comparison leg (V-03's fix), confirmed present and passing in the full-suite run. |
| 9 | ADR-0010 records the runaway, D-26 correction, V-01-as-porting-error, Q1 ruling + rejected alternative, D-48 re-pin | VERIFIED | Read `docs/decisions/0010-...md:1-160` in full — all five items present with specific, falsifiable detail (line ranges, commit hashes, `git merge-base --is-ancestor 8520f90 HEAD → NO` re-verification). |
| 10 | Upstream defect filed for CR-01 only | VERIFIED | `gh issue view 90 --repo agenticapps-eu/claude-workflow` — issue is OPEN, filed by the project's own account, titled "migration 0029: strip runaway when provenance is present but the exact §11 heading is drifted/absent (CR-01)". Body explicitly states CR-02 is "already closed by this repo's own PR #89" and does not mention V-01. URL (`https://github.com/agenticapps-eu/claude-workflow/issues/90`) recorded in both `09.1-UPSTREAM-CR-01.md:3` and `docs/decisions/0010:546`. |

**Score:** 11/11 success criteria VERIFIED.

## Requirements Coverage (the 10 IDs assigned to this phase)

| Requirement | Source Plan(s) | Status | Evidence |
|---|---|---|---|
| ANCHOR-05 | 09.1-04, 09.1-05, 09.1-06 | VERIFIED | Alternation present at all terminator/anchor sites (`0009:304`, `:387`); corrected wording ("`## ` line, an anchored `gitnexus:start` marker, or EOF") matches REQUIREMENTS.md's corrected text verbatim (`0009:59-61`). Live suite coverage independently confirmed by mutation (criterion 6 above). |
| MIGR-01 | 09.1-01, 09.1-02, 09.1-05 | VERIFIED | `0009:5-6` frontmatter correct; pre-flight (`:98`) accepts both `0.6.0`/`0.7.0` **and** now reads the correct, project-durable path (`.codex/workflow-version.txt`), closing Phase 9's V-01 BLOCKER. |
| MIGR-04 | 09.1-05 | VERIFIED | Unified inject path (`0009:256-427`); fixture `01-gitnexus-led-inject` and state-C PASS live — an absent block is injected at the anchor, unregressed by the CR-01 refuse-gate changes. |
| MIGR-06 | 09.1-06 | VERIFIED | Conjunctive idempotency predicate (`0009:162-171`) plus fixture `12-idempotent-rerun`, which — unlike Phase 9's untested claim — genuinely exercises a second real Apply run against a region-led healed file and asserts byte-identical output. |
| MIGR-07 | 09.1-04 | VERIFIED | `state-a` rewritten to isolate position as the sole variable (V-02 closed); assertion can now fail for the reason it claims to guard. |
| MIGR-08 | 09.1-02 | **DELIVERED, execution-untested** — see note below | `0009:459-462` Step 2 Apply (`echo "0.7.0" > .codex/workflow-version.txt`) is unconditional, unchanged in substance from Phase 9's already-correct Step 3, and reachable now that V-01 is fixed. However: no fixture in `run-tests.sh` actually **executes** Step 2's Apply block and asserts the resulting file content. The only Step-2-adjacent test (`no-scaffolder-tree`'s 4th assertion, `run-tests.sh:4363-4376`) checks that Step 2's TEXT does not name a `skills/` path — it never runs Step 2. |
| MIGR-09 | 09.1-02, 09.1-03 | VERIFIED | Direct-edit precedent honored (no migration step touches this repo's own scaffolder skill); `.codex/workflow-version.txt` and `skills/agentic-apps-workflow/SKILL.md` both read `0.7.0`; `test_drift`'s new third leg (`run-tests.sh:3233-3246`) catches a future split — confirmed present and passing. |
| TEST-02 | 09.1-01, 09.1-03, 09.1-04 | VERIFIED | RED-before-GREEN auditable in commit history for both criterion 0 (`dc540b7` RED → `f1a1da8`/`2fe23d4` GREEN) and the runaway fixtures (`8c4adb3` RED → `9927acd`/`21aeeaa`/`c36deab` GREEN). |
| TEST-03 | 09.1-04, 09.1-06 | VERIFIED | All ten original cases plus the four widened post-review additions (`07-prose-mention-not-a-region`, `08-rollback-region-led`, `09-two-provenance-heal`, `10-corrupt-mirror-refused` a/b/c) are present and PASS live, confirmed by the `0009`-filtered run above (fixture-by-fixture). |
| DOC-01 | 09.1-07 | VERIFIED | ADR-0010 records the anchor decision, both rejected alternatives (Option A "anchor before region if one exists", Option B "always after H1") with §12-advisory reasoning, the corrected ANCHOR-05 invariant, and the upstream filing — all read directly. |

**Coverage:** 10/10 requirement IDs accounted for. 0 orphaned (all 10 IDs appear in at least
one plan's `requirements:` frontmatter and are present in REQUIREMENTS.md's Phase 9
traceability table — REQUIREMENTS.md maps this milestone's requirements to "Phase 9" as a
single row set; Phase 9.1 was inserted after that table was written, so the row label did
not change, but the ID itself is unambiguous and present).

### Note on MIGR-08 (not a blocker)

MIGR-08's Step 2 Apply is a single, unconditional, branch-free line, already proven correct
by direct inspection, and was already rated "DELIVERED-UNTESTED" in Phase 9's own
verification before this phase touched it — its status is unchanged, not regressed. It is
**not** one of ROADMAP's 11 enumerated Success Criteria (0–10) for this phase, and none of
this phase's `must_haves` promised an execution-level test for it (09.1-02's own must_have
was scoped to the migration *document* containing the correct path, which is satisfied).
Given the phase's own adversarial mandate against assertions that "read as coverage" without
being exercised, this is flagged here as a genuine, low-risk residual gap in test coverage —
worth a follow-up fixture (`_m0009_apply` the Step 2 block, assert
`.codex/workflow-version.txt` reads `0.7.0`) — but it does not block this phase's stated
goal (closing the data-loss paths in the strip/insert mechanism) and is not treated as a
FAILED must-have.

## Anti-Patterns Scanned

| File | Result |
|---|---|
| `migrations/0009-spec-11-region-aware-placement.md` | No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers |
| `migrations/run-tests.sh` | No debt markers in the phase's diff region; one pre-existing, out-of-scope stale comment noted below (IN-01) |
| `docs/decisions/0010-region-aware-spec-11-placement.md` | No debt markers |
| `skills/setup-codex-agenticapps-workflow/SKILL.md` | No debt markers |

**IN-01 (info, non-blocking):** `run-tests.sh:4085` still reads "mk_project supplies `.git` +
SKILL.md at 0.6.0" although `_m0009_mk_project` (confirmed by direct read, `:3411-3416`) has
not created a `SKILL.md` since 09.1-01. This was already flagged in `09.1-REVIEW.md` (IN-01)
and in `09.1-06-SUMMARY.md`; it is a documentation-drift artifact with zero functional
impact (the test it comments on self-verifies the real fixture state regardless). Recorded,
not actioned — cosmetic.

## Code Review Findings (09.1-REVIEW.md) — disposition

Per this task's explicit brief, the review's 2 critical / 2 warning / 1 info findings were
checked for regression status rather than re-litigated:

| Finding | Disposition | Independent corroboration |
|---|---|---|
| CR-01 (review's own numbering): CRLF defeats `$`-anchored regexes | Pre-existing, out of phase scope | The `$`-anchored `gitnexus:start` alternation this hazard depends on was already rated "DELIVERED-UNTESTED" in **Phase 9's own** verification (`09-VERIFICATION.md:138`, ANCHOR-05), i.e. it predates 09.1. Upstream's own "Known limitations" section at `f9354cc` (cited in ADR-0010 and in the filed issue #90's own body) already lists CRLF as an open, undesigned-for hazard — corroborating it is not new. |
| CR-02 (review's own numbering): exact quotation inside a fenced code block latches the strip | Pre-existing hazard class, out of phase scope | Same upstream "Known limitations" citation covers fenced/quoted markers; not part of this phase's ANCHOR-05/CR-01/CR-02(orig)/CR-03 scope, which the ROADMAP explicitly bounds to the runaway/anchoring/idempotency/version-split defects enumerated in criteria 0–10. |
| WR-01: zero-byte `AGENTS.md` hard-aborts | Pre-existing, not a regression | Confirmed directly: `git show 6dbea5f:migrations/0009-...md` shows the `[ ! -f AGENTS.md ]` guard (uses `-f`, not `-s`) and the "produced no output" abort message both already present at the phase's base commit, unmodified by any 09.1 plan. |
| WR-02: CRLF/fenced hazards undisclosed in 0009's own Notes | Real, unaddressed by this phase | Not part of the 10 requirement IDs or the 11 success criteria. Legitimate documentation follow-up. |
| IN-01: stale comment | Confirmed still present | See Anti-Patterns section above. |

None of these five findings are must-haves this phase's ROADMAP goal or requirement IDs
call for; none block goal achievement. WR-02 and IN-01 are recorded as documentation
follow-ups, not gaps.

## Human Verification Required

None. Every must-have in this phase was resolvable by direct code reading, live suite
execution, and (for the two highest-value regression claims) independent mutation testing
in a disposable scratch clone — no visual, real-time, or external-service judgment was
required beyond fetching the upstream issue via `gh`, which was done directly.

## Gaps Summary

No gaps block this phase's goal. All 11 ROADMAP success criteria (0–10) and all 10 assigned
requirement IDs are verified against the actual codebase and a live, unmodified 345 PASS /
0 FAIL / 1 SKIP suite run — not against SUMMARY.md prose. Two of the highest-risk regression
claims (criteria 5 and 6 — the exact class of "guard that reads as coverage but cannot fail"
this phase exists to prevent) were independently re-proven by mutation in a throwaway git
clone rather than accepted on the ADR's or SUMMARY's word.

One residual, non-blocking item is recorded for future follow-up: MIGR-08's Step 2 Apply
has no execution-level fixture (see note above) — correct by inspection, untested by run.
The two code-review criticals and one warning already confirmed pre-existing/out-of-scope
by the orchestrator are re-confirmed here via independent evidence (Phase 9's own prior
verification, upstream's own documented "Known limitations", and a base-commit diff) rather
than taken on trust.

---

_Verified: 2026-07-15T22:15:00Z_
_Verifier: Claude (gsd-verifier)_
