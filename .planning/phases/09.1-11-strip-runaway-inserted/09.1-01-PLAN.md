---
phase: 09.1-11-strip-runaway-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/run-tests.sh
autonomous: true
requirements: [MIGR-01, TEST-02]

must_haves:
  truths:
    - "No 0009 sandbox manufactures a synthetic skills/agentic-apps-workflow/SKILL.md"
    - "A no-scaffolder-tree fixture exists and is OBSERVED FAILING against the current 0009"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "_m0009_mk_project shaped like a real target project; no-scaffolder-tree fixture"
      contains: "no-scaffolder-tree"
  key_links:
    - from: "_m0009_mk_project"
      to: ".codex/workflow-version.txt"
      via: "printf '0.6.0\\n' into the sandbox"
      pattern: "workflow-version\\.txt"
---

<objective>
Stop the 0009 suite from manufacturing the precondition it should be checking, and
capture criterion 0's BLOCKER as a **failing** fixture against the current, unfixed 0009.

Purpose: V-01 shipped green because `_m0009_mk_project` (`run-tests.sh:3366-3372`)
`mkdir -p`s a `skills/agentic-apps-workflow/` tree and writes a synthetic `version: 0.6.0`
SKILL.md into every 0009 sandbox — the exact practice `run-tests.sh:918-919` explicitly
refuses for 0008 ("no 0008 sandbox here manufactures a synthetic SKILL.md"). **314 PASS /
0 FAIL is fully consistent with a migration that never runs on any real project.**

Output: a RED suite. That RED is this plan's deliverable (TEST-02), not a defect —
09-03's precedent, recorded in STATE.md. Plan 09.1-02 turns it green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md

**CLAUDE.md's GitNexus mandate is INERT for this plan.** GitNexus indexes no shell
functions (`run-tests.sh` = 1 File node, 0 Function nodes, proven via Cypher in Phase 9)
and `detect_changes()` reports against the main checkout, not this worktree. **Grep is
ground truth.** Do not run, and do not claim, GitNexus impact analysis here.

**Baseline before you touch anything:** full suite **314 PASS / 0 FAIL / 1 SKIP**;
`bash migrations/run-tests.sh 0009` → **34 PASS**. In a worktree expect **2 SKIP** (one
check probes `../agenticapps-workflow-core`, unresolvable from a worktree) —
environmental, not a regression.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make _m0009_mk_project shaped like a real target project</name>

  <read_first>
    - `migrations/run-tests.sh:3346-3408` — the `_m0009_*` helper family you are editing
    - `migrations/run-tests.sh:911-925` — **the in-repo precedent**: 0008's refusal to
      manufacture a synthetic SKILL.md. Read the comment verbatim; it is the rule you are
      applying to 0009.
    - `migrations/run-tests.sh:3888-3936` — case `10-corrupt-mirror-refused`, the ONLY
      existing consumer of `$pf_block`. Its comment claims "THE MIRROR IS THE ONLY
      VARIABLE" — understand why removing the synthetic SKILL.md breaks that claim until
      09.1-02 lands.
    - `migrations/0009-spec-11-region-aware-placement.md:85-141` — the pre-flight whose
      guard 2 greps the manufactured path.
    - `.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md` — V-01's writeup.
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    Run `grep -n '_m0009_mk_project' migrations/run-tests.sh` FIRST and enumerate every
    call site — the change below affects all of them.

    In `_m0009_mk_project` (`run-tests.sh:3366-3372`):
    - DELETE `skills/agentic-apps-workflow` from the `mkdir -p` on `:3368`. Keep `.git`
      (0009 pre-flight guard 1 needs `test -d .git`).
    - DELETE the entire `printf -- '---\nname: agentic-apps-workflow\nversion: 0.6.0\n…'`
      heredoc-style write to `$p/skills/agentic-apps-workflow/SKILL.md` (`:3369-3370`).
    - ADD `.codex` to the `mkdir -p`, and write the project's OWN durable version record:
      `printf '0.6.0\n' > "$p/.codex/workflow-version.txt"`. This is the 0008 precedent
      (`0008:73-79` reads the floor from exactly this file) and the shape every real
      target project has.
    - Keep `printf '%s\n' "$p"` as the function's output contract.

    Rewrite the function's doc comment (`:3361-3365`). It currently advertises the
    synthetic SKILL.md as a feature ("a SKILL.md at `version: 0.6.0` (the D-39 version
    gate)"). Replace with: the sandbox is shaped like a REAL target project — `.git` plus
    `.codex/workflow-version.txt` at `0.6.0`, and **deliberately no `skills/` tree**,
    because no target project has one. Cite `run-tests.sh:918-919` as the in-repo
    precedent this now follows, and 0008's T-08-38 note (`0008:470-487`) as the reason.

    Do NOT touch `migrations/0009-spec-11-region-aware-placement.md` in this plan. The
    pre-flight fix is 09.1-02's job, and fixing both here forfeits the RED that criterion 0
    explicitly requires ("That fixture must be observed FAILING against the current 0009
    first").
  </action>

  <acceptance_criteria>
    - `grep -c 'skills/agentic-apps-workflow' migrations/run-tests.sh` returns a count that
      does NOT include any line inside `_m0009_mk_project`; specifically
      `sed -n '3361,3380p' migrations/run-tests.sh | grep -c 'skills/agentic-apps-workflow'`
      is `0`.
    - `sed -n '3361,3380p' migrations/run-tests.sh | grep -c 'workflow-version.txt'` is
      `>= 1`.
    - The function's doc comment cites `918-919` (verifiable:
      `sed -n '3361,3372p' migrations/run-tests.sh | grep -c '918'` is `>= 1`).
    - `bash migrations/run-tests.sh 0009` reports **at least one FAIL**, and the FAIL set
      includes the assertion labelled
      `10-corrupt-mirror-refused (c) healthy mirror: pre-flight PASSES with exit 0` —
      it now gets exit=3 because the current pre-flight greps a `skills/` path that no
      longer exists. **This specific FAIL is the point of the task.** Record its exact
      label and the `got exit=` value.
    - Cases 10(a) and 10(b) still report PASS — but record in the SUMMARY that they now
      pass for the WRONG reason (the version gate aborts before the mirror guards run),
      and that 09.1-02 restores their isolation. A pass you cannot explain is not evidence.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-01-t1.log; grep -c 'FAIL' /tmp/09.1-01-t1.log</automated>
  </verify>

  <done>`_m0009_mk_project` creates `.git` + `.codex/workflow-version.txt`=0.6.0 and no `skills/` tree; the `0009` filter reports 10(c) FAILing with exit=3.</done>
</task>

<task type="auto">
  <name>Task 2: Port 0008's no-scaffolder-tree regression fixture into test_migration_0009, and observe it RED</name>

  <read_first>
    - `migrations/run-tests.sh:1625-1710` — **the port source.** 0008's `no-scaffolder-tree`
      fixture. Read all four of its assertions and note that assertion 1
      (`test ! -e "$tmp2/skills"`) guards the FIXTURE ITSELF so it cannot silently stop
      testing what it claims. Reproduce that property.
    - `migrations/0008-plan-review-gate.md:470-487` — T-08-38, the divergence note naming
      this exact defect in 0007 and calling it "a defect this migration does not
      replicate".
    - `migrations/0008-plan-review-gate.md:337-350` — the MIGR-08 / MIGR-09 separation
      rule this fixture will assert for 0009.
    - `migrations/run-tests.sh:3409-3500` — `test_migration_0009`'s extraction gates
      (`pf_block`, `idem_block`, `apply_block`) and the `_m0009_fail` idiom for a case
      whose input is missing. Reuse both.
    - `migrations/run-tests.sh:3374-3388` — `_m0009_apply`. **The subshell is mandatory**
      (T-09-07): `$pf_block` `exit 3`s, and un-subshelled that kills the harness mid-suite
      and silently hides every later assertion.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Port Shape:
      0008's no-scaffolder-tree → 0009" — the 0008→0009 translation table.
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    Add a fixture named `no-scaffolder-tree` to `test_migration_0009`, placed immediately
    after the `10-corrupt-mirror-refused` case and before the function's closing brace.
    Name it WITHOUT a number, matching 0008's own naming for the fixture it is ported from
    (numbered fixtures 11-15 are reserved: `11-prose-mention-provenance` is upstream's,
    per Q4).

    Build its sandbox from `_m0009_mk_project` (now realistic after Task 1) plus
    `_m0009_mk_fake_home` with the healthy `$mirror`, plus an `AGENTS.md` carrying a
    healthy ON-anchor §11 block (`$PROV_LIT` + `cat "$mirror"` + a following
    `## Deployment`). Gate the whole fixture on `[ "$pf_ok" = "1" ]` and emit `_m0009_fail`
    for each assertion in the `else`, so a down extraction gate reports FAILED rather than
    vanishing.

    Assert exactly four things, mirroring 0008's four:

    1. **The fixture guards itself.** `test ! -e "$p/skills"` — the sandbox really has no
       local `skills/` tree. Without this the fixture can silently stop testing its own
       premise, which is how V-01 shipped.
    2. **Pre-flight passes with no `skills/` tree.** Run `$pf_block` through `_m0009_apply`
       and assert exit **0**. This is the criterion-0 assertion.
    3. **Step 1's idempotency check runs cleanly** (exit <= 1, i.e. no *errors*) in that
       sandbox — 0008's assertion 3.
    4. **The MIGR-08 / MIGR-09 separation holds as a document contract.** Assert that NO
       executable surface of 0009 names `skills/agentic-apps-workflow/SKILL.md`. Check
       three surfaces, not the raw file — 0009's *prose* legitimately discusses the path
       when it records the divergence, and a bare `grep -c … == 0` on the whole document
       would be self-invalidating the moment 09.1-02 writes that prose:
         (a) the `applies_to:` frontmatter list — extract lines between `^applies_to:` and
             the next non-indented key, and assert none contains `skills/`;
         (b) `$pf_block` — assert it does not contain `skills/agentic-apps-workflow`;
         (c) every extracted Step Apply block — assert none contains
             `skills/agentic-apps-workflow`.

    Follow the existing `_m0009_ok $?`/label idiom so a failure names itself, and carry the
    case label in the assertion text.

    Then run the suite and **record the RED**. Commit with `test(09.1-01): …` and put the
    exact PASS/FAIL counts plus the failing assertion labels in the commit body. The commit
    is the audit trail: Phase 9's `run-tests.sh` was blob-hash identical between its RED and
    GREEN commits, which is what made its GREEN credible.
  </action>

  <acceptance_criteria>
    - `grep -c 'no-scaffolder-tree' migrations/run-tests.sh` is `>= 5` (label appears in the
      fixture header comment and in all four assertion labels).
    - `bash migrations/run-tests.sh 0009` FAILs on the assertion asserting pre-flight exits
      0 with no `skills/` tree. Record the label and the observed exit code — it MUST be
      **3** (the version gate aborting), not 0. **This is the recorded RED observation
      required by criterion 0.**
    - `bash migrations/run-tests.sh 0009` FAILs on the MIGR-08/MIGR-09 separation assertion:
      `applies_to` still names `skills/agentic-apps-workflow/SKILL.md` at `0009:9`, and
      Step 2's Apply still seds it at `0009:369`.
    - The self-guard assertion (`test ! -e "$p/skills"`) PASSes — proving the fixture tests
      what it claims. If this one FAILs, Task 1 did not land.
    - The commit message body records: baseline `34 PASS` on the `0009` filter, the new
      counts, and every FAIL label verbatim.
    - `git show --stat HEAD` lists `migrations/run-tests.sh` and **nothing else**. If
      `migrations/0009-*.md` appears, the RED has been forfeited — revert and redo.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-01-t2.log; grep -E 'FAIL|PASS' /tmp/09.1-01-t2.log | grep -c 'no-scaffolder-tree'</automated>
  </verify>

  <done>The `no-scaffolder-tree` fixture exists with four assertions, at least two are OBSERVED FAILING against the unmodified 0009, the RED is committed, and the commit touches only `run-tests.sh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's `AGENTS.md` → 0009's awk | untrusted file content crosses into a state machine that rewrites it in place |
| the test suite → the migration | the suite is the ONLY control proving the migration is safe; a suite that manufactures its own preconditions provides no assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Repudiation | `_m0009_mk_project` (`run-tests.sh:3366-3372`) | mitigate | The fixture manufactures the very condition under test, so a green suite asserts nothing about reality (V-01: 314 PASS on a migration that never runs). Task 1 removes the manufacture; Task 2's self-guard assertion (`test ! -e "$p/skills"`) makes the removal itself falsifiable. |
| T-09.1-02 | Denial of Service | migration 0009 pre-flight | mitigate | 0009 exits 3 on every real project, so the §11 heal never runs and the data-loss defect it exists to close stays open. Task 2 pins this as a failing assertion; 09.1-02 fixes it. |
| T-09.1-03 | Repudiation | assertions that cannot fail | mitigate | Every assertion added here is checked against a state that makes it fail (the RED observation). Cases 10(a)/(b) are explicitly recorded as "passing for the wrong reason" rather than counted as coverage. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | **No packages are installed by this phase** — all work is in-repo Markdown, shell, and two version files. RESEARCH § "Package Legitimacy Audit" confirms the gate has no input; `slopcheck` has nothing to scan. |
</threat_model>

<verification>
- `bash migrations/run-tests.sh 0009` runs to completion (no harness abort — proves the
  mandatory subshell in `_m0009_apply` was preserved).
- The `0009` filter's PASS+FAIL total is **greater than** the 34 baseline (assertions were
  added, not swapped).
- `git diff --stat` touches `migrations/run-tests.sh` only.
</verification>

<success_criteria>
- No 0009 sandbox writes a synthetic `skills/agentic-apps-workflow/SKILL.md`.
- The `no-scaffolder-tree` fixture exists with all four ported assertions.
- **At least two of its assertions are OBSERVED FAILING against the unmodified 0009, and
  the observation (labels + exit codes + counts) is recorded in a `test(09.1-01)` commit.**
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at
  plan start.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-01-SUMMARY.md` when done.
Record verbatim: the baseline counts, the post-change counts, every FAIL label, and the
observed exit code for the pre-flight assertion. 09.1-02 consumes this as its RED evidence.
</output>
</content>
</invoke>
