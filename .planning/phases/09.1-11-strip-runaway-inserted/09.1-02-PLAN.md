---
phase: 09.1-11-strip-runaway-inserted
plan: 02
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - migrations/0009-spec-11-region-aware-placement.md
autonomous: true
requirements: [MIGR-01, MIGR-08, MIGR-09]

must_haves:
  truths:
    - "0009's pre-flight version floor passes on a project with AGENTS.md + .codex/ and no skills/ tree"
    - "No executable surface of 0009 names a project-relative skills/ path"
    - "0009 records 0.7.0 in .codex/workflow-version.txt (MIGR-08) and bumps nothing in this repo (MIGR-09 is a direct edit)"
  artifacts:
    - path: "migrations/0009-spec-11-region-aware-placement.md"
      provides: "pre-flight floor read from the project's own durable record; Step 2 deleted"
      contains: "workflow-version.txt"
  key_links:
    - from: "0009 pre-flight guard 2"
      to: ".codex/workflow-version.txt"
      via: "grep -qE floor check"
      pattern: "grep -qE '\\^0\\\\\\.\\(6\\|7\\)\\\\\\.0\\$'"
---

<objective>
Close criterion 0, the phase's BLOCKER: make migration 0009 actually run on a real target
project.

Purpose: V-01. 0009's pre-flight (`:95-101`) greps the **project-relative** path
`skills/agentic-apps-workflow/SKILL.md` for its version floor, fails, and exits 3 —
**Step 1, the entire §11 heal, never runs.** Same path is sed'd at `:369` and named in
`applies_to` at `:9`. This is a regression against an established, documented precedent:
0008 names this exact defect in 0007 (T-08-38, `0008:470-487`) and calls it "a defect this
migration does not replicate".

**It is OUR porting error, not an inherited defect.** Upstream greps
`.claude/skills/agentic-apps-workflow/SKILL.md` — a path upstream's own setup skill
creates (`f9354cc:setup/SKILL.md:146`). Our port dropped the `.claude/` prefix. On the
Codex host, skills install **globally** at `${CODEX_HOME}/skills/…` and the project's
version lives in `.codex/workflow-version.txt`. `skills/agentic-apps-workflow/` is *this
scaffolder repo's own source tree* — which is exactly why it looked right to its author.
**Do not file this upstream.**

Root cause: MIGR-08 (the migration records the version in the target project) and MIGR-09
(**this repo's own** bump) were conflated into one migration. 0008 kept them apart on
purpose. Step 2 must be deleted in its entirety.

Output: the RED observed in 09.1-01 turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-01-SUMMARY.md
@.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md

**CLAUDE.md's GitNexus mandate is INERT** — this plan edits one Markdown document.
GitNexus indexes no Markdown prose. Grep is ground truth. Do not claim GitNexus analysis.

<interfaces>
<!-- The 0008 precedent you are porting. Read it from the source; do not transcribe. -->

`migrations/0008-plan-review-gate.md:73-79` — the version floor. Its shape:
a `grep -qE` on `.codex/workflow-version.txt` against an anchored two-version alternation,
an `||` block that reads INSTALLED with `cat … 2>/dev/null`, prints an ABORT naming the
installed version and the needed floor, points at `$update-codex-agenticapps-workflow`,
and `exit 3`. 0009's analog shifts the version pair only: `0.(5|6).0` → `0.(6|7).0`.

`migrations/0008-plan-review-gate.md:322-334` — Step 4, the MIGR-08 analog (records the
version in the TARGET project). 0009's Step 3 is already this and is already correct.

`migrations/0008-plan-review-gate.md:337-350` — the rule: "There is no step that bumps a
target project's local scaffolder trigger skill's SKILL.md, and none should be added."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the version floor from the project's own durable record (V-01)</name>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md:85-151` — the pre-flight you are
      editing, all four guards plus the missing-AGENTS.md permissiveness note.
    - `migrations/0008-plan-review-gate.md:73-79` — **the port source.** Read it verbatim.
    - `migrations/0008-plan-review-gate.md:470-487` — T-08-38. The divergence note you are
      writing the 0009 analog of.
    - `skills/update-codex-agenticapps-workflow/SKILL.md:30-40` — Stage A step 1 reads
      `.codex/workflow-version.txt`. This is why a project without that file is not a
      migration target at all, and why there is no fresh-install regression to trade for.
    - `migrations/run-tests.sh:3439-3483` — the pre-flight shape guard is anchored on the
      MIRROR PATH, deliberately NOT on `test -s` or a variable name. Your edit must keep
      `spec-mirrors/11-coding-discipline-0.4.0.md` present in the pre-flight or every
      0009 fixture's extraction gate goes down.
  </read_first>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    Replace pre-flight guard 2 (`0009:92-101`) with the 0008 precedent, version pair
    shifted to `^0\.(6|7)\.0$`:
    - DELETE the `SKILL_FILE=skills/agentic-apps-workflow/SKILL.md` assignment entirely.
    - `grep -qE` the anchored floor against `.codex/workflow-version.txt`.
    - In the `||` block, read `INSTALLED=$(cat .codex/workflow-version.txt 2>/dev/null)`,
      print an ABORT naming `${INSTALLED:-unknown}` and the needed `0.6.0`, point at
      `$update-codex-agenticapps-workflow`, state the supported floor `0.6.0 -> 0.7.0`,
      and `exit 3`.
    - Keep accepting BOTH 0.6.0 and 0.7.0. That is deliberate and load-bearing: an
      idempotent re-run on an already-migrated project must not abort. Preserve the
      existing comment that says so.

    **The ABORT message must NOT contain the substring `missing or empty`.** That string is
    guard 3's exclusive diagnostic and 09.1-06 uses it to isolate the `test -s` layer
    (criterion 5). Verified: guard 4 says "is missing its final section", which does not
    collide. Do not introduce a collision here.

    Update the guard's comment to state that the floor is read from the project's OWN
    durable record per 0008's precedent, and add a divergence note in the document's prose
    modelled on T-08-38 (`0008:470-487`). The note MUST say:
    - 0009 v1 greped the project-relative `skills/agentic-apps-workflow/SKILL.md`, which no
      target project has, so it aborted with exit 3 on every real install;
    - this was **a codex-side porting error, not an inherited upstream defect** — upstream
      greps `.claude/skills/agentic-apps-workflow/SKILL.md`, a path its own setup skill
      creates (`f9354cc:setup/SKILL.md:146`); our port dropped the `.claude/` prefix;
    - on this host, skills install globally at `${CODEX_HOME}/skills/…` and the project
      version lives in `.codex/workflow-version.txt`;
    - it is fixed here per 0008's precedent, cited as `0008:73-79`.

    Do NOT touch guards 1, 3, or 4. Do NOT touch `migrations/run-tests.sh`.
  </action>

  <acceptance_criteria>
    - `grep -c 'SKILL_FILE' migrations/0009-spec-11-region-aware-placement.md` is `0`
      (returns `3` today — `:95`, `:96`, `:97`).

    **The three greps below use a STRUCTURAL anchor (`/^## Pre-flight/,/^## Steps/`), not
    hardcoded line numbers. This task replaces guard 2 with a differently-sized block,
    shifting every line after it and pushing content out of any pinned range. Verified
    against the live tree at planning time: they return `1`, `0`, `1` today.**

    - The pre-flight names no `skills/agentic-apps-workflow`:
      `sed -n '/^## Pre-flight/,/^## Steps/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'skills/agentic-apps-workflow'`
      is `0` (returns `1` today — genuinely RED).
    - `sed -n '/^## Pre-flight/,/^## Steps/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'workflow-version.txt'`
      is `>= 1` (returns `0` today — genuinely RED).
    - The pre-flight still contains the mirror path —
      `sed -n '/^## Pre-flight/,/^## Steps/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'spec-mirrors/11-coding-discipline-0.4.0.md'`
      is `>= 1`. This returns `1` today and must STAY `1`: it is a no-regression check, not
      a RED. If it drops to `0`, every 0009 fixture's extraction gate goes down and the
      suite reports "extraction failed" instead of real signal.
    - **No `missing or empty` collision:** the version guard's own message must not carry it.
      `sed -n '/^# 2\./,/^$/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'missing or empty'` is `0`.
    - `bash migrations/run-tests.sh 0009` — the assertion
      `pre-flight PASSES ... with no skills/ tree` (09.1-01's RED) now PASSes with exit 0.
    - `10-corrupt-mirror-refused (c) healthy mirror ... exit 0` returns to PASS, restoring
      case 10's "THE MIRROR IS THE ONLY VARIABLE" isolation that 09.1-01 recorded as broken.
    - The divergence note names the porting error explicitly:
      `grep -c 'porting error' migrations/0009-spec-11-region-aware-placement.md` is `>= 1`,
      and `grep -c '\.claude/skills' migrations/0009-spec-11-region-aware-placement.md` is `>= 1`.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-02-t1.log; grep -c 'no-scaffolder-tree' /tmp/09.1-02-t1.log</automated>
  </verify>

  <done>Pre-flight guard 2 reads `.codex/workflow-version.txt`, no `skills/` path remains in the pre-flight, and 09.1-01's pre-flight RED is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Step 2 entirely, separating MIGR-08 from MIGR-09</name>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md:1-13` — the `applies_to` list.
    - `migrations/0009-spec-11-region-aware-placement.md:363-392` — Step 2 (to delete),
      Step 3 (to keep and renumber), and the "Steps 2 and 3 run unconditionally" note.
    - `migrations/0009-spec-11-region-aware-placement.md:143-151, 205-212, 394-415` — every
      other prose site that references Step 2/Step 3 by number.
    - `migrations/0008-plan-review-gate.md:337-350` — **the rule.** Read verbatim.
    - `migrations/0008-plan-review-gate.md:322-334` — Step 4, the MIGR-08 analog.
  </read_first>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    **First, prove the renumber is safe.** Run
    `grep -n 'extract_step_block "$MIGRATION_0009"' migrations/run-tests.sh`. Confirm every
    call passes step `1`. If any call extracts step 2 or 3, STOP and report — the
    renumbering below would silently retarget it, and IN-01 already notes that
    `### Step 1` prefix-matches `### Step 10`.

    Then:
    - DELETE `### Step 2: Bump the scaffolder version (implements_spec unchanged)` in its
      entirety (`0009:363-375`) — heading, idempotency check, pre-condition, the fenced
      `sed -i.0009.bak` Apply, the `implements_spec` parenthetical, and the Rollback line.
      This step seds a path no target project has, and shipping this repo's own scaffolder
      bump to other people's repos is exactly what `0008:337-350` forbids.
    - DELETE the `applies_to` entry at `0009:9`
      (`- skills/agentic-apps-workflow/SKILL.md  # version bump 0.6.0 -> 0.7.0 (Step 2)`).
    - RENUMBER `### Step 3: Record the new project version` → `### Step 2`. This is the
      MIGR-08 step, it is already correct and codex-native — **keep its body untouched**.
      It was unreachable only because V-01 aborted first.
    - Update every prose reference to the old numbering. Note these edits FIX existing
      latent prose bugs rather than introduce churn: `:148` currently claims "Step 2 never
      records `0.7.0`" — false today (Step 3 records it), true after the renumber. Sweep
      `:143-151`, `:205-212`, `:384-392`, and the Skip cases at `:407-415`, changing
      "Steps 2 and 3" → "Step 2" and similar. Verify with
      `grep -n 'Step 3' migrations/0009-spec-11-region-aware-placement.md` returning
      nothing.
    - ADD a prose note recording the MIGR-08 / MIGR-09 separation, modelled on
      `0008:337-350` and citing it: the migration records the version **in the target
      project** (Step 2); **this repo's own** scaffolder bump is a direct edit in the
      phase's commit, never a migration step shipped to other people's repos. Note that
      MIGR-09 is satisfied by 09.1-03's direct edit.

    Do NOT touch Step 1. Do NOT touch `migrations/run-tests.sh`.
  </action>

  <acceptance_criteria>
    - `grep -c 'skills/agentic-apps-workflow/SKILL.md' migrations/0009-spec-11-region-aware-placement.md`
      counts ONLY prose occurrences inside the T-08-38-style divergence note and the
      MIGR-08/09 separation note. Verify no executable surface carries it:
      `grep -n 'sed -i.*skills/agentic-apps-workflow' migrations/0009-spec-11-region-aware-placement.md`
      returns nothing, and the `applies_to` block contains no `skills/` entry
      (`sed -n '/^applies_to:/,/^requires:/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'skills/'` is `0`).
    - `grep -c '^### Step 3' migrations/0009-spec-11-region-aware-placement.md` is `0`.
    - `grep -c '^### Step 2: Record the new project version' migrations/0009-spec-11-region-aware-placement.md` is `1`.
    - `grep -c 'Steps 2 and 3' migrations/0009-spec-11-region-aware-placement.md` is `0`.
    - The MIGR-08 step's body is unchanged: the file still contains
      `echo "0.7.0" > .codex/workflow-version.txt`.
    - `bash migrations/run-tests.sh 0009` — the MIGR-08/MIGR-09 separation assertion added
      in 09.1-01 Task 2 now PASSes (its three surfaces — `applies_to`, `$pf_block`, every
      Step Apply — are clean).
    - `bash migrations/run-tests.sh 0009` reports **0 FAIL**, and PASS exceeds the 34
      baseline.
    - `bash migrations/run-tests.sh` (full) reports **0 FAIL**. Expect 1 SKIP on the main
      checkout, 2 in a worktree.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | tee /tmp/09.1-02-t2.log; grep -E '^(PASS|FAIL|SKIP)|[0-9]+ (passed|failed)' /tmp/09.1-02-t2.log | tail -5</automated>
  </verify>

  <done>Step 2 is gone, `applies_to` has 2 entries, old Step 3 is now Step 2 with its body intact, all prose renumbered, and the `0009` filter is 0 FAIL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration document → every target project | 0009's steps execute against repos this host does not own; a step that stats or writes a path outside the target project's real shape is a defect shipped to strangers |
| update engine → migration frontmatter | `applies_to` and `to_version` drive the engine; a stale entry misdescribes the blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-04 | Denial of Service | 0009 pre-flight guard 2 (`:95-101`) | mitigate | Greps a project-relative `skills/` path no target project has ⇒ exit 3 on every real install ⇒ the §11 heal never runs and CR-01's data-loss path stays open. Task 1 reads the floor from `.codex/workflow-version.txt` per `0008:73-79`. |
| T-09.1-05 | Tampering | 0009 Step 2's `sed -i` (`:369`) | mitigate | A migration step that rewrites a scaffolder file in someone else's repo — forbidden by `0008:337-350`. Task 2 deletes the step entirely rather than repointing it. |
| T-09.1-06 | Repudiation | `applies_to:9` | mitigate | Frontmatter naming a file the migration must never touch misdescribes the blast radius to the update engine and to any reviewer. Deleted with the step. |
| T-09.1-07 | Spoofing | shell expansion in the ABORT diagnostic | mitigate | `INSTALLED` is interpolated from an untrusted project file into an `echo`. It is read with `cat … 2>/dev/null`, quoted with `${INSTALLED:-unknown}`, and only ever echoed — never eval'd, never used as a path. This is `0008:73-79`'s verbatim shape. |
| T-09.1-08 | Repudiation | diagnostic-string collision | mitigate | Guard 2's new message must not contain `missing or empty`, which is guard 3's exclusive discriminator (criterion 5 depends on it). Asserted in acceptance criteria. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash migrations/run-tests.sh 0009` → 0 FAIL, PASS > 34.
- `bash migrations/run-tests.sh` → 0 FAIL (1 SKIP main / 2 SKIP worktree).
- The 0009 fixture extraction gates are all up — no assertion reads "NOT ASSERTED:
  extraction failed".
- `git diff --stat` touches `migrations/0009-spec-11-region-aware-placement.md` only.
</verification>

<success_criteria>
- 0009's pre-flight reads its floor from `.codex/workflow-version.txt`, accepting 0.6.0 and 0.7.0.
- No `skills/` path in pre-flight, in any Step, or in `applies_to`.
- Step 2 deleted; old Step 3 renumbered to Step 2 with its body byte-identical.
- The document records the V-01 divergence as a **porting error**, citing `0008:470-487`
  and `f9354cc:setup/SKILL.md:146`.
- Every RED from 09.1-01 is GREEN, and the full suite is 0 FAIL.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-02-SUMMARY.md` when done.
Record: the RED→GREEN transition for each 09.1-01 assertion by label, the final counts,
and the confirmation that `extract_step_block` only ever targets step 1.
</output>
