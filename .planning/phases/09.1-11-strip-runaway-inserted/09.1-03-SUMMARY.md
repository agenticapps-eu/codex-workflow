---
phase: 09.1-11-strip-runaway-inserted
plan: 03
subsystem: testing
tags: [bash, awk, migrations, drift-test, version-coupling]

# Dependency graph
requires:
  - phase: 09.1-01
    provides: "Deliberately RED 0009 filter (wave 1) — orthogonal to this plan's scope"
provides:
  - "test_drift() gains a third, consumer-owned leg comparing this repo's .codex/workflow-version.txt against skills/agentic-apps-workflow/SKILL.md's version: field"
  - "This repo's .codex/workflow-version.txt now reads 0.7.0, matching SKILL.md"
  - "Criterion 8 (V-03) closed: the version split is fixed and the suite now catches a future recurrence"
affects: [09.1-04, migrations/run-tests.sh, ".codex/workflow-version.txt"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "MECHANISM (vendored/pinned submodule helper) vs POLICY (consumer-owned assertion) split per ADR-0035 — extend the consumer's wrapper function, never edit vendor/"
    - "RED-before-GREEN as two separate commits for a testable defect: write the failing assertion first, observe and record the FAIL, then fix the underlying fact in a second commit"

key-files:
  created: []
  modified:
    - migrations/run-tests.sh
    - .codex/workflow-version.txt

key-decisions:
  - "Extended test_drift() in place (not a new function) — the third leg is a sibling check within the same drift concern, keeping the `drift` filter as the single entry point for both legs"
  - "Confirmed via `git submodule status` that vendor/agenticapps-shared stayed pinned at 1f5d543 throughout — the new leg lives entirely in the consumer file"

patterns-established:
  - "Falsifiability proof pattern for a new suite assertion: verify the mutation itself lands (cat the file) before trusting the FAIL/PASS transition it produces — an unverified mutation demo is a dead assertion (per Phase 9's `sed: bad flag` no-op precedent cited in the plan)"

requirements-completed: [MIGR-09, TEST-02]

duration: ~15min
completed: 2026-07-15
---

# Phase 09.1 Plan 03: Close the version-drift split (V-03) Summary

**Extended `test_drift()` with a consumer-owned third leg comparing this repo's own `.codex/workflow-version.txt` against `skills/agentic-apps-workflow/SKILL.md`'s `version:` field, observed it RED against the real 0.6.0/0.7.0 split, then fixed the version file to close criterion 8.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-15T18:20:00Z (approx, first read)
- **Completed:** 2026-07-15T18:32:34Z (Task 2 commit)
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `test_drift()` now has a third leg that reads `.codex/workflow-version.txt` and `skills/agentic-apps-workflow/SKILL.md`'s `version:` and fails naming both values on mismatch — closing the exact blind spot that let V-03 slip through unnoticed (the original leg only ever compared SKILL.md against the latest migration's `to_version`, never against this repo's own record).
- The RED was observed for real before any fix: `skill_v=0.7.0`, `proj_v=0.6.0`, committed as `test(09.1-03)` with the version file still at 0.6.0.
- `.codex/workflow-version.txt` bumped to `0.7.0` as a direct edit (MIGR-09), matching 0008's `98c06f5` precedent of bumping both records in one commit. `SKILL.md` was untouched (already `0.7.0` from Phase 9).
- The new leg was proven falsifiable under a verified mutation, not just observed green once: wrote `0.6.0` back, confirmed via `cat` that the mutation landed, ran `drift` and got 1 FAIL naming both versions, then restored `0.7.0`, confirmed the restore landed, and got 0 FAIL.
- A3 verified: `${CODEX_HOME:-$HOME/.codex}/skills/agentic-apps-workflow` is a symlink resolving to this repo's `skills/agentic-apps-workflow` (not a stale copy) — MIGR-09's bump reaches the running host.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add test_drift's consumer-side third leg, and observe it RED** - `fb6b148` (test)
2. **Task 2: Bump this repo's own version record to 0.7.0 (MIGR-09 direct edit)** - `7a1e7bc` (fix)

_RED (`fb6b148`) committed strictly before GREEN (`7a1e7bc`), per plan requirement — confirmed via `git log --oneline -4`._

## Files Created/Modified
- `migrations/run-tests.sh` - `test_drift()` extended with a third, consumer-owned leg (20 lines added, existing `run_drift_test` leg untouched)
- `.codex/workflow-version.txt` - bumped `0.6.0` → `0.7.0`

## Decisions Made
- Extended the existing `test_drift()` function rather than adding a new top-level filter function, so `bash migrations/run-tests.sh drift` remains the single entry point covering both the mechanism leg (`run_drift_test`) and the new consumer policy leg.
- Did not touch `vendor/agenticapps-shared/migrations/lib/drift-test.sh` — confirmed via `git submodule status` before and after both commits that the submodule stayed pinned at `1f5d543`.

## Deviations from Plan

**1. [Rule 3 - Blocking] Submodule was not checked out in this worktree**
- **Found during:** Task 1 setup, before writing the RED assertion
- **Issue:** `vendor/agenticapps-shared` showed as uninitialized (`git submodule status` reported a leading `-`), so `run_drift_test` (sourced from `vendor/agenticapps-shared/migrations/lib/drift-test.sh`) was unavailable and the harness would hard-fail on any invocation.
- **Fix:** Ran `git submodule update --init --recursive`, which checked out the submodule at the pinned commit `1f5d543bc6ca080ab6e3ba188df33cf3d193e3d4` (matches the plan's stated pin) — no version change, purely a local worktree checkout operation, not a content edit.
- **Files modified:** None (submodule checkout only; not a tracked content change in this worktree's diff).
- **Verification:** `git submodule status` before and after both commits reports `1f5d543...vendor/agenticapps-shared (v1.0.0)`, unmodified.
- **Committed in:** N/A — not a commit-worthy change; the submodule pointer in the parent repo was already correct, only the local checkout was missing.

---

**Total deviations:** 1 auto-fixed (1 blocking, submodule checkout)
**Impact on plan:** Necessary to run the harness at all in this worktree. No scope creep — the submodule content and pin were never touched.

## Issues Encountered
None beyond the submodule checkout noted above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

- Criterion 8 (V-03) is closed: both version records read `0.7.0`, and `test_drift` will hard-fail if they ever split again.
- **Scoped verification only** (per plan and wave-2 note): `bash migrations/run-tests.sh drift` → 0 FAIL, 2 PASS. `bash migrations/run-tests.sh 0004` (untouched neighbour filter) → 0 FAIL, 2 PASS.
- **Full suite is NOT a gate for this plan** and was run once for information only: `bash migrations/run-tests.sh 0009` → 35 PASS / 3 FAIL. These 3 FAILs are EXPECTED per the wave-2 note in the plan's `<context>` — they are 09.1-01's deliberate RED (0009 filter), being turned green concurrently by 09.1-02 in a parallel worktree. They are not this plan's concern and were left untouched.
- 09.1-04 is the first plan that can legitimately gate on the full suite, once 09.1-02 has merged.

---
*Phase: 09.1-11-strip-runaway-inserted*
*Completed: 2026-07-15*

## Self-Check: PASSED

- FOUND: migrations/run-tests.sh
- FOUND: .codex/workflow-version.txt (reads `0.7.0`)
- FOUND: .planning/phases/09.1-11-strip-runaway-inserted/09.1-03-SUMMARY.md
- FOUND: commit fb6b148 (Task 1, test)
- FOUND: commit 7a1e7bc (Task 2, fix)
