---
phase: 09.1-11-strip-runaway-inserted
plan: 03
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - migrations/run-tests.sh
  - .codex/workflow-version.txt
autonomous: true
requirements: [MIGR-09, TEST-02]

must_haves:
  truths:
    - "This repo's .codex/workflow-version.txt and skills/agentic-apps-workflow/SKILL.md both read 0.7.0"
    - "The suite FAILS if those two ever diverge again"
  artifacts:
    - path: ".codex/workflow-version.txt"
      provides: "this repo's own workflow version record"
      contains: "0.7.0"
    - path: "migrations/run-tests.sh"
      provides: "test_drift's consumer-side third leg comparing the version file to SKILL.md"
      contains: "workflow-version.txt"
  key_links:
    - from: "test_drift()"
      to: ".codex/workflow-version.txt"
      via: "direct read + comparison against SKILL.md's version:"
      pattern: "workflow-version\\.txt"
---

<objective>
Close criterion 8 (V-03): this repo's `.codex/workflow-version.txt` reads `0.6.0` while
`skills/agentic-apps-workflow/SKILL.md:3` reads `0.7.0`. Make them agree, and make the
suite catch a future split.

Purpose: 0008's precedent bumped **both** in one commit (`98c06f5`). Phase 9 bumped only
SKILL.md. Nothing caught it because `test_drift` never reads the version file — it only
compares SKILL.md against the latest migration's `to_version`. Same root confusion as V-01:
MIGR-08 (the migration's job, in the target project) conflated with MIGR-09 (this repo's
own record).

**This plan carries a free RED.** The third leg is RED the moment you write it, against the
real current fact (`skill_v=0.7.0`, `proj_v=0.6.0`). Write the assertion first, watch it
fail, then fix the fact. Two commits, in that order.

Output: `test_drift` gains a third, consumer-owned leg; the version split is closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md

**CLAUDE.md's GitNexus mandate is INERT** — shell + a one-line version file. Grep is
ground truth. Do not claim GitNexus analysis.

**Do NOT edit anything under `vendor/`.** `run_drift_test` lives in
`vendor/agenticapps-shared/migrations/lib/drift-test.sh`, a git submodule pinned at
`1f5d543`. Its own header states the rule (ADR-0035): the lib ships the **MECHANISM**
only; the **POLICY** is a consumer-owned invariant. "This repo's own record agrees with its
scaffolder" is a policy this repo owns. A task whose file list includes `vendor/` is wrong
by construction — the change will not commit from here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_drift's consumer-side third leg, and observe it RED</name>

  <read_first>
    - `migrations/run-tests.sh:3193-3211` — `test_drift()`, the function you are extending.
      Note its existing comment already names the ADR-0035 mechanism/policy split — you are
      following a rule the file already states, not inventing one.
    - `vendor/agenticapps-shared/migrations/lib/drift-test.sh` — **read the header only**,
      to confirm the mechanism/policy rule. Do not edit this file.
    - `skills/agentic-apps-workflow/SKILL.md:1-6` — the frontmatter carrying `version:`.
    - `.codex/workflow-version.txt` — this repo's own record. One line.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Code Example 5"
      — the leg's shape.
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    Extend `test_drift()` with a third leg, placed after the existing `run_drift_test`
    call and inside the same function. It must:
    - read the scaffolder version:
      `skill_v=$(grep -m1 '^version:' "$REPO_ROOT/skills/agentic-apps-workflow/SKILL.md" | awk '{print $2}')`
    - read this repo's own record: `proj_v=$(cat "$REPO_ROOT/.codex/workflow-version.txt" 2>/dev/null)`
    - PASS iff they are string-equal; FAIL naming both values and citing V-03.
    - use the surrounding `PASS=$((PASS+1))` / `FAIL=$((FAIL+1))` idiom so the counters stay
      consistent with the rest of the harness.

    Declare `skill_v`/`proj_v` `local` to match the file's convention.

    Write a comment above the leg stating: the MECHANISM (`run_drift_test`) is a pinned
    submodule and is NOT edited; the POLICY is consumer-owned per ADR-0035; V-03 slipped
    through precisely because nothing compared these two files; 0008's `98c06f5` bumped
    both in one commit and this leg is what makes that precedent enforceable.

    **Then run it and record the RED before fixing anything.** Commit with
    `test(09.1-03): …` and put the observed `skill_v` / `proj_v` values and the FAIL label
    in the commit body. The version file must still read `0.6.0` at this commit.
  </action>

  <acceptance_criteria>
    - `sed -n '/^test_drift()/,/^}/p' migrations/run-tests.sh | grep -c 'workflow-version.txt'`
      is `>= 1`.
    - `grep -rc 'vendor/' migrations/run-tests.sh` shows no NEW `vendor/` write path;
      `git diff --stat` lists `migrations/run-tests.sh` and nothing under `vendor/`.
      `git submodule status` reports `vendor/agenticapps-shared` unchanged at `1f5d543`.
    - `bash migrations/run-tests.sh drift` reports **exactly 1 FAIL**, and the FAIL message
      names both versions — `SKILL.md=0.7.0` and `.codex/workflow-version.txt=0.6.0`.
      **This is the recorded RED observation for criterion 8.**
    - The existing `run_drift_test` leg still PASSes (you extended the function, not
      replaced it): the `drift` filter reports `>= 1 PASS`.
    - `.codex/workflow-version.txt` still reads `0.6.0` at this commit —
      `git show HEAD:.codex/workflow-version.txt` is `0.6.0`. If it reads `0.7.0`, the RED
      has been forfeited: revert and redo as two commits.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh drift 2>&1 | tee /tmp/09.1-03-t1.log; grep -c 'FAIL' /tmp/09.1-03-t1.log</automated>
  </verify>

  <done>`test_drift` has a third consumer-side leg, it is OBSERVED FAILING with both version values named, and the RED is committed with the version file still at 0.6.0.</done>
</task>

<task type="auto">
  <name>Task 2: Bump this repo's own version record to 0.7.0 (MIGR-09 direct edit)</name>

  <read_first>
    - `.codex/workflow-version.txt` — current content.
    - `migrations/0008-plan-review-gate.md:337-350` — the rule: this repo's own bump is a
      **direct edit in the phase's commit**, never a migration step. This task is that
      direct edit.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-03-SUMMARY.md` is not yet
      written — read your own Task 1 RED output instead.
    - `migrations/0009-spec-11-region-aware-placement.md:1-13` — confirm `to_version: 0.7.0`
      is what you are aligning to.
  </read_first>

  <files>.codex/workflow-version.txt</files>

  <action>
    Write `0.7.0` to `.codex/workflow-version.txt` (one line, trailing newline, no other
    content). This is MIGR-09's direct edit, matching 0008's `98c06f5` precedent which
    bumped both records in one commit. `skills/agentic-apps-workflow/SKILL.md` is already
    at `0.7.0` from Phase 9, so this is the half that was missed.

    This repo self-applies its workflow and its own `AGENTS.md` §11 is already on-anchor,
    so 0009 has nothing to heal here — recording `0.7.0` is factually correct, not a claim
    about work not done.

    **Also verify assumption A3 and record the result in the SUMMARY** (this is a one-time
    check, not a permanent assertion — it is host-environment dependent and must not become
    a suite assertion that fails in CI): confirm that
    `${CODEX_HOME:-$HOME/.codex}/skills/agentic-apps-workflow` is a **symlink resolving
    into this repo** (`test -L` + `readlink`), not a stale copy. If it is a copy, MIGR-09's
    bump never reaches the running host — report that as a finding; do not try to fix it in
    this plan.

    Commit separately from Task 1, with `fix(09.1-03): …`.
  </action>

  <acceptance_criteria>
    - `cat .codex/workflow-version.txt` is exactly `0.7.0`.
    - `grep -c '^version: 0.7.0$' skills/agentic-apps-workflow/SKILL.md` is `1` (unchanged —
      you did not touch it).
    - `bash migrations/run-tests.sh drift` reports **0 FAIL**. Task 1's RED is now GREEN.
    - `bash migrations/run-tests.sh` (full) reports **0 FAIL**.
    - **The leg is proven falsifiable, not just green.** Temporarily write `0.6.0` back,
      confirm `bash migrations/run-tests.sh drift` reports 1 FAIL, then restore `0.7.0` and
      confirm 0 FAIL. Record both observations. Before trusting the mutation, assert it
      landed: `cat .codex/workflow-version.txt` must actually read `0.6.0` during the
      mutated run. In Phase 9 a `sed: bad flag` silently no-op'd and the run *looked* like
      a passing demo — an unverified mutation demo is itself a dead assertion.
    - `git log --oneline -2` shows the `test(09.1-03)` RED commit BEFORE the
      `fix(09.1-03)` GREEN commit.
    - A3's finding (symlink vs copy) is recorded in the SUMMARY with the actual
      `readlink` output.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh drift 2>&1 | tee /tmp/09.1-03-t2.log; grep -c 'FAIL' /tmp/09.1-03-t2.log</automated>
  </verify>

  <done>Both version records read 0.7.0, the drift filter is 0 FAIL, and the new leg has been observed failing under a verified mutation and passing after restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo's published version claim → every project it scaffolds | `SKILL.md`'s `version:` is what installs globally; `.codex/workflow-version.txt` is what the update skill reads. A split means the host claims one version and records another. |
| pinned submodule → consumer | `vendor/agenticapps-shared` ships mechanism; policy assertions must not be pushed across that boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-09 | Repudiation | version split (V-03) | mitigate | The host reports two different installed versions depending on which file you read, so the update chain's pending-migration calculation is unreliable. Task 2 aligns them; Task 1's leg makes a future split a hard FAIL. |
| T-09.1-10 | Repudiation | `test_drift` before this plan | mitigate | A drift test that never reads the version file is a control that cannot detect the drift it is named for. Task 1's leg is proven falsifiable by a verified mutation (write 0.6.0 → FAIL → restore → PASS). |
| T-09.1-11 | Tampering | `vendor/agenticapps-shared` submodule | mitigate | Editing the pinned mechanism to satisfy a consumer policy is a cross-repo change that will not commit and would break five other consumers. Explicitly out of scope; asserted via `git submodule status` unchanged at `1f5d543`. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash migrations/run-tests.sh drift` → 0 FAIL, `>= 2 PASS`.
- `bash migrations/run-tests.sh` (full) → 0 FAIL.
- `git submodule status` → `vendor/agenticapps-shared` at `1f5d543`, unmodified.
- `git diff --stat` touches only `migrations/run-tests.sh` and `.codex/workflow-version.txt`.
</verification>

<success_criteria>
- `.codex/workflow-version.txt` and `skills/agentic-apps-workflow/SKILL.md` both read `0.7.0`.
- `test_drift` compares them, and the comparison has been **observed failing** — once for
  free against the real split, and once under a verified mutation.
- Nothing under `vendor/` changed.
- Two commits, RED before GREEN.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-03-SUMMARY.md` when done.
Record: the free-RED observation (both version values), the mutation demo's before/after,
proof the mutation landed, and A3's symlink-vs-copy finding with actual `readlink` output.
</output>
</content>
