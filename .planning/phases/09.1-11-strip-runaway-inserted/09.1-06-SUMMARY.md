---
phase: 09.1-11-strip-runaway-inserted
plan: 06
subsystem: testing
tags: [bash, awk, migration-harness, run-tests.sh, migration-0009, mutation-testing]

# Dependency graph
requires:
  - phase: 09.1-11-strip-runaway-inserted plan 05
    provides: "PROV_RE anchored at four sites, the refuse gate + strip awk's unresolved/END{exit 4} guard, strip_rc dispatch, h2-count strip-integrity guard kept after its own verified mutation — the fully-green base this plan mutates against"
provides:
  - "The D-28.1 document-contract checks (case 10 layers 1/2) now match a comment-stripped pf_code view, not the pre-flight's raw text — closing CR-03's self-satisfying-comment gap"
  - "Case 10(a)'s diagnostic-isolation assertion — the only thing that discriminates guard 3 (test -s) from guard 4 (tail sentinel) on a zero-byte mirror, per WR-04"
  - "12-idempotent-rerun — ANCHOR-05's only live suite coverage of the strip terminator's alternation on an ordinary second run against an already-healed region-led file"
  - "Two recorded mutation-gate demos (deletion of test -s, narrowing of the strip terminator) with landed-proof, observed RED labels, and restore-proof"
affects: [09.1-07-strip-runaway-inserted]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Comment-stripped document-contract matching: derive pf_code = pf_block with '^[[:space:]]*#' lines removed before any case/grep-based structural assertion, so a guard's own prose describing itself cannot satisfy a check for the guard's presence"
    - "Diagnostic-text isolation over exit-code isolation: when two guards produce the same exit code on the same input by construction (WR-04), assert on the diagnostic message — the only discriminator — rather than the exit code, which cannot tell them apart"
    - "Fixture-BEFORE-state built by running the migration, not hand-writing it: 12-idempotent-rerun's before-state is 01-gitnexus-led-inject's shape run through Apply once, so the fixture cannot drift from what 0009 actually emits"
    - "Mutation gate discipline: every guard-liveness claim is deleted/narrowed, landed-proof captured via a before/after grep count, run and observed RED with the exact FAIL labels recorded, then restored and reverified via git diff --exit-code and a 0-FAIL rerun — never asserted from reasoning alone"

key-files:
  created: []
  modified:
    - migrations/run-tests.sh

key-decisions:
  - "12-idempotent-rerun's BEFORE state uses the region-LED shape (healed block immediately followed by <!-- gitnexus:start -->), not the off-anchor-but-healthy state-A shape, because state A's idempotency check short-circuits Apply entirely and never reaches the strip terminator — only a region-led healed file puts the terminator's alternation on the critical path of an ordinary second run."
  - "Case 10(a)'s new diagnostic assertion keeps the existing rc -eq 3 assertion rather than replacing it (per plan instruction) — the rc assertion is still true and worth keeping, it just cannot discriminate layers by itself; the new case on $out is what does that job."
  - "migrations/0009-spec-11-region-aware-placement.md is confirmed byte-identical to this plan's base (ab257d3) via git diff --stat returning empty across both task commits — both tasks' mutations were transient and fully restored."

requirements-completed: [MIGR-06, TEST-03, ANCHOR-05]

duration: ~20min
completed: 2026-07-15
---

# Phase 09.1 Plan 06: Make two guards' assertions live, and prove it by mutation Summary

**Tightened the D-28.1 document-contract checks to match comment-stripped pre-flight code instead of raw text (closing CR-03), added case 10(a)'s diagnostic-isolation assertion for the `test -s` vs. tail-sentinel layers (WR-04), and added `12-idempotent-rerun` — ANCHOR-05's only live suite coverage of the strip terminator's alternation — each proven live by a recorded delete-observe-restore mutation gate, with `migrations/0009-spec-11-region-aware-placement.md` verified byte-identical to plan start throughout.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-15T21:17:00Z (approx, Task 2 commit)
- **Tasks:** 2 completed
- **Files modified:** 1 (`migrations/run-tests.sh`); `migrations/0009-spec-11-region-aware-placement.md` mutated transiently in both tasks and restored byte-identical

## Accomplishments

- **Task 1 — criterion 5 / CR-03 closed.** Derived `pf_code` (a comment-stripped view of the extracted pre-flight, via `grep -v '^[[:space:]]*#'`) and matched both D-28.1 `case` blocks against it instead of the raw `$pf_block`. Tightened layer 1's pattern from the bare `*'test -s'*` to the executable shape `*'test -s "$MIRROR"'*`. Added case 10(a)'s diagnostic-isolation assertion on `$out` (`case "$out" in *'missing or empty'*)`), keeping the existing `rc -eq 3` assertion unchanged. Verified A4 live (`grep -c 'missing or empty'` inside guard 4's block is `0`).
- **Task 2 — criterion 6 / MIGR-06 / ANCHOR-05 closed.** Added `12-idempotent-rerun`: builds its BEFORE state by running Step 1 Apply once against a region-led file (same shape as `01-gitnexus-led-inject`), then runs Apply a second time against that healed output and asserts five things (rc 0, byte-identity via `cmp -s`, region markers still paired exactly once, region body survival, exactly one provenance line). All five PASS on the real, unmutated 0009.
- **Both mutation gates executed, observed RED, and restored** — full transcripts below. `migrations/run-tests.sh` grew by 91 lines net (35 in Task 1, 56 in Task 2); `migrations/0009-spec-11-region-aware-placement.md` is confirmed byte-identical to plan start (`git diff --stat` against base `ab257d3d8a16666859b6cb7c5860ac5df25ab171` returns empty for that path).

## Task Commits

1. **Task 1: Make test -s and case 10(a) live assertions, prove by deletion** - `bce8eb4` (fix)
2. **Task 2: Add 12-idempotent-rerun, prove ANCHOR-05 by narrowing terminator** - `b940f09` (feat)

## Files Created/Modified

- `migrations/run-tests.sh` — `pf_code` derivation + both D-28.1 `case` blocks re-anchored (Task 1); case 10(a)'s diagnostic-isolation assertion added (Task 1); `12-idempotent-rerun` fixture added, gated on `apply_ok`, with an `_m0009_fail` fallback in the extraction-down branch (Task 2).

## Mutation Demo 1 (Task 1) — full transcript

**Baseline before mutation:** `git status --short migrations/0009-spec-11-region-aware-placement.md` → clean. `grep -cF 'test -s "$MIRROR"' migrations/0009-spec-11-region-aware-placement.md` → `1`.

**Delete command:** removed lines 114-119 (the executable `test -s "$MIRROR" || { ...; exit 3; }` block), leaving the comment at :106-112 untouched.

**Landed-proof:** `grep -cF 'test -s "$MIRROR"' migrations/0009-spec-11-region-aware-placement.md` → `0` (dropped from `1`).

**Observed RED** (`bash migrations/run-tests.sh 0009`, verbatim FAIL labels):
```
FAIL 0009 Pre-flight carries D-28.1 layer 1 (test -s — zero-byte mirror guard)
FAIL 10-corrupt-mirror-refused (a) zero-byte mirror refused, but not by test -s (wrong layer)
=== Summary ===
  PASS: 56
  FAIL: 2
```
Both new assertions went RED, exactly as required. `10-corrupt-mirror-refused (a)`'s original `rc -eq 3` assertion stayed **PASS** under the mutation (`got exit=3`) — this is WR-04's point demonstrated, not a bug in the mutation: guard 4 (the tail sentinel) also fails a zero-byte mirror with exit 3, so the exit code alone cannot isolate layer 1; only the diagnostic text can, and it did.

**Restore:** `git checkout migrations/0009-spec-11-region-aware-placement.md`. `grep -cF 'test -s "$MIRROR"'` back to `1`. `git diff --exit-code migrations/0009-spec-11-region-aware-placement.md` → clean (exit 0). `bash migrations/run-tests.sh 0009` → `PASS: 58`, `0 FAIL`.

## Mutation Demo 2 (Task 2) — full transcript

**Baseline before mutation:** `git status --short migrations/0009-spec-11-region-aware-placement.md` → clean. Alternation count `grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md` → `2` (line 304, the strip terminator; line 387, the insert anchor).

**Edit:** narrowed line 304 ONLY — `in_block && swallowed_own_h2 && (/^## / || /^<!-- gitnexus:start -->$/)` → `in_block && swallowed_own_h2 && (/^## /)`. Line 387 (the insert's anchor) left untouched, isolating the mutation to the terminator exactly as ANCHOR-05 describes.

**Landed-proof:** alternation count dropped `2` → `1`.

**Observed RED** (`bash migrations/run-tests.sh 0009`, verbatim FAIL labels):
```
FAIL 12-idempotent-rerun: second run leaves AGENTS.md BYTE-IDENTICAL to the first run's output (true idempotency, MIGR-06)
FAIL 12-idempotent-rerun: region markers still paired exactly once each (start=0 end=1) — ANCHOR-05's only live suite coverage
=== Summary ===
  PASS: 61
  FAIL: 2
```
Observed marker counts under the mutation: `start=0 end=1` — an orphaned, unpaired `<!-- gitnexus:end -->`, exactly the signature the plan predicted (the narrowed terminator runs past `<!-- gitnexus:start -->` on the already-healed region-led file, consuming the marker itself). `12-idempotent-rerun`'s other three assertions (`rc -eq 0`, body-content survival, exactly-one-provenance-line) stayed PASS — the destruction is a marker/content-loss defect, not a crash.

**Restore:** `git checkout migrations/0009-spec-11-region-aware-placement.md`. Alternation count back to `2`. `git diff --exit-code migrations/0009-spec-11-region-aware-placement.md` → clean. Full suite (`bash migrations/run-tests.sh`) → `PASS: 344`, `SKIP: 2`, `0 FAIL`.

## Byte-identity verification (per plan's non-negotiable requirement)

`migrations/0009-spec-11-region-aware-placement.md` is listed in `files_modified` only because both tasks mutate it transiently. Verified byte-identical at plan end:
```
git diff --stat ab257d3d8a16666859b6cb7c5860ac5df25ab171 HEAD -- migrations/0009-spec-11-region-aware-placement.md
```
returns **empty** — confirmed after both task commits, not just the working tree.

## Decisions Made

Documented above under `key-decisions` frontmatter: the region-led (not off-anchor-healthy) shape choice for fixture 12's BEFORE state, keeping (not replacing) case 10(a)'s original `rc -eq 3` assertion alongside the new diagnostic assertion, and the explicit byte-identity verification methodology for 0009.

## Deviations from Plan

None — both tasks executed as written. The plan's own acceptance-criteria grep patterns (`pf_code` count `>= 3`, the `grep -v` derivation count `>= 1`, `test -s "$MIRROR"` count `>= 1`, A4's re-confirmed `0`, `12-idempotent-rerun` count `>= 5`, `11-idempotent-rerun` count `0`) were all verified before each commit and matched exactly.

## Issues Encountered

- `vendor/agenticapps-shared` submodule was not initialized in this worktree (the same environment gap every prior 09.1 plan recorded). Ran `git submodule update --init --recursive` before the first `run-tests.sh` invocation could proceed. Read-only, non-destructive, produced no working-tree changes to commit — environment setup, not a plan deviation.
- Confirmed the stale-comment note flagged in `09.1-05-SUMMARY.md`'s prior-wave-context (the `10-corrupt-mirror-refused` fixture's comment block still claiming "mk_project supplies `.git` + SKILL.md at 0.6.0") — this plan's Task 1 edit vicinity is immediately adjacent (case 10(a)'s new assertion sits inside that same fixture) but the stale comment itself is above the vicinity this task's action specified (the D-28.1 `case` blocks and case 10(a)'s new assertion only), so it was left as-is per the prior-wave-context instruction to report rather than fix out-of-vicinity detours. Reported here again for 09.1-07's housekeeping pass.

## Next Phase Readiness

- Criteria 5 and 6 are both closed with recorded mutation-gate evidence (this SUMMARY's two full transcripts) for 09.1-07's ADR to cite as satisfying evidence.
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at plan start (verified via `git diff --stat` against the plan's base commit, empty for that path across both task commits).
- Full suite: 344 PASS / 0 FAIL / 2 SKIP (worktree; main checkout would show 1 SKIP per prior-wave-context).
- No blockers for 09.1-07.

---
*Phase: 09.1-11-strip-runaway-inserted*
*Completed: 2026-07-15*
