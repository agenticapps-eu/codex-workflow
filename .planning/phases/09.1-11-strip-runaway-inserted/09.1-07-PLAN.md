---
phase: 09.1-11-strip-runaway-inserted
plan: 07
type: execute
wave: 6
depends_on: [09.1-05, 09.1-06]
files_modified:
  - docs/decisions/0010-region-aware-spec-11-placement.md
  - skills/setup-codex-agenticapps-workflow/SKILL.md
autonomous: false
requirements: [DOC-01]

must_haves:
  truths:
    - "ADR-0010 records the runaway, the D-26 correction, V-01 as a porting error, the Q1 ruling with its rejected alternative, and D-48's re-pin to f9354cc"
    - "An upstream defect is filed against claude-workflow for CR-01 only"
    - "The setup-SKILL note cites Step 3, not Step 6"
  artifacts:
    - path: "docs/decisions/0010-region-aware-spec-11-placement.md"
      provides: "corrected decision record"
      contains: "f9354cc"
  key_links:
    - from: "ADR-0010"
      to: "upstream f9354cc"
      via: "D-48's re-pin, with 8520f90 recorded as the superseded PR-branch commit"
      pattern: "f9354cc"
---

<objective>
Record what this phase learned, and tell upstream about the one defect they still carry.

Purpose: ADR-0010 currently asserts, as a load-bearing decision, that the strip is "bounded
by construction, so a drifted block cannot cause a runaway" (`:154-155`). **That is false
and it was reproduced.** It also pins upstream at `8520f90` (`:5`, `:189`, `:214`, `:220`)
— a PR-branch commit that **never landed on main**. An ADR that carries a falsified claim
and a dead citation is worse than no ADR: the next reader will trust it.

CR-01 is still live at upstream HEAD (`f9354cc:0029:222-241`), and upstream's new "Known
limitations" section lists CRLF and fenced markers but **not** the runaway — they are
unaware, not accepting. That makes this new information to them.

Output: a corrected ADR, an upstream bug report, and WR-02's one-line citation fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-06-SUMMARY.md
@.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md
@.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md

**CLAUDE.md's GitNexus mandate is INERT** — Markdown only.

**Scope note.** RESEARCH's "Deferred Ideas" lists WR-02 as out of scope; the phase
orchestrator subsequently ruled it **in scope** (cheap, one citation). The orchestrator's
ruling wins. WR-05, IN-01, IN-02, IN-03, IN-04, and migration 0007's identical pre-flight
bug remain **out of scope** — 0008 already deferred 0007 explicitly (`0008:484-487`:
"different migration, own scope … fixing it means re-testing a shipped upgrade path").
Do not let this plan widen.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ADR-0010 (criterion 9 / DOC-01)</name>

  <read_first>
    - `docs/decisions/0010-region-aware-spec-11-placement.md` — **in full.** Specifically
      `:5` (the pin), `:135` (D-25/D-26), `:154-155` (the falsified "bounded by
      construction" claim), `:189`, `:214-220` (D-48), and `:265` (which cites
      `0000-baseline.md:102` correctly — the contrast that proves WR-02 is the *skill's*
      error, not the ADR's).
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md` — the branch
      division (refuse gate vs END guard), the criterion-4 KEEP/REMOVE determination, the
      final alternation count.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-06-SUMMARY.md` — both mutation
      demos.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Pitfall 1" — the
      D-26 reconciliation (prose drift vs heading drift), and § "State of the Art" — the
      full old→new table.
    - `migrations/0009-spec-11-region-aware-placement.md` — the final shipped text. The ADR
      must describe what actually shipped, not what was planned.
  </read_first>

  <files>docs/decisions/0010-region-aware-spec-11-placement.md</files>

  <action>
    Add a dated correction section (2026-07-15, Phase 9.1) and amend the affected decisions
    in place. Do not silently rewrite history — record what was believed, what is now known,
    and what changed it. Cover all five items criterion 9 names:

    **(1) The runaway (CR-01).** The strip's entry was decoupled from its exit: a drifted
    `## Coding Discipline (NON-NEGOTIABLE)` heading latches `in_block` and `in_block { next }`
    eats to EOF — reproduced, 16 lines → 4, destroying `## Critical Project Rules` and
    `## Deployment`. All three guards passed: the post-strip `grep -q` is satisfied by the
    *insert* pass re-adding the heading it checks for. Record the reachability argument —
    the abort branch fires on `heading present AND provenance absent`; the runaway needs the
    **inverse** — because without it "drifted heading" reads as exotic.

    **(2) The D-26 correction.** D-26's *claim* at `:154-155` — "bounded by construction, so
    a drifted block cannot cause a runaway" — **is false**. D-26's *principle* survives: the
    strip stays blind to the block's **prose**, because content fidelity is 0004's job. The
    reconciliation is that heading drift and prose drift are different, and the canonical
    mirror proves it — it has exactly one `## ` line, on line 1, and that line is the
    heading (now asserted in `test_migration_0004`). The heading is a structural boundary
    marker, not content. So: prose drift → stay blind and repair; heading drift → the
    boundary is gone, refuse. State the corrected claim: **bounded only when the heading
    matches — enforced by the refuse gate and the END fail-closed guard.** This is D-25's
    rejected bug class resurfacing inside the boundary chosen to prevent it.

    **(3) V-01, as a PORTING ERROR — not an inherited defect.** 0009's pre-flight greped the
    project-relative `skills/agentic-apps-workflow/SKILL.md`, which no target project has,
    so it exited 3 on every real install and Step 1 never ran. This is a regression against
    an established, documented precedent: `0008:470-487` (T-08-38) names this exact defect
    in 0007 and calls it "a defect this migration does not replicate". **Upstream is not at
    fault** — upstream greps `.claude/skills/agentic-apps-workflow/SKILL.md`, a path its own
    setup skill creates (`f9354cc:setup/SKILL.md:146`); **our port dropped the `.claude/`
    prefix**. On this host, skills install globally at `${CODEX_HOME}/skills/…` and the
    project version lives in `.codex/workflow-version.txt`; `skills/agentic-apps-workflow/`
    is *this scaffolder repo's own source tree*, which is why it looked right to its author
    and why the suite manufactured a synthetic SKILL.md to keep it green. Record the root
    cause: MIGR-08 and MIGR-09 conflated, which 0008 kept apart on purpose (`0008:337-350`).
    Record that **314 PASS / 0 FAIL was fully consistent with a migration that never ran.**

    **(4) The Q1 ruling, with its rejected alternative.** Decision: provenance present +
    exact H2 absent ⇒ **refuse, exit 3, file byte-identical**. One rule covers both
    reproduced runaway shapes because both are exactly that condition. It closes CR-01 by
    construction — a strip that refuses to run cannot run away — and it is consistent with
    0009's existing never-overwrite-a-hand-paste branch. **Rejected: heal-and-duplicate**
    (un-latch at the structural boundary and insert anyway): no data loss, but it silently
    leaves two similar §11 headings that a future migration's own conflict grep would trip
    on, and RESEARCH reproduced that un-latching alone still runs away to EOF on the
    orphaned-provenance shape (8 → 4). Record that the reviewer's un-latch rule was
    therefore **not adopted**, and that the refuse gate is the mechanism while the END guard
    (`END { if (unresolved || (in_block && !swallowed_own_h2)) exit 4 }`) is the backstop for
    the mixed-provenance shape the file-global gate cannot see. Cite fixture 15 as the
    END guard's falsifiability proof.

    **(5) D-48's re-pin.** Record **both** refs, per RESEARCH Q5 — citing a dead PR-branch
    commit as the upstream reference will confuse the next reader:
    - pinned at `8520f90` during Phase 9; it was a **PR-branch commit that never landed on
      main** (`git merge-base --is-ancestor 8520f90 HEAD` → NO);
    - PR #89 squash-merged as **`f9354cc`**, upstream HEAD;
    - upstream **already fixed CR-02** there (anchored `PROV_RE` in all three sites, plus a
      `11-prose-mention-provenance` fixture); **9.1 ported that fix rather than re-inventing
      it**;
    - 9.1 **diverges** on CR-01, which remains live upstream at `f9354cc:0029:222-241`.
    Update `:5`, `:189`, `:214`, `:220` to cite `f9354cc`, marking `8520f90` as "as pinned
    during Phase 9 (PR branch, never merged)".

    **Also record**, from the SUMMARYs: the criterion-4 KEEP/REMOVE determination for the
    h2-count guard and its reasoning; the WR-01 mirror single-`## ` coupling the refuse gate
    rests on, now asserted in `test_migration_0004`; and awk portability (A2) as a known
    limitation — `END { exit 4 }` is POSIX and verified on BSD awk 20200816, `gawk`/`mawk`
    are not installed here, so portability is argued rather than verified. This is the same
    exposure the existing awk already carries; the phase does not widen it.
  </action>

  <acceptance_criteria>
    - `grep -c 'bounded by construction' docs/decisions/0010-region-aware-spec-11-placement.md`
      — every remaining occurrence is inside the correction, explicitly marked false. Verify
      no unqualified assertion survives by reading `:154-155`'s amended text.
    - `grep -c 'f9354cc' docs/decisions/0010-region-aware-spec-11-placement.md` is `>= 4`
      (the header pin plus the D-48 correction sites).
    - `grep -c '8520f90' docs/decisions/0010-region-aware-spec-11-placement.md` is `>= 1`
      AND every occurrence is qualified as a PR-branch commit that never merged — the
      history is recorded, not erased.
    - `grep -c 'porting error' docs/decisions/0010-region-aware-spec-11-placement.md` is `>= 1`.
    - `grep -c 'heal-and-duplicate\|heal and duplicate' docs/decisions/0010-region-aware-spec-11-placement.md`
      is `>= 1` — the rejected alternative is named, per DOC-01's own wording ("including
      the rejected alternative").
    - `grep -c 'T-08-38\|0008:470' docs/decisions/0010-region-aware-spec-11-placement.md` is `>= 1`.
    - `grep -c 'exit 4\|END guard\|fail-closed' docs/decisions/0010-region-aware-spec-11-placement.md`
      is `>= 1`.
    - `grep -c '314 PASS' docs/decisions/0010-region-aware-spec-11-placement.md` is `>= 1` —
      the phase's central lesson is recorded, not just its fixes.
    - Every claim about what shipped matches the shipped file. Spot-check at least the
      criterion-4 KEEP/REMOVE statement against
      `grep -c 'h2_out' migrations/0009-spec-11-region-aware-placement.md`. An ADR that
      misdescribes the code is the defect class this phase closes, in prose.
  </acceptance_criteria>

  <verify>
    <automated>for s in f9354cc 8520f90 'porting error' 'heal-and-duplicate' 'T-08-38' '314 PASS'; do printf '%s: ' "$s"; grep -c "$s" docs/decisions/0010-region-aware-spec-11-placement.md; done</automated>
  </verify>

  <done>ADR-0010 records all five criterion-9 items, cites `f9354cc` with `8520f90` marked as never-merged, and every claim matches the shipped 0009.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — the setup-SKILL note cites Step 6; it is Step 3</name>

  <read_first>
    - `skills/setup-codex-agenticapps-workflow/SKILL.md:118-130` — the note. Line 124 reads
      "`0000-baseline.md`'s Step 6 is a plain append of `templates/agents-md-additions.md`".
    - `migrations/0000-baseline.md:91-102` — **Step 3: Append AGENTS.md sections from
      template**, whose Apply is
      `cat "${CODEX_HOME:-…}/skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md" >> AGENTS.md`
      at `:102`. This is the step the note describes.
    - `migrations/0000-baseline.md:139` — **Step 6: Append global AGENTS.md additions
      (Option A install only)** — a *different* template appended to `${CODEX_HOME}/AGENTS.md`,
      skipped on Option B. Not what the note is about.
    - `docs/decisions/0010-region-aware-spec-11-placement.md:265` — cites
      `0000-baseline.md:102` **correctly**. The ADR and the skill contradict each other;
      the ADR is right.
    - `skills/setup-codex-agenticapps-workflow/SKILL.md:96, 174-175` — other, *correct*
      "Step 6" references (the Option A/B distinction). **Do not touch these.** Only `:124`
      is wrong.
  </read_first>

  <files>skills/setup-codex-agenticapps-workflow/SKILL.md</files>

  <action>
    In the note at `:124`, change the citation from Step 6 to **Step 3**, and cite the line:
    `0000-baseline.md`'s **Step 3** (`:102`) is the plain append of
    `templates/agents-md-additions.md`. The note's substantive claim is unchanged and
    correct — that template contains no §11, so setup carries no §11 placement logic and the
    anchor rule has exactly one source: the migration chain.

    Leave the surrounding note text alone. Leave `:96` and `:174-175` alone — those Step 6
    references are about the Option A/B global-AGENTS.md distinction and are correct.

    This is a one-line citation fix. Do not widen it into the note's other claims.
  </action>

  <acceptance_criteria>
    - `sed -n '120,128p' skills/setup-codex-agenticapps-workflow/SKILL.md | grep -c 'Step 6'`
      is `0`.
    - `sed -n '120,128p' skills/setup-codex-agenticapps-workflow/SKILL.md | grep -c 'Step 3'`
      is `>= 1`.
    - The other Step 6 references survive:
      `grep -c 'Step 6' skills/setup-codex-agenticapps-workflow/SKILL.md` is `>= 3` (the
      `:96` and `:174-175` Option A/B references). If this hits 0, the fix over-reached —
      revert.
    - The citation is checkable and correct:
      `sed -n '91,102p' migrations/0000-baseline.md | grep -c 'agents-md-additions.md'` is
      `>= 1` — i.e. Step 3 really is the step that appends that template.
    - `bash migrations/run-tests.sh` → 0 FAIL (the layout checks read this skill).
  </acceptance_criteria>

  <verify>
    <automated>sed -n '120,128p' skills/setup-codex-agenticapps-workflow/SKILL.md | grep -c 'Step 6'; grep -c 'Step 6' skills/setup-codex-agenticapps-workflow/SKILL.md; bash migrations/run-tests.sh layout 2>&1 | grep -cE '\bFAIL\b'</automated>
  </verify>

  <done>The note cites Step 3 (`0000-baseline.md:102`), the Option A/B Step 6 references are untouched, and the suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: File the upstream defect against claude-workflow — CR-01 ONLY (criterion 10)</name>

  <read_first>
    - `.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md` — the repro to
      retarget from `AGENTS.md` to `CLAUDE.md` (upstream's filename).
    - `git -C ../claude-workflow show f9354cc:migrations/0029-region-aware-spec-11-placement.md`
      — read `:222-241` (CR-01, live) and `:411-420` (the "Known limitations (no live
      instance, not fixed by design)" section, which lists CRLF and fenced-code-block
      markers but **not** the runaway).
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "What the upstream
      bug report should contain" — the content checklist.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md` — the fix that
      worked here, offered as a suggestion rather than a demand.
  </read_first>

  <action>
    Automate everything up to the outbound claim; the checkpoint approves the claim, it does
    not do the work.

    1. Probe the tooling: `gh auth status` and
       `gh repo view agenticapps-eu/claude-workflow --json name`. Record whether `gh` is
       authenticated — RESEARCH did not probe it, so this is unknown until you look.
    2. Re-verify the ref is still live: `git -C ../claude-workflow fetch && git -C ../claude-workflow log --oneline -1 origin/main`.
       Read `f9354cc:0029:222-241` and `:411-420` and confirm, at the moment of filing, that
       (a) the runaway is still present and (b) "Known limitations" still omits it. If either
       has changed, the report's framing is wrong — stop and report.
    3. Draft the full issue body to
       `.planning/phases/09.1-11-strip-runaway-inserted/09.1-UPSTREAM-CR-01.md`, covering the
       checklist in `<what-built>`. Retarget the `09-CR-01-REPRO.md` repro from `AGENTS.md`
       to `CLAUDE.md`, and re-run it against upstream's own 0029 in a scratch dir so the line
       counts you quote are measured on THEIR file, not ours. A repro they cannot reproduce
       will be closed.
    4. Print the drafted body and STOP for approval. Do not file before the checkpoint —
       this is an outbound claim made under the user's GitHub identity against another
       team's repo.
    5. On approval: file via `gh issue create --repo agenticapps-eu/claude-workflow`.
       If `gh` is unauthenticated, say so plainly and hand the user the draft path rather
       than asking them to authenticate mid-phase.
  </action>

  <what-built>
    Claude has drafted the upstream defect report and verified `gh` availability. The report
    covers **CR-01 only** and contains:
    - target `agenticapps-eu/claude-workflow`, referencing `f9354cc:0029:222-241`;
    - the repro from `09-CR-01-REPRO.md`, retargeted to `CLAUDE.md`;
    - **the reachability argument** — the Apply conflict branch is `heading present AND
      provenance absent`; the runaway needs the **inverse**, so it falls through to the
      strip. This is the load-bearing part: reviewers dismiss "drifted heading" as exotic
      without it;
    - that all three guards pass because the *insert* re-adds the heading the post-strip
      `grep -q` checks for;
    - the orphaned-provenance extension — upstream's own `:282`-equivalent prose admits the
      migration can produce that state itself;
    - an acknowledgement of PR #89, and the point that anchoring `PROV_RE` (which they did)
      does **not** close the runaway;
    - the note that their "Known limitations" section omits the runaway, so this is new
      information rather than something they triaged and accepted;
    - the fix that worked here (refuse gate + END fail-closed guard), offered as a
      suggestion.

    **Explicitly NOT filed:** CR-02 (already fixed at `f9354cc` by their own PR #89 —
    reporting it would be noise) and V-01 (**ours** — a codex-side porting error; upstream's
    `.claude/skills/…` path is correct for their host).
  </what-built>

  <how-to-verify>
    1. Review the drafted report body Claude prints before filing.
    2. Confirm its scope is CR-01 only — CR-02 and V-01 must not appear as defect claims.
    3. Confirm the reachability argument is present and legible to someone with no context
       on this phase.
    4. Approve filing, or file it yourself via the web UI if you prefer to own the thread.
    5. Record the resulting issue URL.

    Filing is external to this repo and needs your GitHub identity — `gh` may not be
    authenticated in this environment, and criterion 10 says "filed", not "merged".
  </how-to-verify>

  <acceptance_criteria>
    - The issue exists at `agenticapps-eu/claude-workflow` and its URL is recorded in the
      SUMMARY and in ADR-0010's follow-ups.
    - The report cites `f9354cc:0029:222-241` — a live ref at upstream HEAD, not the dead
      `8520f90`.
    - The report contains the reachability argument (the inverse-condition point).
    - The report does NOT claim CR-02 or V-01 as upstream defects. If either appears, it is
      wrong and must be edited before the task is done.
    - If filing is blocked (no `gh` auth, no approval), the drafted body is saved into the
      SUMMARY verbatim so the user can file it manually, and the blocker is recorded — do
      not mark criterion 10 satisfied on an unfiled draft.
  </acceptance_criteria>

  <resume-signal>Type "approved" to file via `gh`, "filed &lt;url&gt;" if you filed it yourself, or describe changes needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → every future reader | a decision record carrying a falsified claim is trusted and re-applied; D-26's "bounded by construction" is how the runaway shipped past review |
| this repo → six sibling repos carrying 0029 | CR-01 is live at upstream HEAD; not reporting it leaves every downstream repo exposed to the same data destruction |
| this repo → upstream's issue tracker | a report is an outbound claim; a wrong one (CR-02, V-01) costs upstream's trust and buries the real finding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-32 | Repudiation | ADR-0010 `:154-155` | mitigate | The ADR asserts as a decision the exact claim that was falsified by reproduction. A reader trusting it would build the next migration on the same false boundary. Task 1 corrects the claim while preserving D-26's surviving principle. |
| T-09.1-33 | Repudiation | ADR-0010's `8520f90` citations | mitigate | Four citations point at a PR-branch commit that never merged, so the "upstream reference" is unreachable from upstream's main. Task 1 re-pins to `f9354cc` and records `8520f90` as never-merged rather than deleting it. |
| T-09.1-34 | Tampering (data destruction, third-party) | `f9354cc:0029:222-241` | transfer | CR-01 is live upstream and their "Known limitations" omits it — they are unaware. Task 3 transfers the finding via a bug report. Disposition is transfer, not mitigate: we do not own that repo. |
| T-09.1-35 | Repudiation | an over-scoped upstream report | mitigate | Filing CR-02 (fixed at HEAD) or V-01 (ours) as upstream defects would be false claims that bury the real one. Task 3's acceptance criteria assert their absence. |
| T-09.1-36 | Repudiation | WR-02's wrong citation | mitigate | The setup skill and ADR-0010:265 contradict each other about which baseline step appends the template. A reader following the skill's Step 6 pointer finds a different template and concludes the note is wrong about §11 too. Task 2 fixes the citation. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` (full) → **0 FAIL** (1 SKIP main / 2 SKIP worktree). This
  is the phase gate.
- ADR-0010 contains all five criterion-9 items (grep-checked in Task 1).
- The upstream issue URL is recorded, or the blocker + verbatim draft are.
- `git diff --stat` touches only `docs/decisions/0010-*.md` and
  `skills/setup-codex-agenticapps-workflow/SKILL.md`.
</verification>

<success_criteria>
- ADR-0010 records: the runaway; the D-26 correction; V-01 as a **porting error** against
  0008's T-08-38 precedent; the Q1 refuse ruling with heal-and-duplicate named as the
  rejected alternative; and D-48's re-pin from `8520f90` (never merged) to `f9354cc`, with
  CR-02's fix ported rather than re-invented.
- The upstream defect is filed for **CR-01 only**, noting that upstream's "Known
  limitations" omits the runaway.
- The setup-SKILL note cites Step 3.
- Full suite 0 FAIL.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-07-SUMMARY.md` when done.
Record: the upstream issue URL (or the blocker + the verbatim draft), the final full-suite
counts, and a phase-level tally of every mutation demo with its landed-proof — that tally
is the phase's evidence that its assertions are alive.
</output>
</content>
