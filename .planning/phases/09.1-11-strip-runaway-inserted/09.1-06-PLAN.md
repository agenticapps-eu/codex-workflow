---
phase: 09.1-11-strip-runaway-inserted
plan: 06
type: execute
wave: 5
depends_on: [09.1-05]
files_modified:
  - migrations/run-tests.sh
  - migrations/0009-spec-11-region-aware-placement.md
autonomous: true
requirements: [MIGR-06, TEST-03, ANCHOR-05]

must_haves:
  truths:
    - "Deleting the test -s pre-flight guard turns the suite RED"
    - "Narrowing the strip terminator turns the suite RED"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "comment-stripped document contract, diagnostic-isolated case 10(a), 12-idempotent-rerun"
      contains: "12-idempotent-rerun"
  key_links:
    - from: "the 0009 document contract check"
      to: "the pre-flight's executable lines only"
      via: "grep -v '^[[:space:]]*#' before the case match"
      pattern: "grep -v .\\^\\[\\[:space:\\]\\]\\*#"
    - from: "case 10(a)"
      to: "guard 3's exclusive diagnostic"
      via: "case on 'missing or empty', not on the exit code"
      pattern: "missing or empty"
---

<objective>
Make two guards that currently cannot fail actually fail, and prove it by deleting them.

Purpose: criteria 5 and 6 exist because **a green suite survived deleting the guard**.
`test -s` can be removed from 0009's pre-flight today and `run-tests.sh` stays at 314 PASS
(CR-03), for two independent reasons: the document check `*'test -s'*` matches the
pre-flight's own **comments** (`0009:108`, `:120`), and case 10(a) passes for the wrong
layer (guard 4's tail sentinel also fails a zero-byte mirror with the same `exit 3`).
Separately, narrowing the strip terminator does NOT fail the suite — ANCHOR-05 is covered
live only by `migrations/validate-0009-anchor.sh` counter-case B. 09-REVIEW.md calls the
idempotent-rerun fixture "the highest-value gap this phase produced": the difference between
"the alternation is correct" and "the suite would catch it if it stopped being correct".

**The mutation gate is non-negotiable.** Each named guard MUST be deleted, the suite
observed RED, and the guard restored. **The recorded RED is the deliverable, not the
claim.** A plan that asserts "the assertion is live" without recording the RED has not
satisfied the criterion — that is the exact failure this phase closes.

**`migrations/0009-*.md` is listed in `files_modified` because both tasks mutate it
transiently. It MUST be byte-identical at plan end** — asserted below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-VALIDATION.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md

**CLAUDE.md's GitNexus mandate is INERT** — shell + Markdown. Grep is ground truth.

**Verified at planning time — you may rely on this (A4):** pre-flight guard 4's diagnostic
reads *"is missing its final section — it looks truncated or corrupt"*. It does **not**
contain the substring `missing or empty`, which is guard 3's exclusive wording at
`0009:112`. So a `case` on `missing or empty` discriminates layer 1 from layer 2. Re-confirm
with one grep before locking the pattern; 09.1-02 rewrote guard 2 nearby.

**Before mutating anything: `git status` must be clean.** Every mutation below is
delete → observe → restore. If the working tree is dirty you cannot prove the restore.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make test -s's assertion live (criterion 5 / CR-03), and prove it by deletion</name>

  <read_first>
    - `migrations/run-tests.sh:3439-3483` — the pre-flight shape guard and BOTH D-28.1
      document-contract `case` blocks. Read the long comment at `:3439-3459`: it explains
      why the shape guard is anchored on the **mirror path**, deliberately NOT on `test -s`,
      because case 10 mutation-tests those operators and coupling the loader to them would
      mask the signal. **Your change must not violate that** — you are fixing the two
      D-28.1 `case` blocks, not the `assert_extracted_shape` call.
    - `migrations/run-tests.sh:3888-3936` — case `10-corrupt-mirror-refused`, all three
      modes. Note (c) exists so "a pre-flight that refused everything" cannot read as a
      working guard.
    - `migrations/0009-spec-11-region-aware-placement.md:103-141` — guards 3 and 4 as they
      now stand. Confirm the comments at `:108`/`:120` still contain the literal `test -s`
      (that is what makes the current check self-satisfying), and re-confirm A4. Verified
      at planning time: the bare string `test -s` appears 3× in `:103-125` — the comments
      at `:108` and `:120`, plus the executable line at `:111`; comment-stripping leaves
      exactly 1. (`:114` is `echo "       Re-install: …"` and does NOT contain `test -s`;
      `:285` does, but it is in Step 1's Apply, not the pre-flight.)
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Code Example 3"
      and § "Pitfall 3" — why 10(a) cannot discriminate by exit code (WR-04: guard 4
      subsumes guard 3 on a zero-byte mirror **by construction**, so no exit-code assertion
      can tell them apart; the message is the only discriminator).
  </read_first>

  <files>migrations/run-tests.sh, migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    **(1) The document contract.** Before the two D-28.1 `case` blocks, derive a
    comment-stripped view of the extracted pre-flight:
    `pf_code="$(printf '%s\n' "$pf_block" | grep -v '^[[:space:]]*#')"`.
    Match against `$pf_code`, not `$pf_block`, and tighten layer 1's pattern from the bare
    `*'test -s'*` to `*'test -s "$MIRROR"'*` so it matches the executable shape rather than
    any mention. Apply the same `$pf_code` treatment to layer 2's `*'Goal-Driven Execution'*`
    for the same reason — it does not collide today, but a check that only works by accident
    of comment wording is one prose edit from being dead.
    Comment it: `grep -c`-style substring checks count comments, so a header that documents
    a guard silently satisfies the check for that guard. Cite CR-03.

    **(2) Case 10(a) — assert the diagnostic, not the exit code.** Add an assertion on
    `$out`: `case "$out" in *'missing or empty'*)` PASS "10(a) zero-byte mirror refused BY
    THE test -s layer" / `*)` FAIL "…refused, but not by test -s (wrong layer)". **Keep**
    the existing `[ "$rc" -eq 3 ]` assertion — it is still true and still worth asserting;
    you are adding the layer-isolation assertion it cannot provide. Comment it with WR-04's
    structural point: guard 4 also fails a zero-byte mirror with exit 3, so the exit code
    cannot isolate layer 1; the message is the only thing that differs. This mirrors how the
    harness already controls the version gate 12 lines earlier.

    **(3) THE MUTATION GATE — this is the deliverable.**
    - Delete `test -s "$MIRROR" || { …; exit 3; }` from `0009:111-116` (guard 3's
      executable lines; leave its comment).
    - **Assert the mutation landed.** `grep -cF 'test -s "$MIRROR"' migrations/0009-spec-11-region-aware-placement.md`
      must drop from `1` to `0` (verified: it is `1` today). Use `grep -F` — the pattern
      contains `$` and `"`. In Phase 9 a `sed: bad flag` silently
      no-op'd and the run *looked* like a passing demo. **An unverified mutation demo is
      itself a dead assertion.**
    - Run `bash migrations/run-tests.sh 0009`. **BOTH** new assertions must go RED: the
      layer-1 document contract, and 10(a)'s diagnostic isolation.
    - `git checkout migrations/0009-spec-11-region-aware-placement.md` to restore; confirm
      `git diff` on that file is empty and the suite is 0 FAIL again.
    - Record every observation.

    Expect 10(a)'s `rc -eq 3` assertion to STAY GREEN under the mutation (guard 4 catches
    the zero-byte mirror). That is not a failure of the demo — it is WR-04's point,
    demonstrated. Record it explicitly.
  </action>

  <acceptance_criteria>
    - `sed -n '/^test_migration_0009()/,/^}/p' migrations/run-tests.sh | grep -c 'pf_code'`
      is `>= 3` (the derivation plus both `case` subjects).
    - `grep -c "grep -v '\^\[\[:space:\]\]\*#'" migrations/run-tests.sh` is `>= 1`.
    - `grep -cF 'test -s "$MIRROR"' migrations/run-tests.sh` is `>= 1` — layer 1
      matches the executable shape, not a bare `test -s`. **Use `grep -F`**: the
      pattern contains both `$` and `"`, and a non-`-F` form needs escaping so
      baroque it becomes a defect source in its own right. Verified against the live
      tree: this returns `0` today (genuinely RED) and `1` on 0009 (`:111`).
    - A4 re-confirmed:
      `sed -n '/^grep -q .\^### 4\\. Goal-Driven/,/^}/p' migrations/0009-spec-11-region-aware-placement.md | grep -c 'missing or empty'`
      is `0`. If this is `>= 1`, the `case` pattern cannot discriminate — STOP and report.
    - **The mutation demo is recorded** with: the delete command, the before/after
      `grep -cF 'test -s "$MIRROR"'` counts on 0009 (**proof it landed** — `1` → `0`),
      the exact FAIL labels observed, and the post-restore 0 FAIL.
    - **Both** new assertions were observed RED under the mutation. If only one goes RED,
      the other is still dead — fix it and re-run the gate.
    - `git diff migrations/0009-spec-11-region-aware-placement.md` is EMPTY at task end.
    - `bash migrations/run-tests.sh 0009` → 0 FAIL after restore.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code migrations/0009-spec-11-region-aware-placement.md && bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-06-t1.log; grep -cE '\bFAIL\b' /tmp/09.1-06-t1.log</automated>
  </verify>

  <done>Both D-28.1 document checks read comment-stripped code, 10(a) isolates layer 1 by diagnostic, the deletion mutation drove both assertions RED with proof it landed, and 0009 is restored byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: 12-idempotent-rerun (criterion 6 / MIGR-06 / ANCHOR-05), and prove it by narrowing the terminator</name>

  <read_first>
    - `migrations/run-tests.sh:3599-3670` — `01-gitnexus-led-inject` and
      `02-inside-region-move`. Fixture 12's BEFORE state is `01`'s AFTER state; build it by
      running Apply twice rather than hand-writing the healed file, so the fixture cannot
      drift from what the migration actually produces.
    - `migrations/0009-spec-11-region-aware-placement.md:229-273, 336-346` — the strip
      terminator alternation at `:265` and the prose calling it "the highest-severity
      mechanic in this migration": a `/^## /`-only terminator "skips straight past
      `<!-- gitnexus:start -->` on a file this migration has already healed, and consumes the
      ENTIRE GitNexus region plus everything up to the next `## ` or EOF — **on an ordinary
      idempotent re-run**."
    - `.planning/phases/09-region-aware-11-placement/09-REVIEW.md` — the `11-idempotent-rerun`
      entry: without this fixture, narrowing the terminator does NOT fail the suite.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-04-SUMMARY.md` — the Q4
      numbering note. This fixture is `12-`; `11-` is upstream's prose-mention port.
  </read_first>

  <files>migrations/run-tests.sh, migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    Add `12-idempotent-rerun` to `test_migration_0009`, gated on `[ "$apply_ok" = "1" ]`
    with `_m0009_fail` in the `else`.

    BEFORE state: a **region-led** file (the shape that makes the terminator matter — the
    healed block is followed by `<!-- gitnexus:start -->`, not by a `## `). Run `$apply_block`
    once to heal it, snapshot the result, then run `$apply_block` a **second** time and
    assert against the snapshot. Building the BEFORE by running the migration is the point:
    a hand-written "healed" file can drift from what 0009 actually emits, and then the
    fixture stops testing the re-run path.

    Assert:
    - `rc` is 0 on the second run;
    - the second run leaves `AGENTS.md` **byte-identical** to the first run's output
      (`cmp -s`) — true idempotency, MIGR-06;
    - the region markers are still paired exactly once each (`grep -c -x` on start and end);
    - the region's own body content survived;
    - exactly ONE provenance line remains.

    Comment it: this is ANCHOR-05's only live suite coverage. The terminator and the anchor
    are one decision, not two, and must move together; a narrowed terminator eats the
    region on an ordinary re-run.

    **THE MUTATION GATE — the deliverable.**
    - In `0009:265`, narrow the strip **terminator** from
      `(/^## / || /^<!-- gitnexus:start -->$/)` to `/^## /`. Change the terminator site
      ONLY — leave the insert's anchor at `:288` intact, so the mutation isolates the
      terminator exactly as ANCHOR-05 describes.
    - **Assert the mutation landed:**
      `grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md`
      must drop from 2 to 1. If it stays 2, the edit did not apply — do not trust the run.
    - Run `bash migrations/run-tests.sh 0009`. `12-idempotent-rerun` **must FAIL**. Record
      which assertions fail and the observed region-marker counts (an orphaned, unpaired
      `<!-- gitnexus:end -->` is the signature).
    - `git checkout migrations/0009-spec-11-region-aware-placement.md`; confirm the count is
      back to 2, `git diff` on that file is empty, and the suite is 0 FAIL.

    If the fixture does NOT go RED under the narrowed terminator, **the fixture is the
    problem, not the mutation** — it is not exercising the region-led re-run path. Fix the
    fixture and re-run the gate. Do not record a green as a pass.
  </action>

  <acceptance_criteria>
    - `grep -c '12-idempotent-rerun' migrations/run-tests.sh` is `>= 5`.
    - `grep -c '11-idempotent-rerun' migrations/run-tests.sh` is `0` — the ROADMAP's
      criterion-6 label is aliased to `12-` per Q4; `11-` is upstream's prose-mention port.
    - `bash migrations/run-tests.sh 0009` → `12-idempotent-rerun` PASSes all five
      assertions on the unmutated 0009.
    - **The mutation demo is recorded** with: the exact edit, the before/after alternation
      counts on 0009 (2 → 1 → 2, **proof it landed and was restored**), the FAIL labels
      observed, the observed marker counts under the mutation, and the post-restore 0 FAIL.
    - `git diff migrations/0009-spec-11-region-aware-placement.md` is EMPTY at task end, and
      `grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md`
      is `2`.
    - `bash migrations/run-tests.sh` (full) → **0 FAIL**.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code migrations/0009-spec-11-region-aware-placement.md && bash migrations/run-tests.sh 2>&1 | tee /tmp/09.1-06-t2.log; grep -cE '\bFAIL\b' /tmp/09.1-06-t2.log</automated>
  </verify>

  <done>`12-idempotent-rerun` exists, passes on the real 0009, and has been OBSERVED FAILING under a verified terminator-narrowing mutation that was then restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test suite → the migration | the suite is the only control asserting the migration is safe; a guard the suite cannot see fail is not covered |
| a migration document's comments → its own contract check | prose describing a guard can satisfy a substring check for that guard, making the header self-invalidating |
| an already-healed `AGENTS.md` → an idempotent re-run | the most ordinary operation is the one a narrowed terminator destroys the region on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-27 | Repudiation | the `*'test -s'*` document check (`run-tests.sh:3467-3470`) | mitigate | CR-03: matches the pre-flight's own comments at `0009:108`/`:120`, so the guard can be deleted and the suite stays green. **A validation control that always passes is a security control failure.** Task 1 strips comments before matching and proves the fix by deletion. |
| T-09.1-28 | Repudiation | case 10(a)'s exit-code assertion | mitigate | Guard 4 subsumes guard 3 on a zero-byte mirror with the same exit 3 (WR-04), so 10(a) passes for the wrong layer. Task 1 asserts the diagnostic — the only discriminator. |
| T-09.1-29 | Tampering (data destruction) | the strip terminator (`0009:265`) | mitigate | A `/^## /`-only terminator consumes the entire GitNexus region on an ordinary idempotent re-run and leaves an orphaned `<!-- gitnexus:end -->`. Task 2's fixture is ANCHOR-05's only live suite coverage; the mutation gate proves it catches the narrowing. |
| T-09.1-30 | Repudiation | an unverified mutation demo | mitigate | Phase 9 had a `sed: bad flag` silently no-op and the run *looked* like a passing demo. Both tasks assert the mutation LANDED (before/after grep counts) before trusting the result, and assert the restore via `git diff --exit-code`. |
| T-09.1-31 | Tampering | a mutation left un-restored | mitigate | Both tasks restore via `git checkout` and gate the task's automated verify on `git diff --exit-code migrations/0009-*.md`. A shipped mutation would be a live data-destruction bug. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `git diff --exit-code migrations/0009-spec-11-region-aware-placement.md` → clean. **Both
  mutations restored.**
- `bash migrations/run-tests.sh` (full) → 0 FAIL (1 SKIP main / 2 SKIP worktree).
- The alternation copy count in 0009 is `2`.
- Fixtures 01-11, 13, 14, 15 and `no-scaffolder-tree` all still PASS — no weakening.
- Three mutation demos are recorded across the phase so far (this plan's two, plus 09.1-05
  Task 3's), each with landed-proof.
</verification>

<success_criteria>
- The `test -s` guard can be deleted and **the suite goes RED** — observed, recorded, restored.
- The strip terminator can be narrowed and **the suite goes RED** — observed, recorded, restored.
- `12-idempotent-rerun` exists and passes on the real 0009.
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at
  plan start.
- Full suite 0 FAIL.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-06-SUMMARY.md` when done.
Record both mutation demos in full: command, landed-proof counts, FAIL labels, restore
proof. 09.1-07's ADR cites them as the evidence that criteria 5 and 6 are satisfied.
</output>
