---
phase: 09.1-11-strip-runaway-inserted
plan: 01
subsystem: testing
tags: [bash, migration-harness, tdd, run-tests.sh, migration-0009]

# Dependency graph
requires: []
provides:
  - "_m0009_mk_project shaped like a real target project (.git + .codex/workflow-version.txt=0.6.0, no skills/ tree)"
  - "no-scaffolder-tree fixture in test_migration_0009, ported from 0008's own fixture"
  - "Recorded RED evidence (labels + exit codes + counts) for 09.1-02 to consume"
affects: [09.1-02-strip-runaway-inserted]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sandbox fixtures for migration 0009 must be built from _m0009_mk_project/_m0009_mk_fake_home, never hand-rolled, so every fixture inherits the realistic (no skills/ tree) shape"
    - "Fixtures gate on the shared extraction-shape flags (pf_ok/idem_ok/apply_ok) and emit _m0009_fail per assertion when a gate is down, so a broken extractor reports FAILED rather than vanishing"

key-files:
  created: []
  modified:
    - migrations/run-tests.sh

key-decisions:
  - "Removed the synthetic skills/agentic-apps-workflow/SKILL.md manufacture from _m0009_mk_project (Task 1) and observed the resulting RED (Task 2) as two separate commits, per criterion 0's explicit requirement that removal-then-observation not be collapsed into fixing the pre-flight in the same step"
  - "Named the ported fixture 'no-scaffolder-tree' without a number, matching 0008's own naming, since numbered fixtures 11-15 are reserved for other work (Q4)"
  - "Checked the MIGR-08/MIGR-09 separation on three executable surfaces (applies_to frontmatter, extracted pre-flight, every extracted Step Apply block) rather than a whole-document grep, because 0009's own prose legitimately discusses the skills/ path when documenting the divergence"

requirements-completed: [MIGR-01, TEST-02]

duration: ~20min
completed: 2026-07-15
---

# Phase 09.1 Plan 01: Strip the manufactured 0009 precondition and record RED Summary

**Removed `_m0009_mk_project`'s synthetic `skills/agentic-apps-workflow/SKILL.md` manufacture and ported 0008's `no-scaffolder-tree` regression fixture into `test_migration_0009`, producing a genuinely RED suite (35 PASS / 3 FAIL on the `0009` filter) that criterion 0 explicitly requires before 09.1-02 can fix the pre-flight.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-15T18:23:00Z
- **Tasks:** 2 completed
- **Files modified:** 1 (`migrations/run-tests.sh`)

## Accomplishments

- `_m0009_mk_project` no longer `mkdir -p`s `skills/agentic-apps-workflow/` or writes a synthetic `version: 0.6.0` SKILL.md into every 0009 sandbox — the exact practice `run-tests.sh:917-918` already refuses for 0008. It now builds a sandbox shaped like a real target project: `.git` + `.codex/workflow-version.txt` = `0.6.0`, deliberately no `skills/` tree.
- This exposed the BLOCKER V-01 shipped green over: 0009's pre-flight still greps `skills/agentic-apps-workflow/SKILL.md` for its version floor, so it now exits 3 on every case that no longer manufactures that path — `bash migrations/run-tests.sh 0009` went from 34 PASS / 0 FAIL to 33 PASS / 1 FAIL on this change alone.
- Ported 0008's own `no-scaffolder-tree` fixture (`run-tests.sh:1638-1707`) into `test_migration_0009` with all four of its assertions, adapted to 0009's shape (extracted `$pf_block`/`$idem_block` run through `_m0009_apply`, plus a document-contract check across three executable surfaces instead of 0008's runtime-only check).
- Observed and recorded the RED: two of the four new assertions FAIL against the current, unfixed 0009 — exactly the "observed FAILING against the current 0009 first" requirement in criterion 0.

## Task Commits

1. **Task 1: Make _m0009_mk_project shaped like a real target project** - `2f4d9d5` (fix)
2. **Task 2: Port 0008's no-scaffolder-tree regression fixture into test_migration_0009, and observe it RED** - `dc540b7` (test)

_Note: These land as `fix`/`test` commit types rather than a single `feat`, because Task 1 is a bug fix (a test harness manufacturing its own precondition) and Task 2 is the RED half of a TDD cycle — no `feat`/GREEN commit exists yet by design; 09.1-02 supplies it._

## Files Created/Modified

- `migrations/run-tests.sh` - `_m0009_mk_project` rewritten (Task 1); `no-scaffolder-tree` fixture added to `test_migration_0009` (Task 2)

## Baseline / Post-Change Counts (verbatim, for 09.1-02's RED evidence)

**Full suite baseline (plan start, non-worktree):** 314 PASS / 0 FAIL / 1 SKIP.
**`0009` filter baseline (plan start):** 34 PASS / 0 FAIL.

**After Task 1** (`bash migrations/run-tests.sh 0009`): **33 PASS / 1 FAIL.**
- `FAIL 10-corrupt-mirror-refused (c) healthy mirror: pre-flight PASSES with exit 0 (the direction that proves it is not refusing everything) — got exit=3`
- Cases 10(a) and 10(b) still PASS, but for the WRONG reason: the version gate now aborts with exit 3 before the mirror guards under test ever run, so "refuses with exit 3" is true but not because of the mirror guard. Plan 09.1-02 restores their isolation.

**After Task 2** (`bash migrations/run-tests.sh 0009`): **35 PASS / 3 FAIL** (38 total assertions, up from the 34 baseline — assertions were added, not swapped).

All three FAIL labels, verbatim:
```
FAIL 10-corrupt-mirror-refused (c) healthy mirror: pre-flight PASSES with exit 0 (the direction that proves it is not refusing everything) — got exit=3
FAIL no-scaffolder-tree: pre-flight PASSES with no skills/ tree present (criterion 0) — got exit=3
FAIL no-scaffolder-tree: MIGR-08/MIGR-09 separation — no executable surface (applies_to frontmatter, pre-flight, every Step Apply) names skills/agentic-apps-workflow/SKILL.md
```

The two PASS assertions the new fixture also introduces (both expected, neither depends on the removed synthetic SKILL.md):
```
PASS no-scaffolder-tree: fixture has no local skills/ directory (self-guard — proves Task 1 landed)
PASS no-scaffolder-tree: Step 1's idempotency check runs cleanly (exit <= 1) with no skills/ tree — got exit=0
```

**Full suite after both tasks** (in this worktree): 314 PASS / 3 FAIL / 2 SKIP. The 2 SKIP (vs. the 1 SKIP baseline) is environmental — one check probes `../agenticapps-workflow-core`, unresolvable from a worktree, per the plan's own context note — not a regression. The 3 FAIL are exactly the three `0009`-filter FAILs above; no other test in the 314-PASS suite regressed, and the harness ran to completion with no abort (confirming `_m0009_apply`'s mandatory subshell was preserved through both edits).

**Observed exit code for the pre-flight assertion (criterion 0):** `exit=3` (the version gate aborting on the removed `skills/agentic-apps-workflow/SKILL.md` path) — NOT `exit=0`, which is the FAIL this task exists to produce.

## Decisions Made

- Split removal (Task 1) and RED-observation (Task 2) into two separate commits rather than one, so the audit trail shows the manufactured-precondition removal and its consequence as distinct, individually-verifiable steps — matching the plan's explicit instruction that collapsing them "forfeits the RED that criterion 0 explicitly requires."
- Rewrote `_m0009_mk_project`'s doc comment to avoid the literal substring `skills/agentic-apps-workflow` (used "local scaffolder-skill tree" instead) so the function-scoped acceptance-criteria grep for that exact path count (`0`) passes while the comment still explains, in prose, what the function deliberately does not create and why (citing `run-tests.sh:917-918` and `0008:470-487`).
- Built the `no-scaffolder-tree` fixture's AGENTS.md using the same healthy-ON-anchor shape as `03-healthy-noop` above it (`$PROV_LIT` + `cat "$mirror"` + a following `## Deployment` heading) rather than inventing a new shape, keeping the fixture consistent with the existing suite's idiom.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' acceptance criteria (grep counts, exit codes, PASS/FAIL labels, `git show --stat` scope) were verified to match the plan's predicted values exactly before each commit.

## Issues Encountered

- The `vendor/agenticapps-shared` submodule was not initialized in this worktree, so the first `bash migrations/run-tests.sh 0009` run aborted with `error: agenticapps-shared submodule not initialized.` Ran `git submodule update --init --recursive` (a read-only, non-destructive setup step required by this repo's own documented `submodules: recursive` dependency in PROJECT.md's Constraints) before any verification could proceed. This is environment setup, not a plan deviation — no plan file or task action depended on it, and it produced no working-tree changes to commit.

## Next Phase Readiness

- The RED this plan records — labels, exit codes (`exit=3` on both new FAILs), and counts (35 PASS / 3 FAIL on the `0009` filter) — is the exact evidence 09.1-02 needs to consume as its starting point.
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at plan start (verified via `git diff --stat` returning nothing for that path across both commits) — 09.1-02 owns the pre-flight fix that turns this suite GREEN.
- No blockers.

---
*Phase: 09.1-11-strip-runaway-inserted*
*Completed: 2026-07-15*

## Self-Check: PASSED

- FOUND: `.planning/phases/09.1-11-strip-runaway-inserted/09.1-01-SUMMARY.md`
- FOUND: commit `2f4d9d5` (Task 1)
- FOUND: commit `dc540b7` (Task 2)
