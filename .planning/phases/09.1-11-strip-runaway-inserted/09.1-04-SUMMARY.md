---
phase: 09.1-11-strip-runaway-inserted
plan: 04
subsystem: testing
tags: [bash, awk, migration-harness, run-tests.sh, migration-0009, red-suite]

# Dependency graph
requires:
  - phase: 09.1-11-strip-runaway-inserted plan 02
    provides: "0009 pre-flight fixed + Step 2 deleted/renumbered (BLOCKER closed) — the base this plan's RED fixtures run against"
  - phase: 09.1-11-strip-runaway-inserted plan 03
    provides: "test_drift's third leg + this repo's own version record at 0.7.0 — an untouched neighbour filter this plan re-verified stayed green"
provides:
  - "Fixtures 13-runaway-drifted-h2, 14-runaway-orphan-provenance, 15-mixed-provenance-unresolved — reproduce CR-01 (the strip's decoupled entry/exit condition) as OBSERVED FAILING assertions against the current, unfixed 0009"
  - "Fixture 11-prose-mention-provenance — ported from claude-workflow f9354cc (PR #89), reproduces CR-02 (unanchored PROV_RE) as an OBSERVED FAILING content-survival assertion"
  - "state-a is genuinely off-anchor (position is the only variable) and its D-31/MIGR-07 assertion is proven falsifiable by a verified mutation"
  - "The vendored mirror's single-`## ` invariant (WR-01/Q2) is asserted in test_migration_0004 and proven falsifiable by a verified mutation"
affects: [09.1-05-strip-runaway-inserted]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A fixture that has not been observed RED is an assertion, not evidence — every new fixture in this plan was run against the unfixed target and its FAIL output recorded verbatim before commit"
    - "Falsifiability proof by verified mutation: before trusting a PASS, temporarily mutate the fixture/target, confirm the mutation landed (grep/cat), observe the FAIL, then restore and confirm the mutation is gone before re-observing PASS — applied to both state-a's expected-argument and the mirror's single-`## ` guard"
    - "Port, don't re-derive: fixture 11 was read via `git show <pinned-sha>:<path>` from the upstream repo, never its working tree, and re-verified the pin had not moved before reading"

key-files:
  created: []
  modified:
    - migrations/run-tests.sh

key-decisions:
  - "Fixtures 13/14/15 assert the FULL Apply chain's result (strip + insert + mv), not an isolated extraction of the strip awk alone, per the plan's literal `_m0009_apply \"$p\" \"$h\" \"$apply_block\"` instruction. This means the recorded 'before -> after' figures differ in kind from 09-CR-01-REPRO.md's own isolated-strip methodology: the STRIP stage alone reproduces the repro doc's line counts exactly (13: only the first 4 of 16 original lines survive verbatim, matching '16 -> 4'; 14: only the first 4 of 8 survive, matching RESEARCH's '8 -> 4'), but the FULL chain's final AGENTS.md grows past that (85 lines) because the insert pass's END fallback re-vendors a fresh copy of the mirror on top of the 4-line survivor when no `## ` heading remains in the stripped output to anchor on. Both figures are recorded below — this is a strictly stronger finding than the repro doc's, not a contradiction of it: it additionally shows *why* 0009's own post-strip heading guard is fooled (the reinsertion satisfies it)."
    - "Fixture 15's BEFORE state uses two real, distinct provenance+heading pairs (block 1 healthy via `cat \"$mirror\"`, block 2 drifted) rather than a synthetic proxy, so the file-global refuse-gate blind spot described in the plan's <interfaces> section is reproduced exactly as specified, not approximated."
    - "The mirror single-`## ` invariant mutation test was performed directly against the real vendored file (`skills/.../11-coding-discipline-0.4.0.md`), backed up via `cp` before mutation and restored via `cp` after — not via `git checkout --`, to avoid any risk of interacting with this task's own uncommitted `run-tests.sh` changes in the same working tree."

requirements-completed: [ANCHOR-05, TEST-02, TEST-03, MIGR-07]

duration: ~50min
completed: 2026-07-15
---

# Phase 09.1 Plan 04: Capture every reproduced runaway-strip path as a failing fixture Summary

**Added four new fixtures (11, 13, 14, 15) to `test_migration_0009`, each OBSERVED FAILING against the current, unfixed `migrations/0009-spec-11-region-aware-placement.md` with recorded before/after evidence — reproducing CR-01's drifted/orphaned-provenance runaway-strip defect and CR-02's unanchored-provenance-regex defect as falsifiable RED assertions — and closed two dead-assertion gaps (V-02's on-anchor `state-a` fixture, and the vendored mirror's unasserted single-`## ` invariant), both proven falsifiable by verified mutations.**

## Performance

- **Duration:** ~50 min
- **Completed:** 2026-07-15T19:05:00Z (approx, Task 3 commit)
- **Tasks:** 3 completed
- **Files modified:** 1 (`migrations/run-tests.sh`)

## Accomplishments

- **Fixture 13-runaway-drifted-h2** reproduces `09-CR-01-REPRO.md` verbatim: provenance present, H2 drifted to `## Coding Discipline (RENAMED — drifted)`, followed by two real project headings. FAILS its `rc -eq 3` assertion (observed `rc=0` — the migration silently "succeeds" while destroying data) and its `cmp -s` byte-identical assertion.
- **Fixture 14-runaway-orphan-provenance** reproduces 0009's own acknowledged orphan state (`0009:282`): provenance present, no heading, no following `## ` — tail runs to EOF. Same FAIL shape as 13.
- **Fixture 15-mixed-provenance-unresolved** reproduces the ONE shape the file-global refuse gate cannot see: two provenance blocks, the first healthy (satisfying the gate's "exact H2 present" check for the whole file), the second drifted. `## Deployment` is confirmed ABSENT from the after-state — the exact harm 09.1-05's END guard exists to prevent, and the only thing that makes that guard falsifiable.
- **Fixture 11-prose-mention-provenance**, ported (not re-derived) from `claude-workflow f9354cc` (PR #89) after re-verifying the pin had not moved (`origin/main` still `f9354cc`), reproduces CR-02 as an independent second confirmation: an unanchored `PROV_RE` substring-matches a prose mention of the marker inside a guard comment, destroying real project content ("IMPORTANT PROJECT RULE...") between the prose and the real block. FAILS its content-survival assertion (before=91, after=85 lines — shorter, confirming destruction).
- **`state-a` rewritten to be genuinely off-anchor** (V-02): the prior fixture placed the block ON the anchor while also carrying a trailing region, varying two things at once, so its "off-anchor" assertion could not fail for the reason it claimed. The rewrite places the block below a real project heading with no region anywhere, isolating position as the sole variable — proven falsifiable by a verified mutation (temporarily changed the expected argument to `not-applied`, confirmed it FAILED, restored, confirmed it PASSED again).
- **The vendored mirror's single-`## ` invariant (WR-01/Q2)** — the fact 0009's strip pass and 09.1-05's refuse gate both silently depend on — is now asserted in `test_migration_0004`, proven falsifiable by a verified mutation directly against the real vendored file (appended a second `## ` heading, confirmed the mirror-content assertion FAILED, restored from a backup, confirmed clean).

## Task Commits

1. **Task 1: Fixtures 13/14/15 — observed RED against unfixed 0009** - `8c4adb3` (test)
2. **Task 2: Port 11-prose-mention-provenance from claude-workflow f9354cc — observed RED** - `6c5bb3d` (test)
3. **Task 3: state-a genuinely off-anchor (V-02) + mirror single-`## ` invariant (WR-01)** - `d360fac` (test)

## Files Created/Modified

- `migrations/run-tests.sh` — fixtures 11, 13, 14, 15 added to `test_migration_0009`; `state-a` rewritten off-anchor with a self-guard assertion; `test_migration_0004` gained the mirror's single-`## ` invariant guard.

## Upstream pin verification (Task 2, requested by acceptance criteria)

```
git -C ../claude-workflow fetch
git -C ../claude-workflow log --oneline -1 origin/main
f9354cc fix(migration): 0029 — anchor §11 above any GitNexus region (2.6.0 → 2.7.0) (#89)
```
Still `f9354cc`, matching the plan's pin — the port proceeded. Both `setup.sh` and `verify.sh` were read via `git show f9354cc:<path>`, never the working tree.

## Recorded RED — every FAIL label, verbatim (`bash migrations/run-tests.sh 0009`)

```
FAIL 11-prose-mention-provenance: content between the prose mention and the next heading survives ('IMPORTANT PROJECT RULE') — the content-survival assertion (CR-02's independent second reproduction)
FAIL 13-runaway-drifted-h2: Apply refuses with exit 3 on a drifted H2 (CR-01) — before=16 after=85 lines, got exit=0
FAIL 13-runaway-drifted-h2: AGENTS.md is BYTE-IDENTICAL after refusal — the assertion that actually catches CR-01 (current 0009 truncates the file)
FAIL 13-runaway-drifted-h2: diagnostic names the offending provenance line
FAIL 13-runaway-drifted-h2: surviving-content check — '## Critical Project Rules' and '## Deployment' both present (names the harm as data loss, redundant with cmp but reads as content destruction)
FAIL 14-runaway-orphan-provenance: Apply refuses with exit 3 on an orphaned provenance line — before=8 after=85 lines, got exit=0
FAIL 14-runaway-orphan-provenance: AGENTS.md is BYTE-IDENTICAL after refusal
FAIL 14-runaway-orphan-provenance: diagnostic names the offending provenance line
FAIL 15-mixed-provenance-unresolved: Apply refuses with exit 3 on a shape the file-global refuse gate cannot see — before=94 after=88 lines, got exit=0
FAIL 15-mixed-provenance-unresolved: AGENTS.md is BYTE-IDENTICAL after refusal
FAIL 15-mixed-provenance-unresolved: diagnostic names the offending provenance line
FAIL 15-mixed-provenance-unresolved: '## Deployment' survives — its absence is the harm the END guard (09.1-05) exists to prevent
```
12 FAILs total (>= 8 required by `<verification>`), distributed 11:1, 13:4, 14:3, 15:4. Every `rc` observed for the three exit-3 assertions was **0** — confirmed silent "success" while destroying data, exactly as `<verification>` requires recording. `bash migrations/run-tests.sh 0009`: **45 PASS / 12 FAIL**. Full suite: **326 PASS / 12 FAIL / 2 SKIP**.

## Before -> after line-count evidence

Two measurements are recorded per fixture, because they answer different questions and the plan's acceptance criteria (16→4, 8→4) match the first one exactly:

| Fixture | Original lines in | Original lines surviving verbatim (STRIP-stage evidence, matches 09-CR-01-REPRO.md / RESEARCH exactly) | Final AGENTS.md size after the FULL Apply chain (strip + insert + mv) |
|---|---|---|---|
| 13-runaway-drifted-h2 | 16 | **4** (byte-identical prefix; `## Critical Project Rules` and `## Deployment` + bodies permanently gone) | 85 (4 survivors + a freshly re-vendored 81-line block, because no `## ` remained in the stripped output for the insert to anchor above — the END fallback appends) |
| 14-runaway-orphan-provenance | 8 | **4** (same shape) | 85 (same END-fallback re-vendor) |
| 15-mixed-provenance-unresolved | 94 | n/a (not a full-file wipeout — block 1 heals correctly, `## Workflow` survives) | 88 (`## Deployment` and its body confirmed ABSENT — `grep -c -x -- '## Deployment'` = 0) |
| 11-prose-mention-provenance | 91 | n/a (destruction is localized, not a full-file wipeout) | 85 (shorter than before — `IMPORTANT PROJECT RULE` confirmed destroyed) |

**Why two numbers for 13/14, not one:** `09-CR-01-REPRO.md`'s own "16 → 4" finding measured the STRIP awk's output in isolation. This plan's fixtures run the plan's mandated `_m0009_apply "$p" "$h" "$apply_block"` — the FULL three-stage chain (strip, insert, `mv`) — because that is what actually reaches disk. Both are verified true simultaneously (confirmed by direct standalone extraction and execution, independent of the harness, before committing): the first 4 lines of the final `AGENTS.md` are byte-identical to the first 4 lines of the original (`head -4` compared), matching the repro doc's finding exactly, **and** the final file is 85 lines because the insert pass's END fallback re-vendors a fresh mirror on top. This second fact is additional evidence beyond the repro doc, not a contradiction of it — it is precisely why 0009's own post-strip heading guard (`grep -q '^## Coding Discipline (NON-NEGOTIABLE)$'`) is satisfied: the very re-insertion that follows the destruction re-creates the heading the guard is looking for.

## Falsifiability mutation demos (both required by acceptance criteria; both performed and reverted cleanly)

**1. `state-a` (Task 3a).** Temporarily changed `assert_check`'s expected argument from `"applied"` to `"not-applied"`:
- Confirmed the mutation landed: `grep -n 'sa" "not-applied"' migrations/run-tests.sh` → line 3610.
- Ran `bash migrations/run-tests.sh 0009 | grep 'state A:'` → `✗ state A: ... (expected not-applied, got exit=0)` — **FAIL**, as required.
- Restored `"applied"`, confirmed via `grep -n 'sa" "applied"'` → line 3610.
- Re-ran → `✓ state A: ... (expected applied, exit=0)` — **PASS**.

**2. Mirror single-`## ` guard (Task 3b).** Backed up the real vendored mirror (`cp` to `/tmp`), appended a second `## Injected Second Heading` line:
- Confirmed the mutation landed: `grep -c '^## '` on the mirror → `2`.
- Ran `bash migrations/run-tests.sh 0004` → `FAIL mirror carries 2 '## ' line(s), first at line 1 (expected exactly 1, on line 1 ...)` — **FAIL**, as required (also cascaded a `FAIL` on the byte-identity assertion above it, expected side-effect of mutating a file that assertion also reads).
- Restored the mirror from the `/tmp` backup, confirmed `git status --short` on that path was empty (clean).
- Re-ran → `3 PASS / 0 FAIL`.

## Q4 numbering note (recorded per plan's `<objective>` instruction)

The ROADMAP's criterion-6 label `11-idempotent-rerun` is **aliased, not satisfied literally** by this plan. Per Q4's ruling, `11-` was reserved for upstream's ported `11-prose-mention-provenance` fixture (added in this plan); `12-idempotent-rerun` is reserved for plan 09.1-06, which has not yet run. `grep -c "'12-"` on `run-tests.sh` after this plan is `0`, confirming `12-` has not been claimed early.

## Decisions Made

Documented above under `key-decisions` frontmatter: the strip-stage-vs-full-chain line-count methodology for fixtures 13/14, fixture 15's use of two real distinct blocks rather than a synthetic proxy, and the `cp`-based (not `git checkout --`) mutation/restore protocol for the mirror guard test.

## Deviations from Plan

None — all three tasks executed as written, with all acceptance criteria verified before each commit. The two-measurement line-count reporting above is a clarification of methodology (the plan's literal "16 → 4" / "8 → 4" figures are the STRIP-stage subset of what the fixtures observe), not a deviation from any task action, file scope, or assertion the plan specified.

## Stale-comment housekeeping (noted per the prior-wave-context FYI, not fixed)

The `10-corrupt-mirror-refused` fixture's comment block (unchanged by this plan — outside its edit vicinity) still says "mk_project supplies `.git` + SKILL.md at 0.6.0," which 09.1-01 made untrue (`_m0009_mk_project` no longer manufactures a `skills/` tree). This plan's edits did not touch that fixture's vicinity, so per the prior-wave-context instruction this is reported here rather than fixed as an unrelated detour.

## Issues Encountered

- `vendor/agenticapps-shared` submodule was not initialized in this worktree (same environment gap every prior 09.1 plan recorded). Ran `git submodule update --init --recursive` before the first `run-tests.sh` invocation could proceed. Read-only, non-destructive, produced no working-tree changes to commit — environment setup, not a plan deviation.

## Next Phase Readiness

- The RED this plan records — 4 new fixtures, 12 FAIL labels, all recorded exit codes, and both mutation-falsifiability demos — is the exact evidence 09.1-05 needs as its starting point to fix the strip's decoupled entry/exit condition (CR-01) and anchor `PROV_RE` (CR-02).
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at plan start (verified via `git diff --stat 6f595f4 -- migrations/0009-spec-11-region-aware-placement.md` returning nothing across all three commits).
- Fixtures 01-10 and `no-scaffolder-tree` stayed green throughout (ANCHOR-05 — no weakening).
- No blockers for 09.1-05.

---
*Phase: 09.1-11-strip-runaway-inserted*
*Completed: 2026-07-15*

## Self-Check: PASSED

- FOUND: `migrations/run-tests.sh`
- FOUND: `.planning/phases/09.1-11-strip-runaway-inserted/09.1-04-SUMMARY.md`
- FOUND: commit `8c4adb3` (Task 1 — fixtures 13/14/15)
- FOUND: commit `6c5bb3d` (Task 2 — fixture 11 port)
- FOUND: commit `d360fac` (Task 3 — state-a + mirror invariant)
- FOUND: commit `95450b1` (plan metadata / SUMMARY)
- Re-ran `bash migrations/run-tests.sh 0009` at self-check time: 45 PASS / 12 FAIL — matches all figures recorded above.
