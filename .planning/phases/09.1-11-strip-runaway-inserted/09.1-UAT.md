---
status: complete
phase: 09.1-11-strip-runaway-inserted
source: [09.1-01-SUMMARY.md, 09.1-02-SUMMARY.md, 09.1-03-SUMMARY.md, 09.1-04-SUMMARY.md, 09.1-05-SUMMARY.md, 09.1-06-SUMMARY.md, 09.1-07-SUMMARY.md]
started: 2026-07-15T20:53:38Z
updated: 2026-07-16T05:38:21Z
tested_by: claude (agent-run, at user request)
testbed: ~/Sourcecode/agenticapps/workflow-testbed-codex (branch codex-workflow-benchmark, copied into scratchpad sandboxes — testbed left untouched)
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any warm state. From a fresh checkout (`git submodule update --init --recursive`), `bash migrations/run-tests.sh` boots without errors, runs every fixture to completion, and reports 0 FAIL. The suite does not abort partway and no assertion reads "NOT ASSERTED: extraction failed".
result: pass
evidence: |
  Fresh `git clone --local` into scratchpad, submodule uninitialised at start
  (`-1f5d543...`). `git submodule update --init --recursive` checked out the pinned
  1f5d543 (v1.0.0). `bash migrations/run-tests.sh` → exit 0, PASS: 368, FAIL: 0, SKIP: 2.
  `NOT ASSERTED: extraction failed` count = 0; FAIL-line count = 0.
  Both SKIPs benign: "0000-baseline.md is interactive-only" and "core spec repo not
  adjacent" (the ../agenticapps-workflow-core probe, unresolvable from scratchpad).
  0009 filter cold: 87 PASS / 0 FAIL.

### 2. Migration 0009 Runs on a Real Target Project (the BLOCKER)
expected: Migration 0009's pre-flight reads the project's own `.codex/workflow-version.txt` for its version floor (accepting 0.6.0 or 0.7.0), NOT a project-relative `skills/agentic-apps-workflow/SKILL.md` path no real project has. Applied to a project with `.git` + `.codex/workflow-version.txt` and no `skills/` tree, the pre-flight PASSES (exit 0) and Step 1's §11 heal actually runs. Before this phase it aborted exit 3 on every real install.
result: pass
evidence: |
  Sandboxes built from the real testbed's AGENTS.md (no skills/ tree — confirmed):
  - 0.6.0 → pre-flight exit 0
  - 0.7.0 (idempotent re-apply) → pre-flight exit 0
  - 0.2.1 (testbed's actual version, below floor) → exit 3, correctly naming
    "workflow project version is 0.2.1 (need 0.6.0)"
  COUNTERFACTUAL PROVEN: the pre-fix pre-flight (`git show f1a1da8^`) read
  `SKILL_FILE=skills/agentic-apps-workflow/SKILL.md` and on the SAME 0.6.0 sandbox
  produced `grep: skills/agentic-apps-workflow/SKILL.md: No such file or directory` →
  "ABORT: workflow scaffolder version is unknown" → exit 3. The BLOCKER is genuinely closed.

### 3. Drifted §11 Heading Refuses Instead of Destroying AGENTS.md
expected: Given an AGENTS.md carrying a §11 provenance line whose `## Coding Discipline (NON-NEGOTIABLE)` heading has been renamed or is missing, the migration ABORTS with exit 3 and a diagnostic naming the offending provenance line. AGENTS.md is byte-identical afterwards — your `## Critical Project Rules` and `## Deployment` sections survive intact. Previously this silently "succeeded" while truncating a 16-line file to its first 4 lines.
result: pass
evidence: |
  Testbed's real 175-line AGENTS.md with its heading renamed to
  "## Coding Discipline (RENAMED — drifted)":
  → exit 3; diagnostic named the offending line ("5:<!-- spec-source: ... §11 -->")
    and offered both remedies (restore the heading / remove the provenance line).
  → AGENTS.md byte-identical (cmp -s), all 10 `## ` headings survive, 0 leftover temp files.
  COUNTERFACTUAL PROVEN: the pre-fix Apply block (`git show 21aeeaa^`) on the SAME file
  → exit 0 (silent "success"), 175 lines → 85 lines, with ## Development Workflow,
  ## Session handoff, ## Skill routing and ## Workflow Enforcement Hooks (MANDATORY)
  ALL DESTROYED. The refusal is real and it matters.

### 4. Prose and Fenced Mentions of the Marker Are Safe
expected: A prose mention of the spec-11 marker (e.g. inside a guard comment) no longer substring-matches the provenance regex and destroy content between the mention and the next heading — it heals normally. A whole-line quotation of the marker or the §11 heading inside prose or a markdown code fence (e.g. a troubleshooting example showing a healthy block) causes the migration to REFUSE rather than latch onto the example and swallow real content.
result: pass
evidence: |
  (a) Prose mention (substring, not whole-line) + §11 inside a region needing heal:
      → exit 0, block healed above the region, "IMPORTANT PROJECT RULE — DO NOT DELETE ME."
        survived, `## Deployment` survived. CR-02 closed.
  (b) Whole-line marker + heading quoted inside a ```markdown fence, above the testbed's
      real 175-line AGENTS.md:
      → exit 3, byte-identical, sentinel survived. Diagnostic explains it cannot tell a
        real block from an exact quotation and names both remedies.

### 5. CRLF Line Endings Refuse Rather Than Mis-Anchor
expected: Given an AGENTS.md with CRLF line endings, the migration refuses with a clear diagnostic rather than silently mis-anchoring — CRLF defeats every `$`-anchored regex, which would otherwise reinsert the §11 block inside the GitNexus-managed region while reporting success. The refusal is disclosed in the migration's Known limitations / Notes.
result: pass
evidence: |
  Testbed AGENTS.md converted to CRLF (`file` confirms "with CRLF line terminators")
  → exit 3, byte-identical, diagnostic names the concrete remedy
    (`perl -pi -e 's/\r\n$/\n/' AGENTS.md`, then re-run the update skill).
  Disclosed in `## Known limitations`, with the deliberate divergence from upstream's
  0029 recorded.

### 6. Zero-Byte AGENTS.md Skips Informationally
expected: A zero-byte AGENTS.md routes through the informational skip path (migration reports nothing to heal and moves on cleanly), not an unrecoverable abort.
result: pass
evidence: |
  0-byte AGENTS.md → exit 0 with "INFO: migration 0009 Step 1 — no AGENTS.md in project
  (or it is empty); §11 heal skipped." plus the scaffold-then-re-run path. Not an abort —
  so Step 2 still records the version and the project is never stranded.

### 7. Idempotent Re-Run Leaves AGENTS.md Byte-Identical
expected: Running migration 0009 a second time against an already-healed, region-led AGENTS.md exits 0 and leaves the file BYTE-IDENTICAL to the first run's output. The `<!-- gitnexus:start -->` / `<!-- gitnexus:end -->` markers stay paired exactly once each, the region body survives, and exactly one provenance line remains.
result: pass
evidence: |
  Region-led file, §11 at region head. First run exit 0; second run exit 0 and
  byte-identical (cmp -s) to the first run's output. Markers start=1 end=1,
  exactly 1 provenance line.

### 8. Version Records Agree at 0.7.0
expected: This repo's `.codex/workflow-version.txt` reads `0.7.0`, matching `skills/agentic-apps-workflow/SKILL.md`'s `version:` field. `bash migrations/run-tests.sh drift` passes, and would hard-fail (naming both values) if the two records ever split again.
result: pass
evidence: |
  `.codex/workflow-version.txt` = 0.7.0; SKILL.md `version: 0.7.0`.
  `bash migrations/run-tests.sh drift` → 2 PASS / 0 FAIL, including
  "this repo's .codex/workflow-version.txt (0.7.0) agrees with its scaffolder SKILL.md".

### 9. ADR-0010 Reads Honestly and Upstream Is Told
expected: `docs/decisions/0010-region-aware-spec-11-placement.md` carries a dated Correction section: D-26's "bounded by construction" claim is quoted and explicitly marked FALSE (not silently deleted), V-01 is recorded as our own porting error with upstream absolved, and the dead `8520f90` pin is re-cited to `f9354cc` with the never-merged history preserved. The upstream CR-01 defect report was filed as issue #90 against agenticapps-eu/claude-workflow.
result: pass
evidence: |
  Correction section present. "bounded by construction" appears exactly once — quoted,
  immediately followed by "**This claim is FALSE**". f9354cc: 14 refs; 8520f90: 9 refs
  (history preserved); "porting error": 3 refs.
  Upstream issue #90 EXISTS and is OPEN:
  https://github.com/agenticapps-eu/claude-workflow/issues/90
  "migration 0029: strip runaway when provenance is present but the exact §11 heading is
  drifted/absent (CR-01)" — criterion 10, left open by 09.1-07, is now closed.

### 10. Full Suite Green
expected: `bash migrations/run-tests.sh` reports 0 FAIL across the whole suite (~345 PASS / 1 SKIP on a main checkout; the 1 SKIP is the `../agenticapps-workflow-core` probe). `bash migrations/run-tests.sh 0009` reports 0 FAIL across its ~63 assertions, covering states A–D, fixtures 01–15, and `no-scaffolder-tree`.
result: pass
evidence: |
  Working tree: exit 0, PASS: 369, FAIL: 0, SKIP: 1. 0009 filter: 87 PASS / 0 FAIL.
  Counts exceed the SUMMARYs' recorded 345/63 because the post-review fix commits
  (CR-01 CRLF, CR-02 fenced-marker, WR-01 zero-byte, WR-02 disclosure) added assertions.
  Higher than recorded, 0 FAIL — no regression.

### 11. §11 Block at the TAIL of a GitNexus Region (probe — discovered during test 4 setup)
expected: When the §11 block sits inside a GitNexus-managed region, the heal moves it above the region and leaves the region's `<!-- gitnexus:start -->` / `<!-- gitnexus:end -->` markers paired exactly once each — the same marker-pairing invariant fixture 02 and 12-idempotent-rerun assert.
result: issue
reported: "A/B with identical content, varying ONLY §11's position inside the region: §11 at region HEAD (the suite's fixture-02 shape) -> markers before[start=1 end=1] after[start=1 end=1], correct. §11 at region TAIL -> markers before[start=1 end=1] after[start=1 end=0], exit=0. The strip silently eats the '<!-- gitnexus:end -->' marker and reports success, leaving an unterminated GitNexus region."
severity: major
reachability: |
  NOT reachable via 0001/0004. Both inject §11 "immediately before the first `## ` heading"
  (the migration's own preamble cites 0001:91 / 0004:77), so on a region-led file §11 always
  lands at the region HEAD, immediately before that heading — which terminates the strip and
  protects the end marker. The TAIL shape requires a hand-edit or third-party placement of
  §11 after the region's own `## ` headings. Narrow, but silent when it happens.

## Summary

total: 11
passed: 10
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Healing a §11 block out of a GitNexus-managed region leaves the region's start/end markers paired exactly once each"
  status: failed
  reason: "User reported: A/B with identical content, varying ONLY §11's position inside the region: §11 at region HEAD -> after[start=1 end=1], correct. §11 at region TAIL -> after[start=1 end=0], exit=0. The strip silently eats the '<!-- gitnexus:end -->' marker and reports success, leaving an unterminated GitNexus region."
  severity: major
  test: 11
  root_cause: "The strip awk's terminator (migrations/0009-spec-11-region-aware-placement.md:349) is `in_block && swallowed_own_h2 && (/^## / || /^<!-- gitnexus:start -->$/)` — it includes the region's START marker but NOT its END marker. When §11 is the last element inside a region (no `## ` heading between the block and `<!-- gitnexus:end -->`), the strip runs past the end marker hunting a `## ` and consumes it, halting only at the next heading outside the region. This is the mirror image of the start-marker case that ANCHOR-05's alternation exists to prevent (09.1-06's mutation demo 2 observed the inverse signature, start=0 end=1). No guard catches it: the h2-count strip-integrity guard counts only `## ` headings, and the region markers are not `## ` lines, so h2_out == h2_in - h2_own still holds and the guard passes."
  artifacts:
    - path: "migrations/0009-spec-11-region-aware-placement.md"
      issue: "strip terminator at :349 omits the `/^<!-- gitnexus:end -->$/` alternative that would stop the strip at the region boundary"
    - path: "migrations/run-tests.sh"
      issue: "fixture 02-inside-region-move places §11 at the region HEAD, where a following `## Always Do` terminates the strip — so its marker-pairing assertion cannot fail on the tail shape. No fixture covers §11 at the region tail."
  missing:
    - "Add `/^<!-- gitnexus:end -->$/` to the strip terminator's alternation (or refuse on the tail shape, consistent with this migration's fail-closed posture for shapes it cannot safely resolve)"
    - "Add a RED-first fixture placing §11 at the region TAIL, asserting markers stay paired start=1/end=1 — observed FAILING against the current strip before the fix"
    - "If the ruling is refuse-rather-than-heal, disclose the shape in `## Known limitations` alongside CRLF and fenced markers"
  debug_session: ""
  notes: "Reachability is narrow — 0001/0004 cannot produce the tail shape (they inject before the FIRST `## `, which puts §11 at the region head). Requires a hand-edit or third-party placement. Severity is inferred as major because the failure is silent (exit 0, reports success) and leaves an unterminated managed region that a later `gitnexus analyze` could expand over real content — but the narrow reachability is a legitimate argument for downgrading to minor, or for accepting-and-disclosing rather than fixing. This is the user's call."
  disposition: accepted-and-disclosed
  disposition_by: user (2026-07-16)
  disposition_rationale: |
    Ruled: disclose, do not fix. Rationale — a fifth guard on an inference-based
    strip buys one narrow, hand-edit-only shape while adding mechanism to the wrong
    foundation. The durable fix is paired start/end markers for §11, which retires
    the whole class (CR-01's runaway, orphan-provenance run-to-EOF, and this) rather
    than guarding each instance. Recorded as the lead item in ADR-0010's open
    follow-ups for a successor migration.

    Separately, the collision was removed at its source in this repo: GitNexus no
    longer writes a managed region into AGENTS.md/CLAUDE.md (commit 38e3478,
    `analyze --skip-agents-md`), so the tail shape is unreachable here. Migration
    0009 still ships to projects that may carry a region, which is why the shape is
    disclosed in 0009's own `## Known limitations` rather than dropped.
  disclosed_in:
    - path: "migrations/0009-spec-11-region-aware-placement.md"
      section: "## Known limitations → 'NOT refused — §11 at the tail of a managed region (open)'"
    - path: "docs/decisions/0010-region-aware-spec-11-placement.md"
      section: "## Open follow-ups (paired-markers item + the tail-shape item it subsumes)"
