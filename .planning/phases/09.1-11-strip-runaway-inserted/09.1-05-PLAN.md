---
phase: 09.1-11-strip-runaway-inserted
plan: 05
type: execute
wave: 4
depends_on: [09.1-04]
files_modified:
  - migrations/0009-spec-11-region-aware-placement.md
autonomous: true
requirements: [ANCHOR-05, MIGR-01, MIGR-04]

must_haves:
  truths:
    - "Provenance present + exact H2 absent ⇒ 0009 exits 3 and leaves AGENTS.md byte-identical"
    - "A prose mention of the provenance line cannot trigger the strip"
    - "A provenance entry that never finds its heading cannot latch in_block to EOF"
    - "The strip's failure produces a diagnostic distinguishable from 'produced no output'"
    - "An absent §11 block is still injected at the anchor (MIGR-04 unregressed)"
  artifacts:
    - path: "migrations/0009-spec-11-region-aware-placement.md"
      provides: "anchored PROV_RE, the refuse gate, the END fail-closed guard, a distinguishable strip diagnostic"
      contains: "swallowed_own_h2"
  key_links:
    - from: "Step 1 Apply's refuse gate"
      to: "the anchored PROV_RE"
      via: "grep -qE on a whole-line match"
      pattern: "\\^<!-- spec-source"
    - from: "the strip awk's END clause"
      to: "the shell's status dispatch"
      via: "exit 4"
      pattern: "exit 4"
---

<objective>
Close CR-01 and CR-02: make migration 0009 incapable of destroying user content in the
states it does not abort on. Turn 09.1-04's four RED fixtures GREEN.

Purpose: 0009's strip decouples its **entry** condition (an unanchored provenance
substring) from its **exit** condition (which requires `swallowed_own_h2`, set only by the
exact `## Coding Discipline (NON-NEGOTIABLE)` heading). A drifted heading latches
`in_block` forever and `in_block { next }` eats to EOF. All three guards pass — the
post-strip `grep -q` is satisfied by the *insert* pass re-adding the heading it checks for
— so `mv` commits the truncation. This falsifies ADR-0010 D-26's "bounded by construction".

**This phase fixes defects in a migration whose own purpose was fixing a defect.** Do not
add a guard you have not watched fail.

Output: 09.1-04's fixtures 11/13/14/15 go GREEN; fixtures 01-10 and `no-scaffolder-tree`
**stay** GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-04-SUMMARY.md
@.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md

**CLAUDE.md's GitNexus mandate is INERT** — this plan edits one Markdown document.
Grep is ground truth. Do not claim GitNexus analysis.

<interfaces>
<!-- Locked decisions. These are NOT discretionary. -->

**Q1 (user ruling, 2026-07-15): REFUSE.** Provenance present + exact H2 absent ⇒ abort
exit 3, leaving `AGENTS.md` byte-identical. ONE rule covers BOTH reproduced runaway shapes
(drifted-H2 and orphaned-provenance) because both are exactly "provenance present, exact H2
absent". This closes CR-01 **by construction**: a strip that refuses to run cannot run
away. Consistent with 0009's existing never-overwrite-a-hand-paste conflict branch
(`:213-228`). The abort message must name the orphaned provenance line and tell the
operator to restore the heading or remove the line.
**Rejected: heal-and-duplicate** (un-latch at the boundary and insert anyway) — no data
loss, but silently leaves two similar §11 headings that a future migration's own conflict
grep would trip on.

**Do NOT add the reviewer's un-latch rule** (`in_block && !swallowed_own_h2 && (/^## / || …) { in_block = 0; print; next }`).
Three reasons, all load-bearing:
  1. It implements the **rejected** heal-and-duplicate semantics for the mixed-provenance
     shape — it silently deletes the drifted block's body and inserts a canonical block
     alongside it.
  2. RESEARCH reproduced that it is insufficient anyway: un-latching alone still runs away
     to EOF on the orphaned-provenance shape (8 → 4 lines).
  3. Without it, the mixed shape latches and the END guard refuses — which is exactly the
     ruling's intent, and it keeps the anchor alternation at **2 copies** (`:265`, `:288`).
Verified at planning time: `run-tests.sh` has NO `anchor-parity` count assertion, so
Q3's inverse-no-weakening trap does not fire. Confirm the count is still 2 anyway.

**Defense-in-depth (do NOT rely on it alone):** the refuse gate is file-global and cannot
see the mixed-provenance shape (exact H2 present via block 1). Fixture 15 is that shape.
The END guard is what catches it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchor PROV_RE at all three sites, ported from upstream f9354cc</name>

  <read_first>
    - `git -C ../claude-workflow show f9354cc:migrations/0029-region-aware-spec-11-placement.md`
      — read `:142`, `:146`, `:175`, `:223`. **The port source.** Upstream fixed CR-02 in
      PR #89 before we did.
    - `migrations/0009-spec-11-region-aware-placement.md:159-168` — the idempotency check
      (the provenance grep at `:161` and the in-region awk trigger at `:165`).
    - `migrations/0009-spec-11-region-aware-placement.md:200-273` — `PROV_RE`'s definition
      at `:204` and the strip's entry regex at `:257`.
    - `migrations/0009-spec-11-region-aware-placement.md:352-361` — why 0009's Rollback is
      `git checkout AGENTS.md` (D-47). Upstream's fourth anchoring site is its Rollback
      strip pass; **we have no analog and nothing to port there.**
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Upstream Delta"
      — the site-by-site mapping table.
  </read_first>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    Re-verify the pin first: `git -C ../claude-workflow fetch && git -C ../claude-workflow log --oneline -1 origin/main`
    must be `f9354cc`. Read the analog with `git show f9354cc:<path>`, never the working tree.

    Anchor the provenance regex (`^…$`) at the **three** sites 0009 carries it unanchored,
    mapping to upstream's:
    - `0009:161` (idempotency-check provenance grep) ← `f9354cc:0029:142`
    - `0009:165` (idempotency-check in-region awk trigger) ← `f9354cc:0029:146`
    - `0009:204` (`PROV_RE`) and `0009:257` (strip-pass entry) ← `f9354cc:0029:175`, `:223`

    **Do NOT lose `@[^[:space:]]+`'s deliberate any-version match.** Anchoring adds `^` and
    `$`; it must not narrow the version class. Idempotent re-runs across versions depend on
    the strip recognizing an OLD version's provenance line — narrowing it to `@0\.4\.0`
    would leave stale provenance lines unstrippable and is exactly the orphaned-provenance
    state this phase exists to close. (`0009:161`'s grep is version-pinned by design — that
    is the *idempotency* predicate, a different question from the *strip*'s entry. Anchor
    it; do not widen it.)

    Add a prose note recording the port with attribution: anchored per upstream `f9354cc`
    (PR #89), which fixed CR-02 there; ported rather than re-derived because the fix is
    validated across six repos and re-deriving guarantees prose divergence from the repo
    0009 is a port of.
  </action>

  <acceptance_criteria>
    - `grep -c 'spec-source: agenticapps-workflow-core' migrations/0009-spec-11-region-aware-placement.md`
      — every occurrence used as a **regex** is anchored. Verify no unanchored regex
      remains: `grep -nE "/<!-- spec-source|'<!-- spec-source" migrations/0009-spec-11-region-aware-placement.md`
      returns only lines where the pattern is a literal string (`PROV=`, prose, echo), never
      a match target.
    - `grep -c '\^<!-- spec-source: agenticapps-workflow-core@\[\^\[:space:\]\]+ §11 -->\$' migrations/0009-spec-11-region-aware-placement.md`
      is `>= 2` (the `PROV_RE` definition and the strip entry).
    - The any-version class survives:
      `grep -c '@\[\^\[:space:\]\]+' migrations/0009-spec-11-region-aware-placement.md` is
      `>= 2`. If this drops, the anchoring narrowed the version match — revert.
    - `bash migrations/run-tests.sh 0009` — `11-prose-mention-provenance` now PASSes on all
      four of its assertions, and the recorded after-state line count equals the before-state
      (the prose mention no longer triggers the strip).
    - `03-healthy-noop` (byte-identical after Apply) still PASSes — anchoring did not make
      the strip miss a real block.
    - `09-two-provenance-heal` still PASSes — the any-version match still strips both.
    - `grep -c 'f9354cc' migrations/0009-spec-11-region-aware-placement.md` is `>= 1`.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-05-t1.log; grep -E 'prose-mention-provenance|healthy-noop|two-provenance' /tmp/09.1-05-t1.log</automated>
  </verify>

  <done>`PROV_RE` is anchored at all three sites with `@[^[:space:]]+` intact, fixture 11 is GREEN, and fixtures 03/09 are unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: The refuse gate + the END fail-closed guard + a distinguishable strip diagnostic</name>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md:200-334` — the whole Apply block.
      Specifically: the existing hand-paste conflict branch (`:213-228`) whose idiom you are
      inverting; the strip awk (`:255-273`); the `if awk … && [ -s … ]; then` dispatch
      (`:273`); and the misleading `else` at `:327-332`.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Code Example 1"
      — the verified composite awk and its 8-shape result table. **Take the `unresolved`
      flag and the END clause; do NOT take the un-latch rule** (see `<interfaces>`).
    - `.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md` § "Reachability" —
      why the runaway needs the *inverse* of the existing abort branch's condition.
    - `migrations/run-tests.sh:3490-3496` — the Apply shape guard asserts `gitnexus:start`
      is present in the extracted block. Your restructure must keep it, or every fixture's
      extraction gate goes down.
  </read_first>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    **(a) The refuse gate (Q1's mechanism).** Add a branch to Step 1 Apply's if/elif chain,
    AFTER the existing hand-paste conflict `elif` (`:213-228`) and BEFORE the `else`:
    condition = `grep -qE "$PROV_RE" AGENTS.md && ! grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md`.
    This is the literal inverse of the guard already at `:213-214`, and it runs before any
    file surgery, so the file is byte-identical by construction.

    Its message must:
    - name the orphaned provenance line — include the output of
      `grep -nE "$PROV_RE" AGENTS.md` so the operator gets a line number and the text;
    - tell the operator the two ways out: restore the `## Coding Discipline
      (NON-NEGOTIABLE)` heading immediately below the provenance line, or remove the
      provenance line;
    - `exit 3`.
    Follow `:218-228`'s formatting and tone — the existing refusal is the house style.

    **The message must NOT contain `missing or empty`** — that is pre-flight guard 3's
    exclusive discriminator, which 09.1-06 uses to isolate the `test -s` layer (criterion 5).

    **(b) The END fail-closed guard.** In the strip awk:
    - In the ENTRY rule, before setting `in_block = 1`, add
      `if (in_block && !swallowed_own_h2) { unresolved = 1 }` — a second provenance line
      arriving while a previous entry never found its heading.
    - Reset `swallowed_own_h2 = 0` on entry.
    - Add `END { if (unresolved || (in_block && !swallowed_own_h2)) exit 4 }`.
    - **Change nothing else.** The terminator alternation at `:265` is ANCHOR-05 and stays
      exactly as-is. Do NOT add the un-latch rule.
    Use **exit 4**, not exit 1, so the shell can discriminate this failure from a genuine
    awk error. Note in a comment: a legitimate block running to EOF has
    `swallowed_own_h2 == 1` and is unaffected (fixture 06).

    **(c) The distinguishable diagnostic (criterion 4, part 1).** The current dispatch is
    `if awk '…' > AGENTS.md.0009.strip && [ -s AGENTS.md.0009.strip ]; then … else …`.
    Any non-zero awk status routes to `:329`'s "the strip pass produced no output" — which
    is **false and misleading** for an END-guard refusal and would send the operator hunting
    a disk-full error. Restructure to capture the status and dispatch on it:
    - run the awk, capture `strip_rc=$?`;
    - `strip_rc == 4` → `rm -f AGENTS.md.0009.strip`; print an ABORT explaining that a
      provenance line was never followed by its `## Coding Discipline (NON-NEGOTIABLE)`
      heading, name the line(s) via `grep -nE "$PROV_RE" AGENTS.md`, give the same two ways
      out as (a), state `AGENTS.md` is untouched; `exit 3`;
    - `strip_rc != 0` or the strip file is empty → the EXISTING "produced no output" branch,
      unchanged (it is correct for its actual cause);
    - otherwise → the existing insert pass, unchanged.
    Preserve the temp-file cleanup on every exit path — `0009:405` asserts no
    `AGENTS.md.0009.{strip,tmp}` is left behind.

    **(d) Correct the false comment.** `0009:244-249` claims the strip "is bounded
    structurally, so a drifted block cannot cause a runaway" — the claim itself is on
    `:244-245`. (**`:238-243` is a DIFFERENT comment** — the `swallowed_own_h2` reset
    explanation — and is correct; leave it alone.) That is the D-26 claim
    09-REVIEW.md falsified. Rewrite it: D-26's *principle* survives — the strip stays blind
    to the block's **prose**, because content fidelity is 0004's job. D-26's *claim* is
    false — the strip is bounded **only when the heading matches**. The heading is a
    structural boundary marker, not content: the canonical mirror has exactly one `## ` line
    and it is the heading (asserted by 09.1-04's `test_migration_0004` guard). So prose
    drift → stay blind and repair; heading drift → the boundary is gone, refuse.
  </action>

  <acceptance_criteria>
    - `grep -c 'exit 4' migrations/0009-spec-11-region-aware-placement.md` is `>= 1`.
    - `grep -c 'unresolved' migrations/0009-spec-11-region-aware-placement.md` is `>= 2`.
    - **The D-26 false claim is gone from the migration.** Match 0009's ACTUAL wording,
      which wraps across `:244`→`:245`:
      `# The strip is deliberately BLIND to the block's content: it is bounded` /
      `# structurally, so a drifted block cannot cause a runaway, and no verbatim`.
      So assert:
      `grep -c 'structurally, so a drifted block' migrations/0009-spec-11-region-aware-placement.md`
      is `0` (returns `1` today — genuinely RED), and
      `grep -c 'a drifted block cannot cause a runaway' migrations/0009-spec-11-region-aware-placement.md`
      is `0` (returns `1` today — genuinely RED).
      **Do NOT grep 0009 for `bounded by construction`.** Verified: that string returns `0`
      in 0009 TODAY, before you change anything — it is **ADR-0010's** phrasing
      (`0010:155`, count 1 there), not 0009's. A criterion built on it passes on arrival
      and asserts nothing, which is the exact defect class this plan exists to remove.
      Correcting the ADR's copy of the claim is 09.1-07 Task 1's job, not this plan's.
    - **The un-latch rule is absent.** The alternation copy count is still exactly 2:
      `grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md`
      is `2`.
    - The Apply shape guard's anchor survives:
      `grep -c 'gitnexus:start' migrations/0009-spec-11-region-aware-placement.md` is
      `>= 2`, and `bash migrations/run-tests.sh 0009` reports no "NOT ASSERTED: extraction
      failed".
    - **09.1-04's four RED fixtures are GREEN.** `bash migrations/run-tests.sh 0009` PASSes
      every assertion of `13-runaway-drifted-h2`, `14-runaway-orphan-provenance`,
      `15-mixed-provenance-unresolved`: `rc == 3`, `cmp -s` byte-identical, the diagnostic
      names the provenance line, and the diagnostic does NOT contain `produced no output`.
    - **13 and 14 are caught by the refuse gate; 15 is caught by the END guard.** Prove the
      division of labour rather than assuming it: record, for each fixture, which branch
      fired (the diagnostics differ). If 15 is caught by the refuse gate, the gate is not
      file-global as designed and the END guard is untested — investigate before proceeding.
    - **MIGR-04 unregressed:** `01-gitnexus-led-inject`, `04-no-agentsmd`, `06-no-heading-eof`
      still PASS. An absent block is still injected at the anchor; the refuse gate must not
      fire when provenance is absent.
    - **ANCHOR-05 unregressed — no weakening:** `02-inside-region-move` (the D-24 terminator
      assertion) and `08-rollback-region-led` still PASS.
    - `bash migrations/run-tests.sh` (full) reports **0 FAIL**.
    - No `AGENTS.md.0009.strip` or `.tmp` is left behind on the refusal paths — assert via
      the fixtures' sandboxes after Apply.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | tee /tmp/09.1-05-t2.log; grep -cE '\bFAIL\b' /tmp/09.1-05-t2.log</automated>
  </verify>

  <done>The refuse gate, the END guard, and the status dispatch are in place; fixtures 11/13/14/15 are GREEN with the branch division recorded; fixtures 01-10 unregressed; full suite 0 FAIL.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion 4 — determine whether a strip-integrity guard can actually fail, and refuse to ship it if it cannot</name>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md:298-332` — the post-strip guards as
      they now stand after Task 2, especially the `grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md.0009.tmp`
      at `:307`.
    - `.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md` § "Why the guards do
      not catch it" — the guard is satisfied by the pass that follows it.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Code Example 2"
      — the h2-count guard, and its own admission that it "is defeated by the orphan-PROV
      shape".
    - `migrations/run-tests.sh:3463-3483` — the D-28.1 two-layer precedent, and WR-04's
      finding that layer 1 (`test -s`) is **subsumed** by layer 2 for correctness and kept
      as a *diagnostic* layer. This is the precedent for keeping a subsumed guard — but only
      if you say so explicitly.
  </read_first>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <action>
    Criterion 4 requires the post-strip guard to be one that **can actually fail**. Task 2
    already delivered half of it: the `strip_rc == 4` branch is a post-strip guard whose
    failure is observed live by fixture 15. This task settles the other half honestly.

    RESEARCH recommends adding the h2-count strip-integrity guard (Code Example 2) on
    `AGENTS.md.0009.strip`, pre-insert. **Determine its reachability before shipping it.**
    With the refuse gate + END guard in place, every shape that loses a foreign `## ` may
    already abort at `strip_rc == 4` — in which case the h2-count guard is dead by
    construction, and shipping it would reproduce the exact defect class this phase closes.

    Run the determination as an experiment, not an argument:
    1. Add the h2-count guard between the passes, per Code Example 2. Follow the
       `grep -c … | tr -d ' '` idiom (BSD `grep -c` can emit leading whitespace).
    2. Mutate: delete ONLY the `END { … exit 4 }` clause from the strip awk.
    3. **Assert the mutation landed** before trusting the run —
       `grep -c 'exit 4' migrations/0009-spec-11-region-aware-placement.md` must have
       decreased. In Phase 9 a `sed: bad flag` silently no-op'd and the run *looked* like a
       passing demo; an unverified mutation demo is itself a dead assertion.
    4. Run `bash migrations/run-tests.sh 0009` and observe fixture 15.
    5. Restore the END clause and re-run.

    Then act on the result — this is a real branch, not a formality:
    - **If fixture 15 still refuses under the mutation** (the h2-count guard caught it):
      the guard is a live backstop layer. KEEP it. Document the layering explicitly,
      citing D-28.1/WR-04 as the precedent for a subsumed-but-retained diagnostic layer,
      and state which shape reaches which layer.
    - **If nothing reaches the h2-count guard under any shape**: it is dead by construction.
      **REMOVE it.** Criterion 4 is satisfied by the `strip_rc == 4` branch, which fixture 15
      observes failing. Record the rejection and the reasoning — 09.1-07's ADR must carry it.

    Either way: **re-scope the `grep -q` at `:307` honestly.** Rewrite its comment. It does
    NOT guard strip integrity — it cannot, because the insert regenerates the very heading it
    looks for (that is CR-01's third guard). What it actually guards is **mirror integrity**:
    a zero-byte or corrupt mirror makes the insert emit a provenance line with no block, and
    this catches that. Say that, and say plainly that strip integrity is guarded by
    `strip_rc` instead. A comment that overstates a guard's reach is how D-26 shipped.
  </action>

  <acceptance_criteria>
    - The reachability determination is recorded in the SUMMARY with: the mutation command,
      **proof the mutation landed** (the before/after `grep -c 'exit 4'` counts), fixture
      15's observed `rc` and diagnostic under the mutation, and the resulting KEEP/REMOVE
      decision with its reasoning.
    - The decision is actually reflected in the file — the SUMMARY's claim and
      `grep -c 'h2_out' migrations/0009-spec-11-region-aware-placement.md` agree (`>= 1` if
      KEEP, `0` if REMOVE). A recorded decision that does not match the file is a dead
      assertion about a dead assertion.
    - The END clause is restored:
      `grep -c 'exit 4' migrations/0009-spec-11-region-aware-placement.md` is back to its
      pre-mutation count, and `bash migrations/run-tests.sh 0009` is 0 FAIL.
    - `:307`'s comment no longer claims to guard the strip:
      `sed -n '/grep -q .\^## Coding Discipline (NON-NEGOTIABLE)\$. AGENTS.md.0009.tmp/,-12p' `
      is awkward — instead verify by reading: the comment above the insert's guard names
      **mirror** integrity and points at `strip_rc` for strip integrity.
      `grep -c 'mirror integrity\|strip integrity' migrations/0009-spec-11-region-aware-placement.md`
      is `>= 1`.
    - `bash migrations/run-tests.sh` (full) reports **0 FAIL** after restore.
    - The alternation copy count is STILL exactly 2 (Q3 re-check after all edits):
      `grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md`
      is `2`. **If this reads 3, do not "fix" the assertion — a good change failing an
      assertion is not license to weaken it.** Verify no count assertion regressed
      (`bash migrations/run-tests.sh` 0 FAIL) and record the deliberate change in the SUMMARY
      for 09.1-07's ADR.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 2>&1 | tee /tmp/09.1-05-t3.log; grep -cE '\bFAIL\b' /tmp/09.1-05-t3.log; grep -c '/\^## / || /\^<!-- gitnexus:start -->\$/' migrations/0009-spec-11-region-aware-placement.md</automated>
  </verify>

  <done>The h2-count guard's reachability is determined by a verified mutation, the KEEP/REMOVE decision is executed and recorded, the `grep -q` guard's comment is honestly re-scoped, and the full suite is 0 FAIL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's `AGENTS.md` → the strip awk | **the phase's core boundary.** Hand-editable, untrusted content drives a state machine that rewrites the file in place and `mv`-commits the result. |
| `AGENTS.md` prose → the strip's entry condition | prose can impersonate a structural marker unless the regex is whole-line anchored |
| the strip pass → the insert pass | the insert regenerates the evidence the post-strip guard looks for, blinding it |
| untrusted file content → shell interpolation | provenance line text flows into `echo` diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-18 | Tampering (data destruction) | strip awk `in_block { next }` (`:271`) | mitigate | CR-01. Task 2(a)'s refuse gate closes it **by construction** — provenance present + exact H2 absent aborts before any surgery, so a strip that refuses to run cannot run away. Verified by fixtures 13/14 going GREEN. |
| T-09.1-19 | Tampering (data destruction) | the mixed-provenance shape the file-global gate cannot see | mitigate | Task 2(b)'s `unresolved` flag + `END { … exit 4 }`. Verified by fixture 15 going GREEN, and by the Task 3 mutation showing what happens without it. |
| T-09.1-20 | Tampering (marker mimicry / injection) | unanchored `PROV_RE` (`:204`, `:257`, `:161`, `:165`) | mitigate | CR-02. Task 1 anchors `^…$` at all three sites, ported from `f9354cc`. Whole-line match required. ASVS V5 — this task **is** the input-validation control. |
| T-09.1-21 | Repudiation (silent failure) | the post-strip `grep -q` on `.tmp` (`:307`) | mitigate | Satisfied by the insert re-adding the heading it checks for — a guard that reports success on a destroyed file. Task 2(c) adds a guard on the artifact the failure is *about* (`strip_rc`); Task 3 re-scopes the comment so it stops overstating its reach. |
| T-09.1-22 | Repudiation | the misleading `else` at `:329` | mitigate | An END-guard refusal would print "the strip pass produced no output" — false, and it misdirects the operator to disk space. Task 2(c) dispatches on status; fixture 15 asserts the message is NOT that string. |
| T-09.1-23 | Repudiation | shipping a guard that cannot fail | mitigate | Task 3 forces a reachability determination by verified mutation and **removes** the h2-count guard if nothing reaches it. Adding an unfalsifiable guard here would reproduce CR-03 inside CR-03's own fix. |
| T-09.1-24 | Tampering (injection) | provenance text interpolated into diagnostics | mitigate | The refuse gate echoes `grep -nE "$PROV_RE" AGENTS.md` output. It is only ever echoed — never eval'd, never used as a path or a regex. `$PROV_RE` is a fixed literal defined in the block, not derived from file content. Quote all expansions. |
| T-09.1-25 | Tampering | `mv` committing a truncated result | mitigate | Every new refusal path runs BEFORE `mv` and `rm -f`s the temp files, leaving `AGENTS.md` byte-identical — asserted by `cmp -s` in fixtures 13/14/15 and by `0009:405`'s no-temp-files-left check. |
| T-09.1-26 | Denial of Service | awk portability (A2) | accept | `END { exit 4 }` and uninitialized-flag truthiness are POSIX and verified on BSD awk 20200816. `gawk`/`mawk` are not installed here, so portability is argued from POSIX, not verified. **Same exposure the existing awk already carries** — this phase does not widen it. Record as a known limitation in 09.1-07's ADR. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash migrations/run-tests.sh 0009` → 0 FAIL, and fixtures 11/13/14/15 all PASS.
- `bash migrations/run-tests.sh` (full) → 0 FAIL (1 SKIP main / 2 SKIP worktree).
- Fixtures 01-10 and `no-scaffolder-tree` unregressed — **no assertion was weakened to
  achieve green.** Confirm by diffing: `git diff migrations/run-tests.sh` is EMPTY. This
  plan must not touch the suite at all.
- The alternation copy count is 2.
- `git diff --stat` touches `migrations/0009-spec-11-region-aware-placement.md` only.
</verification>

<success_criteria>
- `PROV_RE` anchored at all three sites, `@[^[:space:]]+` intact, ported from `f9354cc`
  with attribution.
- The refuse gate aborts exit 3 with `AGENTS.md` byte-identical, naming the orphaned
  provenance line and both remedies.
- The END guard catches the mixed shape the file-global gate cannot see, with a
  distinguishable diagnostic.
- The un-latch rule is absent; the alternation stays at 2 copies.
- D-26's false "bounded by construction" comment is corrected in the migration document.
- Criterion 4's guard reachability is determined by verified mutation and acted on.
- **`migrations/run-tests.sh` is byte-identical to its state at plan start.** The fixtures
  are satisfied by the code, never the reverse.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md` when done.
Record: each 09.1-04 fixture's RED→GREEN transition; which branch caught each fixture
(refuse gate vs END guard); the Task 3 reachability determination with mutation proof and
the KEEP/REMOVE decision; the final alternation copy count. 09.1-07's ADR consumes all of it.
</output>
