---
phase: 09.1-11-strip-runaway-inserted
plan: 05
subsystem: migrations
tags: [awk, bash, migration-0009, cr-01, cr-02, strip-runaway, fail-closed]

# Dependency graph
requires:
  - phase: 09.1-11-strip-runaway-inserted plan 04
    provides: "Fixtures 11/13/14/15 — the four RED fixtures reproducing CR-01 (decoupled strip entry/exit) and CR-02 (unanchored PROV_RE) against the unfixed migration"
provides:
  - "PROV_RE anchored (^...$) at all four sites, ported from claude-workflow f9354cc with attribution — closes CR-02"
  - "The refuse gate: provenance present + exact H2 absent aborts exit 3, AGENTS.md byte-identical by construction, before any file surgery"
  - "The strip awk's unresolved flag + END { exit 4 } fail-closed guard for the mixed-provenance shape the file-global refuse gate cannot see"
  - "A distinguishable strip diagnostic — strip_rc dispatch replaces the misleading 'produced no output' branch for the new failure class"
  - "The h2-count strip-integrity guard, verified reachable by mutation and KEPT as a live backstop layer"
  - "D-26's false 'bounded structurally... cannot cause a runaway' comment corrected in the migration document"
affects: [09.1-06-strip-runaway-inserted, 09.1-07-strip-runaway-inserted]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fail-closed state-machine guard: an awk scan that can latch into an unresolved state (a marker whose exit condition never fires) records that explicitly (unresolved flag) and refuses via a distinct exit code (4) at END, rather than letting 'consume until terminator' silently run to EOF"
    - "Refuse-by-construction: a pre-surgery predicate that is the literal inverse of an existing guard, placed before any file is touched, so the abort path is byte-identical by construction rather than by cleanup"
    - "strip_rc dispatch: capture an awk pass's exit status explicitly ($?) instead of folding all non-zero-or-empty outcomes into one branch, so a new failure class gets its own diagnostic instead of inheriting an unrelated one's misleading message"
    - "Layered-but-honest guards: when a downstream guard's evidence is regenerated by a later pass (the classic CR-01 defect), don't just add another guard on the regenerated evidence — determine by verified mutation whether an upstream guard already subsumes it, and say so explicitly in the comment rather than letting the guard overstate its own reach (the D-26 lesson)"
    - "Reachability determined by verified mutation, not assumed: before shipping a defense-in-depth guard, delete the layer it might be redundant with, confirm the deletion landed (a grep count that must change), observe whether the guard still fires, and only then decide KEEP or REMOVE"

key-files:
  created: []
  modified:
    - migrations/0009-spec-11-region-aware-placement.md

key-decisions:
  - "Q1 implemented literally: REFUSE, not heal-and-duplicate. The refuse-gate elif's condition is the exact inverse of the existing hand-paste-conflict elif immediately above it (grep -qE \"$PROV_RE\" AGENTS.md && ! grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md), so provenance present + exact H2 absent aborts before any surgery runs — the file is byte-identical by construction, not by a cleanup path."
  - "The un-latch rule was NOT added, per the plan's explicit prohibition. The terminator alternation at the strip's swallow rule (in_block && swallowed_own_h2 && (/^## / || /^<!-- gitnexus:start -->$/)) is untouched — confirmed by the alternation copy count staying at exactly 2 throughout all three tasks."
  - "The idempotency check's provenance grep (now anchored, current-version-pinned 0.4.0) was anchored but its version class was NOT widened — it stays pinned by design, because it answers 'is the CURRENT version already applied', a different question from the strip's entry condition ('is there a provenance line of ANY version to heal'). The other three anchored sites keep @[^[:space:]]+ intact."
  - "Task 3's h2-count guard was KEPT, not removed, based on a verified mutation result: deleting ONLY the strip awk's END clause left fixture 15 still refusing (rc=3, byte-identical), caught by the h2-count guard's own distinct diagnostic rather than the END guard's. This is the opposite of RESEARCH's tentative expectation (the guard's own note flagged it might be 'dead by construction' once the refuse gate + END guard existed) — the empirical mutation is what actually settled it, per the plan's instruction to run this as an experiment, not an argument."
  - "The post-insert grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md.0009.tmp guard's comment was re-scoped to say it guards MIRROR integrity (a zero-byte or corrupt mirror), not strip integrity — it cannot guard strip integrity because it looks for the very heading the insert pass itself just wrote. Strip integrity is now guarded by strip_rc (refuse gate + END guard) AND the h2-count backstop."

requirements-completed: [ANCHOR-05, MIGR-01, MIGR-04]

duration: ~70min
completed: 2026-07-15
---

# Phase 09.1 Plan 05: Close CR-01 and CR-02 — turn the four RED fixtures GREEN Summary

**Anchored `PROV_RE` at all four sites (ported from upstream `f9354cc`, closing CR-02), added a pre-surgery refuse gate that is the literal inverse of the existing hand-paste guard plus a strip-awk `unresolved`/`END{exit 4}` fail-closed guard for the one mixed-provenance shape the refuse gate cannot see (closing CR-01), gave the new failure class its own diagnostic instead of inheriting the misleading "produced no output" message, corrected the false D-26 "bounded structurally" comment, and determined by a verified mutation (not assumption) that the h2-count strip-integrity guard is a live backstop — turning all four of 09.1-04's RED fixtures GREEN while the full 338-assertion suite stays 0 FAIL throughout.**

## Performance

- **Duration:** ~70 min
- **Completed:** 2026-07-15T21:10:00Z (approx, Task 3 commit)
- **Tasks:** 3 completed
- **Files modified:** 1 (`migrations/0009-spec-11-region-aware-placement.md`)

## Accomplishments

- **Task 1 — CR-02 closed.** Anchored `PROV_RE` (`^...$`) at all four sites the migration carries it: the idempotency check's provenance grep (`:164`, exact-version, anchored but deliberately NOT widened), its in-region awk trigger (`:168`), the `PROV_RE` definition (`:207`), and the strip pass's entry regex (`:260`) — the latter three keep `@[^[:space:]]+`'s any-version class intact. Ported from `claude-workflow @ f9354cc` (`0029:142,146,175,223`, PR #89) with an attribution note recorded in `## Notes`, per the pin re-verified live (`origin/main` still `f9354cc`). Fixture `11-prose-mention-provenance` went fully GREEN (4/4 assertions); `03-healthy-noop` and `09-two-provenance-heal` stayed unregressed.
- **Task 2 — CR-01 closed.** Added the refuse-gate `elif` (Q1's mechanism): `grep -qE "$PROV_RE" AGENTS.md && ! grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md` aborts `exit 3` before any file surgery, naming the offending line(s) via `grep -nE "$PROV_RE" AGENTS.md` and both remedies. Added the strip awk's `unresolved` flag (set when a second provenance line arrives while a prior one is still unresolved) and `END { if (unresolved || (in_block && !swallowed_own_h2)) exit 4 }` for the ONE shape the file-global refuse gate cannot see — a healthy provenance+heading pair earlier in the file satisfies the gate's whole-file predicate while a drifted pair later in the file still runs away. Restructured the dispatch to capture `strip_rc=$?` explicitly and branch on it: `rc==4` gets its own distinguishable ABORT message (NOT the misleading "produced no output" text), `rc!=0`-or-empty keeps the existing branch, otherwise the insert pass runs unchanged. Corrected the false D-26 comment: the strip's PRINCIPLE (blind to prose) survives; its CLAIM ("bounded structurally... cannot cause a runaway") does not — it is bounded only when the exact heading matches. Fixtures `13-runaway-drifted-h2`, `14-runaway-orphan-provenance`, `15-mixed-provenance-unresolved` went fully GREEN. Full suite: 338 PASS / 0 FAIL / 2 SKIP.
- **Task 3 — criterion 4 settled by verified mutation, not assumption.** Added the h2-count strip-integrity guard (`h2_out == h2_in - h2_own`, else abort `exit 3`) between the strip and insert passes. Mutated by deleting ONLY the strip awk's `END { ... exit 4 }` clause; confirmed the mutation landed (`grep -c 'exit 4'` dropped `1 -> 0`); re-ran fixture 15 and observed it STILL refused (`rc=3`, byte-identical, `## Deployment` survives) — via a diagnostic that names NO provenance line at all ("the strip removed structural headings it does not own"), proving the h2-count guard is what caught it, not a residue of the removed clause. Decision: **KEEP**. Restored the `END` clause (`exit 4` count back to 1) and re-ran: full suite 338 PASS / 0 FAIL / 2 SKIP.

## Task Commits

1. **Task 1: Anchor PROV_RE at all three (actually four) sites, ported from upstream f9354cc** - `9927acd` (fix)
2. **Task 2: The refuse gate + the END fail-closed guard + a distinguishable strip diagnostic** - `21aeeaa` (fix)
3. **Task 3: Determine h2-count guard reachability by verified mutation; KEEP** - `c36deab` (feat)

## Files Created/Modified

- `migrations/0009-spec-11-region-aware-placement.md` — `PROV_RE` anchored at four sites with upstream attribution (Task 1); refuse-gate `elif`, strip awk's `unresolved`/`END{exit 4}` guard, `strip_rc` dispatch, D-26 comment correction (Task 2); h2-count strip-integrity guard added and kept after a verified-mutation reachability test, post-insert guard comment re-scoped to "mirror integrity, not strip integrity" (Task 3).

## RED -> GREEN Transition (09.1-04's four fixtures, all assertions)

| Fixture | Before (this plan) | After Task 1 | After Task 2 | Caught by |
|---|---|---|---|---|
| `11-prose-mention-provenance` | 1 PASS / 1 FAIL | 4/4 PASS | 4/4 PASS | N/A (anchoring itself is the fix — no refuse fires, it's a legitimate heal) |
| `13-runaway-drifted-h2` | 1 PASS / 4 FAIL | 1 PASS / 4 FAIL (unchanged — CR-01 is Task 2's job) | 5/5 PASS | **the refuse gate** (`elif` — single block, drifted H2 with real headings after it) |
| `14-runaway-orphan-provenance` | 1 PASS / 3 FAIL | unchanged | 4/4 PASS | **the refuse gate** (no heading at all, orphaned to EOF) |
| `15-mixed-provenance-unresolved` | 1 PASS / 4 FAIL | unchanged | 5/5 PASS | **the END guard** — confirmed empirically, not assumed: the refuse gate is file-global and a HEALTHY block 1 satisfies "exact H2 present" for the whole file, so the gate never fires; the strip's sequential scan is what latches on block 2's drift |

`bash migrations/run-tests.sh 0009` after Task 1: **46 PASS / 11 FAIL** (down from 09.1-04's baseline 45/12). After Task 2: **57 PASS / 0 FAIL**. Full suite after Task 2 and again after Task 3: **338 PASS / 0 FAIL / 2 SKIP**.

## Division of labour — proven, not assumed (Task 2 acceptance criterion)

Manually reproduced fixtures 13/14/15 in isolated sandboxes (extracted Step 1's Apply block from the migration document itself, ran it against each fixture's exact before-state) and captured the raw diagnostic text:

- **13 and 14** — diagnostic begins `"ABORT: AGENTS.md contains a spec-11 provenance line whose '## Coding Discipline (NON-NEGOTIABLE)' heading is drifted or absent."` — this is the refuse-gate `elif`'s message. `exit 3` fires before the strip awk ever runs.
- **15** — diagnostic begins `"ABORT: migration 0009 Step 1 — a spec-11 provenance line was never followed by its '## Coding Discipline (NON-NEGOTIABLE)' heading before the strip pass reached EOF or a second provenance line."` — this is the `strip_rc == 4` branch's message, reached only because the refuse gate's file-global predicate is blind to this shape (block 1's healthy heading satisfies "exact H2 present" for the whole file).

This confirms the design intent stated in `run-tests.sh`'s own fixture-15 comment ("Only the END guard can catch this shape") empirically, not by re-reading the comment.

## Task 3 — reachability determination (full detail, per output spec)

**Mutation command:** deleted the strip awk's trailing `END { if (unresolved || (in_block && !swallowed_own_h2)) exit 4 }` clause only, leaving `unresolved` computed-but-unused and every other line byte-identical.

**Proof the mutation landed:** `grep -c 'exit 4' migrations/0009-spec-11-region-aware-placement.md` — before: `1`, after deletion: `0`.

**Fixture 15's observed result under the mutation** (manually reproduced, isolated sandbox): `rc=3`. Diagnostic:
```
ABORT: migration 0009 Step 1 — the strip removed structural headings
       it does not own (expected 3 '## ' headings
       to survive, found 1). AGENTS.md left untouched.
```
`cmp -s` byte-identical held; `## Deployment` (the harm the END guard targets) survived because the file was never touched — the refusal happens before `mv`. `bash migrations/run-tests.sh 0009` under the mutation: all of 15's assertions still PASS except "diagnostic names the offending provenance line" (correctly FAILS — the h2-count guard's message does not name a provenance line at all, only `13`/`14`'s refuse-gate message and the (now-deleted) END guard's message do that).

**Decision: KEEP.** Fixture 15 still refuses under the mutation, caught by a demonstrably different diagnostic than the END guard's own — this is not a coincidence or a leftover of the deleted clause, it is the h2-count guard actively firing (`h2_out=1` vs expected `3`, computed independently from `AGENTS.md` and `AGENTS.md.0009.strip`, both of which exist regardless of the `END` clause). This is a live backstop layer for fixture 15's shape specifically, not dead by construction — the precedent is D-28.1/WR-04's kept-but-subsumed `test -s` diagnostic layer, now cited in the guard's own comment.

**Restoration:** `cp`'d the pre-mutation file back over the working copy, confirmed `diff` empty, then re-verified `grep -c 'exit 4'` back to `1` and the full suite 0 FAIL before committing.

**Reflected in the file:** `grep -c 'h2_out' migrations/0009-spec-11-region-aware-placement.md` is `3` (present, per the KEEP decision).

## Verification (plan's `<verification>` section, all confirmed)

- `bash migrations/run-tests.sh 0009` -> **57 PASS / 0 FAIL** (all four target fixtures GREEN).
- `bash migrations/run-tests.sh` (full) -> **338 PASS / 0 FAIL / 2 SKIP**.
- Fixtures 01-10 and `no-scaffolder-tree` unregressed — spot-checked `01-gitnexus-led-inject`, `02-inside-region-move`, `03-healthy-noop`, `04-no-agentsmd`, `06-no-heading-eof`, `08-rollback-region-led`, `09-two-provenance-heal` PASS explicitly; the full-suite 0 FAIL covers the rest.
- `git diff --stat` for `migrations/run-tests.sh` since plan start is **empty** — the fixture suite was never touched, satisfying the plan's "the fixtures are satisfied by the code, never the reverse" instruction.
- The alternation copy count (`/^## / || /^<!-- gitnexus:start -->\$/`) is exactly **2** — confirmed after every task, including the final state.
- `git diff --stat 517bef4238192daf5f4c1cdeac5d12c28aa2f399 HEAD` touches `migrations/0009-spec-11-region-aware-placement.md` only.

## Decisions Made

Documented above under `key-decisions` frontmatter: the literal-inverse refuse-gate implementation of Q1, the un-latch rule's deliberate absence, the idempotency-check's exact-version pin staying narrow while the other three sites keep any-version, the h2-count guard's KEEP decision by verified mutation, and the post-insert guard's re-scoped "mirror integrity, not strip integrity" comment.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug/clarification] Task 1's title said "three sites" but the file has four unanchored occurrences used as regex match targets**
- **Found during:** Task 1, initial grep survey
- **Issue:** The plan's task name and acceptance criteria describe "three sites" (`0009:161`, `0009:165`, and `0009:204`/`0009:257` grouped as one pair), but a direct `grep -n` found the marker used as a regex match target at four distinct line numbers (`164`, `168`, `207`, `260` in this plan's line numbering) — the pair at `204`/`257` in the plan's own numbering are genuinely two separate sites (`PROV_RE`'s definition and the strip's entry regex), each independently anchored.
- **Fix:** Anchored all four; the acceptance criteria's own count assertion (`>= 2` for the anchored-pattern count) accommodated this without needing a criterion change. No scope change — this is the same fix the plan's `<read_first>` section already names as four upstream sites (`f9354cc:0029:142,146,175,223`).
- **Files modified:** `migrations/0009-spec-11-region-aware-placement.md`
- **Verification:** `grep -nE "/<!-- spec-source|'<!-- spec-source" migrations/0009-spec-11-region-aware-placement.md` returns only the one literal-string `PROV=` assignment, confirming no unanchored regex match target remains.
- **Committed in:** `9927acd` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — a plan-prose miscount that did not change scope or acceptance criteria, only clarified which four line numbers the plan's own "three sites" language and `<read_first>` upstream mapping already pointed to).
**Impact on plan:** None on scope; all acceptance criteria for all three tasks were verified exactly as specified, including the `git diff` emptiness check on `run-tests.sh` and the alternation-count-stays-2 check.

## Issues Encountered

- `vendor/agenticapps-shared` submodule was not initialized in this worktree (same environment gap every prior 09.1 plan recorded). Ran `git submodule update --init --recursive` before the first `run-tests.sh` invocation could proceed. Read-only, non-destructive, produced no working-tree changes to commit — environment setup, not a plan deviation.
- The upstream pin re-verification (`git -C <claude-workflow path> fetch && git ... log --oneline -1 origin/main`) required an absolute path rather than the plan's literal `../claude-workflow`, because this plan executes inside a nested git worktree (`.claude/worktrees/agent-.../`) rather than the repo root the plan's relative path assumes. Resolved by using the absolute path to the sibling `claude-workflow` checkout; the pin (`f9354cc`) matched, so the port proceeded exactly as planned. Not a deviation from the plan's intent, only from its literal relative-path syntax under worktree isolation.

## Next Phase Readiness

- CR-01 and CR-02 are both closed in `migrations/0009-spec-11-region-aware-placement.md`; all four of 09.1-04's RED fixtures (11/13/14/15) are GREEN with their branch division proven empirically.
- The alternation copy count (2), the un-latch rule's absence, and the D-26 comment correction are all in place for 09.1-07's ADR to consume.
- Task 3's full reachability-determination trail (mutation command, before/after `exit 4` counts, fixture 15's diagnostic under the mutation, KEEP decision and reasoning) is recorded above verbatim for the same reason.
- `migrations/run-tests.sh` is byte-identical to its state at plan start — confirmed via `git diff --stat` returning empty for that file across all three task commits.
- No blockers for 09.1-06 or 09.1-07.

---
*Phase: 09.1-11-strip-runaway-inserted*
*Completed: 2026-07-15*

## Self-Check: PASSED

- FOUND: `migrations/0009-spec-11-region-aware-placement.md`
- FOUND: `.planning/phases/09.1-11-strip-runaway-inserted/09.1-05-SUMMARY.md`
- FOUND: commit `9927acd` (Task 1 — PROV_RE anchoring)
- FOUND: commit `21aeeaa` (Task 2 — refuse gate + END guard + diagnostic)
- FOUND: commit `c36deab` (Task 3 — h2-count guard, KEEP by verified mutation)
- FOUND: commit `592be3a` (plan metadata / SUMMARY)
- Re-ran `bash migrations/run-tests.sh` at self-check time: 338 PASS / 0 FAIL / 2 SKIP — matches all figures recorded above.
