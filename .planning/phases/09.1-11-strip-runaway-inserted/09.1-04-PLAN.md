---
phase: 09.1-11-strip-runaway-inserted
plan: 04
type: execute
wave: 3
depends_on: [09.1-02, 09.1-03]
files_modified:
  - migrations/run-tests.sh
autonomous: true
requirements: [ANCHOR-05, TEST-02, TEST-03, MIGR-07]

must_haves:
  truths:
    - "A fixture reproduces the drifted-H2 runaway and is OBSERVED FAILING against the current 0009"
    - "A fixture reproduces the orphaned-provenance runaway and is OBSERVED FAILING"
    - "A fixture reproduces the mixed-provenance runaway that a file-global refuse gate cannot see, and is OBSERVED FAILING"
    - "A fixture proves a prose mention of the provenance line cannot trigger the strip, and is OBSERVED FAILING"
    - "state-a is genuinely off-anchor, so the D-31/MIGR-07 assertion can fail for the reason it claims"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "fixtures 11, 13, 14, 15; off-anchor state-a; the mirror single-## guard"
      contains: "13-runaway-drifted-h2"
  key_links:
    - from: "each runaway fixture"
      to: "_m0009_apply"
      via: "extracted Step 1 Apply run in a mandatory subshell"
      pattern: "_m0009_apply"
---

<objective>
Capture every reproduced data-loss path as a **failing** fixture against the current,
unfixed 0009 — before any awk is touched. Fix the two fixtures that currently assert
nothing.

Purpose: **a fixture that has not been observed RED is not evidence. It is an assertion.**
Phase 9 shipped three assertions that read as coverage but could never fail (CR-03, V-02,
and the post-strip `grep -q` satisfied by the insert pass that follows it), and its suite
was 314 PASS / 0 FAIL while the migration never ran on any real project. This plan is the
structural defense against repeating that.

Output: a RED suite with four new failing fixtures. That RED is this plan's deliverable
(TEST-02). 09.1-05 turns it green. Do not "fix" it here.

**Fixture numbering (Q4 ruling).** Existing fixtures are 01-10; `07-prose-mention-not-a-region`
is the *marker* twin, so `11-` is free. Upstream's ported fixture takes it:
`11-prose-mention-provenance`. `12-idempotent-rerun` is reserved for 09.1-06. This plan
adds 11, 13, 14, 15. The ROADMAP's criterion-6 label "`11-idempotent-rerun`" is therefore
**aliased, not satisfied literally** — record that in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md
@.planning/phases/09.1-11-strip-runaway-inserted/09.1-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md
@.planning/phases/09-region-aware-11-placement/09-REVIEW.md
@.planning/phases/09-region-aware-11-placement/09-VERIFICATION.md

**CLAUDE.md's GitNexus mandate is INERT** — shell fixtures. GitNexus indexes no shell
functions. Grep is ground truth. Do not claim GitNexus analysis.

<interfaces>
<!-- The behavior 09.1-05 will implement. Write the fixtures against THIS contract. -->

**The Q1 ruling (user, 2026-07-15) — REFUSE, not heal.**
Provenance present + exact `## Coding Discipline (NON-NEGOTIABLE)` heading absent
⇒ **abort exit 3**, leaving `AGENTS.md` byte-identical. ONE rule covers BOTH reproduced
runaway shapes (drifted-H2 and orphaned-provenance) because both are exactly "provenance
present, exact H2 absent". The abort message must name the orphaned provenance line and
tell the operator to restore the heading or remove the line.
Rejected: heal-and-duplicate (silently leaves two similar §11 headings).

**The refuse gate is file-global and therefore blind to one shape.** In a file with TWO
provenance lines where the first block is healthy and the second has drifted, the exact H2
IS present (block 1 has it), so the gate does not fire. That shape falls through to the
strip and is caught by the END guard:
`END { if (unresolved || (in_block && !swallowed_own_h2)) exit 4 }`.
Fixture 15 is the ONLY thing that makes that END guard falsifiable. Without it, the END
guard is defense-in-depth theater — a guard never observed catching anything.

**All three refusal shapes present the same observable contract:**
exit **3**, `AGENTS.md` byte-identical, and a diagnostic that names the offending
provenance line and does NOT read "the strip pass produced no output".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three runaway fixtures — 13-drifted-h2, 14-orphan-provenance, 15-mixed-provenance-unresolved — observed RED</name>

  <read_first>
    - `.planning/phases/09-region-aware-11-placement/09-CR-01-REPRO.md` — **the shape
      fixture 13 must reproduce.** 16 lines in, 4 lines out; provenance present, H2 drifted
      to `## Coding Discipline (RENAMED — drifted)`, followed by `## Critical Project Rules`
      and `## Deployment`. Reproduce THIS file, not an approximation.
    - `migrations/0009-spec-11-region-aware-placement.md:200-334` — Step 1's Apply: the
      conflict branch (`:213-228`), the strip awk (`:255-273`), the insert (`:286-306`), and
      the three post-strip guards. Understand why all three pass on the runaway.
    - `migrations/run-tests.sh:3595-3810` — the existing fixture idiom. Copy the shape of
      `05-unmanaged-conflict` (`:3730-3750`) for the exit-3 + byte-identical assertion pair;
      it is the closest analog and already does exactly what you need.
    - `migrations/run-tests.sh:3770-3805` — `09-two-provenance-heal`. Fixture 15 is its
      dangerous sibling; read it so 15's shape differs only in the SECOND block's heading.
    - `migrations/run-tests.sh:3374-3388` — `_m0009_apply`. **The subshell is mandatory**
      (T-09-07): these blocks `exit 3`, and un-subshelled that kills the harness and hides
      every later assertion behind a truncated PASS count.
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    Add three fixtures to `test_migration_0009`, after `10-corrupt-mirror-refused` and
    before the `no-scaffolder-tree` fixture 09.1-01 added. Gate all three on
    `[ "$apply_ok" = "1" ]` with `_m0009_fail` for each assertion in the `else`.

    Each fixture: build its BEFORE state with `printf` into `$tmp` (D-34), snapshot it
    (`cp "$p/AGENTS.md" "$p/AGENTS.md.before"`), run `$apply_block` via `_m0009_apply`,
    capture `out` and `rc`, then assert.

    **13-runaway-drifted-h2** (criteria 1, 2 / ANCHOR-05 / CR-01). BEFORE = the
    09-CR-01-REPRO.md file verbatim.

    **14-runaway-orphan-provenance** (criterion 2 / ANCHOR-05). BEFORE = a short file with
    the provenance line, NO §11 heading at all, and NO following `## ` — the tail runs to
    EOF. `0009:282` documents that the migration itself produces this state ("the rest of
    the file, plus an orphaned provenance line"), and 09-REVIEW.md lists it as reachable.
    RESEARCH reproduced 8 lines → 4.

    **15-mixed-provenance-unresolved** (criterion 2's completeness — **the END guard's only
    falsifiability proof**). BEFORE = provenance #1 + the EXACT `## Coding Discipline
    (NON-NEGOTIABLE)` heading + `cat "$mirror"` + `## Workflow` + provenance #2 +
    `## Coding Discipline (RENAMED — drifted)` + some body + `## Deployment` + EOF.
    Write a comment stating why this fixture exists: the file-global refuse gate CANNOT see
    this shape (the exact H2 is present, from block 1), so it falls through to the strip and
    only the END guard catches it. A guard never observed catching anything is not a guard.

    Assert FOUR things per fixture, all four falsifiable:
    1. `[ "$rc" -eq 3 ]` — the migration refuses.
    2. `cmp -s "$p/AGENTS.md" "$p/AGENTS.md.before"` — `AGENTS.md` is **byte-identical**.
       This is the assertion that actually catches CR-01: on the current 0009 the file is
       truncated, so `cmp` fails.
    3. The diagnostic names the offending provenance line — `case "$out" in *"$PROV_LIT"*)`.
    4. The diagnostic is NOT the misleading `else` branch — assert `$out` does **not**
       contain `produced no output`. Without this, an awk that exits non-zero routes to
       `0009:329`'s "the strip pass produced no output" message, which is false for this
       failure and would tell the operator to look for a disk-full error.

    Also assert the surviving-content property explicitly for 13 (`## Critical Project
    Rules` and `## Deployment` still present) — it is redundant with `cmp` but it is the
    assertion that names the actual harm, so a failure reads as data loss rather than as
    a checksum mismatch.

    **Then run the suite and record the RED.** Commit `test(09.1-04): …` with the exact
    failing labels, the observed `rc`, and the observed line counts (before → after) for
    each fixture in the commit body. Those line counts ARE the reproduction.
  </action>

  <acceptance_criteria>
    - `grep -c '13-runaway-drifted-h2' migrations/run-tests.sh` is `>= 5`;
      same for `14-runaway-orphan-provenance` and `15-mixed-provenance-unresolved`.
    - `bash migrations/run-tests.sh 0009` FAILs on **all three** fixtures' `rc -eq 3`
      assertion. Record the observed `rc` for each — it MUST be **0** (the migration
      silently "succeeded" while destroying data). **This is the recorded RED for
      criteria 1 and 2.**
    - `bash migrations/run-tests.sh 0009` FAILs on all three fixtures' `cmp -s`
      byte-identical assertion.
    - For fixture 13, the recorded before → after line counts reproduce 09-CR-01-REPRO.md:
      **16 → 4**. If you get different numbers, your fixture is not the repro — fix the
      fixture, not the expectation.
    - For fixture 14, the recorded before → after reproduces RESEARCH's **8 → 4**.
    - For fixture 15, `## Deployment` is recorded as ABSENT from the after-state — that
      absence is the harm the END guard exists to prevent.
    - `bash migrations/run-tests.sh 0009` runs to completion — no harness abort. If the
      suite exits early with a truncated PASS count, `_m0009_apply`'s subshell was dropped.
    - `git show --stat HEAD` lists `migrations/run-tests.sh` and **nothing else**. If
      `migrations/0009-*.md` appears, the RED is forfeited — revert and redo.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-04-t1.log; grep -E 'runaway|unresolved' /tmp/09.1-04-t1.log</automated>
  </verify>

  <done>Fixtures 13, 14, 15 exist with four falsifiable assertions each, all three are OBSERVED FAILING against the unmodified 0009 with reproduced line counts, and the RED is committed touching only `run-tests.sh`.</done>
</task>

<task type="auto">
  <name>Task 2: Port upstream f9354cc's 11-prose-mention-provenance fixture — observed RED</name>

  <read_first>
    - `git -C ../claude-workflow show f9354cc:migrations/test-fixtures/0029/11-prose-mention-provenance/setup.sh`
    - `git -C ../claude-workflow show f9354cc:migrations/test-fixtures/0029/11-prose-mention-provenance/verify.sh`
      — note it states the impact it measured: the unanchored bug "turns this fixture's
      91-line input into 85 lines". That is an independent second reproduction of CR-02.
    - `git -C ../claude-workflow show f9354cc:migrations/test-fixtures/0029/11-prose-mention-provenance/expected-exit`
    - `migrations/run-tests.sh:3810-3860` — this repo's own `07-prose-mention-not-a-region`,
      the **marker** twin. Fixture 11 is its **provenance** twin; mirror its structure and
      its comment style.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Upstream Delta"
      and § "Pattern 2: Port, don't re-derive".
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    **Verify the pin before reading it.** Run
    `git -C ../claude-workflow fetch && git -C ../claude-workflow log --oneline -1 origin/main`.
    It must still be `f9354cc`. (Confirmed at planning time, but upstream revised 0029 four
    times in one afternoon — if HEAD has moved, report it and stop rather than porting from
    a moving target.) Read the analog with `git -C ../claude-workflow show f9354cc:<path>`,
    never from its working tree.

    Port `11-prose-mention-provenance` into `test_migration_0009` as an inline fixture.
    Translate: `CLAUDE.md` → `AGENTS.md`; upstream's per-directory `setup.sh`/`verify.sh`
    harness → this repo's inline `_m0009_mk_project` + `_m0009_mk_fake_home` +
    `_m0009_apply` + `_m0009_ok` idiom. **Translate the shape and the assertions, not the
    layout.**

    Do NOT re-derive the fixture from first principles. It is validated across six repos and
    re-deriving guarantees prose divergence from the repo 0009 is a port of. Attribute it in
    a comment: ported from `claude-workflow f9354cc` (PR #89), which fixed CR-02 upstream
    before we did.

    The BEFORE state: a healthy on-anchor §11 block, plus — further down the file — a line
    that merely *mentions* the provenance comment (backticked / indented prose, not a
    whole-line match), followed by real project content the unanchored strip would eat.
    Plausibility is not hypothetical: 0009's own abort message (`:225`) instructs operators
    to paste that exact line into `AGENTS.md`.

    Assert:
    - the prose-mention line survives verbatim in the after-state;
    - the content between the prose mention and the next heading survives;
    - exactly ONE real (whole-line) provenance line remains after Apply;
    - `rc` is 0 (this is a legitimate heal/no-op, NOT a refusal — the anchored regex must
      not turn a prose mention into a refuse-gate trigger either).

    **Then run and record the RED.** Commit `test(09.1-04): …` with the before → after line
    counts.
  </action>

  <acceptance_criteria>
    - `git -C ../claude-workflow log --oneline -1 origin/main` recorded in the SUMMARY, and
      it is `f9354cc`. If not, the task reports and stops.
    - `grep -c '11-prose-mention-provenance' migrations/run-tests.sh` is `>= 4`.
    - `grep -c 'f9354cc' migrations/run-tests.sh` is `>= 1` — the port is attributed.
    - `bash migrations/run-tests.sh 0009` FAILs on the content-survival assertion. Record
      the before → after line counts; the after-state MUST be shorter than the before-state.
      **This is the recorded RED for criterion 3.**
    - The fixture number is `11-`, NOT `12-` — and `12-` does not appear in
      `run-tests.sh` yet (it is 09.1-06's).
    - `git show --stat HEAD` lists `migrations/run-tests.sh` only.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0009 2>&1 | tee /tmp/09.1-04-t2.log; grep -c '11-prose-mention-provenance' /tmp/09.1-04-t2.log</automated>
  </verify>

  <done>`11-prose-mention-provenance` is ported from `f9354cc` with attribution, and is OBSERVED FAILING against the unanchored `PROV_RE` with recorded line counts.</done>
</task>

<task type="auto">
  <name>Task 3: Make state-a genuinely off-anchor (V-02), and pin the mirror's single-## invariant (WR-01)</name>

  <read_first>
    - `migrations/run-tests.sh:3506-3516` — today's `state-a`. Note it places the block
      BEFORE the first `## ` heading, i.e. exactly **ON** the anchor.
    - `migrations/run-tests.sh:3548-3554` — the assertion at `:3553` labelled
      "D-31/MIGR-07", which claims to prove a healthy-but-off-anchor block is left alone.
      It cannot fail for that reason, because its fixture is on-anchor. **V-02 — the same
      dead-assertion class as CR-03.**
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Code Example 4"
      — the off-anchor shape.
    - `migrations/0009-spec-11-region-aware-placement.md:191-196` — D-31: MIGR-07 falls out
      of the predicate rather than a special case. The rewritten fixture must still return
      `applied`.
    - `migrations/run-tests.sh:488-521` — `test_migration_0004`, where the mirror guard goes.
    - `.planning/phases/09.1-11-strip-runaway-inserted/09.1-RESEARCH.md` § "Pitfall 1" and
      § "Open Questions" Q2 — why the strip's correctness depends on the mirror having
      exactly one `## ` line.
  </read_first>

  <files>migrations/run-tests.sh</files>

  <action>
    **(a) state-a (criterion 7 / MIGR-07 / V-02).** Rewrite `state-a`'s fixture
    (`run-tests.sh:3509-3516`) to be genuinely off-anchor per RESEARCH § "Code Example 4":
    put the block BELOW a real project `## Project Overview` heading, with a following
    `## Deployment`, and **no region anywhere in the file** — so the block's position is the
    ONLY variable. Today's fixture varies two things at once (position AND a trailing
    region), which is why its assertion reads as coverage it does not provide.

    Keep the expected argument `applied`. The behavior is already correct — the Phase 9
    verifier ran this exact shape live against `0009:160-167` and got exit 0 — so this
    fixture should pass on the first try. **Its value is that it can now FAIL** if someone
    "optimizes" the predicate to also check anchor position. Update the assertion's comment
    to say exactly that, and to state what a failure would mean.

    A green result here is expected and is NOT a RED-discipline violation: this plan's RED
    deliverable is Tasks 1 and 2. Record in the SUMMARY that state-a is green-on-arrival
    and why, so the next reader does not mistake it for an unobserved assertion.

    **(b) The mirror's single-`## ` invariant (WR-01 / Q2).** The strip swallows the block's
    own `## ` heading explicitly and then terminates at the NEXT `## `. That is only correct
    because the canonical mirror has exactly one `## ` line, on line 1. 09.1-05's refuse
    gate rests on the same invariant. It is currently unasserted — and it lives in a file
    this repo vendors rather than authors.

    Add a guard in `test_migration_0004`, next to the existing line-count assertion:
    assert the mirror has exactly ONE `## ` line and it is on line 1. Use
    `grep -c '^## ' "$mirror" | tr -d ' '` (BSD `grep -c` can emit leading whitespace —
    follow the `tr -d ' '` idiom the file already uses) and
    `grep -n '^## ' "$mirror" | head -1 | cut -d: -f1`. Comment it as: the strip's
    single-heading swallow depends on this; if the mirror ever gains a second `## `, the
    strip terminates early and leaves body behind.
  </action>

  <acceptance_criteria>
    - `sed -n '/State A —/,/^  # State B/p' migrations/run-tests.sh | grep -c 'gitnexus'` is
      `0` — the rewritten state-a has no region anywhere, so position is the only variable.
    - The rewritten state-a places `## Project Overview` ABOVE the provenance line:
      verify by reading the fixture back — the `printf '## Project Overview...'` call must
      precede the `printf '%s\n' "$PROV_LIT"` call.
    - `bash migrations/run-tests.sh 0009` — the `state A: ... (D-31/MIGR-07)` assertion
      PASSes.
    - **The assertion is proven falsifiable.** Temporarily change the fixture's expected
      argument from `applied` to `not-applied`, confirm the assertion FAILs, restore, and
      confirm it passes. Assert the mutation landed before trusting it (`grep` the fixture
      for `not-applied` during the mutated run). Record both observations. Without this the
      rewritten fixture is exactly the assertion V-02 condemned.
    - `sed -n '/^test_migration_0004()/,/^}/p' migrations/run-tests.sh | grep -c "grep -c '\^## '"`
      is `>= 1`.
    - `bash migrations/run-tests.sh 0004` reports 0 FAIL, with the new mirror guard PASSing.
    - The mirror guard is proven falsifiable: it asserts `== 1`, not `>= 1`. A count of 1
      today and 2 tomorrow must fail.
  </acceptance_criteria>

  <verify>
    <automated>bash migrations/run-tests.sh 0004 2>&1 | tee /tmp/09.1-04-t3a.log; bash migrations/run-tests.sh 0009 2>&1 | grep 'D-31/MIGR-07'</automated>
  </verify>

  <done>state-a is off-anchor with no region, its assertion passes and has been observed failing under a verified mutation, and the mirror's single-`## ` invariant is asserted in `test_migration_0004`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's `AGENTS.md` → 0009's strip awk | untrusted, hand-editable content crosses into an unbounded state machine that rewrites the file in place. **This is the phase's whole subject.** |
| prose in `AGENTS.md` → the strip's entry condition | a substring match lets ordinary prose impersonate a structural marker |
| upstream `claude-workflow` → this repo | ported code crosses a repo boundary; a moving HEAD makes the port unreproducible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-12 | Tampering (data destruction) | 0009 strip awk `in_block { next }` (`:271`) | mitigate | CR-01: a drifted H2 latches `in_block` and eats to EOF, destroying user content (16→4 lines) while all three guards report success. Fixtures 13/14/15 pin it as a failing assertion here; 09.1-05 fixes it. |
| T-09.1-13 | Tampering (marker mimicry) | unanchored `PROV_RE` (`0009:204,257`) | mitigate | CR-02: a backticked prose mention of the provenance line is treated as the block's entry, deleting everything between it and the real block. Fixture 11 (ported from `f9354cc`) pins it; 09.1-05 anchors the regex. |
| T-09.1-14 | Repudiation | the `state-a` fixture (`:3509-3516`) | mitigate | V-02: an on-anchor fixture behind an off-anchor assertion label. Task 3 makes the fixture match its claim and proves the assertion falsifiable by a verified mutation. |
| T-09.1-15 | Repudiation | the END guard, if fixture 15 is omitted | mitigate | A guard never observed catching anything is defense-in-depth theater. Fixture 15 constructs the ONLY shape the file-global refuse gate cannot see, making the END guard falsifiable. |
| T-09.1-16 | Tampering | vendored mirror gaining a second `## ` | mitigate | The strip's single-heading swallow and 09.1-05's refuse gate both rest on the mirror having exactly one `## ` line. Task 3(b) asserts it in `test_migration_0004`. |
| T-09.1-17 | Spoofing | porting from a moving upstream HEAD | mitigate | Task 2 re-verifies `origin/main == f9354cc` before reading, and reads via `git show f9354cc:<path>` — never the working tree. Upstream revised 0029 four times in one afternoon. |
| T-09.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase (RESEARCH § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `bash migrations/run-tests.sh 0009` runs to completion (no harness abort — the mandatory
  subshell survived).
- The `0009` filter reports **>= 8 new FAILs** across fixtures 11/13/14/15.
- No assertion reads "NOT ASSERTED: extraction failed" — the extraction gates are up.
- Fixtures 01-10 and `no-scaffolder-tree` still PASS: the terminator alternation and every
  criterion-0 assertion must **stay green — no weakening** (ANCHOR-05).
- `bash migrations/run-tests.sh 0004` and `drift` → 0 FAIL.
- `git diff --stat` touches `migrations/run-tests.sh` only.
</verification>

<success_criteria>
- Fixtures 11, 13, 14, 15 exist, each with falsifiable assertions on exit code,
  byte-identity, and diagnostic content.
- **All four are OBSERVED FAILING against the unmodified 0009**, with reproduced line
  counts recorded (13: 16→4; 14: 8→4; 15: `## Deployment` destroyed; 11: shorter output).
- `state-a` is genuinely off-anchor and its assertion has been observed failing under a
  verified mutation.
- The mirror's single-`## ` invariant is asserted.
- `migrations/0009-spec-11-region-aware-placement.md` is byte-identical to its state at
  plan start.
</success_criteria>

<output>
Create `.planning/phases/09.1-11-strip-runaway-inserted/09.1-04-SUMMARY.md` when done.
Record verbatim: every FAIL label, each fixture's before → after line counts, the upstream
HEAD check, the two mutation demos (state-a, mirror guard) with proof they landed, and the
Q4 numbering note (criterion 6's `11-idempotent-rerun` label is aliased to `12-`).
</output>
</content>
