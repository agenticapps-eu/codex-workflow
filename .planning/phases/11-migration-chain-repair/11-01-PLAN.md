---
phase: 11-migration-chain-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0010-heal-0007-knowledge-capture.md
  - migrations/run-tests.sh
autonomous: true
requirements: [MIGR-10]
must_haves:
  truths:
    - "A 0.4.0 sandbox carrying none of migration 0007's artifacts, after migration 0010 is applied, has the knowledge_capture config block in .planning/config.json, the 'Knowledge Capture — Ritual Tail' section in AGENTS.md, and .codex/workflow-version.txt reading 0.5.0 (D-03, D-06)"
    - "The test_migration_0010 fixture is RED before migrations/0010-heal-0007-knowledge-capture.md exists and GREEN after it is authored (D-06)"
    - "Migration 0010's pre-flight fence, its applies_to block, and every Step Apply block contain no 'skills/agentic-apps-workflow' substring; the migration carries no payload-presence detection branch (D-01, D-02, D-07)"
    - "bash migrations/run-tests.sh (unfiltered, including test_drift) exits 0 after 0010 lands"
  artifacts:
    - path: "migrations/0010-heal-0007-knowledge-capture.md"
      provides: "The 0007-fix forward migration (re-delivers 0007 Steps 1/2/4, drops Step 3)"
      contains: "from_version: 0.4.0"
    - path: "migrations/run-tests.sh"
      provides: "test_migration_0010 fixture + dispatch registration + drift-target fix in test_drift"
      contains: "test_migration_0010"
  key_links:
    - from: "migrations/run-tests.sh test_migration_0010"
      to: "migrations/0010-heal-0007-knowledge-capture.md"
      via: "extract_step_block / extract_preflight_block"
      pattern: "extract_step_block .*MIGRATION_0010"
    - from: "migrations/run-tests.sh test_drift"
      to: "migrations/*.md to_version fields"
      via: "semver-max drift-target selection (not filename sort)"
      pattern: "to_version"
---

<objective>
Deliver MIGR-10: a new forward migration `0010-heal-0007-knowledge-capture.md`
that heals migration 0007's chain break so real (non-scaffolder) installs stuck
at 0.4.0 can reach 0.5.0 with 0007's payload delivered — then keep the full test
suite green after this version-backport lands.

Root cause (per 11-CONTEXT.md): 0007's pre-flight greps a scaffolder-relative
path `skills/agentic-apps-workflow/SKILL.md` that no real target project has, so
it aborts (exit 3) before writing anything. 0008/0009 read
`.codex/workflow-version.txt` correctly but their floor checks can never pass
because the version record 0007 was meant to advance never moved.

Purpose: re-enable 0008/0009's already-correct floor logic for the fleet.
Output: migration 0010 (Steps 1/2/4 payload, corrected pre-flight), its
mutation-disciplined fixture, and a drift-test fix so 0010's lower to_version
does not falsely trip test_drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-migration-chain-repair/11-CONTEXT.md
@.planning/phases/11-migration-chain-repair/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

Current version state (confirmed 2026-07-16):
- .codex/workflow-version.txt = 0.7.0 ; skills/agentic-apps-workflow/SKILL.md version = 0.7.0
- migration to_versions: 0006→0.4.0, 0007→0.5.0, 0008→0.6.0, 0009→0.7.0 (current drift target)
- Migration 0010 will carry to_version: 0.5.0 (a version-backport into the 0.4.0→0.5.0 slot 0007 occupies)

extract helpers already in migrations/run-tests.sh:
- extract_step_block <doc_path> <step_number> <label>   (run-tests.sh:104-117)
- extract_preflight_block <doc_path>                     (run-tests.sh:124-133)
- assert_extracted_shape <label> <text> <required_substring>  (run-tests.sh:142+; non-empty AND contains substring; caller MUST gate execution on its return)
- assert_check <label> <check> <fixture_dir> applied|not-applied  (vendor/agenticapps-shared/migrations/lib/helpers.sh:49-79)

test_migration_0007 (run-tests.sh:749+) resolves templates via
$REPO_ROOT/skills/setup-codex-agenticapps-workflow/templates/... and hardcodes
REPO_NAME="codex-workflow" (no git init, inline apply logic).
test_migration_0009's extract-and-execute call shape (run-tests.sh:3476-3479) is
the pattern to copy: pf_block/idem_block/apply_block via extract_*_block.

drift mechanism (DO NOT edit — pinned submodule): run_drift_test picks the
latest migration by `ls .../[0-9][0-9][0-9][0-9]-*.md | sort | tail -1`
(vendor/agenticapps-shared/migrations/lib/drift-test.sh:47) — filename sort, not
to_version. 0010 sorts last but is NOT the drift target.
</interfaces>

@migrations/0008-plan-review-gate.md
@migrations/0007-knowledge-capture.md
@migrations/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author test_migration_0010 fixture + dispatch registration; observe RED</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh:100-160 (extract_step_block, extract_preflight_block, assert_extracted_shape)
    - migrations/run-tests.sh:749-870 (test_migration_0007 — template-path resolution + inline apply of Steps 1/2, sandbox mktemp -d shape)
    - migrations/run-tests.sh:3453-3651 and 4527-4596 (test_migration_0009 — extract-and-execute call shape + no-scaffolder-tree + document-contract separation assertions)
    - migrations/run-tests.sh:1651-1727 (0008 no-scaffolder-tree sandbox construction with quoted heredocs)
    - migrations/run-tests.sh:4668-4692 (dispatch registration block for 0009 / drift / layout)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md (D-06, D-07)
  </read_first>
  <behavior>
    - RED expectation: with no migrations/0010-heal-0007-knowledge-capture.md yet, extraction of 0010's pre-flight and Step Apply blocks is empty, assert_extracted_shape FAILs, and the suite exits non-zero.
    - D-06 assertions (after 0010 exists): a mktemp -d sandbox seeded with .codex/workflow-version.txt=0.4.0 and NONE of 0007's artifacts (no knowledge_capture key, no ritual-tail section), after executing 0010's Step 1/2/3 Apply blocks, has jq '.knowledge_capture.enabled' true in .planning/config.json, grep '^## Knowledge Capture — Ritual Tail (spec §15)' AGENTS.md, and .codex/workflow-version.txt == 0.5.0.
    - D-07 assertion: extracted pre-flight block, applies_to block, and Step 1/2/3 Apply blocks each contain no 'skills/agentic-apps-workflow' substring.
    - D-07 sandbox shape: the sandbox has no local skills/ directory (mirror the 0008 no-scaffolder-tree assertion at run-tests.sh:1651-1727).
  </behavior>
  <action>
    Add a new `test_migration_0010()` function to migrations/run-tests.sh modeled
    on `test_migration_0009`'s extract-and-execute shape. Define
    `MIGRATION_0010="$REPO_ROOT/migrations/0010-heal-0007-knowledge-capture.md"`.
    Extract the pre-flight via `extract_preflight_block`, the `applies_to` block
    via the awk range used at run-tests.sh:4583 (from `^applies_to:` to next
    top-level key), and Steps 1, 2, 3 Apply blocks via `extract_step_block
    "$MIGRATION_0010" N Apply`. Gate EVERY extraction with `assert_extracted_shape`
    before use (each with its required substring: Step 1 → `.planning/config.json`,
    Step 2 → `AGENTS.md`, Step 3 → `.codex/workflow-version.txt`).

    D-06 delivery fixture: create a sandbox with `mktemp -d`; `git init` it and set
    `CODEX_HOME="$REPO_ROOT"` so the extracted Apply blocks resolve
    `$CODEX/skills/setup-codex-agenticapps-workflow/templates/...` and
    `git rev-parse --show-toplevel`; seed `.codex/workflow-version.txt` with `0.4.0`;
    create `.planning` and an `AGENTS.md` carrying the
    `<!-- END: agentic-apps-workflow sections -->` marker but NO knowledge_capture
    key and NO ritual-tail section. Execute the extracted Step 1/2/3 Apply blocks
    inside the sandbox. Then assert (via assert_check / jq / grep / content
    equality): knowledge_capture.enabled is true, the ritual-tail heading is
    present in AGENTS.md, and `.codex/workflow-version.txt` reads exactly `0.5.0`.

    D-07 document-contract: for the extracted pre-flight, applies_to, and each Step
    Apply block, assert none contains the literal substring `skills/agentic-apps-workflow`
    (the exact substring per D-07 — NOT `skills/` broadly, which would false-positive
    on the legitimate `skills/setup-codex-agenticapps-workflow/templates/...` template
    references 0010 correctly retains). Also assert the sandbox has no local `skills/`
    directory (no-scaffolder-tree shape per D-07), mirroring run-tests.sh:1651-1727.

    Register dispatch immediately after 0009's block (run-tests.sh:4676-4678):
    `if [ -z "$FILTER" ] || [ "$FILTER" = "0010" ]; then test_migration_0010; fi`.
    Use quoted heredoc delimiters ('JSON'/'MD') for all static sandbox content;
    keep every write inside the mktemp sandbox; `trap 'rm -rf "$tmp"' RETURN`.
  </action>
  <acceptance_criteria>
    - `grep -c 'test_migration_0010' migrations/run-tests.sh` ≥ 2 (definition + dispatch).
    - Dispatch block for FILTER=0010 exists between 0009's block and the drift block.
    - Every extracted block in test_migration_0010 is passed to `assert_extracted_shape` before it is executed or asserted-on.
    - `bash migrations/run-tests.sh 0010` exits NON-ZERO (RED) — migration 0010 does not exist yet, so extraction is empty and assert_extracted_shape fails. Capture this RED output; it is success criterion #1's observed-failing state.
    - No write in test_migration_0010 targets a path outside its `mktemp -d` sandbox (grep the function for absolute writes; all mutations are under `$tmp`).
  </acceptance_criteria>
  <done>test_migration_0010 (definition + FILTER=0010 dispatch) exists in run-tests.sh with every extracted block gated by assert_extracted_shape, and `bash migrations/run-tests.sh 0010` exits non-zero (the observed RED baseline for success criterion #1) with all sandbox writes confined to the mktemp dir.</done>
  <verify>
    <automated>bash migrations/run-tests.sh 0010; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author migration 0010 (0007-fix); observe the fixture GREEN</name>
  <files>migrations/0010-heal-0007-knowledge-capture.md</files>
  <read_first>
    - migrations/0008-plan-review-gate.md:54-93 (pre-flight — the VERBATIM-reuse floor-check pattern, D-01)
    - migrations/0007-knowledge-capture.md:49-72 (buggy floor line to NOT copy; template-existence checks at 67-72 that ARE valid to port)
    - migrations/0007-knowledge-capture.md:77-171 (Steps 1/2/4 payload to re-deliver; Step 3 to DROP)
    - migrations/0008-plan-review-gate.md:470-487 (Notes-section precedent for documenting the deliberate no-skills-path divergence)
    - migrations/README.md:73-100 (four mandatory step subsections + idempotency contract)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>
    Create `migrations/0010-heal-0007-knowledge-capture.md`. Frontmatter: `id: 0010`,
    `slug: heal-0007-knowledge-capture`, `from_version: 0.4.0`, `to_version: 0.5.0`,
    `applies_to` listing EXACTLY `.planning/config.json`, `AGENTS.md`,
    `.codex/workflow-version.txt` (D-03 — no `skills/**/SKILL.md` path anywhere),
    `requires: []`, `optional_for: []`.

    Pre-flight (D-01, verbatim reuse of 0008's pattern): copy
    migrations/0008-plan-review-gate.md:54-93's structure, changing only — the git
    check and jq check stay identical; the version-floor regex becomes
    `^0\.(4|5)\.0$` (accept 0.4.0 for fresh apply, 0.5.0 for idempotent re-apply,
    same shape as 0008's `^0\.(5|6)\.0$`); the ABORT text names `0.4.0`/`0.5.0` and
    migration 0010; the two template-existence checks are the two 0007 Steps 1/2
    need — `config-knowledge-capture.json` and `agents-md-additions.md` (ported from
    0007 lines 67-72). DROP 0008's exit-5 check-plan-review.sh verifier check (that
    is 0008-specific). Do NOT reintroduce 0007's
    `grep -qE '^version: 0\.(4|5)\.0$' skills/agentic-apps-workflow/SKILL.md` line —
    that scaffolder-relative grep is the exact V-01-class bug 0010 exists to fix
    (D-03, D-07). Per D-01 do NOT add payload-presence detection (no checking for a
    missing knowledge_capture block or ritual-tail) to the migration code.

    Steps (D-03): Step 1 = seed the host-neutral `knowledge_capture` block into
    `.planning/config.json` (copy near-verbatim from 0007:77-109 — idempotency check,
    pre-condition, jq merge-or-create Apply resolving `<repo-name>`, Rollback).
    Step 2 = insert the "Knowledge Capture — Ritual Tail (spec §15)" section into
    `AGENTS.md` (copy near-verbatim from 0007:111-151 — the awk extract-from-template
    then insert-before-END-marker two-pass; keep the getline-from-file form, it is
    load-bearing for BSD/macOS awk). Step 3 = record the new project version:
    idempotency `grep -q '^0.5.0$' .codex/workflow-version.txt`, pre-condition
    `.codex/` exists, Apply `echo "0.5.0" > .codex/workflow-version.txt`, Rollback
    `echo "0.4.0" > .codex/workflow-version.txt` (this is 0007's Step 4 renumbered to
    0010's Step 3). DROP 0007's Step 3 (the `skills/agentic-apps-workflow/SKILL.md`
    scaffolder version bump — a MIGR-09 immutability violation, D-03).

    Add Post-checks, Skip cases, Compatibility, Notes, References sections per the
    README contract. In Notes, record — mirroring the precedent at
    0008:470-487 — that 0010 heals migration 0007's chain break by reading
    `.codex/workflow-version.txt` exclusively and touches no target-project `skills/`
    path, referencing 0007 by number.
  </action>
  <acceptance_criteria>
    - `grep -E '^id: 0010$' migrations/0010-heal-0007-knowledge-capture.md` matches; `from_version: 0.4.0` and `to_version: 0.5.0` present.
    - The migration's applies_to lists exactly `.planning/config.json`, `AGENTS.md`, `.codex/workflow-version.txt` and no path containing `skills/agentic-apps-workflow`.
    - The pre-flight fence greps `.codex/workflow-version.txt` and contains no `skills/agentic-apps-workflow` substring; it retains the two `skills/setup-codex-agenticapps-workflow/templates/...` existence checks.
    - The migration has exactly three Steps (1 config.json, 2 AGENTS.md, 3 version record); there is no scaffolder version-bump step and no `sed ... skills/agentic-apps-workflow/SKILL.md` anywhere.
    - Every Step carries all four subsections (Idempotency check / Pre-condition / Apply / Rollback) per migrations/README.md:73-80.
    - `bash migrations/run-tests.sh 0010` exits 0 (GREEN) — the D-06 delivery fixture and D-07 document-contract from Task 1 now pass. This GREEN, contrasted with Task 1's RED, is success criterion #1's observed RED→GREEN transition.
  </acceptance_criteria>
  <done>migrations/0010-heal-0007-knowledge-capture.md exists (id 0010, from_version 0.4.0, to_version 0.5.0, applies_to the three project files only, three steps with all four mandatory subsections, corrected `.codex/workflow-version.txt`-only pre-flight, no scaffolder-relative grep and no dropped-Step-3 scaffolder bump), and `bash migrations/run-tests.sh 0010` now exits 0 — the RED→GREEN transition of success criterion #1 is observed.</done>
  <verify>
    <automated>bash migrations/run-tests.sh 0010</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Fix test_drift so 0010's backport to_version keeps the suite green</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh:3212-3249 (test_drift — leg 1 run_drift_test call + leg 2 SKILL.md vs .codex/workflow-version.txt)
    - vendor/agenticapps-shared/migrations/lib/drift-test.sh:34-63 (run_drift_test — filename-sort selection; DO NOT edit, it is the pinned mechanism per ADR-0035)
    - .planning/phases/11-migration-chain-repair/11-PATTERNS.md:290-296 (drift-test interaction caution)
  </read_first>
  <action>
    In `test_drift` (migrations/run-tests.sh), replace the leg-1 `run_drift_test`
    selection with a consumer-owned drift-target selection by highest `to_version`
    across `migrations/*.md`, not by filename sort. Compute the maximum `to_version`
    (grep `^to_version:` from every `migrations/[0-9][0-9][0-9][0-9]-*.md`, take the
    semver-highest) and assert it equals `skills/agentic-apps-workflow/SKILL.md`'s
    `version:` field. This is the true drift invariant; run_drift_test's filename
    `sort | tail -1` is a proxy that a version-backport breaks. Add a load-bearing
    comment stating: migration 0010 is a backport into the 0.4.0→0.5.0 slot — its
    filename sorts last but its to_version (0.5.0) is below the drift target (0.7.0,
    from 0009); the drift target is selected by to_version, not filename. Leave
    leg 2 (SKILL.md version == .codex/workflow-version.txt, run-tests.sh:3239-3248)
    unchanged. Do NOT edit vendor/agenticapps-shared/migrations/lib/drift-test.sh
    (mechanism is pinned; only the consumer policy changes, per ADR-0035).

    Portability: this runs on the ubuntu + macOS CI matrix — do NOT use `sort -V`
    (unreliable on macOS BSD sort). Use a numeric field sort
    (`sort -t. -k1,1n -k2,2n -k3,3n`) or an awk max over the to_version values.
  </action>
  <acceptance_criteria>
    - `bash migrations/run-tests.sh drift` exits 0 with migration 0010 present (drift target resolves to 0.7.0 via 0009, matching SKILL.md 0.7.0).
    - test_drift selects the drift target by semver-max to_version, evidenced by a comment naming 0010 as the version-backport exception; no `sort -V` is used (`grep -c 'sort -V' migrations/run-tests.sh` == 0).
    - `git diff --stat vendor/agenticapps-shared/` shows no change (the pinned mechanism is untouched).
    - Sanity mutation (perform, then restore): temporarily editing `skills/agentic-apps-workflow/SKILL.md` `version:` to `0.9.9` makes `bash migrations/run-tests.sh drift` exit non-zero (RED); restoring `0.7.0` returns it to exit 0 (GREEN) — proving the leg still detects real drift.
    - `bash migrations/run-tests.sh` (unfiltered) exits 0.
  </acceptance_criteria>
  <done>test_drift selects the drift target by semver-max to_version (0.7.0 via 0009, matching SKILL.md) with a comment naming 0010 as the version-backport exception and no `sort -V`, the pinned vendor drift mechanism is unedited, the sanity mutation goes RED then GREEN on restore, and `bash migrations/run-tests.sh` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| update-skill → target project files | Migration 0010's Apply blocks cross from the scaffolder ($CODEX_HOME, read-only source) into the project working tree — untrusted-write surface. |
| test harness → mktemp sandbox | test_migration_0010 executes extracted Apply blocks; all writes must stay inside the `mktemp -d` sandbox. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering/Elevation | migration 0010 Apply blocks | mitigate | applies_to lists exactly the three project files; Steps write only to .planning/config.json, AGENTS.md, .codex/workflow-version.txt; template reads from $CODEX_HOME are read-only; acceptance criteria assert no write outside those three paths and no `skills/agentic-apps-workflow` reference. |
| T-11-02 | Injection/Tampering | run-tests.sh sandbox heredocs + var expansion | mitigate | all static fixture content uses quoted heredoc delimiters ('JSON'/'MD'); sandbox is `mktemp -d` with `trap rm -rf RETURN`; CODEX_HOME is set to $REPO_ROOT (trusted repo), not attacker input. |
| T-11-03 | Tampering | executing extracted Step Apply blocks | mitigate | blocks are extracted from the repo's own committed 0010 doc (trusted), gated by assert_extracted_shape (non-empty + expected path substring) before execution, and run cd-isolated inside the mktemp sandbox. |
| T-11-SC | Tampering | package installs | accept | this plan adds no npm/pip/cargo installs — no supply-chain surface. |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` exits 0 (full suite, including test_drift and test_migration_0010, on the same harness Phase 10's CI runs).
- Success criterion #1 (RED→GREEN) is captured: Task 1 records `bash migrations/run-tests.sh 0010` RED before 0010 exists; Task 2 records it GREEN after.
- Success criterion #2 (document-contract): the D-07 assertion in test_migration_0010 proves 0010's executable surface carries no `skills/agentic-apps-workflow` substring.
</verification>

<success_criteria>
- migrations/0010-heal-0007-knowledge-capture.md exists: id 0010, from_version 0.4.0, to_version 0.5.0, three steps, corrected pre-flight, no scaffolder-relative grep.
- test_migration_0010 in run-tests.sh proves the 0.4.0→0010→payload+0.5.0 delivery and the no-`skills/agentic-apps-workflow` document contract, observed RED-before / GREEN-after.
- test_drift stays green with 0010's backport to_version present; the pinned drift mechanism is unedited.
- Full suite exits 0.
</success_criteria>

<output>
Create `.planning/phases/11-migration-chain-repair/11-01-SUMMARY.md` when done.
</output>
