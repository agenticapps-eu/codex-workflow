---
phase: 11-migration-chain-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/update-codex-agenticapps-workflow/SKILL.md
autonomous: true
requirements: [MIGR-11]
must_haves:
  truths:
    - "update-codex-agenticapps-workflow/SKILL.md Stage D documents a defined, non-looping recovery path for an operator stuck on migration 0007's permanently-aborting pre-flight: re-run the update, migration 0010 applies instead (0007 is superseded)"
    - "Stage D documents recovery for the manual-0.5.0-escape operator: apply migration 0010 via the --migration 0010 flag to obtain 0007's missing Steps 1/2/4 payload — the manual-escape operator is handled by documentation, not code (D-02)"
    - "The runbook is concise (a numbered/bulleted subsection inside Stage D), not a new top-level section and not one-line-too-thin (D-04)"
  artifacts:
    - path: "skills/update-codex-agenticapps-workflow/SKILL.md"
      provides: "Stage D recovery runbook covering the two stuck-operator states (D-04a, D-04b)"
      contains: "0010"
  key_links:
    - from: "SKILL.md Stage D recovery runbook"
      to: "the --migration NNNN flag"
      via: "documented at SKILL.md:110"
      pattern: "--migration 0010"
---

<objective>
Deliver MIGR-11: document the operator recovery path in
`update-codex-agenticapps-workflow/SKILL.md` Stage D so a project stuck on
migration 0007's permanent pre-flight abort has a defined, non-looping way
forward once migration 0010 supersedes the same 0.4.0→0.5.0 transition.

Purpose: close the "dead end" for real installs — code fixes the fleet
(migration 0010, plan 11-01); docs fix the operator who already hand-escaped.
Output: a concise recovery-runbook subsection inside Stage D (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-migration-chain-repair/11-CONTEXT.md
@.planning/phases/11-migration-chain-repair/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. -->

Migration 0010 (authored in plan 11-01, may run in parallel — its contract is
locked by 11-CONTEXT.md D-01/D-03 and is stable to write against):
- id 0010, from_version 0.4.0, to_version 0.5.0
- supersedes migration 0007 for the 0.4.0→0.5.0 transition
- re-delivers 0007's Steps 1/2/4 payload (knowledge_capture config block +
  AGENTS.md ritual-tail section + 0.5.0 version record) with a corrected
  pre-flight that reads .codex/workflow-version.txt exclusively.

The update skill already documents `--migration NNNN` (apply only the named
migration) at skills/update-codex-agenticapps-workflow/SKILL.md:110.
Stage D — Apply is at SKILL.md:72-87. Failure modes style precedent (bold
lead-in + terse imperative + one line of why) is at SKILL.md:123-139.
</interfaces>

@skills/update-codex-agenticapps-workflow/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Stage D recovery runbook (two stuck-operator states)</name>
  <files>skills/update-codex-agenticapps-workflow/SKILL.md</files>
  <read_first>
    - skills/update-codex-agenticapps-workflow/SKILL.md:72-87 (Stage D — Apply, insertion site + style)
    - skills/update-codex-agenticapps-workflow/SKILL.md:105-139 (Flags table incl. --migration NNNN; Failure modes bullet style precedent)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md (D-02, D-04)
    - .planning/phases/11-migration-chain-repair/11-PATTERNS.md:101-134 (Stage D style precedent, no prior runbook analog)
  </read_first>
  <action>
    Add a concise "Recovery — a migration whose pre-flight always aborts"
    subsection inside `### Stage D — Apply` in
    skills/update-codex-agenticapps-workflow/SKILL.md (D-04 — a numbered sub-step or
    bulleted block, NOT a new top-level `##` section). Match the existing Stage D /
    Failure-modes style (bold lead-in per bullet, terse imperative, one line of
    "why"). Cover the two operator states from D-04:

    (a) Operator stuck on migration 0007's permanently-aborting pre-flight (0007
    greps a scaffolder-relative `skills/agentic-apps-workflow/SKILL.md` path no real
    project has, so it exits 3 before writing). Recovery: migration 0007 is
    superseded by migration 0010 for the same 0.4.0→0.5.0 transition — re-run
    `$update-codex-agenticapps-workflow`; because the project is still at 0.4.0,
    0010 applies instead and delivers 0007's payload. Non-looping: after 0010 the
    version record reads 0.5.0 and 0007 no longer selects.

    (b) Manual-0.5.0-escape operator (D-02): forced `.codex/workflow-version.txt` to
    0.5.0 to get past 0007's abort, so the project reads 0.5.0 but carries NONE of
    0007's Steps 1/2/4 payload. Recovery: apply migration 0010 explicitly with the
    already-documented flag — `$update-codex-agenticapps-workflow --migration 0010`
    — whose 0.4.0/0.5.0 re-apply floor accepts a 0.5.0 project and delivers the
    missing knowledge_capture config block and AGENTS.md ritual-tail section
    idempotently.

    Keep it act-on-able but not exhaustive per-state prose (doc-rot risk per D-04).
    This is documentation only — introduce no executable/config content.
  </action>
  <acceptance_criteria>
    - A recovery subsection exists INSIDE `### Stage D — Apply` (between SKILL.md:72-87's numbered steps and Stage E), not as a new top-level `##` heading — verify the new heading is `###`/`####` or a bolded bullet, not `## `.
    - The subsection names migration `0010`, states it supersedes migration `0007` for the 0.4.0→0.5.0 transition, and describes the re-run path for state (a).
    - The subsection references `--migration 0010` for the manual-0.5.0-escape operator (state b) and names the two payload artifacts recovered (knowledge_capture config block, AGENTS.md ritual-tail section).
    - `grep -c '0010' skills/update-codex-agenticapps-workflow/SKILL.md` ≥ 2 and `grep -q -- '--migration 0010' skills/update-codex-agenticapps-workflow/SKILL.md`.
    - The change touches only skills/update-codex-agenticapps-workflow/SKILL.md and adds no shell/JSON code blocks.
  </acceptance_criteria>
  <done>A concise recovery subsection lives inside `### Stage D — Apply` (not a new top-level `##` section) covering both operator states: it names migration 0010 as superseding 0007 for the 0.4.0→0.5.0 transition with the re-run path (state a) and references `--migration 0010` to recover the knowledge_capture config block and AGENTS.md ritual-tail section (state b); the verify grep passes and only SKILL.md changed with no added code blocks.</done>
  <verify>
    <automated>grep -q -- '--migration 0010' skills/update-codex-agenticapps-workflow/SKILL.md && grep -q 'supersed' skills/update-codex-agenticapps-workflow/SKILL.md && test "$(grep -c '0010' skills/update-codex-agenticapps-workflow/SKILL.md)" -ge 2</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ← documentation | The runbook instructs an operator to run commands on their own project; the risk surface is misleading/looping guidance, not code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Repudiation/Denial-of-service | recovery runbook prose | mitigate | the documented path is verifiably non-looping (after 0010, version reads 0.5.0 and 0007 no longer selects); acceptance criteria assert it names the superseding migration and the terminating state, so an operator is not sent into a re-abort loop. |
| T-11-07 | Tampering | SKILL.md edit scope | mitigate | change is markdown-only in one file; acceptance criteria forbid adding executable/config blocks — no new file-write surface is introduced. |
| T-11-SC | Tampering | package installs | accept | this plan adds no npm/pip/cargo installs — no supply-chain surface. |
</threat_model>

<verification>
- The Stage D recovery subsection reads as a defined, non-looping procedure for both operator states (D-04a, D-04b).
- Automated grep confirms migration 0010, the supersession statement, and the `--migration 0010` recovery command are present.
</verification>

<success_criteria>
- update-codex-agenticapps-workflow/SKILL.md Stage D documents the operator path for a project stuck on 0007's permanent pre-flight abort, once 0010 supersedes the same 0.4.0→0.5.0 transition — readable as a defined, non-looping procedure (success criterion #3).
</success_criteria>

<output>
Create `.planning/phases/11-migration-chain-repair/11-03-SUMMARY.md` when done.
</output>
