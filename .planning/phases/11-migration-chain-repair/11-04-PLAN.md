---
phase: 11-migration-chain-repair
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - skills/update-codex-agenticapps-workflow/SKILL.md
autonomous: true
requirements: [MIGR-11]
must_haves:
  truths:
    - "Stage D's recovery runbook is TRUE against Stage A's documented selection algorithm: for a project stuck at 0.4.0 it does NOT claim a bare re-run of the update makes 0007 stop selecting (Stage A step 4 has no supersession rule); it prescribes `--migration 0010` to skip 0007's permanently-aborting pre-flight and apply only its corrected replacement."
    - "The Flags table's `--migration NNNN` definition documents that the flag applies the named migration regardless of Stage A's pending computation — bypassing the `to_version > project_version` boundary check (it also matches `to_version == project_version` for idempotent re-apply) — so `--migration 0010` actually selects 0010 for a project already recorded at exactly 0.5.0."
    - "Both stuck-operator states (still at 0.4.0, and manually forced to 0.5.0) resolve to the same defined, non-looping command `$update-codex-agenticapps-workflow --migration 0010`, and following it terminates with `.codex/workflow-version.txt == 0.5.0` and 0007's Steps 1/2/4 payload delivered — no re-abort loop, no silent 'up-to-date' no-op."
  artifacts:
    - path: "skills/update-codex-agenticapps-workflow/SKILL.md"
      provides: "Stage D recovery runbook rewritten to match Stage A's real selection algorithm, plus an amended --migration NNNN flag definition that supports both recovery bullets"
      contains: "--migration 0010"
  key_links:
    - from: "Stage D recovery bullet (project stuck at 0.4.0)"
      to: "the `--migration NNNN` flag"
      via: "recovery routes around 0007's aborting pre-flight by filtering the apply set to only 0010"
      pattern: "--migration 0010"
    - from: "Flags table `--migration NNNN` row"
      to: "Stage A step 4 pending-selection boundary"
      via: "documented bypass of the `to_version > project_version` check so a re-apply at the current version is possible"
      pattern: "to_version|bypass|regardless of pending|re-apply"
---

<objective>
Close the SC#3 / MIGR-11 blocker: `update-codex-agenticapps-workflow/SKILL.md`
Stage D's recovery runbook (added by plan 11-03) asserts outcomes that Stage A's
own documented pending-migration-selection algorithm does not produce, so the
"non-looping recovery path" is not actually true against the skill's own
mechanics (CR-01/CR-02 in 11-REVIEW.md, confirmed in 11-VERIFICATION.md).

Fix family chosen: **make the runbook honest (option ii), not invent a
supersession rule (option i).** Rationale, verified against the codebase:
- There is NO separate code-level migration runner — migrations are applied by an
  agent following THIS SKILL.md (an executable specification); `run-tests.sh` is
  only a test harness. So the SKILL.md text IS the apply behavior; the fix must
  make Stage A, the Flags table, and the recovery bullets mutually consistent.
- A version-slot supersession rule (option i) is unsafe here: migrations 0002 and
  0003 legitimately share `from_version: 0.2.0` AND `to_version: 0.2.0` as
  additive co-residents, so "same to_version → higher id supersedes" would
  wrongly drop 0002. Making it safe would require a new `supersedes:` frontmatter
  field on 0010 — a data/spec change that contradicts 11-CONTEXT.md D-02 ("the
  manual-escape operator is handled by documentation, not code").

Therefore: rewrite the two Recovery bullets so BOTH stuck-operator states route
through `$update-codex-agenticapps-workflow --migration 0010`, and amend the
Flags table's `--migration NNNN` definition so that flag genuinely supports both
bullets (skip a permanently-aborting earlier migration; re-apply a migration to a
project already at its `to_version`).

Purpose: give a stuck fleet a recovery path that is defined, non-looping, AND
true against the skill's own selection algorithm — not merely grep-plausible.
Output: an internally consistent Stage D + Flags table in one file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-migration-chain-repair/11-CONTEXT.md
@.planning/phases/11-migration-chain-repair/11-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the working tree. No exploration required. -->

Stage A step 4 — the pending-selection algorithm the runbook must be consistent
with (skills/update-codex-agenticapps-workflow/SKILL.md:45-52), UNCHANGED by this
plan:
  "select those whose from_version <= project version AND to_version > project
   version. If none, log 'project is up-to-date'. If any, sort by id ascending;
   this is the apply order."
  => There is NO supersession clause. Do not add one. The runbook must be true
     under this exact algorithm.

Consequence to encode honestly (independently derived, matches 11-VERIFICATION.md):
- Project at 0.4.0: pending = {0007, 0010} (both from_version 0.4.0, to_version
  0.5.0); sorted ascending → 0007 is tried FIRST and its pre-flight aborts
  (exit 3). A plain re-run does NOT skip to 0010. Recovery: `--migration 0010`.
- Project hand-forced to 0.5.0: pending never includes 0010 (its to_version
  0.5.0 is NOT > 0.5.0). Recovery: `--migration 0010`, but ONLY works if the flag
  bypasses the `to_version > project_version` boundary.

Current Flags table row to amend (skills/update-codex-agenticapps-workflow/SKILL.md:135):
  "| `--migration NNNN` | Apply only the named migration (skip other pending).
   Useful for testing one migration in isolation. |"
  => Today this reads as a FILTER over an already-computed pending set. That is
     exactly why the second recovery bullet fails at 0.5.0. Amend it to state the
     flag overrides Stage A's pending computation.

Migration 0010 contract (migrations/0010-heal-0007-knowledge-capture.md:1-12,
already shipped & verified by 11-01): id 0010, from_version 0.4.0, to_version
0.5.0; its own pre-flight floor accepts 0.4.0 (fresh) and 0.5.0 (idempotent
re-apply); re-delivers 0007's Steps 1/2/4 payload (knowledge_capture config
block + AGENTS.md ritual-tail section + 0.5.0 version record).

Existing Stage D recovery text to REWRITE lives at
skills/update-codex-agenticapps-workflow/SKILL.md:88-111 (the two bullets that
currently claim "0007 no longer selects" / "0010 applies instead").
Failure-modes bullet style precedent: SKILL.md:148-164 (bold lead-in + terse
imperative + one line of why).
</interfaces>

@skills/update-codex-agenticapps-workflow/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the `--migration NNNN` flag definition to override Stage A's pending computation</name>
  <files>skills/update-codex-agenticapps-workflow/SKILL.md</files>
  <read_first>
    - skills/update-codex-agenticapps-workflow/SKILL.md:35-52 (Stage A steps 1-4 — the pending algorithm the flag overrides; read the exact boundary wording)
    - skills/update-codex-agenticapps-workflow/SKILL.md:130-137 (the Flags table, incl. the current `--migration NNNN` row at line 135)
    - .planning/phases/11-migration-chain-repair/11-VERIFICATION.md:39-41 (the two `missing` items — missing-b is this task)
  </read_first>
  <action>
    Rewrite ONLY the `| `--migration NNNN` | ... |` row of the Flags table
    (currently SKILL.md:135) so the flag's definition genuinely supports being
    used for recovery. The new cell text must state all three of:
    (1) it applies ONLY the named migration and skips every other migration,
        pending or not;
    (2) it OVERRIDES Stage A step 4's pending computation — specifically it
        bypasses the `to_version > project_version` boundary check, so it also
        matches a migration whose `to_version == project_version` (i.e. an
        idempotent re-apply to a project already recorded at that version);
    (3) one clause of "why": use it to apply a corrected replacement for a
        permanently-aborting earlier migration, or to re-deliver a migration's
        payload to a project already at its `to_version`.
    Keep it to the table-cell prose register (one dense sentence or two short
    ones); do NOT add a fenced code block. The old text "Apply only the named
    migration (skip other pending). Useful for testing one migration in
    isolation." must NOT survive verbatim as the sole content — the
    boundary-override semantics are the whole point.
    Leave Stage A step 4 (SKILL.md:45-52) UNCHANGED — do not add a supersession
    rule there (option i is rejected; see objective).
  </action>
  <acceptance_criteria>
    - The `--migration NNNN` Flags row explicitly documents that the flag applies
      the named migration REGARDLESS of Stage A's pending set / bypasses the
      `to_version > project_version` boundary (also matching `to_version ==
      project_version` for re-apply). Verify by extracting the flag's table row
      and asserting it matches boundary-override semantics — e.g. the row text
      contains at least one of: `to_version`, `bypass`, `regardless of pending`,
      `already at`, `re-apply` / `re-deliver`. A row that still reads only "apply
      only the named migration (skip other pending)" FAILS.
    - Stage A step 4 (SKILL.md ~45-52) is unchanged and still selects purely by
      `from_version <= project version AND to_version > project version` sorted by
      id ascending — grep confirms no `supersede`/`superseded`/`same to_version`
      clause was added to the Stage A section.
    - Only skills/update-codex-agenticapps-workflow/SKILL.md changed; no fenced
      shell/JSON block added to the Flags table.
  </acceptance_criteria>
  <verify>
    <automated>F=skills/update-codex-agenticapps-workflow/SKILL.md; row=$(grep -n '`--migration NNNN`' "$F" | head -1); echo "$row" | grep -qiE 'to_version|bypass|regardless of pending|already at|re-apply|re-deliver' && ! sed -n '/### Stage A/,/### Stage B/p' "$F" | grep -qiE 'supersede|superseded|same to_version'</automated>
  </verify>
  <done>The `--migration NNNN` Flags row documents that the flag overrides Stage A's pending computation and matches `to_version == project_version` for idempotent re-apply; Stage A step 4 is untouched (no supersession clause); only SKILL.md changed with no added code fence.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Stage D recovery runbook to match Stage A's real algorithm (both states → --migration 0010)</name>
  <files>skills/update-codex-agenticapps-workflow/SKILL.md</files>
  <read_first>
    - skills/update-codex-agenticapps-workflow/SKILL.md:88-111 (the two recovery bullets to REWRITE — they currently claim a bare re-run drops 0007)
    - skills/update-codex-agenticapps-workflow/SKILL.md:45-52 (Stage A step 4 — the algorithm the rewrite must be true against)
    - skills/update-codex-agenticapps-workflow/SKILL.md:130-137 (the `--migration NNNN` row amended in Task 1 — the mechanism this runbook now consumes)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md:59-67 (D-04 — concise runbook, two operator states)
    - .planning/phases/11-migration-chain-repair/11-VERIFICATION.md:8-41 (the failed truth + both `missing` items — missing-a is this task)
  </read_first>
  <action>
    Rewrite the "Recovery — a migration whose pre-flight always aborts"
    subsection (SKILL.md:88-111), keeping it inside `### Stage D — Apply` (a
    bolded-lead-in bullet block, NOT a new top-level `##` section) and concise per
    D-04. Make the two bullets HONEST about Stage A step 4's actual behavior:

    (a) **Stuck on 0007's aborting pre-flight (project still records 0.4.0).**
    State the real mechanics: because 0007 and 0010 both target the 0.4.0→0.5.0
    transition, Stage A computes BOTH as pending and — sorting by id ascending —
    tries 0007 FIRST, whose pre-flight aborts (exit 3) before writing. A plain
    re-run therefore loops on the same abort. Do NOT claim "0007 no longer
    selects". Instead: run `$update-codex-agenticapps-workflow --migration 0010`
    to apply ONLY 0010 (the corrected replacement) and skip 0007's dead-end
    pre-flight. State termination: after 0010 completes, `.codex/workflow-version.txt`
    reads 0.5.0 and the 0.4.0→0.5.0 slot is satisfied.

    (b) **Manually forced to 0.5.0 to escape the abort.** State that at 0.5.0
    Stage A's pending formula never selects 0010 (its to_version 0.5.0 is not
    `>` 0.5.0), so a plain update reports "up-to-date" and delivers nothing.
    Recovery is the SAME command: `$update-codex-agenticapps-workflow
    --migration 0010`, which — per the amended flag definition (Task 1) —
    bypasses that boundary and re-applies 0010 idempotently, delivering the
    missing knowledge_capture config block and AGENTS.md ritual-tail section.

    You may keep an intro sentence describing 0010 as the corrected replacement
    for 0007, but it must NOT assert an algorithmic supersession that Stage A does
    not implement. Add a short cross-reference from these bullets to the
    `--migration NNNN` flag row so a reader can confirm the boundary-override
    mechanism. Documentation only — no executable/config blocks.
  </action>
  <acceptance_criteria>
    - CONSISTENCY GATE (the anti-shallow-grep check): SKILL.md must NOT contain a
      bare-re-run supersession claim (e.g. "0007 no longer selects" or "0010
      applies instead" or "re-run ... applies 0010 instead") UNLESS Stage A step 4
      contains an actual supersession rule. Since this plan adds no Stage A
      supersession rule, that phrasing must be ABSENT. Verify: `grep -iE '0007 no
      longer selects|0010 applies instead|applies instead'` returns nothing while
      the Stage A section has no `supersede` clause. A plausible-but-wrong prose
      edit that reinstates the old "0010 applies instead" claim FAILS here.
    - The 0.4.0-stuck recovery bullet (state a) prescribes
      `--migration 0010` (not a bare re-run) and states 0007 sorts first and
      aborts on a plain re-run — i.e. it describes the ACTUAL Stage A ordering.
    - The manual-0.5.0 recovery bullet (state b) prescribes the same
      `--migration 0010` command and states plainly that Stage A's pending formula
      would otherwise report up-to-date at 0.5.0 (why the boundary-override flag is
      required), naming the two recovered artifacts (knowledge_capture config
      block, AGENTS.md ritual-tail section).
    - The recovery subsection is inside `### Stage D — Apply` (a bolded bullet
      block), not a new top-level `##` heading, and adds no shell/JSON code fence.
    - Only skills/update-codex-agenticapps-workflow/SKILL.md changed.
  </acceptance_criteria>
  <verify>
    <automated>F=skills/update-codex-agenticapps-workflow/SKILL.md; if grep -qiE '0007 no longer selects|0010 applies instead|applies instead' "$F"; then echo "FAIL: false bare-re-run supersession claim survives"; exit 1; fi; grep -q -- '--migration 0010' "$F" || { echo "FAIL: recovery does not prescribe --migration 0010"; exit 1; }; test "$(grep -c -- '--migration 0010' "$F")" -ge 2 || { echo "FAIL: --migration 0010 not prescribed for both operator states"; exit 1; }; echo OK</automated>
  </verify>
  <done>Both recovery bullets route through `--migration 0010`, the false "0010 applies instead"/"0007 no longer selects" claims are gone, state (a) describes 0007-sorts-first-and-aborts, state (b) explains the boundary-override need, and the runbook is now true against Stage A step 4; only SKILL.md changed, no code fence added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ← documentation | The runbook tells an operator which command to run on their own project. The risk surface is misleading/looping guidance and silent no-ops, not code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Denial-of-service (recovery loop) | Stage D recovery bullet (0.4.0-stuck) | mitigate | The rewrite removes the false "plain re-run drops 0007" claim and routes to `--migration 0010`, which — per Stage A + the amended flag — applies only 0010 and terminates at 0.5.0. Acceptance CONSISTENCY GATE fails if the looping claim is reinstated. |
| T-11-09 | Repudiation (silent no-op) | Stage D recovery bullet (manual-0.5.0) | mitigate | The amended `--migration NNNN` definition documents the boundary bypass so `--migration 0010` actually selects 0010 at 0.5.0 instead of reporting "up-to-date"; acceptance asserts the flag row carries boundary-override semantics. |
| T-11-10 | Tampering | SKILL.md edit scope | accept | Markdown-only edit to one file; acceptance forbids new executable/config blocks. No new file-write or execution surface is introduced. |
| T-11-SC | Tampering | package installs | accept | This plan adds no npm/pip/cargo installs — no supply-chain surface. |
</threat_model>

<verification>
- The Stage D recovery runbook is consistent with Stage A step 4's unchanged
  selection algorithm: no bare-re-run supersession claim survives, and both
  operator states resolve to `--migration 0010`.
- The Flags table's `--migration NNNN` row documents the boundary override that
  makes `--migration 0010` work for a project already at 0.5.0.
- Re-verification target: a reader following the runbook verbatim reaches 0.5.0
  with 0007's payload, with no re-abort loop and no silent "up-to-date" no-op.
</verification>

<success_criteria>
- SC#3 / MIGR-11: `update-codex-agenticapps-workflow/SKILL.md` Stage D documents a
  defined, NON-LOOPING recovery path for both stuck-operator states that is TRUE
  against Stage A's documented selection algorithm and the Flags table's
  `--migration NNNN` definition — not merely grep-plausible prose.
</success_criteria>

<output>
Create `.planning/phases/11-migration-chain-repair/11-04-SUMMARY.md` when done.
</output>
