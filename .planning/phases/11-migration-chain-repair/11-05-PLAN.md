---
phase: 11-migration-chain-repair
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - migrations/run-tests.sh
autonomous: true
requirements: [MIGR-10]
must_haves:
  truths:
    - "test_migration_0010 EXECUTES its extracted pre-flight block against seeded-version sandboxes (like test_migration_0009 does), so migration 0010's version-floor regex is proven correct by execution — not merely present as text: a project at 0.4.0/0.5.0 is accepted, a project below (0.3.0) or above (0.6.0) the 0.4.0→0.5.0 slot is rejected (non-zero exit)."
    - "A mutation to 0010's version-floor regex (e.g. `^0\\.(4|5)\\.0$` → `0\\.(4|6)\\.0`) makes at least one seeded-version assertion go RED — the version-floor is now mutation-proven, closing WR-02."
    - "test_migration_0010's D-06 delivery block asserts 0010's `<repo-name>` placeholder resolved in the written `.planning/config.json` (no literal `<repo-name>` remains), matching the check test_migration_0007 already makes and the 'ALWAYS true on success' claim in 0010's own Post-checks — closing WR-03."
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "Hardened test_migration_0010: executes the extracted pre-flight against seeded-version sandboxes and asserts <repo-name> placeholder resolution"
      contains: "extract_preflight_block"
  key_links:
    - from: "test_migration_0010 version-floor assertions"
      to: "migration 0010's extracted pre-flight block (pf_block)"
      via: "execute pf_block against sandboxes seeded at 0.3.0/0.4.0/0.5.0/0.6.0 and assert accept/reject"
      pattern: "pf_block"
    - from: "test_migration_0010 D-06 delivery block"
      to: "0010 Step 1 config merge output (.planning/config.json)"
      via: "assert <repo-name> resolved, no placeholder left"
      pattern: "<repo-name>"
---

<objective>
Close the two WARNING-level coverage gaps in 11-VERIFICATION.md that sit on
migration 0010's fixture (MIGR-10). These are NOT part of the SC#3 blocker
(that is plan 11-04) but are folded here because both target the same function
(`test_migration_0010` in `migrations/run-tests.sh`), share the "prove the
extracted block by execution" theme, and touch a file with zero overlap with
11-04's SKILL.md edit — so they run in the same wave, in parallel, without
scope-mixing.

- WR-02: `test_migration_0010` extracts its pre-flight block (`pf_block`) and
  substring-checks it (D-07) but NEVER executes it against a sandbox — unlike
  `test_migration_0009`, which evals its extracted pre-flight against
  seeded-version project dirs. The version-floor regex that is the entire point
  of MIGR-10 is proven present as text, not proven correct by execution; a
  mutation like `0\.(4|6)\.0` would survive the suite.
- WR-03: the D-06 delivery assertions omit the `<repo-name>` placeholder-resolution
  check that 0010's own Post-checks calls "ALWAYS true on success" and that
  `test_migration_0007` already asserts.

Purpose: make MIGR-10's "mutation-proven" framing true for the version-floor,
and bring 0010's delivery coverage to parity with 0007's.
Output: a hardened `test_migration_0010` (execution-backed floor + placeholder
assertion), suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-migration-chain-repair/11-CONTEXT.md
@.planning/phases/11-migration-chain-repair/11-PATTERNS.md
@.planning/phases/11-migration-chain-repair/11-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the working tree. -->

Current test_migration_0010 (migrations/run-tests.sh:4834-4970):
- Line 4852: `pf_block="$(extract_preflight_block "$MIGRATION_0010" 2>/dev/null)"`
- Line 4859: `assert_extracted_shape "0010 Pre-flight" "$pf_block" '.codex/workflow-version.txt'`
- Line 4877: substring-only D-07 check (`grep -q 'skills/agentic-apps-workflow'`)
  => pf_block is EXTRACTED and SHAPE/SUBSTRING-checked but never executed. WR-02.

Execution precedent — test_migration_0009 evals its extracted pre-flight against
seeded sandboxes (migrations/run-tests.sh:3619-3658, and calls at 4367/4396/4405/4711):
- `pf_block="$(extract_preflight_block "$MIGRATION_0009" 2>/dev/null)"`
- helper `_m0009_apply "$project_dir" "$home_dir" "$pf_block"` runs the block and
  returns its exit code; sandboxes are seeded to different versions to assert the
  floor accepts/rejects. Mirror this shape for 0010 (build the equivalent runner
  or reuse the helper if in scope).

<repo-name> placeholder assertion precedent — test_migration_0007
(migrations/run-tests.sh:837-845):
  jq -e '.knowledge_capture.note | endswith("/codex-workflow.md")' .planning/config.json
  && ! grep -qF '<repo-name>' .planning/config.json
  => copy this shape into 0010's D-06 block (adjust the endswith to 0010's
     sandbox repo dir name). WR-03.

Migration 0010 pre-flight contract (migrations/0010-heal-0007-knowledge-capture.md):
- version-floor regex accepts 0.4.0 (fresh) and 0.5.0 (idempotent re-apply):
  `grep -qE '^0\.(4|5)\.0$' .codex/workflow-version.txt` (verbatim reuse of 0008's
  pattern per 11-CONTEXT.md D-01, refloored to 0.4/0.5).
- pre-flight ALSO requires: `.git` dir, `jq` on PATH, and two templates under
  `$CODEX_HOME/skills/setup-codex-agenticapps-workflow/templates/`
  (`config-knowledge-capture.json`, `agents-md-additions.md`). The seeded-version
  sandbox must satisfy all of these so the ONLY variable under test is the version
  file (isolate the floor regex).
- 0010 Post-checks (migrations/0010-heal-0007-knowledge-capture.md:207-212) assert
  `! grep -qF '<repo-name>' .planning/config.json` as "ALWAYS true on success".

House rule (migrations/test-fixtures/README.md): NO static fixture files — every
sandbox is synthesized inline via `mktemp -d` + heredocs inside the test function.
</interfaces>

@migrations/run-tests.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute test_migration_0010's extracted pre-flight against seeded-version sandboxes (WR-02)</name>
  <files>migrations/run-tests.sh</files>
  <behavior>
    - Seed a sandbox (git repo, jq present, both CODEX templates present) at
      `.codex/workflow-version.txt` = 0.4.0 → executing pf_block exits 0 (accepted).
    - Same sandbox seeded at 0.5.0 → pf_block exits 0 (idempotent re-apply accepted).
    - Seeded at 0.3.0 → pf_block exits non-zero (below floor, rejected).
    - Seeded at 0.6.0 → pf_block exits non-zero (above 0010's slot, rejected — this
      is the assertion the `0\.(4|6)\.0` mutation flips).
    - Mutation-proof: mutating 0010's floor regex to `0\.(4|6)\.0` turns at least the
      0.5.0 (accept→reject) and 0.6.0 (reject→accept) assertions RED.
  </behavior>
  <read_first>
    - migrations/run-tests.sh:4834-4970 (current test_migration_0010 — the pf_block extraction at 4852, shape gate at 4859; insertion site for the new execution assertions)
    - migrations/run-tests.sh:3619-3660 (test_migration_0009's pf_block extraction + shape gate)
    - migrations/run-tests.sh:4360-4410 (test_migration_0009's `_m0009_apply "$dir" "$home" "$pf_block"` execution calls against seeded sandboxes — the pattern to mirror)
    - migrations/run-tests.sh:148-160 (extract_preflight_block definition)
    - migrations/0010-heal-0007-knowledge-capture.md pre-flight section (the floor regex + git/jq/template requirements the sandbox must satisfy)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md:36-50 (D-01 verbatim floor reuse; D-05 mutation-proof discipline)
  </read_first>
  <action>
    Inside `test_migration_0010`, after the existing shape gate on `pf_block`
    (run-tests.sh:4859), add an execution-backed floor assertion, mirroring
    test_migration_0009's `_m0009_apply`-against-seeded-sandbox pattern (build the
    equivalent small runner for 0010, or reuse the 0009 helper if it is
    function-scope-visible). For each seeded version in {0.3.0, 0.4.0, 0.5.0,
    0.6.0}: synthesize a sandbox (mktemp -d; `git init`; write both CODEX
    templates under `$CODEX_HOME/skills/setup-codex-agenticapps-workflow/templates/`;
    NO local `skills/` tree per D-07) that differs ONLY in
    `.codex/workflow-version.txt`, execute the extracted `pf_block` in it, capture
    the exit code, and assert: 0.4.0 and 0.5.0 → exit 0; 0.3.0 and 0.6.0 →
    exit non-zero. Use the existing PASS/FAIL counters and `assert_extracted_shape`
    gate so a silently-empty pf_block FAILS loudly (Pitfall #5). Do NOT
    hand-transcribe the pre-flight — it must be the extracted block. Do NOT
    manufacture a `skills/` tree in any sandbox. Skip `jq`-gated setup only if the
    harness's existing jq-availability guard is triggered (match the sibling
    fixtures' behavior), otherwise ensure jq is present so the version file is the
    sole variable. Register nothing new in dispatch — 0010 is already wired
    (run-tests.sh:5021). Then perform the mutation ritual (D-05): comment out /
    reflavor 0010's floor regex, run `bash migrations/run-tests.sh 0010`, observe
    RED on the 0.5.0/0.6.0 assertions, restore, observe GREEN; record both
    transcripts in the SUMMARY for the verifier to independently re-run.
  </action>
  <acceptance_criteria>
    - `test_migration_0010` EXECUTES the extracted `pf_block` (not a
      hand-transcribed copy) against at least four sandboxes that differ only in
      `.codex/workflow-version.txt` (0.3.0, 0.4.0, 0.5.0, 0.6.0), asserting
      accept(0)/reject(non-zero) per the 0.4.0→0.5.0 floor. Verify the new code
      references `pf_block` in an execution context (eval / a runner call), not
      only in a `grep`/substring context.
    - MUTATION-PROOF (the anti-shallow check): with 0010's floor regex mutated to
      `0\.(4|6)\.0`, `bash migrations/run-tests.sh 0010` exits non-zero (RED) on the
      0.5.0 and/or 0.6.0 assertion; with the regex restored it exits 0 (GREEN). The
      SUMMARY records both transcripts; the assertion is NOT satisfiable by the
      pre-existing text-substring D-07 check alone.
    - No local `skills/` tree is created in any 0010 sandbox (D-07); the pre-flight
      is obtained via `extract_preflight_block`, gated by `assert_extracted_shape`
      before execution.
    - `bash migrations/run-tests.sh 0010` is GREEN on the unmutated tree; full
      suite (`bash migrations/run-tests.sh`) remains 0 FAIL.
    - Only migrations/run-tests.sh changed.
  </acceptance_criteria>
  <verify>
    <automated>bash migrations/run-tests.sh 0010 && grep -n "pf_block" migrations/run-tests.sh | grep -qiE 'eval|_m00.*apply|apply.*pf_block|run.*pf_block'</automated>
  </verify>
  <done>test_migration_0010 executes its extracted pre-flight against 0.3.0/0.4.0/0.5.0/0.6.0 sandboxes and asserts the floor; the `0\.(4|6)\.0` mutation drives RED (transcript in SUMMARY), the restore drives GREEN; no `skills/` tree is manufactured; suite green; only run-tests.sh changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert 0010's &lt;repo-name&gt; placeholder resolution in the D-06 delivery block (WR-03)</name>
  <files>migrations/run-tests.sh</files>
  <behavior>
    - After 0010's Step 1 config merge is applied in the D-06 sandbox, the written
      `.planning/config.json` contains NO literal `<repo-name>` string.
    - The resolved value ends with the sandbox's actual repo directory name (parity
      with test_migration_0007's `endswith("/codex-workflow.md")` check, adjusted to
      the 0010 sandbox dir name).
    - Breaking 0010's placeholder resolution (leaving `<repo-name>` unresolved)
      turns this assertion RED.
  </behavior>
  <read_first>
    - migrations/run-tests.sh:4900-4970 (test_migration_0010's D-06 delivery-assertion block — where the config-block delivery is checked; insertion site)
    - migrations/run-tests.sh:837-845 (test_migration_0007's `<repo-name>` placeholder assertion — the exact shape to mirror)
    - migrations/0010-heal-0007-knowledge-capture.md:207-212 (Post-checks: `! grep -qF '<repo-name>'` "ALWAYS true on success")
    - .planning/phases/11-migration-chain-repair/11-VERIFICATION.md:99 (WR-03 description)
  </read_first>
  <action>
    In `test_migration_0010`'s D-06 block, after the assertion that 0010's Step 1
    config block was delivered to `.planning/config.json`, add the `<repo-name>`
    placeholder-resolution assertion mirroring test_migration_0007
    (run-tests.sh:837-845): assert (a) no literal `<repo-name>` remains
    (`! grep -qF '<repo-name>' .planning/config.json`), and (b) the resolved note
    path ends with the sandbox's real repo directory name (adapt 0007's
    `jq -e '... | endswith("/<dir>.md")'` to the 0010 sandbox's mktemp dir
    basename). Use the existing PASS/FAIL counters. Documentation of intent: this
    brings 0010's delivery coverage to parity with 0007 and enforces 0010's own
    "ALWAYS true on success" Post-check. No new sandbox needed — reuse the D-06
    delivery sandbox already built in the fixture.
  </action>
  <acceptance_criteria>
    - test_migration_0010's D-06 block asserts BOTH `! grep -qF '<repo-name>'
      .planning/config.json` AND that the resolved note path ends with the sandbox
      repo dir name — mirroring run-tests.sh:837-845. Verify the new assertion
      references `<repo-name>` within the `test_migration_0010()` function body (function-scoped, not a fixed line range).
    - The assertion is inside the already-built D-06 delivery sandbox (no
      hand-transcribed config); it FAILS if `<repo-name>` is left unresolved.
    - `bash migrations/run-tests.sh 0010` GREEN; full suite 0 FAIL.
    - Only migrations/run-tests.sh changed.
  </acceptance_criteria>
  <verify>
    <automated>bash migrations/run-tests.sh 0010 && awk '/^test_migration_0010\(\)/,/^}/' migrations/run-tests.sh | grep -qF '<repo-name>'</automated>
  </verify>
  <done>test_migration_0010's D-06 block asserts 0010's `<repo-name>` placeholder resolved (no literal placeholder left; note path ends with the sandbox dir name), matching 0007's coverage and 0010's Post-checks; suite green; only run-tests.sh changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ← migration document | The fixture extracts and executes a bash block from a `.md` migration under `mktemp` sandboxes. Untrusted-input surface is the extracted block; it runs only in throwaway sandboxes, never against the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | executed pre-flight block runs in a sandbox | mitigate | The pre-flight is executed only inside `mktemp -d` sandboxes that carry no real project state and no local `skills/` tree (D-07); `assert_extracted_shape` gates against an empty/degenerate block before any execution, so a silently-empty extraction FAILS loudly rather than vacuously passing. |
| T-11-12 | Spoofing (false GREEN) | version-floor assertion | mitigate | Mutation ritual (D-05) proves the assertion goes RED under a floored-regex mutation and GREEN on restore; the verifier independently re-runs the cycle rather than trusting the SUMMARY transcript. |
| T-11-SC | Tampering | package installs | accept | This plan adds no npm/pip/cargo installs — it only exercises `jq`/`git`, already required by the migration suite. No supply-chain surface. |
</threat_model>

<verification>
- `bash migrations/run-tests.sh 0010` is GREEN and the full suite reports 0 FAIL.
- The version-floor is mutation-proven: `0\.(4|6)\.0` mutation drives RED on the
  0.5.0/0.6.0 seeded-version assertions; restore drives GREEN (verifier re-runs).
- The D-06 block now asserts `<repo-name>` placeholder resolution, at parity with
  test_migration_0007 and 0010's own "ALWAYS true on success" Post-check.
</verification>

<success_criteria>
- WR-02 closed: test_migration_0010 proves 0010's version-floor regex by execution
  (accept 0.4.0/0.5.0, reject 0.3.0/0.6.0), mutation-proven.
- WR-03 closed: test_migration_0010 asserts 0010's `<repo-name>` placeholder
  resolves with no literal placeholder remaining.
- These are coverage hardenings on MIGR-10 (already SATISFIED); no phase success
  criterion regresses; suite stays green.
</success_criteria>

<output>
Create `.planning/phases/11-migration-chain-repair/11-05-SUMMARY.md` when done.
</output>
