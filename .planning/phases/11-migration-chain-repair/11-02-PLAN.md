---
phase: 11-migration-chain-repair
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - migrations/run-tests.sh
autonomous: true
requirements: [MIGR-08]
must_haves:
  truths:
    - "MIGR-08's fixture extracts migration 0008's Step 4 Apply block via extract_step_block (never a hand-copied transcription) (D-05)"
    - "The extracted Step 4 Apply block is executed against a sandbox whose .codex/workflow-version.txt is seeded at the pre-migration value 0.5.0 — payload/version scope only, no multi-migration E2E chain harness (D-05, D-06)"
    - "After execution, .codex/workflow-version.txt equals exactly 0.6.0 by content equality (cksum/diff), not a grep -q substring match"
    - "Breaking the write line makes the fixture RED; restoring it makes the fixture GREEN"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "The MIGR-08 Step-4 extract-execute-assert fixture + dispatch registration"
      contains: "extract_step_block"
  key_links:
    - from: "migrations/run-tests.sh MIGR-08 fixture"
      to: "migrations/0008-plan-review-gate.md Step 4 Apply block"
      via: "extract_step_block <doc> 4 Apply"
      pattern: "extract_step_block .*0008.* 4 Apply"
---

<objective>
Deliver MIGR-08: a mutation-proven fixture that closes the last residual of the
can't-fail-assertion class Phase 9.1 existed to close. The fixture extracts
migration 0008's Step 4 Apply block from the real document via `extract_step_block`
(never transcribed), executes it against a sandbox seeded at the pre-migration
version 0.5.0, and asserts EXACT `.codex/workflow-version.txt` content equality
against 0.6.0.

Purpose: prove the version-record write actually happens, and that the assertion
can fail — the exact discipline the milestone enforces.
Output: a new fixture in migrations/run-tests.sh, mutation-proven RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-migration-chain-repair/11-CONTEXT.md
@.planning/phases/11-migration-chain-repair/11-PATTERNS.md
@.planning/phases/11-migration-chain-repair/11-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

extract_step_block <doc_path> <step_number> <label>   (migrations/run-tests.sh:104-117)
assert_extracted_shape <label> <text> <required_substring>  (migrations/run-tests.sh:142+)
assert_check <label> <check> <fixture_dir> applied|not-applied  (vendor/agenticapps-shared/migrations/lib/helpers.sh:49-79)

migration 0008 Step 4 (migrations/0008-plan-review-gate.md:322-334):
  Idempotency check: grep -q '^0.6.0$' .codex/workflow-version.txt
  Pre-condition: .codex/ exists
  Apply: echo "0.6.0" > .codex/workflow-version.txt
  Rollback: echo "0.5.0" > .codex/workflow-version.txt

0009's live extract-and-execute call shape to copy (migrations/run-tests.sh:3476-3479).
Dispatch registration precedent (migrations/run-tests.sh:4668-4692).
</interfaces>

@migrations/0008-plan-review-gate.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MIGR-08 fixture — extract, execute, and assert exact equality on 0008 Step 4 (mutation-proven)</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh:100-160 (extract_step_block, assert_extracted_shape)
    - migrations/run-tests.sh:3476-3479 (0009's extract-and-execute call shape — the exact pattern to copy)
    - migrations/run-tests.sh:1651-1727 (0008 no-scaffolder-tree sandbox construction)
    - migrations/run-tests.sh:4668-4692 (dispatch registration)
    - migrations/0008-plan-review-gate.md:322-334 (Step 4 — the block being extracted and asserted against)
    - vendor/agenticapps-shared/migrations/lib/helpers.sh:49-79 (assert_check / run_check)
    - .planning/phases/11-migration-chain-repair/11-CONTEXT.md (D-05)
  </read_first>
  <behavior>
    - Extraction: extract_step_block "$MIGRATION_0008" 4 Apply returns a non-empty block containing `.codex/workflow-version.txt`; assert_extracted_shape gates it before execution.
    - Pre-state: sandbox `.codex/workflow-version.txt` seeded at 0.5.0 (the pre-migration value per D-05); the Step 4 idempotency check `grep -q '^0.6.0$'` is not-applied against this pre-state.
    - Post-state: after executing the extracted Apply block, `.codex/workflow-version.txt` content equals exactly `0.6.0` — asserted by content equality (cksum or diff against a `printf '0.6.0\n'` reference), NOT `grep -q`.
    - Mutation: with the migration's `echo "0.6.0" > ...` write line commented out, the fixture is RED; restored, GREEN.
  </behavior>
  <action>
    Add a dedicated fixture function `test_migration_0008_step4_write()` to
    migrations/run-tests.sh, placed adjacent to `test_migration_0008`. Define
    `MIGRATION_0008="$REPO_ROOT/migrations/0008-plan-review-gate.md"`. Extract the
    Step 4 Apply block with `extract_step_block "$MIGRATION_0008" 4 Apply` and gate
    it with `assert_extracted_shape` requiring the substring
    `.codex/workflow-version.txt` (a silently-empty extraction must FAIL loudly, not
    vacuously pass — Pitfall #5). Create a `mktemp -d` sandbox with a `.codex/`
    directory; seed `.codex/workflow-version.txt` with `0.5.0` (the pre-migration
    value per D-05 — this is the no-scaffolder-tree shape; do NOT manufacture a local
    skills/ tree). Assert via assert_check that the Step 4 idempotency check
    (`grep -q '^0.6.0$' .codex/workflow-version.txt`) is `not-applied` on the
    pre-state. Execute the extracted Apply block inside the sandbox (cd-isolated).
    Then assert EXACT content equality: compare the sandbox's
    `.codex/workflow-version.txt` against a reference `printf '0.6.0\n'` using `cmp`
    or `cksum` (D-05 — exact equality, never `grep -q`, which would pass on any file
    merely containing the substring). Register dispatch under a stable filter key
    (e.g. `0008-step4`) mirroring run-tests.sh:4672-4674. Keep all writes inside the
    sandbox; `trap 'rm -rf "$tmp"' RETURN`.

    Mutation-proof ritual (D-05, perform during authoring and record in the SUMMARY):
    comment out migration 0008's `echo "0.6.0" > .codex/workflow-version.txt` Apply
    line, re-run the fixture, observe RED (exact-equality assertion fails); restore
    the line, re-run, observe GREEN. Note in the SUMMARY that the verifier
    independently re-runs this cycle rather than trusting the claim.
  </action>
  <acceptance_criteria>
    - The fixture calls `extract_step_block "$MIGRATION_0008" 4 Apply` — verify with `grep -c 'extract_step_block .*4 Apply' migrations/run-tests.sh` ≥ 1; there is NO hand-copied `echo "0.6.0"` literal used as the executed block (the executed content comes only from extraction).
    - The extracted block is passed through `assert_extracted_shape` before execution.
    - The sandbox seeds `.codex/workflow-version.txt` at `0.5.0` before execution.
    - The post-execution assertion uses `cmp`/`cksum`/`diff` for exact content equality against `0.6.0`, not `grep -q` (`grep -c 'grep -q' within the new function == 0 for the equality assertion`).
    - The sandbox has no local `skills/` directory.
    - `bash migrations/run-tests.sh 0008-step4` exits 0 (GREEN).
    - Mutation evidence recorded in the SUMMARY: write line commented → RED; restored → GREEN.
    - `bash migrations/run-tests.sh` (unfiltered) exits 0.
  </acceptance_criteria>
  <done>The MIGR-08 fixture executes only the `extract_step_block "$MIGRATION_0008" 4 Apply` block (assert_extracted_shape-gated, no transcription) against a 0.5.0-seeded no-skills-tree sandbox and asserts exact `0.6.0` content equality via cmp/cksum/diff; `bash migrations/run-tests.sh 0008-step4` and the unfiltered suite exit 0, and the write-line-comment-out RED → restore GREEN cycle is recorded in the SUMMARY.</done>
  <verify>
    <automated>bash migrations/run-tests.sh 0008-step4 && bash migrations/run-tests.sh</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mktemp sandbox | The fixture executes an Apply block extracted from a migration document; all writes must stay inside the `mktemp -d` sandbox. |
| migration document → executed shell | extract_step_block pulls text from 0008 and the fixture executes it — the document is the trust source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | executing the extracted Step 4 Apply block | mitigate | block is extracted from the repo's own committed 0008 doc (trusted), gated by assert_extracted_shape (non-empty + must contain `.codex/workflow-version.txt`) before execution, and run cd-isolated inside the mktemp sandbox seeded only at 0.5.0. |
| T-11-05 | Tampering | fixture sandbox writes escaping the sandbox | mitigate | sandbox is `mktemp -d` with `trap rm -rf RETURN`; the executed block writes only the relative `.codex/workflow-version.txt` path; equality assertion reads only inside the sandbox. |
| T-11-SC | Tampering | package installs | accept | this plan adds no npm/pip/cargo installs — no supply-chain surface. |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` exits 0.
- The executed block is provably the extracted one (no transcription), gated by assert_extracted_shape.
- Mutation cycle RED→GREEN recorded and independently re-runnable by the verifier.
</verification>

<success_criteria>
- MIGR-08's fixture extracts 0008 Step 4 via extract_step_block, executes it against a 0.5.0-seeded sandbox, and asserts exact 0.6.0 content equality.
- Breaking the write line is observed RED; restoring is observed GREEN.
- Full suite exits 0.
</success_criteria>

<output>
Create `.planning/phases/11-migration-chain-repair/11-02-SUMMARY.md` when done.
</output>
