---
phase: 09-region-aware-11-placement
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-03"]
files_modified:
  - migrations/0009-spec-11-region-aware-placement.md
  - skills/agentic-apps-workflow/SKILL.md
autonomous: true
requirements: [ANCHOR-01, ANCHOR-02, ANCHOR-05, MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05, MIGR-06, MIGR-07, MIGR-08, MIGR-09]

must_haves:
  truths:
    - "A correctly anchored §11 block carrying current provenance is left byte-identical (State A)"
    - "A §11 block inside a gitnexus region is moved above the region, region intact and paired (State B)"
    - "An absent §11 block is injected at the region-aware anchor (State C)"
    - "A §11 heading with no provenance aborts with exit 3 and leaves the file unmodified (State D)"
    - "Re-running 0009 on an already-healed file is a no-op and does not eat the GitNexus region"
    - "A healthy-but-off-anchor block is left where it is"
    - "A zero-byte or truncated mirror is refused with exit 3 before any file surgery happens"
    - "After the migration applies, .codex/workflow-version.txt reads 0.7.0"
    - "The full suite is GREEN — every fixture 09-03 wrote RED now passes"
  artifacts:
    - path: "migrations/0009-spec-11-region-aware-placement.md"
      provides: "The region-aware §11 placement migration, 0.6.0 → 0.7.0"
      contains: "to_version: 0.7.0"
      min_lines: 120
    - path: "skills/agentic-apps-workflow/SKILL.md"
      provides: "This repo's own scaffolder version, coupled to the newest migration's to_version"
      contains: "version: 0.7.0"
  key_links:
    - from: "migrations/0009-spec-11-region-aware-placement.md"
      to: "skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md"
      via: "re-vendors §11 by streaming the mirror (D-27), never transcribing it"
      pattern: "getline line <"
    - from: "migrations/0009-spec-11-region-aware-placement.md"
      to: "AGENTS.md"
      via: "strip + region-aware insert, atomic temp-file replace"
      pattern: "AGENTS\\.md\\.0009\\.(strip|tmp)"
    - from: "migrations/run-tests.sh"
      to: "migrations/0009-spec-11-region-aware-placement.md"
      via: "test_migration_0009 extracts and executes this document's own shell"
      pattern: "MIGRATION_0009"
---

<objective>
Author `migrations/0009-spec-11-region-aware-placement.md` and turn plan 09-03's RED fixture
suite GREEN.

This is a PORT of working, six-repo-validated, TDD'd code — `claude-workflow`'s migration 0029
at the pin `8520f90` — not a from-scratch derivation from prose. **Diff against 0029; do not
re-derive it.** Its awk already carries the terminator alternation this repo's D-24 was
originally missing, because upstream hit that bug and fixed it.

Two gates opened this plan and both must be confirmed before the first line is written:
09-01's replay evidence exists and shows PASS (ROADMAP hard ordering 1 — validate before you
write), and 09-03's suite was observed RED (hard ordering 2 — RED before GREEN).

Purpose: close a latent block-destruction defect for every project this host scaffolds.
Output: the migration document, this repo's own version bump, and a green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-region-aware-11-placement/09-CONTEXT.md
@.planning/phases/09-region-aware-11-placement/09-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md
@.planning/phases/09-region-aware-11-placement/09-01-SUMMARY.md
@.planning/phases/09-region-aware-11-placement/09-03-SUMMARY.md

**Upstream reference is PINNED (D-48):** `claude-workflow @ 8520f90d235e0c50b0484b170d595ab6f2cd1173`
("fix(migration): 0029 — close truncated-mirror gap, add its missing test, fix doc drift (third
review)"). Read pinned content with `git -C ../claude-workflow show 8520f90:<path>`. Verified at
plan time: upstream's working tree at 0029's path is byte-identical to the pin, but upstream HEAD
has moved past it. **Do NOT absorb any upstream change after the pin.** 0029 changed four times
during this phase's planning session alone; if you find upstream has moved, that is a note for the
follow-up, not a licence to widen scope mid-execution.

**Do NOT hand-transcribe awk from memory.** Read the pinned document and adapt deliberately. Two
deliberate divergences from 0029 are LOCKED and must not be ported away:
- **D-47 — Rollback is `git checkout AGENTS.md`**, 0004's precedent. Do NOT port 0029's custom
  Rollback awk (`0029:291-326`). That awk is exactly the complexity that required upstream's
  fixture 08 to catch a file-destroying bug; `git checkout` is structurally immune because there is
  no terminator to get wrong. 09-03's case 08 is the regression guard on this choice.
- **D-44 — no anchor-parity guard.** 0029's prose mentions a `check-snapshot-parity`/`anchor-parity`
  build guard. That exists because claude-workflow is a SNAPSHOT host; this host is a REPLAY host,
  and `spec/08-migration-format.md:27-33` makes both conformant. There is no second anchor here to
  guard (setup has no independent placement logic — D-43). Do not port that prose or that guard.

**Migrations 0001 and 0004 are IMMUTABLE.** They appear in `read_first` only and are absent from
`files_modified`. Fix forward: 0009 is a new migration. Never edit them.

**Drift coupling is a hard fail and it fires the moment this document exists.**
`run_drift_test` (`vendor/agenticapps-shared/migrations/lib/drift-test.sh`) compares
`skills/agentic-apps-workflow/SKILL.md`'s `version:` against the NEWEST migration file's
`to_version`. Creating `migrations/0009-*.md` with `to_version: 0.7.0` makes 0009 the newest, so
`migrations/run-tests.sh drift` goes RED unless SKILL.md reads `0.7.0`. That is why Task 1 creates
the frontmatter and bumps SKILL.md together — MIGR-09 is not a bookkeeping afterthought, it is a
same-commit requirement.

**Expected redness during this plan:** after Task 1, `run-tests.sh 0009` is still RED (Step 1 does
not exist yet). GREEN is Task 2's deliverable. `run-tests.sh drift` must be green from Task 1 onward.
</context>

<interfaces>
<!-- Document shape contract. Plan 09-03's extractors are ALREADY WRITTEN against these -->
<!-- exact markers. Deviating from them turns the suite red for a spurious reason. -->

  ## Pre-flight                              → consumed by extract_preflight_block
  <bash-fenced block>                        (first fence under that heading)

  ### Step 1: <title>                        → scope for extract_step_block <doc> 1 <label>
  **Idempotency check:**                     → label "Idempotency check", first fence after it
  <bash-fenced block>
  **Apply:**                                 → label "Apply", first fence after it
  <bash-fenced block>
  **Rollback:** `git checkout AGENTS.md`.    → prose one-liner, NOT a fenced block (D-47)

  ### Step 2: <title>                        → terminates Step 1's extraction scope
  ### Step 3: <title>

<!-- Constants (verified at plan time) -->
  PROV literal: <!-- spec-source: agenticapps-workflow-core@0.4.0 §11 -->   (D-29 — @0.4.0 is the
                block's CONTENT version and is unchanged; do NOT bump it to 0.7.0)
  MIRROR:  ${CODEX_HOME:-$HOME/.codex}/skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md
           79 lines; exactly one `## ` (its own L1); four `### `; last is `### 4. Goal-Driven Execution` at L57.
  Temp files: AGENTS.md.0009.strip, AGENTS.md.0009.tmp   (0004's `NNNN.tmp` convention)

<!-- The pinned awk to adapt — all quoted verbatim in 09-PATTERNS.md with file:line -->
  strip pass ................ pinned 0029:192-210   (swallowed_own_h2 state machine)
  insert pass / anchor ...... pinned 0029:226-246   (the D-21 rule + END EOF fallback)
  region predicate .......... pinned 0029:119-130   (single-pass r/f scan, fail-closed)
  three-branch dispatcher ... pinned 0029:155-172
  pre-flight mirror guards .. pinned 0029:80-109    (test -s, then tail-sentinel grep)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Frontmatter, prose, pre-flight, and this repo's own version bump (MIGR-01, MIGR-09, D-28.1)</name>

  <files>migrations/0009-spec-11-region-aware-placement.md, skills/agentic-apps-workflow/SKILL.md</files>

  <read_first>
    - **GATE — confirm before writing anything.** Read
      `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md`: it must contain a
      PASS for `ZERO CHURN` and for `ABOVE REGION`, and a `## RED OBSERVED (TEST-02)` section. If
      either is missing, STOP and report — the ROADMAP's hard orderings are not satisfied and this
      plan must not proceed.
    - `migrations/0004-revendor-spec-11.md` lines 1-52 — IMMUTABLE, read-only. The frontmatter
      shape and the pre-flight shape to adapt. Note `:44`'s `test -f AGENTS.md || exit 1` — that
      abort is the thing D-33 REJECTS; do not port it.
    - `migrations/0008-plan-review-gate.md` lines 1-40 — D-40's frontmatter model (`id`, `slug`,
      `title`, `from_version`, `to_version`, `applies_to`, `requires`, `optional_for`) and the
      "Why a 0.x minor bump" / "Supported upgrade floor" prose D-42 asks for.
    - Pinned pre-flight:
      `git -C ../claude-workflow show 8520f90:migrations/0029-region-aware-spec-11-placement.md`
      lines 68-118 — the version gate and BOTH mirror guard layers, with the comments explaining
      why each exists. Read the comments; they are the rationale you must localize, not skip.
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-28.1, D-33, D-39, D-40, D-42.
    - `skills/agentic-apps-workflow/SKILL.md` lines 1-10 — the file being modified; `:3` currently
      reads `version: 0.6.0`.
    - `skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md`
      line 57 — confirm the tail sentinel `### 4. Goal-Driven Execution` before hardcoding its grep.
  </read_first>

  <action>
    Create `migrations/0009-spec-11-region-aware-placement.md` with frontmatter per D-40:
    `id: 0009`, `slug: spec-11-region-aware-placement`, a title naming the region-aware §11
    placement heal, `from_version: 0.6.0`, `to_version: 0.7.0`, `applies_to` listing `AGENTS.md`,
    `skills/agentic-apps-workflow/SKILL.md` and `.codex/workflow-version.txt`, `requires: []`,
    `optional_for: []`. Note 0029 has no `optional_for` key — this repo's convention includes it,
    so follow 0004/0008, not 0029, for frontmatter.

    Write the document's opening prose: what the defect is (a §11 block anchored at the first `## `
    lands INSIDE a leading GitNexus region, where gitnexus regeneration destroys it), that on THIS
    host the defect is LATENT (AGENTS.md's region sits at L271, not at the top — there is no broken
    repo here to repair), and the anchor rule in one sentence (D-21: insert immediately before the
    first line that is either a `## ` heading or an anchored `<!-- gitnexus:start -->` marker,
    whichever comes first; EOF if neither).

    State the corrected invariant explicitly, and state that it is WIDENED, not preserved:
    *the block is always followed by a `## ` line, an anchored `<!-- gitnexus:start -->` marker, or
    EOF*. Do not repeat the false "one-alternation delta, so the invariant survives" claim — it is
    false by construction, it was load-bearing, and ANCHOR-05 was corrected because of it. Add the
    "Why a 0.x minor bump" and "Supported upgrade floor" prose per D-42: `0.6.0 → 0.7.0`, single
    hop. Do NOT widen the floor to paper over the update skill's multi-hop chain-selection defect —
    that defect is deferred and does not block this migration, because every live project already
    sits at 0.6.0 after 0008.

    Write the `## Pre-flight` section as a single bash-fenced block containing, in order:
    (1) `test -d .git` guard, 0004's shape;
    (2) the version gate (D-39/MIGR-01) — `grep -qE '^version: 0\.(6\.0|7\.0)$'` against
        `skills/agentic-apps-workflow/SKILL.md`, accepting BOTH so an idempotent re-run on an
        already-migrated project does not abort; on failure echo the installed version and abort
        `exit 3`, pointing at `/update-codex-agenticapps-workflow` (this host's slug, NOT
        claude-workflow's `/update-agenticapps-workflow`);
    (3) mirror guard layer one — `test -s "$MIRROR" || { …; exit 3; }`. Adapt the pinned comment
        explaining why `test -f` is insufficient: it passes on a zero-byte file, exactly what an
        interrupted `git pull` in the scaffolder clone produces, and since D-27 makes the mirror the
        SOLE re-injection source, a zero-byte mirror would strip the project's §11 and inject
        nothing in its place — silently committing a maimed AGENTS.md;
    (4) mirror guard layer two — `grep -q '^### 4\. Goal-Driven Execution$' "$MIRROR" || { …; exit 3; }`.
        Adapt the pinned comment: non-empty is not the same as un-truncated. The block's heading is
        on line 1, so a mirror truncated to a few lines still satisfies `test -s` AND still
        satisfies Step 1's pre-`mv` shape assertion (which greps for that same line-1 heading) —
        both are single-point guards on a continuum. Assert the block's LAST section is present too;
        truncation loses the tail long before the head. Record explicitly that this does NOT
        re-introduce D-25's rejected content sentinel: D-25 rejected coupling the STRIP TERMINATOR
        to §11's last PROSE line (runaway-strip hazard); this is a read-only integrity check on a
        different file, anchored to a structural `### ` heading, that bounds nothing and cannot run
        away.
    Then state in prose, below the fence, that pre-flight is deliberately PERMISSIVE on the
    missing-AGENTS.md path (D-33): Step 1 emits an informational message and Step 2 still runs.
    Explain why, because this diverges from 0004:44 on purpose — the update engine marks a migration
    pending iff `installed >= from_version && installed < to_version`, so an abort here would strand
    the project at 0.6.0 permanently and 0010+ would never become pending.

    Finally, bump `skills/agentic-apps-workflow/SKILL.md` frontmatter `version: 0.6.0` → `version: 0.7.0`
    (MIGR-09). Leave `implements_spec: 0.4.0` UNCHANGED — D-17 from Phase 8 stands; that field
    tracks a full conformance audit, not one gate, and resolving it is explicitly out of scope. This
    bump must land in the SAME commit as the frontmatter above: `run_drift_test` compares SKILL.md's
    version against the newest migration's `to_version`, and 0009 becomes the newest the instant its
    file exists.
  </action>

  <verify>
    <automated>migrations/run-tests.sh drift; echo "drift-exit=$?"; migrations/run-tests.sh layout; echo "layout-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `migrations/run-tests.sh drift` exits 0 — the version coupling holds. A RED here means the
      SKILL.md bump and the frontmatter did not land together.
    - `migrations/run-tests.sh layout` exits 0.
    - `grep -c '^version: 0.7.0$' skills/agentic-apps-workflow/SKILL.md` returns 1, and
      `grep -c '^implements_spec: 0.4.0$' skills/agentic-apps-workflow/SKILL.md` returns 1
      (unchanged — D-17).
    - `grep -c '^to_version: 0.7.0$' migrations/0009-spec-11-region-aware-placement.md` returns 1
      and `grep -c '^from_version: 0.6.0$' …` returns 1.
    - Pre-flight guards are executable code, not prose:
      `awk '/^## Pre-flight/{f=1} f&&/^## Steps/{exit} f' migrations/0009-spec-11-region-aware-placement.md | grep -c 'test -s\|Goal-Driven Execution$'`
      returns at least 2, scoped to the Pre-flight section so the document's explanatory prose
      elsewhere cannot satisfy it.
    - D-33 honored: `awk '/^## Pre-flight/{f=1} f&&/^## Steps/{exit} f' migrations/0009-spec-11-region-aware-placement.md | grep -c 'test -f AGENTS.md'`
      returns 0 — 0004's stranding abort was NOT ported.
    - `git status --porcelain migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md`
      prints nothing.
    - Commit message uses the `feat(09-04):` prefix.
  </acceptance_criteria>

  <done>The migration document exists with correct frontmatter and both mirror guards; the drift test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Step 1 — the region-aware heal (GREEN; ANCHOR-01/02/05, MIGR-02..07, D-47)</name>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md` — the file being modified (Task 1's output).
    - **The pinned source of truth. Read it; do not transcribe from memory.**
      `git -C ../claude-workflow show 8520f90:migrations/0029-region-aware-spec-11-placement.md`
      — lines 119-130 (region predicate), 155-172 (three-branch dispatcher), 192-210 (strip pass),
      226-246 (insert pass). Lines 291-326 are its custom Rollback awk: read it to understand what
      you are deliberately NOT porting (D-47), then do not port it.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` §§ "The anchor rule (D-21)",
      "The strip boundary (D-24)", "The region predicate (D-32)", "The three-branch apply
      dispatcher (D-30)", "Rollback — locked to `git checkout AGENTS.md` (D-47)", and
      "The terminator-alternation checklist" — the last enumerates every awk site that must carry
      the alternation, and is ANCHOR-05's audit list.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md` — the empirical
      proof the rule you are about to write is the right one, including counter-case B's
      demonstration of what the narrow terminator does.
    - `migrations/validate-0009-anchor.sh` — 09-01's `candidate_strip` / `candidate_insert`, already
      validated against real files. Step 1's awk should be recognizably the same rule; a divergence
      means one of the two is wrong.
    - `migrations/0004-revendor-spec-11.md` lines 54-87 — IMMUTABLE, read-only. Its strip awk at
      `:68-74` is the content-sentinel shape D-25 REJECTS (`inblk {next}` until §11's last prose
      line matches — if that line ever drifts, the strip never terminates and deletes the rest of
      the file). Read it to know what NOT to write. Its `:87` `**Rollback:** git checkout AGENTS.md`
      is the precedent you DO follow.
    - `migrations/run-tests.sh` — `test_migration_0009` as 09-03 left it. Its extractions define the
      exact markers this Step must use.
  </read_first>

  <action>
    Add `### Step 1: Heal the §11 block's placement` to the migration document, in the document-shape
    contract from `<interfaces>`. This task turns 09-03's RED suite GREEN.

    **Idempotency check** (a bash-fenced block after the `**Idempotency check:**` line): adapt the pinned region
    predicate. Three conjoined conditions: `[ -f AGENTS.md ]`, then `grep -q` for the literal
    provenance `<!-- spec-source: agenticapps-workflow-core@0\.4\.0 §11 -->`, then a NEGATED
    single-pass awk that sets `r=1` on `/^<!-- gitnexus:start -->$/`, clears it on
    `/^<!-- gitnexus:end -->$/`, and sets `f=1` if the provenance regex is seen while `r` is set,
    exiting `f ? 0 : 1`. Both marker regexes MUST be anchored `^…$` — a substring match would treat
    a file that merely MENTIONS the marker in prose as region-led, which is 09-03's case 07.

    This single-pass scan is D-32's fail-closed rule reached without line-number arithmetic: if
    `gitnexus:end` never appears, `r` stays 1 to EOF, so a provenance line after an unterminated
    `gitnexus:start` is correctly judged in-region and the block is moved above the marker to
    safety. Comment the conjunction explicitly: provenance present AND block-not-in-region is the
    skip condition. Provenance alone must NOT short-circuit the heal — that conjunction is the whole
    point, and it is what 09-03's State B asserts returns non-zero. MIGR-07 falls out of this
    predicate rather than a special case (D-31): a healthy-but-off-anchor block has provenance and
    is not in a region, so it skips — do not add code to detect it and do not move it.

    **Apply** (a bash-fenced block after the `**Apply:**` line): adapt the pinned three-branch dispatcher (D-30).
    No per-state special-casing — three branches only:
    - Branch 1, no AGENTS.md: echo an informational message ("no AGENTS.md in project; §11 heal
      skipped") naming THIS host's skills (`setup-codex-agenticapps-workflow` /
      `update-codex-agenticapps-workflow`), and succeed. Do not abort (D-33).
    - Branch 2, State D: `grep -q '^## Coding Discipline (NON-NEGOTIABLE)$'` AND NOT provenance →
      echo an ABORT explaining the heading was hand-pasted outside this migration's management,
      offer the same two resolutions 0001's conflict block offers, and `exit 3`. The file must be
      untouched at this point — the check runs before any surgery.
    - Branch 3, States B and C unified (D-27): strip-if-a-managed-block-exists, then inject at the
      anchor. B and C are ONE code path because inject always re-vendors from the mirror; B simply
      strips first.

    In branch 3, the strip pass: adapt the pinned `swallowed_own_h2` state machine. Set `in_block`
    on the provenance regex; then, while `in_block && !swallowed_own_h2`, swallow the block's OWN
    `/^## Coding Discipline \(NON-NEGOTIABLE\)$/` heading and set the flag — without this the naive
    "stop at the first `/^## /`" terminates on the block's own heading instead of the block after
    it. Then terminate on `in_block && swallowed_own_h2 && (/^## / || /^<!-- gitnexus:start -->$/)`,
    RESETTING BOTH `in_block` and `swallowed_own_h2` at the terminator and printing the terminator
    line. The reset is not tidiness: a stale-true `swallowed_own_h2` makes a second block's own
    heading look like its terminator and leaves it orphaned — that is 09-03's case 09. Redirect to
    `AGENTS.md.0009.strip` and gate on `[ -s AGENTS.md.0009.strip ]`.

    **The terminator alternation `(/^## / || /^<!-- gitnexus:start -->$/)` is ANCHOR-05 and it is
    the highest-severity mechanic in this phase.** A `/^## /`-only terminator skips straight past
    `<!-- gitnexus:start -->` on a file already healed into State B and consumes the ENTIRE GitNexus
    region plus everything up to the next `## ` or EOF — on MIGR-06's idempotent re-run. That is
    D-25's runaway-strip hazard resurfacing inside the structural boundary chosen to avoid it. The
    structural boundary is only safe once its terminator set equals the anchor's terminator set.
    09-01's counter-case B demonstrated this empirically against real files; do not re-litigate it.

    Do NOT port 0004's content-sentinel strip, and do NOT add a verbatim assertion gating the strip
    (D-26): the structural boundary is bounded by construction, so a drifted block cannot cause a
    runaway, and a drifted-but-managed block is 0004's problem — 0009 must not refuse to PLACE a
    block because its content drifted. Note in prose that a State-B move silently repairs a drifted
    block as a stated consequence (D-28.2), not an accident.

    In branch 3, the insert pass: adapt the pinned anchor awk. Condition
    `!inserted && (/^## / || /^<!-- gitnexus:start -->$/)` — the SAME alternation as the strip
    terminator, one rule at two sites. Print the provenance literal, stream the mirror with
    `while ((getline line < block_file) > 0) print line`, `close()` it, print one blank line, set
    `inserted`, then print the anchor line. The `END` block appends provenance + mirror at EOF when
    nothing matched (ANCHOR-02). Never `cat` the mirror inline and never transcribe §11's prose —
    D-19/the 0007 lesson: the payload comes from the mirror, always.

    Write to `AGENTS.md.0009.tmp`, then gate the `mv` on BOTH `[ -s AGENTS.md.0009.tmp ]` AND
    `grep -q '^## Coding Discipline (NON-NEGOTIABLE)$' AGENTS.md.0009.tmp` — the pre-`mv` shape
    assertion, D-36's prose-level twin: non-empty is not the same as correct, and a zero-byte mirror
    would otherwise produce non-empty-but-wrong output. Only then `mv` over AGENTS.md. Every failure
    branch must `rm -f` its temp files, echo an ABORT, and `exit 3` WITHOUT touching AGENTS.md —
    never redirect awk's output onto the file being read.

    **Rollback** (D-47): the prose one-liner ``**Rollback:** `git checkout AGENTS.md`.`` — 0004's
    precedent. NOT a fenced awk block. Add one sentence recording why: 0029's custom Rollback awk
    needs the same terminator alternation as Apply and is exactly what upstream's fixture 08 had to
    be added to catch a file-destroying bug in; `git checkout` is structurally immune because there
    is no terminator to get wrong, and migration Rollback blocks in this repo already run inside a
    `test -d .git`-guarded context.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0009; echo "0009-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - **GREEN: `migrations/run-tests.sh 0009` exits 0 with `FAIL: 0`.** Every one of 09-03's ten
      cases and its four-state idempotency table now passes. This is the GREEN half of
      RED-before-GREEN; capture the full output for the evidence trail.
    - **Fixtures must NOT be edited to achieve GREEN.** `git diff 09-03's-final-commit -- migrations/run-tests.sh`
      must be empty. The fixtures were frozen at RED; if one must change, that is a signal the
      contract was wrong — STOP, record why, and surface it rather than quietly adjusting the test
      to match the code.
    - **ANCHOR-05 two-site audit (the corrected requirement — every bounding terminator carries the
      alternation).** `awk '/^### Step 1/{f=1} f&&/^### Step 2/{exit} f' migrations/0009-spec-11-region-aware-placement.md | grep -v '^[[:space:]]*#' | grep -c 'gitnexus:start -->\$/'`
      returns at least 3 — the strip terminator, the insert anchor, and the idempotency predicate's
      anchored marker. The `awk` scopes to Step 1 and the `grep -v` drops the comments explaining
      the rule; an unfiltered repo-wide count would pass on this plan's own prose while the code
      carried a narrow terminator. Enumerate each matching site by line number in the task evidence
      and confirm against PATTERNS.md's terminator-alternation checklist.
    - **D-25 not reintroduced:** `awk '/^### Step 1/{f=1} f&&/^### Step 2/{exit} f' migrations/0009-spec-11-region-aware-placement.md | grep -c 'session-level discipline the model brings to every diff'`
      returns 0 — no content sentinel bounds anything.
    - **D-47 honored:** Step 1's Rollback contains the literal `git checkout AGENTS.md` and Step 1
      contains no second fenced `awk` block after `**Rollback:**`. 09-03's case 08 asserts this.
    - **Atomicity:** every `mv` onto AGENTS.md is preceded by both `[ -s … ]` and the
      `grep -q '^## Coding Discipline (NON-NEGOTIABLE)$'` shape assertion. Verify by inspection and
      quote the guarded `mv` lines in the task evidence.
    - **Mutation demonstration (Dimension 8) — mandatory.** Temporarily delete
      `|| /^<!-- gitnexus:start -->$/` from the STRIP terminator only, re-run
      `migrations/run-tests.sh 0009`, and confirm case 02 (`inside-region-move`) and/or the
      idempotent re-run FAILS with the region destroyed. Revert and confirm GREEN returns. If the
      suite stays GREEN with the narrow terminator, the fixtures do not test the thing this phase
      exists to fix — STOP and surface it. Capture both runs' output.
    - `git status --porcelain migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md AGENTS.md`
      prints nothing.
    - Commit message uses the `feat(09-04):` prefix, so `git log` reads `test(RED)` → `feat(GREEN)`.
  </acceptance_criteria>

  <done>Step 1 heals all four states, the suite is GREEN, and the terminator alternation is audited at every site and demonstrated to be load-bearing.</done>
</task>

<task type="auto">
  <name>Task 3: Steps 2 and 3 — bump the scaffolder version and record the project version (MIGR-08, D-41)</name>

  <files>migrations/0009-spec-11-region-aware-placement.md</files>

  <read_first>
    - `migrations/0009-spec-11-region-aware-placement.md` — the file being modified (Task 2's output).
    - `migrations/0004-revendor-spec-11.md` lines 89-106 — IMMUTABLE, read-only. The exact
      three-step shape D-41 follows: Step 2 `sed -i.NNNN.bak -E` version bump with its `rm -f` of
      the backup, and Step 3's `echo "<version>" > .codex/workflow-version.txt`, each with its own
      idempotency check, pre-condition, and rollback.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` § "Step 2/3 (version bump +
      record) — 0004's 3-step shape, D-41".
    - `migrations/0008-plan-review-gate.md` line 332 — 0008's Step 4 `echo "0.6.0" > .codex/workflow-version.txt`,
      the immediate predecessor of the value 0009 overwrites.
  </read_first>

  <action>
    Add `### Step 2: Bump the scaffolder version (implements_spec unchanged)` and
    `### Step 3: Record the new project version`, adapting 0004's shape with `0.2.0`/`0.2.1`
    retargeted to `0.6.0`/`0.7.0` throughout.

    Step 2 — Idempotency check: `grep -q '^version: 0.7.0$' skills/agentic-apps-workflow/SKILL.md`.
    Pre-condition: `grep -q '^version: 0.6.0$' skills/agentic-apps-workflow/SKILL.md`. Apply: a
    `sed -i.0009.bak -E 's/^version: 0\.6\.0$/version: 0.7.0/'` on that file followed by
    `rm -f skills/agentic-apps-workflow/SKILL.md.0009.bak`. Rollback: the reverse `sed`, plus its
    backup cleanup. Leave `implements_spec: 0.4.0` untouched and say so in the step's title and a
    one-line comment (D-17 stands — that field tracks a full conformance audit and is explicitly out
    of scope).

    Step 3 — Idempotency check: `grep -q '^0.7.0$' .codex/workflow-version.txt 2>/dev/null`.
    Pre-condition: `.codex/` exists. Apply: `echo "0.7.0" > .codex/workflow-version.txt` (MIGR-08).
    Rollback: `echo "0.6.0" > .codex/workflow-version.txt`.

    Both steps must run even when Step 1 took its no-AGENTS.md informational-skip path (D-33) —
    state that explicitly in prose so a future reader does not "helpfully" gate the version bump on
    Step 1 having done work, which is precisely the change that would strand a project below
    `to_version` forever.

    Close the document with a short "Verification" section listing what a human can check after
    applying: `.codex/workflow-version.txt` reads `0.7.0`; SKILL.md reads `version: 0.7.0`; and, if
    AGENTS.md exists, the §11 provenance line sits above any leading `<!-- gitnexus:start -->`.
  </action>

  <verify>
    <automated>migrations/run-tests.sh; echo "full-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - **Full suite GREEN: `migrations/run-tests.sh` reports `FAIL: 0`, `SKIP: 1`, and `PASS` at
      least 278.** The pre-phase baseline was 278 PASS / 1 SKIP / 0 FAIL; this phase ADDS
      assertions, so PASS must be ≥ 278 and must never have fallen. Record the exact counts.
    - `migrations/run-tests.sh 0009` exits 0 and `migrations/run-tests.sh drift` exits 0.
    - Step 3 records the right value:
      `awk '/^### Step 3/{f=1} f' migrations/0009-spec-11-region-aware-placement.md | grep -c 'echo "0.7.0" > .codex/workflow-version.txt'`
      returns at least 1.
    - Step-block extraction still works after the additions — Step 1's extraction must not have
      leaked into Step 2 now that Step 2 exists: re-run `migrations/run-tests.sh 0009` and confirm
      the Apply shape guard still passes. (This is the first run where a `### Step 2` heading exists
      to terminate Step 1's scope; before this task the extractor ran to EOF.)
    - **Mutation demonstration (Dimension 8):** temporarily change Step 3's Apply to write `0.6.0`,
      re-run `migrations/run-tests.sh 0009`, and confirm a fixture or the idempotency assertion
      FAILS. If nothing fails, MIGR-08 is untested — record that gap explicitly and surface it
      rather than shipping the illusion of coverage. Revert. Capture both runs.
    - `gitnexus_detect_changes()` run before commit per CLAUDE.md, with affected scope recorded.
      (RESEARCH.md's assessment: migration `.md` documents are unlikely to be indexed as GitNexus
      symbols, and no existing symbol is edited in this plan — record whatever the tool reports,
      including "nothing indexed".)
    - Commit message uses the `feat(09-04):` prefix.
  </acceptance_criteria>

  <done>The migration's three steps are complete, the full suite is green, and the version coupling holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → a user's AGENTS.md | This is file surgery on a user's instruction file. Everything in this plan crosses this boundary; it is the only one that matters. |
| vendored mirror (`~/.codex/…/spec-mirrors/…`) → injected §11 content | D-27 makes the mirror the SOLE re-injection source. Its integrity IS §11's integrity. |
| hand-authored §11 → managed §11 | A user may have hand-pasted a §11 heading. The migration must never adopt or overwrite prose it cannot verify. |
| pinned upstream (`8520f90`) → this migration | Ported awk crosses a repo boundary; upstream has already moved past the pin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Step 1 strip pass — runaway strip destroying a GitNexus region (D-24). **Highest severity in the phase.** | mitigate | Strip terminator carries `(/^## / \|\| /^<!-- gitnexus:start -->$/)`, identical to the anchor's set — one rule, two sites. Enforced three ways: 09-01's counter-case B demonstrated the narrow terminator destroying the region empirically; Task 2's ANCHOR-05 two-site source audit counts the alternation scoped to Step 1 with comments filtered; Task 2's mutation demonstration removes the alternation and REQUIRES the suite to go red. |
| T-09-02 | Tampering | Pre-flight — corrupt/truncated mirror silently maiming §11 (D-28.1) | mitigate | Two layers, both `exit 3` before any surgery: `test -s` catches zero-byte (which `test -f` passes); `grep -q '^### 4\. Goal-Driven Execution$'` catches truncation (which `test -s` AND the pre-`mv` head-only shape assertion both pass). 09-03's case 10 asserts all three mirror states in both directions. |
| T-09-03 | Tampering | Step 1 branch 2 — overwriting a hand-authored §11 (State D) | mitigate | Heading-without-provenance → `exit 3` with the file untouched, inheriting 0001's conflict rule. The check runs BEFORE any surgery. 09-03's case 05 asserts both the exit code AND `cmp -s` byte-identity — an `exit 3` that already mangled the file is still a destroyed §11. |
| T-09-04 | Tampering | Non-atomic write leaving AGENTS.md truncated mid-heal | mitigate | Write to `AGENTS.md.0009.strip` / `.0009.tmp`, gate the `mv` on `[ -s … ]` AND the `grep -q '^## Coding Discipline (NON-NEGOTIABLE)$'` pre-`mv` shape assertion, then `mv` atomically. Never redirect awk's output onto the file being read. Every failure branch `rm -f`s temps and exits 3 without touching AGENTS.md. |
| T-09-13 | Tampering | Stale `swallowed_own_h2` orphaning a second managed block | mitigate | Both flags reset at the terminator; 09-03's case 09 (`two-provenance-heal`) is the regression guard — upstream hit this bug and fixed it. |
| T-09-14 | Elevation of Privilege | Fail-open region predicate leaving the block inside a region gitnexus may regenerate | mitigate | D-32's predicate fails CLOSED: an unterminated `gitnexus:start` counts as in-region (`r` stays 1 to EOF), so the block is moved above the marker to safety — same outcome as the well-formed case, no extra branch. |
| T-09-15 | Tampering | A future author "improving" Rollback into region-eating awk | mitigate | D-47 fixes Rollback at `git checkout AGENTS.md` — structurally immune, no terminator to get wrong. 09-03's case 08 asserts the document's Rollback line and the absence of a Rollback awk fence, so the regression fails loudly. |
| T-09-05 | Tampering | Unpinned upstream logic absorbed mid-execution | mitigate | All ports read `git -C ../claude-workflow show 8520f90:<path>`. D-47/D-44 name the two deliberate divergences up front so they are not "corrected" back toward upstream. |
| T-09-16 | Information Disclosure | Injected §11 prose drifting from the canonical mirror | accept | D-26 — 0009 strips blind and re-vendors from the mirror; content fidelity is 0004's job, and a State-B move repairs drift as a stated side effect (D-28.2). 0009 must not refuse to PLACE a block because its content drifted. |
| T-09-SC | Tampering | Package installs | accept | No `npm`/`pip`/`cargo` installs in this phase; RESEARCH.md's Package Legitimacy Gate scope condition is not met. `awk`/`sed`/`grep`/`git` are already required by every existing migration. |
</threat_model>

<verification>
- `migrations/run-tests.sh` full suite: `FAIL: 0`, `SKIP: 1`, `PASS` ≥ 278.
- `migrations/run-tests.sh 0009` exits 0 — every fixture 09-03 wrote RED is now GREEN.
- `migrations/run-tests.sh drift` exits 0 — SKILL.md `0.7.0` equals 0009's `to_version`.
- `git log --oneline` shows `test(09-03):` commits strictly before `feat(09-04):` commits — the
  auditable RED-before-GREEN evidence.
- `git diff <09-03 final commit> -- migrations/run-tests.sh` is EMPTY — the fixtures were not
  edited to manufacture GREEN.
- `git status --porcelain migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md AGENTS.md`
  prints nothing — immutables untouched, and this host's own (safe, latent-defect) AGENTS.md was
  not "repaired".
- Both mutation demonstrations (Task 2's terminator, Task 3's version) are captured in the SUMMARY.
- **No CI gate exists.** `.github/workflows/ci.yml` is the Phase 0 placeholder (`echo` + `exit 0`);
  CI-01 is deferred. This phase merges on a LOCAL green, exactly as v0.6.0 did. Do not assume CI
  catches a regression it currently cannot.
</verification>

<success_criteria>
- ROADMAP Success Criterion 3: 0009 heals all four states on a real AGENTS.md, is idempotent on
  re-run, and leaves a healthy-but-off-anchor block untouched.
- ROADMAP Success Criterion 4: `.codex/workflow-version.txt` reads `0.7.0` after applying, and this
  repo's own scaffolder is bumped to `0.7.0` in the same change with the drift test green.
- ROADMAP Success Criterion 2's GREEN half: all ten fixture cases pass once 0009 ships.
- ANCHOR-05 as corrected: every terminator bounding the managed section carries the
  `## `/anchored-marker alternation, audited site by site.
- MIGR-01..09 satisfied and evidenced by automated assertions, not by claim.
</success_criteria>

<output>
Create `.planning/phases/09-region-aware-11-placement/09-04-SUMMARY.md` when done.
Record in it: the full suite's exact PASS/FAIL/SKIP counts, the GREEN output of
`migrations/run-tests.sh 0009`, the ANCHOR-05 site-by-site audit (line numbers), both mutation
demonstrations' output, and confirmation that `migrations/run-tests.sh` was not modified in this
plan.
</output>
