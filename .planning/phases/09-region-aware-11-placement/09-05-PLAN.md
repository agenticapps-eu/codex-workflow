---
phase: 09-region-aware-11-placement
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - docs/decisions/0010-region-aware-spec-11-placement.md
  - docs/decisions/README.md
  - skills/setup-codex-agenticapps-workflow/SKILL.md
  - CHANGELOG.md
autonomous: true
requirements: [SETUP-01, DOC-01, DOC-02]

must_haves:
  truths:
    - "An ADR records the anchor decision, BOTH rejected alternatives with their reasoning, and the accepted limitations"
    - "The ADR states the corrected invariant — widened, not preserved — and never repeats the false claim"
    - "The ADR records the pinned upstream SHA the port was made against"
    - "Setup's §11 placement is confirmed in writing to derive solely from migration 0001's replay, with no independent anchor logic"
    - "That record is honest about the multi-hop limitation and does not assert end-state conformance it cannot demonstrate"
    - "CHANGELOG records the fix at release altitude under a 0.7.0 heading"
  artifacts:
    - path: "docs/decisions/0010-region-aware-spec-11-placement.md"
      provides: "The anchor decision record: decisions, rejected alternatives, consequences, accepted limitations"
      contains: "8520f90"
      min_lines: 80
    - path: "docs/decisions/README.md"
      provides: "ADR index row for 0010"
      contains: "0010-region-aware-spec-11-placement.md"
    - path: "CHANGELOG.md"
      provides: "0.7.0 release-altitude entry"
      contains: "## [0.7.0]"
    - path: "skills/setup-codex-agenticapps-workflow/SKILL.md"
      provides: "SETUP-01's pointer: where §11 placement actually comes from"
      contains: "0010-region-aware-spec-11-placement"
  key_links:
    - from: "docs/decisions/0010-region-aware-spec-11-placement.md"
      to: "migrations/0009-spec-11-region-aware-placement.md"
      via: "the ADR records the decision the migration implements"
      pattern: "0009-spec-11-region-aware-placement"
    - from: "CHANGELOG.md"
      to: "docs/decisions/0010-region-aware-spec-11-placement.md"
      via: "0.7.0 entry links the ADR, following the 0.6.0 entry's shape"
      pattern: "docs/decisions/0010"
    - from: "skills/setup-codex-agenticapps-workflow/SKILL.md"
      to: "docs/decisions/0010-region-aware-spec-11-placement.md"
      via: "SETUP-01 pointer so a future anchor change knows where to look"
      pattern: "0010-region-aware-spec-11-placement"
---

<objective>
Record the decision (DOC-01), the single-source setup fact (SETUP-01), and the release-altitude
changelog entry (DOC-02) — closing ROADMAP Success Criterion 6.

Purpose: this phase's most valuable output after the migration itself is the reasoning trail. The
anchor rule looks obvious and is not: the "obvious reading of *put it above the region*" is one of
the two rejected alternatives, and the invariant everyone assumed was preserved is actually
widened. An ADR that records only the decision, without the two rejections and the correction,
would leave the next author to rediscover both by shipping a file-destroying bug.

Output: ADR-0010, an indexed entry, a setup pointer, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-region-aware-11-placement/09-CONTEXT.md
@.planning/phases/09-region-aware-11-placement/09-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md
@.planning/phases/09-region-aware-11-placement/09-04-SUMMARY.md

**ADR number is 0010** — `docs/decisions/` runs 0001-0009; 0009 (plan-review-gate) is the highest.
Slug: `0010-region-aware-spec-11-placement.md`. `docs/decisions/README.md` carries an Index table
that MUST gain a row — an unindexed ADR is invisible.

**Upstream reference is PINNED (D-48):** `claude-workflow @ 8520f90d235e0c50b0484b170d595ab6f2cd1173`.
Recording this SHA **in the ADR** is an explicit D-48 requirement, not a nicety: 0029 changed four
times during this phase's planning session alone, and the pin is what makes the phase's scope
knowable after the fact.

**Do not overclaim (D-23).** §12's placement advisory is *"advisory, lower-case 'should'… not RFC
2119 and not a conformance gate"* — verified at `../agenticapps-workflow-core/spec/12-authoring-conventions.md:95-97`.
The rejected alternative violates an ADVISORY, not a normative gate. Both the source prompt and the
reference design phrase this loosely; the ADR must not.

**Do not assert what cannot be demonstrated (D-45).** SETUP-01's record must state the multi-hop
limitation honestly: a freshly scaffolded project lands at `0.1.0` and the update skill's
chain-selection defect (selects pending migrations ONCE from the initial version and never
recomputes) means "full replay" does not complete in one invocation today. Record the limitation;
do not fix it — it is deferred to its own phase.

**No-op, verified:** CHANGELOG.md has NO "known issues" section (`grep -in "known issue"` returns
zero matches). The source prompt's "retire the known-issues entry" instruction is a no-op here. Do
not invent a section in order to retire it.
</context>

<interfaces>
<!-- ADR shape to follow — docs/decisions/0009-plan-review-gate.md, the most recent -->
<!-- GSD-native precedent in this repo. Quoted in 09-PATTERNS.md § ADR. -->

  # ADR-0010: <title>
  **Status**: Accepted  **Date**: <date>
  **Core contract**: <spec ref>
  **Sibling host**: <cross-reference>
  ## Context
  ## Options considered
  ### A. <rejected>   ### B. <rejected>   ### C. <chosen>
  ## Decision            (numbered; each a locked decision with rationale + verification evidence)
  ## Consequences        (short, declarative — names what follows, no new reasoning)
  ## Verification
  ## Open follow-ups

<!-- CHANGELOG shape — this repo's own `## [0.6.0] — 2026-07-15` entry, CHANGELOG.md:25-45: -->
<!-- bold one-line summary + migration ID + ADR link, then 2-4 sentences of narrative -->
<!-- (what changed, what spec item it closes, why it mattered); a second bullet under the -->
<!-- same `### Added` covers the existing-install migration story. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0010 and index it (DOC-01)</name>

  <files>docs/decisions/0010-region-aware-spec-11-placement.md, docs/decisions/README.md</files>

  <read_first>
    - `docs/decisions/0009-plan-review-gate.md` — the shape precedent, read in full. Note especially
      its Consequences section (short, declarative, no new reasoning) and its decision-11/12
      "known, accepted limitation, recorded rather than fixed" framing — D-47's rollback tradeoff
      reuses that exact rhetorical shape.
    - `docs/decisions/README.md` — the file being modified; its Index table is what gains a row.
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` — D-21 (corrected rationale),
      D-22 (BOTH rejected alternatives), D-23 (§12 advisory precision), D-25, D-28.2, D-47, D-48.
      Read the D-21 correction block carefully; the ADR must state the widened invariant, not the
      false one.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` § "docs/decisions/0010-…" —
      the section-by-section adaptation notes, including what the `**Sibling host**` line should say.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md` — the empirical
      evidence the ADR's Verification section cites. Cite it; do not restate its claims as
      assertions.
    - `../agenticapps-workflow-core/spec/12-authoring-conventions.md` lines 93-113 — read the
      advisory's ACTUAL wording before characterizing it (D-23). Lines 95-97 are the load-bearing
      ones; lines 105-106 name §11 explicitly.
    - `migrations/0009-spec-11-region-aware-placement.md` — what actually shipped; the ADR records
      the decision behind it and must not drift from it.
  </read_first>

  <action>
    Create `docs/decisions/0010-region-aware-spec-11-placement.md` following ADR-0009's section
    shape. Status Accepted, dated today. `**Core contract**` cites
    `agenticapps-workflow-core/spec/12-authoring-conventions.md` §"Placement of behavior-critical
    prose (advisory)" lines 93-113. `**Sibling host**` cites claude-workflow's design doc
    (`docs/superpowers/specs/2026-07-15-spec-11-region-aware-placement-design.md`) and
    `migration-0029`, pinned at `8520f90d235e0c50b0484b170d595ab6f2cd1173` (D-48).

    Context: the latent defect — a §11 block anchored at the first `## ` lands inside a leading
    GitNexus region, where regeneration destroys it. State that on THIS host the defect is latent,
    not live (§11 at L18, region at L271-313), so this migration protects projects this host
    scaffolds rather than repairing this repo.

    Options considered — all three, each with reasoning, as `### A.`/`### B.`/`### C.`:
    - A (rejected, D-22.1): *"anchor before `gitnexus:start` if a region exists, else the first
      `## `."* Worth quoting the reference design's own framing: this is *"the obvious reading of
      'put it above the region', and wrong."* When the region starts late in the file it drops §11
      hundreds of lines down. The region is only the anchor when it comes FIRST. Per D-23, say
      precisely what this violates: §12's placement ADVISORY — lower-case "should", not RFC 2119,
      not a conformance gate. Do not upgrade it to a normative violation.
    - B (rejected, D-22.2): *"always immediately after the H1"* (the reference design's own earlier
      option). Moves the block in every healthy repo for no benefit and breaks the
      followed-by-`## ` invariant.
    - C (chosen, D-21): insert immediately before the first line that is either a `## ` heading or
      an ANCHORED `<!-- gitnexus:start -->` marker, whichever comes first; EOF if neither.

    Decision — numbered, each with rationale AND verification evidence, in ADR-0009's style:
    (1) the anchor rule (C above), with the anchored-regex requirement and why a substring match
        fails only the prose-mention case;
    (2) **the corrected invariant.** State it as WIDENED, not preserved: *the block is always
        followed by a `## ` line, an anchored `<!-- gitnexus:start -->` marker, or EOF.* Record that
        an earlier draft claimed this was "a one-alternation delta, so the structural invariant
        survives: the block is still always followed by a `## ` or EOF" — and that **that claim was
        false and load-bearing**, corrected the same day, independently, in both this repo and the
        sibling. Explain the consequence in one sentence: because every terminator that bounds the
        managed section must carry the same alternation as the anchor, a `/^## /`-only terminator
        runs past the marker and consumes the entire GitNexus region. This is the single most
        important paragraph in the ADR;
    (3) the structural strip boundary, and D-25's rejection of 0004's content sentinel with the
        runaway-strip reasoning (couples the strip to §11's last prose line, and prose drift in that
        exact block is why 0004 had to exist);
    (4) D-27's re-vendor-from-the-mirror choice unifying States B and C into one code path, and
        D-28.1's two mirror guards with why `test -f` was insufficient;
    (5) D-47's Rollback choice (`git checkout AGENTS.md`), framed as ADR-0009's decision-11/12
        shape: a deliberate tradeoff, recorded. Say why it makes the region-eating-rollback bug
        class structurally unreachable here — there is no terminator to get wrong — and that the
        regression guard is kept anyway;
    (6) D-48's pin, with the SHA in full and the reason: 0029 changed four times during this
        phase's planning session, so the scope must be knowable at plan time. Note the same-day
        convergence in one sentence — both hosts landed the same fix within hours, from the same
        design, with no divergence.

    Consequences — short and declarative, no new reasoning (ADR-0009's shape). Must include
    D-28.2's drift-repair side effect as a STATED consequence, not an accident: a State-B move
    silently repairs a drifted block, because the re-injection re-vendors from the mirror.

    Verification — cite `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md`
    for the empirical replay (zero churn on the real file; above-region on a gitnexus-led file; the
    naive anchor failing; the narrow terminator eating the region) and the ten-case fixture suite in
    `migrations/run-tests.sh::test_migration_0009`. Cite the evidence; do not restate its claims as
    fresh assertions.

    Open follow-ups — the deferred items this phase touched and consciously did not absorb:
    0008's inlined Step-3 awk copy at `run-tests.sh:~985` (same drift class as TEST-04, D-37 scoped
    it out); migrations 0002/0003's `from_version == to_version` dead-migration finding; the update
    skill's multi-hop chain-selection defect; `implements_spec` 0.4.0 → 0.5.0+; CI-01; and the note
    back to claude-workflow about the convergence.

    Then add the ADR-0010 row to `docs/decisions/README.md`'s Index table, matching the existing
    rows' format exactly.
  </action>

  <verify>
    <automated>test -s docs/decisions/0010-region-aware-spec-11-placement.md && grep -q '8520f90' docs/decisions/0010-region-aware-spec-11-placement.md && grep -q '0010-region-aware-spec-11-placement.md' docs/decisions/README.md && echo ADR-OK</automated>
    <human-check>Read the ADR. Confirm BOTH rejected alternatives appear with their reasoning, the corrected invariant is stated as widened (and the false claim is recorded as false, not repeated as true), §12 is characterized as advisory and not upgraded to a conformance gate, and the drift-repair side effect appears under Consequences.</human-check>
  </verify>

  <acceptance_criteria>
    - The automated command prints `ADR-OK`.
    - Both rejected alternatives present: the ADR contains an options section with at least three
      `### ` options, and `grep -c 'immediately after the H1' docs/decisions/0010-region-aware-spec-11-placement.md`
      returns at least 1 (D-22.2 — the alternative the source prompt omitted and is most likely to
      be dropped again).
    - The corrected invariant is present and the false claim is NOT asserted as true:
      `grep -c 'anchored .<!-- gitnexus:start -->. marker, or EOF\|marker, or EOF' docs/decisions/0010-region-aware-spec-11-placement.md`
      returns at least 1, AND every occurrence of the phrase "invariant survives" (if any) sits
      inside the passage recording it as FALSE. Verify by reading the surrounding sentence; quote it
      in the task evidence.
    - `grep -c '8520f90d235e0c50b0484b170d595ab6f2cd1173' docs/decisions/0010-region-aware-spec-11-placement.md`
      returns at least 1 — the full SHA, not the short form (D-48).
    - D-23 precision: the ADR contains the word `advisory` in the sentence characterizing §12, and
      does NOT describe §12's placement rule as a conformance gate, a MUST, or RFC 2119. Verify by
      reading; quote the sentence in the task evidence.
    - `docs/decisions/README.md`'s Index has exactly one 0010 row, formatted like its neighbours.
    - `migrations/run-tests.sh` full suite still `FAIL: 0` (this task adds docs only; a red suite
      here means something unrelated broke).
  </acceptance_criteria>

  <done>ADR-0010 records the decision, both rejections, the corrected invariant, the pin, and the accepted limitations; it is indexed.</done>
</task>

<task type="auto">
  <name>Task 2: Record SETUP-01's single-source fact, with its limitation stated honestly</name>

  <files>docs/decisions/0010-region-aware-spec-11-placement.md, skills/setup-codex-agenticapps-workflow/SKILL.md</files>

  <read_first>
    - `docs/decisions/0010-region-aware-spec-11-placement.md` — the file being modified (Task 1's output).
    - `skills/setup-codex-agenticapps-workflow/SKILL.md` lines 83-118 — the file being modified.
      Stage C walks 0000-baseline only; `:109`'s post-check asserts `.codex/workflow-version.txt`
      reads `0.1.0`; `:111` phrases the full chain conditionally. This is the evidence for D-43.
    - `migrations/0000-baseline.md` line 102 — the plain
      `cat templates/agents-md-additions.md >> AGENTS.md` append. No anchor logic.
    - `skills/setup-codex-agenticapps-workflow/templates/agents-md-additions.md` — verify for
      yourself it contains NO §11 (it runs `## Development Workflow` → `## Pre-execution Gate`).
      D-43 rests on this; confirm rather than inherit the claim.
    - `../agenticapps-workflow-core/spec/08-migration-format.md` lines 25-45 — the end state is
      normative, not the mechanism; replay and snapshot are BOTH conformant. This is why this host
      needs no parity guard and claude-workflow does (D-44).
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-43, D-44, D-45.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` § "Manual-Only
      Verifications" — the SETUP-01 row defines the claim-accuracy bar this must meet.
  </read_first>

  <action>
    Add a `## Setup parity (SETUP-01)` section to ADR-0010 recording the verified fact and the
    limitation that bounds it. This is a RECORD-THE-FACT requirement, not a build-a-guard one — do
    not add a parity guard, a test, or any anchor logic to setup.

    Record, each with its file:line evidence: setup has NO independent §11 placement logic
    (`0000-baseline.md:102` is a plain append; `agents-md-additions.md` carries no §11); setup
    applies 0000-baseline ONLY and lands the project at `0.1.0` (`SKILL.md:109`); §11 therefore
    arrives via migration 0001 in the subsequent update chain, and 0009 heals its placement. The
    anchor rule has exactly ONE source — the migration chain — so a future anchor change has exactly
    one place to look. Say that plainly; it is the whole point of the requirement.

    Record why no parity guard is needed (D-44): `spec/08-migration-format.md:27-33` makes the END
    STATE normative rather than the mechanism, and names replay and snapshot as two conformant
    strategies. This host is REPLAY; claude-workflow is SNAPSHOT, which is exactly why it ships
    `check-snapshot-parity.sh` and an anchor-parity guard and this host does not. §08 conformance is
    satisfied here by construction — there is no second anchor to guard.

    Then state the limitation that bounds the claim (D-45), honestly and without hedging: the update
    skill's multi-hop chain-selection defect (it selects pending migrations ONCE from the project's
    initial version and never recomputes) means a freshly scaffolded project at `0.1.0` selects only
    0001, applies it, lands at `0.2.0`, and fails the final target check. So "setup end-state ≡ full
    replay" does NOT complete in one invocation TODAY. Do not assert an end-state conformance this
    host cannot currently demonstrate. The defect is deferred to its own phase and does not block
    0009, whose floor is `0.6.0 → 0.7.0` where every live project already sits after 0008.

    Then add a short pointer note to `skills/setup-codex-agenticapps-workflow/SKILL.md`, adjacent to
    Stage C/D's §11-relevant post-checks, recording that setup deliberately carries NO §11 placement
    logic, that §11 arrives via migration 0001 and is placement-healed by migration 0009, and
    linking `docs/decisions/0010-region-aware-spec-11-placement.md`. Prose only — no behavior
    change, no new check, no new post-check. This is the "so a future anchor change knows where to
    look" half of SETUP-01: the ADR is the reasoning, this pointer is the signpost at the place
    someone would wrongly look first.
  </action>

  <verify>
    <automated>grep -q 'SETUP-01' docs/decisions/0010-region-aware-spec-11-placement.md && grep -q '0010-region-aware-spec-11-placement' skills/setup-codex-agenticapps-workflow/SKILL.md && migrations/run-tests.sh layout; echo "layout-exit=$?"</automated>
    <human-check>Read the Setup parity section. Confirm it states setup applies 0000-baseline only, lands at 0.1.0, and does NOT assert an end-state conformance it cannot demonstrate — the multi-hop limitation must be stated, not elided.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c '0000-baseline.md:102\|0000-baseline' docs/decisions/0010-region-aware-spec-11-placement.md`
      returns at least 1 — the claim carries its evidence.
    - The limitation is present, not elided: the Setup parity section mentions the multi-hop /
      chain-selection defect and the `0.1.0` landing point. Quote the sentence in the task evidence.
      A section that claims single-source purity WITHOUT the limitation fails D-45 and must be revised.
    - `grep -c '0010-region-aware-spec-11-placement' skills/setup-codex-agenticapps-workflow/SKILL.md`
      returns at least 1.
    - **No guard was built:** `git diff -- skills/setup-codex-agenticapps-workflow/SKILL.md` contains
      only prose additions — no new shell, no new post-check, no anchor logic. D-44 makes SETUP-01
      record-the-fact; building a guard is scope creep against an explicit Out-of-Scope entry.
    - `migrations/run-tests.sh layout` exits 0 and the full suite reports `FAIL: 0`.
  </acceptance_criteria>

  <done>SETUP-01's single-source fact is recorded with its evidence and its limitation, and a signpost points at it from setup.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG entry at release altitude (DOC-02)</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` lines 1-46 — the file being modified. The `## [0.6.0] — 2026-07-15` entry at
      `:25-45` is the shape precedent: bold one-line summary + migration ID + ADR link, then 2-4
      sentences of narrative, with a second bullet for the existing-install migration story.
    - `docs/decisions/0010-region-aware-spec-11-placement.md` — the ADR being linked.
    - `migrations/0009-spec-11-region-aware-placement.md` — what shipped; the entry must not
      overclaim beyond it.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` § "Manual-Only
      Verifications" — the DOC-02 row.
  </read_first>

  <action>
    Add a `## [0.7.0] — <date>` section ABOVE the existing `## [0.6.0]` heading, following that
    entry's shape exactly.

    Under `### Fixed` (this is a defect closure, not a feature — 0.6.0 used `### Added` because it
    added a gate), write the primary bullet: a bold one-line summary naming the region-aware §11
    placement fix, the migration ID `0009`, and a link to
    `docs/decisions/0010-region-aware-spec-11-placement.md`. Then 2-4 sentences at RELEASE altitude
    — what a reader of the changelog needs, not what an implementer needs. Say: the §11 Coding
    Discipline block now anchors above a leading GitNexus region instead of inside it; a block
    placed inside such a region was silently destroyed whenever gitnexus regenerated it; the defect
    was LATENT on this host (no repo here was broken) and this closes it for every project this host
    scaffolds. Do not put awk, terminators, or `swallowed_own_h2` in the CHANGELOG — that is what
    the ADR and the migration document are for.

    Add a second bullet for the existing-install story, mirroring 0008's second bullet: existing
    installs pick the fix up by running `/update-codex-agenticapps-workflow`, which applies
    migration 0009 (`0.6.0 → 0.7.0`); the migration is idempotent, heals a block that sits inside a
    region, injects one that is absent, leaves a correctly anchored block byte-identical, and
    refuses with an error rather than overwriting a hand-authored §11 section.

    Do NOT add a "known issues" section. Verified: none exists (`grep -in "known issue" CHANGELOG.md`
    returns zero matches), so the source prompt's "retire the known-issues entry" instruction is a
    no-op here. Do not invent a section in order to retire it.
  </action>

  <verify>
    <automated>grep -q '^## \[0.7.0\]' CHANGELOG.md && grep -q 'docs/decisions/0010-region-aware-spec-11-placement.md' CHANGELOG.md && awk '/^## \[0\.7\.0\]/{p=NR} /^## \[0\.6\.0\]/{q=NR} END{exit !(p>0 && q>p)}' CHANGELOG.md && echo CHANGELOG-OK</automated>
    <human-check>Read the 0.7.0 entry. Confirm it reads at release altitude — a user can tell what changed and why it mattered without knowing what an awk terminator is — and that it does not overclaim beyond what migration 0009 actually does.</human-check>
  </verify>

  <acceptance_criteria>
    - The automated command prints `CHANGELOG-OK`. Its `awk` also asserts ORDERING: the 0.7.0
      heading must appear ABOVE the 0.6.0 heading (`p > 0 && q > p`). A plain `grep -q '0.7.0'`
      would pass for an entry appended at the bottom of the file, which is the wrong place.
    - The entry references migration `0009` and links ADR-0010.
    - `grep -ic 'known issue' CHANGELOG.md` returns 0 — no known-issues section was invented.
    - `grep -c 'swallowed_own_h2\|awk' CHANGELOG.md` returns 0 — implementation mechanics stay out
      of the changelog.
    - `migrations/run-tests.sh` full suite reports `FAIL: 0`, `SKIP: 1`, `PASS` ≥ 278.
  </acceptance_criteria>

  <done>CHANGELOG records the fix at release altitude under a correctly placed 0.7.0 heading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision record → future author | The ADR is the only artifact that carries WHY. An ADR that omits the rejected alternatives or repeats the corrected-false claim actively misleads the next author into shipping the bug this phase closed. |
| setup skill prose → setup behavior | SETUP-01 is record-the-fact. Prose edits to a skill file must not become behavior edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Tampering | ADR repeating the FALSE invariant claim ("one-alternation delta, so the invariant survives"), re-arming the runaway-strip hazard (T-09-01) for the next author | mitigate | Task 1's Decision item 2 mandates the widened wording and requires the false claim be recorded AS false. Acceptance requires reading every "invariant survives" occurrence in context and quoting it — this is the documentation-layer twin of the phase's highest-severity mechanic. |
| T-09-18 | Repudiation | Un-recorded upstream pin, making the phase's scope unknowable after the fact | mitigate | D-48 requires the full SHA in the ADR; acceptance greps for the full 40-char SHA, not the short form. |
| T-09-19 | Spoofing | SETUP-01 asserting an end-state conformance this host cannot demonstrate (D-45) | mitigate | Task 2's acceptance requires the multi-hop limitation be present and quoted; a purity claim without it fails the task. |
| T-09-20 | Tampering | Scope creep — building a setup-side parity guard against an explicit Out-of-Scope entry | mitigate | Task 2's acceptance requires the setup SKILL.md diff be prose-only; D-44's reasoning (replay vs snapshot, §08 end-state normative) is recorded in the ADR so the guard is not "helpfully" added later. |
| T-09-21 | Tampering | Unindexed ADR — invisible decision record | mitigate | `docs/decisions/README.md` is in `files_modified`; acceptance greps the Index for the 0010 row. |
| T-09-SC | Tampering | Package installs | accept | No `npm`/`pip`/`cargo` installs. Documentation-only plan; no dependency surface. |
</threat_model>

<verification>
- `migrations/run-tests.sh` full suite: `FAIL: 0`, `SKIP: 1`, `PASS` ≥ 278 (documentation changes
  must not move the suite; `test_repo_layout` asserts file existence only, so the setup SKILL.md
  prose edit is safe).
- `docs/decisions/0010-region-aware-spec-11-placement.md` exists, is indexed in
  `docs/decisions/README.md`, and carries the full pin SHA.
- CHANGELOG's `## [0.7.0]` heading sits ABOVE `## [0.6.0]`.
- `git status --porcelain migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md AGENTS.md`
  prints nothing.
- `git diff -- skills/setup-codex-agenticapps-workflow/SKILL.md` is prose-only — no shell, no new check.
- The three `<human-check>` items are the same three rows VALIDATION.md's "Manual-Only
  Verifications" table routes to `/gsd:verify-work`. They are prose-quality judgments and are
  verified there; the automated greps above bound the mechanical half.
</verification>

<success_criteria>
- ROADMAP Success Criterion 6 fully closed: setup's §11 placement is confirmed in writing to derive
  solely from migration 0001's replay with no independent anchor logic (SETUP-01); an ADR records
  the anchor decision and its rejected alternatives (DOC-01); CHANGELOG records the fix at release
  altitude (DOC-02).
- The ADR states the invariant as WIDENED and records the false claim as false — the next author
  cannot rediscover the runaway-strip hazard by trusting this repo's own docs.
- Both rejected alternatives (D-22.1 and D-22.2) are recorded with reasoning; §12 is characterized
  precisely as advisory (D-23).
- The upstream pin `8520f90` is recorded, making the phase's scope knowable after the fact (D-48).
- SETUP-01's claim is bounded by its honest limitation (D-45), and no parity guard was built (D-44).
</success_criteria>

<output>
Create `.planning/phases/09-region-aware-11-placement/09-05-SUMMARY.md` when done.
Record in it: the ADR's decision list, the quoted sentence characterizing §12 (proving D-23
precision), the quoted SETUP-01 limitation sentence (proving D-45 honesty), and the full suite's
final PASS/FAIL/SKIP counts for the phase.
</output>
