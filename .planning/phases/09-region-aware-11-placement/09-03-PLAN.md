---
phase: 09-region-aware-11-placement
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - migrations/run-tests.sh
  - .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "test_migration_0009 exists, is wired into the dispatcher, and runs under the 0009 filter — extracting 0009's shell from the pinned document (D-48), never a transcribed copy"
    - "All ten TEST-03 cases are covered as printf-synthesized case labels inside one function — D-34 (this repo's native idiom; per-fixture directories rejected) and D-46 (widened 6 → 10: adds prose-mention, rollback-region-led, two-provenance-heal, corrupt-mirror-refused)"
    - "Step 1's idempotency check is asserted in all four states per D-30's three-branch state machine — State B returns non-zero despite provenance being present (D-38's double-sided contract), State A skips a healthy block, D-31's healthy-but-off-anchor block is skipped by the same predicate with no special case, D-32's region predicate fails closed on an unterminated marker, and D-33's absent instruction file is an informational skip that still permits the version bump"
    - "Case 07 forces D-21's anchored marker regex; case 10 binds both D-28.1 mirror guards; case 08 covers D-47's git-checkout Rollback; the pre-flight accepts both versions per D-39"
    - "The suite is OBSERVED RED before migration 0009 exists, and the observation is recorded (D-36: an assertion never seen failing is not evidence)"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "test_migration_0009 with ten synthesized cases and the four-state idempotency table"
      contains: "test_migration_0009"
    - path: ".planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md"
      provides: "Recorded RED observation — the auditable half of RED-before-GREEN"
      contains: "RED OBSERVED"
  key_links:
    - from: "migrations/run-tests.sh"
      to: "migrations/0009-spec-11-region-aware-placement.md"
      via: "extract_step_block / extract_preflight_block against the (not-yet-existing) 0009 document"
      pattern: "0009-spec-11-region-aware-placement\\.md"
---

<objective>
Write `test_migration_0009` — ten printf-synthesized cases plus the four-state double-sided
idempotency table — against a migration document that DOES NOT EXIST YET, and observe the
suite RED.

This plan is ROADMAP hard ordering constraint 2 ("RED before GREEN") made executable. Its
terminal state is a FAILING suite, on purpose. **That is the deliverable.** A suite never
observed RED is not evidence; it is an assertion. Plan 09-04 turns it GREEN by shipping 0009,
and the auditable trail is the commit shape: `test(RED)` commits here, `feat(GREEN)` commits
there.

Purpose: Phase 8 shipped three assertions that could never match and read as coverage. This
phase's antidote is structural — write the fixtures first, watch them fail, then make them pass.
Output: a complete, failing fixture suite and a recorded RED observation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-region-aware-11-placement/09-CONTEXT.md
@.planning/phases/09-region-aware-11-placement/09-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-01-SUMMARY.md

**THIS PLAN ENDS RED. Do not "fix" the failures.** Every task in this plan commits a suite
that reports `FAIL: > 0` because `migrations/0009-spec-11-region-aware-placement.md` does not
exist. Do not create it, do not stub it, do not add a "skip if the document is missing" guard
to make the suite green — a conditional skip is precisely how a suite that never fails ships as
coverage. Use `test(09-03): ...` as the commit-message prefix for every task in this plan so the
RED phase is legible in `git log`.

**Ten cases, not six or eight (D-46, corrected 2026-07-15; TEST-03 in REQUIREMENTS.md is the
authority).** ⚠ `09-PATTERNS.md`'s Metadata note says *"D-46 locks this phase to 8 cases"* —
**that note is STALE.** It was written before CONTEXT.md's D-46 was widened. Cases 09 and 10 are
IN SCOPE. When PATTERNS.md and CONTEXT.md/REQUIREMENTS.md disagree, CONTEXT.md wins.

**Fixture idiom is locked (D-34):** ten case labels inside ONE `test_migration_0009` function,
each synthesized at test time via `printf` into `$tmp`. **Do NOT port claude-workflow's
per-fixture directory layout** (`test-fixtures/0029/01-gitnexus-led-inject/{setup.sh,verify.sh}`).
`migrations/test-fixtures/README.md`'s "Why no static fixture files" section explicitly rejects
it, and its "Limits" section authorizes this repo's synthesized fallback. Porting the layout
would introduce a second, competing fixture idiom.

**Upstream reference is PINNED (D-48):** `claude-workflow @ 8520f90d235e0c50b0484b170d595ab6f2cd1173`.
Read pinned fixture content with `git -C ../claude-workflow show 8520f90:<path>`.
Adopt the fixtures' INTENT and content-generation logic; reject their layout.
</context>

<interfaces>
<!-- Helpers built by plan 09-02, available in migrations/run-tests.sh: -->

  extract_step_block <doc_path> <step_number> <label>   # label: Apply | Idempotency check
  extract_preflight_block <doc_path>
  assert_extracted_shape <label> <text> <required_substring>

<!-- Shared-lib helper, vendor/agenticapps-shared/migrations/lib/helpers.sh:60-79 -->

  assert_check <label> <check> <fixture_dir> <expected>   # expected: applied | not-applied
  # "applied"  == check exited 0     (already done → skip)
  # "not-applied" == check exited non-zero (please apply)
  # run_check already wraps <check> in a subshell and cd's into <fixture_dir>.

<!-- The document 0009 WILL have (plan 09-04 authors it). These paths are the contract -->
<!-- this plan's extractions are written against; 09-04 must satisfy them: -->

  migrations/0009-spec-11-region-aware-placement.md
    ## Pre-flight                       → extract_preflight_block
    ### Step 1: ...                     → extract_step_block <doc> 1 "Idempotency check"
                                        → extract_step_block <doc> 1 "Apply"
    **Rollback:** `git checkout AGENTS.md`.   (D-47 — prose one-liner, NOT a fenced awk block)

<!-- Constants -->
  PROV literal:  <!-- spec-source: agenticapps-workflow-core@0.4.0 §11 -->
  PROV regex:    <!-- spec-source: agenticapps-workflow-core@[^[:space:]]+ §11 -->
  mirror:  skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md
           (79 lines; one `## ` at L1; four `### `; last is `### 4. Goal-Driven Execution` at L57)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold test_migration_0009, wire the dispatcher, and assert the four-state idempotency table (D-38)</name>

  <files>migrations/run-tests.sh</files>

  <read_first>
    - `migrations/run-tests.sh` lines 79-145 — the file being modified; `test_migration_0001` is
      the idiom to mirror (`mktemp -d`, `trap 'rm -rf "$tmp"' RETURN`, `printf` into `$tmp`,
      `assert_check` with `applied`/`not-applied`).
    - `migrations/run-tests.sh` lines 3140-3195 — the dispatcher's `if [ -z "$FILTER" ] || [ "$FILTER" = "NNNN" ]`
      blocks. The 0009 block goes after 0008's.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` § "Double-Sided Idempotency
      Contract (D-38)" — the four-state table is BINDING; State B is the row the design calls
      "the whole point".
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` § "The region predicate
      (D-32)" — the single-pass `r`/`f` awk the idempotency check will carry.
    - `migrations/test-fixtures/README.md` — the Contract section (double-sided idempotency) and
      the "Why no static fixture files" section (the D-34 rejection rationale to cite in a comment).
    - Pinned upstream: `git -C ../claude-workflow show 8520f90:migrations/test-fixtures/0029/common-verify.sh`
      — how the three extractions and guards are sequenced before any fixture runs.
  </read_first>

  <action>
    Add `test_migration_0009` to `migrations/run-tests.sh`, following `test_migration_0001`'s idiom
    exactly: a `YELLOW === Migration 0009 — Region-aware §11 placement ===` banner, `mktemp -d` into
    `$tmp` with `trap 'rm -rf "$tmp"' RETURN`, and `printf`-synthesized fixtures. Wire it into the
    dispatcher with an `if [ -z "$FILTER" ] || [ "$FILTER" = "0009" ]` block placed after 0008's.

    Set `MIGRATION_0009="$REPO_ROOT/migrations/0009-spec-11-region-aware-placement.md"` and extract
    three blocks up front using 09-02's helpers, each gated by `assert_extracted_shape` before any
    fixture consumes it: the pre-flight block (required substring `test -s`), Step 1's Idempotency
    check (required substring `spec-source: agenticapps-workflow-core`), and Step 1's Apply
    (required substring `gitnexus:start` — this is D-36's exact antidote: a block that carries no
    marker alternation is not Step 1's region-aware apply, whatever else it is).

    Add a `mk_fake_home` helper local to this function: creates
    `$tmp/<name>/codexhome/skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/` and
    copies the repo mirror in, so an extracted block's
    `${CODEX_HOME:-$HOME/.codex}`-derived `MIRROR`/`SPEC_BLOCK` path resolves to a controlled file.
    Add a `mk_project` helper: creates a scratch project root with a `.git` directory (the
    pre-flight's `test -d .git` guard) and `skills/agentic-apps-workflow/SKILL.md` carrying
    `version: 0.6.0` (the pre-flight's version gate, D-39). Every eval of an extracted block MUST
    run in an explicit subshell with `CODEX_HOME` exported to the fake home and cwd set to the
    scratch project root — an extracted block that hits `exit 3` (State D, corrupt mirror) would
    otherwise terminate the whole harness mid-suite and hide every later assertion.

    Assert Step 1's idempotency check in ALL FOUR states from VALIDATION.md's table, using
    `assert_check` against printf-synthesized files:
    - State A — correctly anchored block, current provenance, region present but LATER in the file
      (this repo's real shape): expect `applied` (check exits 0 → skip). This is also MIGR-07's
      guard: healthy-but-off-anchor must be left alone, and it falls out of the predicate rather
      than a special case (D-31).
    - State B — provenance present BUT the block sits inside the region: expect `not-applied`
      (check exits NON-ZERO despite provenance being present). Comment this row explicitly as the
      conjunction the design calls the whole point: provenance alone must not short-circuit the
      heal. A check that returns `applied` here is the defect.
    - State C — no provenance at all: expect `not-applied`.
    - State D — a `## Coding Discipline (NON-NEGOTIABLE)` heading with no provenance: assert this
      is gated by the PRE-FLIGHT conflict branch, not by the idempotency check. Assert it
      separately in Task 2's case 05; here, add a comment pointing at that case so the four-state
      table reads complete.

    Add an `in_region` variant assertion for D-32's fail-closed rule: a file with an unterminated
    `<!-- gitnexus:start -->` (no matching `end`) and provenance after it must be treated as
    in-region → `not-applied`. No separate branch and no extra fixture — it rides on State B's
    predicate, which is exactly why D-32 chose the fail-closed shape.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0009 2>&1 | tail -20; echo "exit=${PIPESTATUS[0]}"</automated>
  </verify>

  <acceptance_criteria>
    - `migrations/run-tests.sh 0009` RUNS the new function (its banner appears in stdout) and
      exits NON-ZERO with `FAIL: > 0`. **This is the expected, required outcome** — the 0009
      document does not exist, so `assert_extracted_shape` must report empty-extraction failures.
      An exit of 0 here means the function silently skipped and the whole plan is worthless.
    - Source assertion: `grep -c 'FILTER" = "0009"' migrations/run-tests.sh` returns 1 —
      the dispatcher is wired.
    - Source assertion, four states present:
      `awk '/^test_migration_0009\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c 'assert_check'`
      returns at least 4. Scoped with `awk` to the function body so other migrations'
      `assert_check` calls cannot inflate the count.
    - State B is asserted `not-applied`, not `applied`: inspect the State B `assert_check` call and
      confirm its fourth argument is `not-applied`. Record the exact line in the task evidence.
    - `migrations/run-tests.sh 0001` still exits 0 — the new function did not disturb 09-02's work.
    - Commit message uses the `test(09-03):` prefix.
  </acceptance_criteria>

  <done>test_migration_0009 exists, is dispatcher-wired, asserts all four idempotency states, and fails because 0009 does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the six locked TEST-03 cases (01-06)</name>

  <files>migrations/run-tests.sh</files>

  <read_first>
    - `migrations/run-tests.sh` — `test_migration_0009` as left by Task 1 (the file being modified).
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` § "Specific Ideas" — the case
      names to prefer, and the requirement that `03-healthy-noop` assert AGENTS.md is
      BYTE-IDENTICAL ("that is what proves zero churn, and it is the fixture that would catch an
      over-eager anchor").
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-30 (three branches), D-33
      (absent AGENTS.md → informational skip, version bump still runs).
    - Pinned upstream fixture intents — read the BEFORE-state generators only, reject the layout:
      `git -C ../claude-workflow show 8520f90:migrations/test-fixtures/0029/01-gitnexus-led-inject/setup.sh`
      and the same path for `02-inside-region-move`, `03-healthy-noop`, `04-no-claudemd`,
      `05-unmanaged-conflict`, `06-no-heading-eof`. Note upstream's case 04 is `04-no-claudemd`;
      this host's equivalent is `04-no-agentsmd`.
    - `AGENTS.md` lines 1-22 — the real healthy shape case 03 models.
  </read_first>

  <action>
    Add the six locked cases as labelled blocks inside `test_migration_0009`, each synthesizing its
    BEFORE state with `printf` into `$tmp/<case>` (D-34), each running the extracted Step 1 Apply in
    a subshell against that scratch root, and each asserting the AFTER state. Use the case labels
    `01-gitnexus-led-inject`, `02-inside-region-move`, `03-healthy-noop`, `04-no-agentsmd`,
    `05-unmanaged-conflict`, `06-no-heading-eof` in the assertion text so failures name themselves.

    `01-gitnexus-led-inject` (MIGR-04 at the region-led anchor): AGENTS.md is an H1, a paragraph,
    then `<!-- gitnexus:start -->` on its own line, region body, `<!-- gitnexus:end -->`, then a
    `## Some Section` heading AFTER the region, and no §11 anywhere. Assert after Apply: the
    provenance line number is strictly LESS than the `<!-- gitnexus:start -->` line number; exactly
    one `^<!-- gitnexus:start -->$` and one `^<!-- gitnexus:end -->$` remain; the region body text is
    still present.

    `02-inside-region-move` (MIGR-03): provenance + full mirror block sit INSIDE the region. Assert
    after Apply: exactly ONE provenance line in the file; it is above `<!-- gitnexus:start -->`; the
    region's own body survives and its markers are still paired. This is the case that proves the
    strip terminator's alternation — 09-01's counter-case B demonstrated the narrow terminator eats
    the region here.

    `03-healthy-noop` (MIGR-02, MIGR-07, zero churn): model this repo's real AGENTS.md shape — a
    correctly anchored provenance + mirror block at the first `## `, a region LATER in the file.
    Because the idempotency check returns `applied`, the migration runtime skips Step 1 entirely;
    assert the file is BYTE-IDENTICAL (`cmp -s` against a pristine copy taken before). Byte-identity
    is the assertion — a `grep`-based "block still present" check would pass for an over-eager
    anchor that moved the block, which is exactly the failure this case exists to catch.

    `04-no-agentsmd` (D-33): scratch root with NO AGENTS.md. Assert the extracted Apply exits ZERO
    (informational skip, not an abort), emits a message naming this host's own skills
    (`update-codex-agenticapps-workflow`, not claude-workflow's slug), and creates no AGENTS.md.
    Comment the rationale: 0004's pre-flight abort would strand the project below `to_version`
    permanently, because the engine marks a migration pending iff `installed >= from && installed < to`.

    `05-unmanaged-conflict` (MIGR-05, State D): a `## Coding Discipline (NON-NEGOTIABLE)` heading
    with hand-written prose and NO provenance. Assert the extracted Apply exits exactly 3 AND the
    file is byte-identical afterwards (`cmp -s` against a pristine copy). Both halves are required:
    an `exit 3` that already mangled the file is still a destroyed hand-authored §11.

    `06-no-heading-eof` (ANCHOR-02): a file with no `## ` heading and no marker at all. Assert after
    Apply that the block is appended at EOF and the provenance line is present.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0009 2>&1 | tail -25; echo "exit=${PIPESTATUS[0]}"</automated>
  </verify>

  <acceptance_criteria>
    - `migrations/run-tests.sh 0009` still exits NON-ZERO (0009 does not exist yet — required).
    - Source assertion, all six labels present:
      `awk '/^test_migration_0009\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c '0[1-6]-\(gitnexus-led-inject\|inside-region-move\|healthy-noop\|no-agentsmd\|unmanaged-conflict\|no-heading-eof\)'`
      returns at least 6.
    - Source assertion, byte-identity is real: case 03 and case 05 each use `cmp -s` (or `diff -q`)
      against a pristine copy — not a `grep` presence check. Verify by inspection and record the
      two lines in the task evidence.
    - **Dry-run liveness demonstration (Dimension 8).** These fixtures cannot pass yet, so their
      liveness must be shown a different way: temporarily point `MIGRATION_0009` at
      `migrations/0001-inject-spec-11-coding-discipline.md` — whose Step 1 Apply carries the NAIVE
      anchor `/^## / && !done` and is self-contained/eval-able — and re-run
      `migrations/run-tests.sh 0009`. Confirm: the Apply shape guard FAILS (0001's block has no
      `gitnexus:start`), and if the guard is bypassed, case 01 fails by inserting the block INSIDE
      the region. Revert the override. Capture both runs' output. This is the fixture-level twin of
      09-01's counter-case A and is what TEST-02's phrase "fails against the current naive anchor"
      means concretely.
    - `migrations/run-tests.sh 0001` still exits 0.
    - Commit message uses the `test(09-03):` prefix.
  </acceptance_criteria>

  <done>The six locked cases exist, are RED, and have been observed failing against the naive anchor rather than merely against an absent document.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-46's four cases (07-10) and record the RED observation (TEST-02)</name>

  <files>migrations/run-tests.sh, .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md</files>

  <read_first>
    - `migrations/run-tests.sh` — `test_migration_0009` as left by Task 2 (the file being modified).
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md` — the file being
      appended to (created by plan 09-01).
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-46 — all four cases and the
      defect each encodes. D-46.3's `swallowed_own_h2` stale-state explanation is the whole
      justification for case 09 and must be understood before writing it.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` § "Concrete translation — one
      claude-workflow fixture dir vs. this repo's `printf` equivalent" — the worked
      `07-prose-mention-not-a-region` translation, already written out for this repo's idiom.
    - Pinned upstream:
      `git -C ../claude-workflow show 8520f90:migrations/test-fixtures/0029/07-prose-mention-not-a-region/setup.sh`
      and the same for `08-rollback-region-led`, `09-two-provenance-heal`, `10-corrupt-mirror-refused`.
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` § "Dimension 8" item 4 —
      case 07 is a dead-assertion detector by design.
  </read_first>

  <action>
    Add the four D-46 cases, same idiom and labelling as Task 2.

    `07-prose-mention-not-a-region` (D-46.1 — forces D-21's ANCHORED regex): follow PATTERNS.md's
    worked translation. An HTML comment near the top MENTIONS the marker inside backticks, so the
    line is NOT exactly `<!-- gitnexus:start -->`; the §11 block is correctly anchored right after
    that comment; there is NO real region anywhere. Assert the idempotency check returns `applied`
    (skip) — a SUBSTRING marker match would judge this file in-region and return `not-applied`.
    Comment that this case passes with a substring match on every OTHER fixture and fails only
    here, which is exactly why it exists.

    `08-rollback-region-led` (D-46.2, kept even under D-47): 0009's Rollback is `git checkout
    AGENTS.md`, so the region-eating-rollback bug class is structurally unreachable here — there is
    no terminator to get wrong. This case is therefore a REGRESSION GUARD on that choice, asserted
    against the migration document itself rather than by executing awk: assert Step 1's `**Rollback:**`
    line in `migrations/0009-spec-11-region-aware-placement.md` contains the literal
    `git checkout AGENTS.md`, AND that Step 1's Rollback carries no fenced `awk` block. Comment
    that its purpose is to fail loudly if a future author "improves" Rollback into the custom awk
    that upstream's fixture 08 exists to catch a file-destroying bug in.

    `09-two-provenance-heal` (D-46.3 — the `swallowed_own_h2` stale-state guard): an AGENTS.md
    carrying TWO provenance + block pairs, each terminated by a real `## ` heading. Assert after
    Apply that exactly ONE provenance line remains, and that the surviving `## ` headings that
    terminated each block are still present. Comment the mechanism precisely: once the strip pass
    swallows the first block's own `## Coding Discipline` heading, `swallowed_own_h2` must RESET at
    the terminator; if it is stale-true when the second provenance line arrives, the second block's
    own heading is mistaken for its terminator and the block is left orphaned in the output.

    `10-corrupt-mirror-refused` (D-46.4 — binds BOTH D-28.1 guard layers, asserted in both
    directions per D-38): exercise the PRE-FLIGHT block, not Step 1, because the mirror guards live
    there. Build three fake homes with `mk_fake_home` and a scratch project with `mk_project`
    (`.git` present, SKILL.md `version: 0.6.0`, so the pre-flight's other guards pass and the mirror
    guard is the only variable):
      (a) ZERO-BYTE mirror (`: > "$mirror"`) → assert the extracted pre-flight exits 3. This is the
          `test -s` layer; `test -f` alone passes here, which is the gap upstream closed.
      (b) TRUNCATED mirror (`head -20` of the real mirror — keeps the L1 `## ` heading, drops
          `### 4. Goal-Driven Execution` at L57) → assert the pre-flight exits 3. This is the
          tail-sentinel layer. Comment why truncation defeats `test -s` AND the pre-`mv` shape
          assertion: both only look at the head of a file that is truncated at the tail.
      (c) HEALTHY mirror (the real repo mirror) → assert the pre-flight exits 0. Without this
          direction, a pre-flight that refused EVERYTHING would pass (a) and (b) and read as a
          working guard.

    Then record the RED observation. Append a `## RED OBSERVED (TEST-02)` section to
    `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md` containing: the date;
    this repo's SHA from `git rev-parse HEAD`; confirmation via `test -f migrations/0009-spec-11-region-aware-placement.md`
    that the document does NOT exist; the VERBATIM output of `migrations/run-tests.sh 0009` showing
    `FAIL: > 0`; and the verbatim output of Task 2's naive-anchor override demonstration. State
    explicitly that TEST-02 is evidenced in two complementary halves — the suite-level RED recorded
    here, and the rule-level naive-anchor counter-replay recorded in 09-01's Task 2 counter-case A —
    and cross-reference both, so a verifier can trace "fails against the current naive anchor" to
    real recorded output rather than to a claim.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0009 2>&1 | tail -30; echo "exit=${PIPESTATUS[0]}"; grep -c 'RED OBSERVED' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - `migrations/run-tests.sh 0009` exits NON-ZERO with `FAIL: > 0` — RED, as required.
    - Source assertion, all ten labels present:
      `awk '/^test_migration_0009\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -cE '^[^#]*"(01|02|03|04|05|06|07|08|09|10)-'`
      returns at least 10. The `[^#]*` prefix keeps the count off the explanatory comments that
      name the same cases — an unfiltered count would report 10 for a function with zero fixtures.
    - Case 10 asserts all three mirror states and case 07 asserts `applied`: verify by inspection,
      quote the four assertion lines in the task evidence.
    - **Case 07 liveness demonstration (Dimension 8 item 4).** Temporarily change the idempotency
      check's marker regex from anchored (`/^<!-- gitnexus:start -->$/`) to a substring match
      (`/gitnexus:start/`) in a scratch copy of the extracted check, run case 07's assertion against
      it, and confirm case 07 FAILS while the other cases still pass. Revert. If case 07 passes with
      a substring match, the fixture is wrong and must be rewritten. Capture the output.
    - `grep -q 'RED OBSERVED' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md`
      succeeds, and the recorded `run-tests.sh 0009` output matches a fresh run.
    - `migrations/run-tests.sh 0001` still exits 0.
    - `gitnexus_detect_changes()` run before commit, per CLAUDE.md, with the affected scope
      recorded in the task evidence. This plan performs the heaviest `run-tests.sh` surgery in
      the phase (adds `test_migration_0009` plus ten fixture cases), so the affected-scope check
      is mandatory here, not optional. Confirm the reported scope is limited to
      `test_migration_0009` and its helpers — any reported change to `test_migration_0001`
      through `test_migration_0008` means the edit leaked out of the new function and MUST be
      corrected before commit.
    - Commit message uses the `test(09-03):` prefix.
  </acceptance_criteria>

  <done>All ten TEST-03 cases exist, the suite is RED, and the RED observation is recorded as auditable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted migration shell → harness process | Fixtures `eval` shell extracted from a migration document. Two extracted paths deliberately call `exit 3` (State D, corrupt mirror). |
| fixture scratch root → real repo | Fixtures execute file surgery. Executed against the real repo root instead of `$tmp`, they would rewrite the developer's own AGENTS.md. |
| fake `CODEX_HOME` → real `~/.codex` | Case 10 deliberately creates zero-byte and truncated mirrors. Pointed at the real Codex home, it would corrupt the developer's installed scaffolder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | Case 10's zero-byte/truncated mirror written to the real `~/.codex` scaffolder bundle | mitigate | `mk_fake_home` builds mirrors ONLY under `$tmp`; every eval exports `CODEX_HOME` to that fake home. Phase verification asserts `git status --porcelain` is clean and no file under `$HOME/.codex` was modified. |
| T-09-11 | Tampering | Fixture Apply executed with cwd at the real repo root, rewriting the developer's AGENTS.md | mitigate | Every eval runs in a subshell explicitly `cd`'d to a `mk_project` scratch root; `trap 'rm -rf "$tmp"' RETURN` bounds the blast radius. Acceptance asserts `git status --porcelain AGENTS.md` is empty. |
| T-09-07 | Denial of Service | Extracted `exit 3` path terminating the harness mid-suite, hiding later assertions | mitigate | Task 1 mandates explicit subshell wrapping for every eval and states the consequence; cases 05 and 10 are the two that exercise `exit 3` and both assert the exit code from a subshell. |
| T-09-08 | Tampering | Dead-by-construction fixtures passing vacuously (Phase 8's defect, hit 3x) | mitigate | Three mandatory liveness demonstrations: the naive-anchor override (Task 2), the substring-regex mutation against case 07 (Task 3), and the plan's terminal RED state itself. All captured to evidence. Source assertions are `awk`-scoped to the function body and comment-filtered. |
| T-09-12 | Repudiation | Claiming RED-before-GREEN without evidence | mitigate | `## RED OBSERVED` section records verbatim output, the repo SHA, and proof the 0009 document is absent; `test(09-03):` commit prefixes make the ordering legible in `git log`. |
| T-09-SC | Tampering | Package installs | accept | No `npm`/`pip`/`cargo` installs. No new dependency surface. |
</threat_model>

<verification>
- `migrations/run-tests.sh 0009` exits NON-ZERO with `FAIL: > 0`. **RED is this plan's required
  terminal state** — do not report it as a defect and do not let a checker "fix" it. GREEN arrives
  in plan 09-04.
- `migrations/run-tests.sh 0001` exits 0 and `migrations/run-tests.sh drift` exits 0 — the RED is
  confined to 0009's own assertions; nothing else regressed. (Drift stays green here because
  `run_drift_test` compares SKILL.md's `version:` against the NEWEST migration's `to_version`, and
  the newest migration is still 0008 at `0.6.0` until 09-04 lands 0009.)
- `git status --porcelain AGENTS.md migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md`
  prints NOTHING.
- `migrations/0009-spec-11-region-aware-placement.md` does NOT exist at this plan's end.
- All ten case labels present; all three liveness demonstrations captured in the SUMMARY.
</verification>

<success_criteria>
- TEST-03 satisfied structurally: ten cases, this repo's synthesized-`printf` idiom, one function,
  no per-fixture directories.
- TEST-02's RED half satisfied with recorded evidence: the suite was OBSERVED failing before 0009
  existed, and observed failing against the naive anchor specifically.
- D-38's four-state table asserted, including State B returning non-zero despite provenance present.
- Every new assertion has a demonstration that it fails when it should.
- ROADMAP hard ordering 2 discharged: 09-04 may now turn the suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/09-region-aware-11-placement/09-03-SUMMARY.md` when done.
Record in it: the RED output of `migrations/run-tests.sh 0009`, the naive-anchor override
demonstration, the case-07 substring mutation demonstration, and an explicit statement that the
plan ends RED by design with GREEN owned by 09-04.
</output>
