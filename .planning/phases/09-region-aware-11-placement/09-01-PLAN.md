---
phase: 09-region-aware-11-placement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/validate-0009-anchor.sh
  - .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md
autonomous: true
requirements: [ANCHOR-01, ANCHOR-02, ANCHOR-03, ANCHOR-04]

must_haves:
  truths:
    - "Replaying the candidate rule against this repo's real AGENTS.md re-derives §11's current position byte-identically (zero churn)"
    - "Replaying the candidate rule against a gitnexus-led AGENTS.md places the block above the gitnexus:start marker"
    - "The naive anchor is empirically observed FAILING the gitnexus-led case — the assertion is proven live, not dead"
    - "A narrow strip terminator is empirically observed destroying the GitNexus region on an already-healed file"
    - "The replay output is recorded in a committed evidence file a verifier can read"
  artifacts:
    - path: "migrations/validate-0009-anchor.sh"
      provides: "Committed empirical replay of the candidate anchor + strip rule against real and synthesized files"
      contains: "gitnexus:start"
    - path: ".planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md"
      provides: "Recorded replay output — the evidence Success Criterion 1 demands"
      contains: "ZERO CHURN"
  key_links:
    - from: "migrations/validate-0009-anchor.sh"
      to: "AGENTS.md"
      via: "reads the repo's real instruction file as replay input"
      pattern: "AGENTS\\.md"
    - from: ".planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md"
      to: "migrations/validate-0009-anchor.sh"
      via: "records the script's verbatim output and the SHA it was run at"
      pattern: "validate-0009-anchor\\.sh"
---

<objective>
Validate the region-aware anchor rule (D-21) and the widened strip terminator (D-24)
EMPIRICALLY, against this host's real AGENTS.md and a synthesized gitnexus-led file,
and record the output as committed evidence — **before** migration 0009's apply-block
is authored.

This plan is ROADMAP hard ordering constraint 1 ("validate before you write") made
executable. Success Criterion 1 demands *recorded evidence*, not an asserted claim:
plan 09-04 may not author 0009's Step 1 Apply until this plan's evidence file exists
and shows PASS.

Purpose: the source design caught a wrong anchor alternative empirically, not by
review. This plan reproduces that discipline on this host before any migration ships.
Output: a committed replay script and a verbatim evidence record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-region-aware-11-placement/09-CONTEXT.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION.md
@.planning/phases/09-region-aware-11-placement/09-PATTERNS.md

**Upstream reference is PINNED (D-48):** `claude-workflow @ 8520f90d235e0c50b0484b170d595ab6f2cd1173`.
Read pinned content with `git -C ../claude-workflow show 8520f90:<path>`. Verified at plan
time: the working tree at that path is byte-identical to the pin, but upstream `HEAD` has moved
past it. Any upstream change after the pin is a deliberate follow-up diff, never absorbed here.

**Verified facts this plan rests on (confirmed at plan time — do not re-derive):**
- `AGENTS.md:18` (`## Coding Discipline (NON-NEGOTIABLE)`) is the FIRST `## ` line in the file.
  `grep -n '^## ' AGENTS.md` returns 18, 98, 110, 141, 155. Provenance sits at L17, mirror
  content at L18-96, one blank line at L97, `## Development Workflow` at L98. The zero-churn
  replay is therefore a real test, not a vacuous one.
- `AGENTS.md:271` / `:313` carry `<!-- gitnexus:start -->` / `<!-- gitnexus:end -->`. The region
  does NOT lead this file — the defect is LATENT here. There is no broken repo to repair; do not
  modify the real AGENTS.md in this plan.
- The mirror is 79 lines with exactly one `## ` (its own L1) and four `### ` subsections, the
  last being `### 4. Goal-Driven Execution` at L57.
</context>

<interfaces>
<!-- The rules under test. All are quoted verbatim in 09-PATTERNS.md with file:line -->
<!-- citations. Read them there and at the pinned source — do not retype from memory. -->

Candidate STRIP terminator (D-24, corrected) — PATTERNS.md section "The strip boundary (D-24)",
sourced from pinned 0029 lines 192-210. The terminator condition is the alternation
`(/^## / || /^<!-- gitnexus:start -->$/)`, gated behind a `swallowed_own_h2` flag that resets
at the terminator.

Candidate ANCHOR condition (D-21) — PATTERNS.md section "The anchor rule (D-21)", sourced from
pinned 0029 lines 226-246. The insert condition is the same alternation
`!inserted && (/^## / || /^<!-- gitnexus:start -->$/)`, with an EOF fallback in END.

NAIVE anchor (the incumbent, under test as the counter-case) —
`migrations/0001-inject-spec-11-coding-discipline.md:91`: `/^## / && !done {`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the replay script with the two candidate replays (ANCHOR-03, ANCHOR-04)</name>

  <files>migrations/validate-0009-anchor.sh</files>

  <read_first>
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` sections "The anchor rule
      (D-21)" and "The strip boundary (D-24)" — the verbatim awk to adapt, retargeting
      `CLAUDE.md` to `AGENTS.md`.
    - Pinned upstream source of truth — read it, do not transcribe from memory:
      `git -C ../claude-workflow show 8520f90:migrations/0029-region-aware-spec-11-placement.md`
      lines 192-210 (strip pass) and 226-246 (insert pass).
    - `migrations/0001-inject-spec-11-coding-discipline.md` lines 79-108 — the naive anchor and
      the exact injected shape (provenance line, then mirror content streamed via
      `getline line < mirror`, then ONE trailing blank line). The candidate insert must reproduce
      this shape exactly or zero-churn fails for a spurious reason.
    - `migrations/run-tests.sh` lines 79-98 — this repo's `printf`-into-`$tmp` synthesis idiom and
      its `mktemp -d` + `trap ... RETURN` convention (D-34). Mirror that style.
    - `AGENTS.md` lines 1-22 and 265-315 — the real replay input, and the region's real position.
  </read_first>

  <action>
    Create `migrations/validate-0009-anchor.sh`, executable via `chmod +x`, with `set -uo pipefail`,
    resolving `REPO_ROOT` via `git rev-parse --show-toplevel` exactly as `run-tests.sh` lines 24-28
    do. Synthesize all working files under a `mktemp -d` scratch dir. This script is a validation
    harness, not a migration: it MUST NOT modify any tracked file, and in particular must never
    write to the repo's real AGENTS.md.

    Define two shell functions carrying the candidate rules, adapted from the pinned 0029 awk named
    in read_first with `CLAUDE.md` retargeted to an input path passed as the first argument:
    `candidate_strip` — the `swallowed_own_h2` state machine terminating on
    `(/^## / || /^<!-- gitnexus:start -->$/)`; and `candidate_insert` — the anchor condition
    `!inserted && (/^## / || /^<!-- gitnexus:start -->$/)` plus the END EOF fallback. Stream the
    mirror with `getline line < block_file`; never `cat` it inline and never transcribe §11's prose.
    Use the repo mirror at
    `skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md`
    and the provenance literal `<!-- spec-source: agenticapps-workflow-core@0.4.0 §11 -->` — per
    D-29, `@0.4.0` is the block's CONTENT version and stays hardcoded; do not bump it to 0.7.0.

    Implement replay CASE 1, ANCHOR-03 zero churn: copy the repo's real AGENTS.md into the scratch
    dir, run `candidate_strip` then `candidate_insert` over the copy, and `diff` the result against
    the untouched original. PASS iff byte-identical. Print the full `diff` output on failure — a
    silent boolean is worthless for the evidence trail.

    Implement replay CASE 2, ANCHOR-04 above-region: synthesize a gitnexus-led AGENTS.md via
    `printf` — an H1 and a paragraph, then `<!-- gitnexus:start -->` on its own line, a region body,
    `<!-- gitnexus:end -->`, and only THEN a `## Some Section` heading after the region. That
    ordering is the point: it is what discriminates D-21's rule from D-22.1's rejected
    "region is always the anchor" alternative. Run `candidate_insert` over it. PASS iff the
    provenance line's line number is strictly less than the `<!-- gitnexus:start -->` line's line
    number, AND the region body is still present with its markers still paired (exactly one
    `^<!-- gitnexus:start -->$` and exactly one `^<!-- gitnexus:end -->$`).

    Print one PASS/FAIL line per case with a stable label containing the literal `ZERO CHURN` for
    case 1 and `ABOVE REGION` for case 2, and exit non-zero if any case fails. Both labels are
    consumed verbatim by Task 3's evidence file.
  </action>

  <verify>
    <automated>bash migrations/validate-0009-anchor.sh; echo "exit=$?"; git status --porcelain AGENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -x migrations/validate-0009-anchor.sh` succeeds.
    - `bash migrations/validate-0009-anchor.sh` exits 0; stdout contains a PASS line carrying
      `ZERO CHURN` and a PASS line carrying `ABOVE REGION`.
    - `git status --porcelain AGENTS.md` prints nothing — the script did not touch the real file.
    - Source assertion, D-24 two-site rule:
      `grep -v '^[[:space:]]*#' migrations/validate-0009-anchor.sh | grep -c 'gitnexus:start -->\$/'`
      returns at least 2 — the alternation must appear in BOTH the strip terminator and the insert
      anchor condition. Exactly one occurrence means only one site carries it and the script is wrong.
      (Comments are filtered first: an unfiltered `grep -c` counts the prose that explains the rule
      and would report success for a script that never implements it.)
    - Source assertion: the script contains `getline line <` and does NOT contain the literal string
      `Think Before Coding` — §11 prose is streamed from the mirror, never transcribed.
  </acceptance_criteria>

  <done>The candidate rule is executable and both replay cases pass against real and synthesized input.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two counter-replays that prove the assertions are live (D-36 / Dimension 8)</name>

  <files>migrations/validate-0009-anchor.sh</files>

  <read_first>
    - `migrations/validate-0009-anchor.sh` — the file being modified (Task 1's output).
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` section "Dimension 8 —
      Anti-Dead-Assertion Contract", items 2 and 3. Item 3 names this task's second counter-replay
      as "the assertion the pre-correction CONTEXT.md would not have caught. Without it the phase
      can ship green and still eat a GitNexus region."
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-24 — the terminator
      alternation rationale ("this is not cosmetic") and D-36's "non-empty is not the same as
      correct".
    - `migrations/0001-inject-spec-11-coding-discipline.md` lines 84-108 — the naive anchor awk to
      replay as the counter-case.
  </read_first>

  <action>
    Extend `migrations/validate-0009-anchor.sh` with two counter-replays. A counter-replay asserts
    that a WRONG rule FAILS. If a counter-replay's wrong rule passes, the corresponding positive
    assertion from Task 1 is dead-by-construction, and the script must exit non-zero and say so.
    This is the direct antidote to the Phase 8 defect where assertions that could never match
    silently passed and read as coverage.

    Add COUNTER-CASE A — the naive anchor must fail ANCHOR-04. Define `naive_insert` carrying
    0001's incumbent condition `/^## / && !done` with no marker alternation, replay it over the
    same synthesized gitnexus-led file from Task 1 case 2, and assert the provenance line lands
    strictly AFTER the `<!-- gitnexus:start -->` line — i.e. inside the region, which is the latent
    defect this whole phase exists to close. Report
    `PASS (counter) NAIVE ANCHOR INSERTS INSIDE REGION` when the naive rule misbehaves as predicted.
    Report FAIL and exit non-zero if the naive rule somehow lands above the region, because that
    would mean case 2 cannot discriminate between the two rules and proves nothing.

    Add COUNTER-CASE B — a narrow strip terminator must destroy the region. This is the
    highest-severity mechanic in the phase (D-24). Build the ALREADY-HEALED State-B file by taking
    Task 1 case 2's candidate output, in which the block sits correctly anchored immediately above
    `<!-- gitnexus:start -->`. Define `narrow_strip`: a copy of `candidate_strip` whose terminator
    condition is `/^## /` ONLY, with the marker alternation removed. Run it over the healed file and
    assert the GitNexus region is DESTROYED — `<!-- gitnexus:start -->` gone while
    `<!-- gitnexus:end -->` remains, the orphaned unpaired region the source design reports, and/or
    the region body lines gone. Then run the real `candidate_strip` over the same healed file and
    assert the region survives intact and paired. Report the two results as
    `PASS (counter) NARROW TERMINATOR EATS REGION` and `PASS WIDENED TERMINATOR PRESERVES REGION`.

    Ensure the script's exit status is non-zero if ANY case — positive or counter — fails.
  </action>

  <verify>
    <automated>bash migrations/validate-0009-anchor.sh; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `bash migrations/validate-0009-anchor.sh` exits 0 and stdout contains all five labels:
      `ZERO CHURN`, `ABOVE REGION`, `NAIVE ANCHOR INSERTS INSIDE REGION`,
      `NARROW TERMINATOR EATS REGION`, `WIDENED TERMINATOR PRESERVES REGION`.
    - Mutation demonstration proving counter-case B is live: temporarily add the
      `|| /^<!-- gitnexus:start -->$/` alternation into `narrow_strip`'s terminator so the "narrow"
      rule is no longer narrow, re-run the script, and confirm it now EXITS NON-ZERO reporting that
      the narrow terminator failed to destroy the region. Revert the mutation and confirm exit 0
      returns. Capture the stdout of BOTH runs into the task's SUMMARY evidence — a counter-case
      never observed failing is not a counter-case.
    - `git status --porcelain AGENTS.md` prints nothing.
  </acceptance_criteria>

  <done>Both wrong rules are empirically observed misbehaving, and both right rules are observed behaving correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Record the replay evidence (Success Criterion 1)</name>

  <files>.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/09-region-aware-11-placement/09-VALIDATION.md` section "Empirical Replay
      Evidence (ANCHOR-03 / ANCHOR-04)" — the recorded-evidence contract this file satisfies.
    - `.planning/ROADMAP.md` Phase 9 Success Criteria — criterion 1 is the acceptance target.
    - `migrations/validate-0009-anchor.sh` — the script whose output is recorded.
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-48 — the upstream pin rule.
  </read_first>

  <action>
    Create `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md`.

    Record, in this order: (1) the exact command run and the date; (2) this repo's SHA from
    `git rev-parse HEAD`; (3) the pinned upstream reference SHA
    `8520f90d235e0c50b0484b170d595ab6f2cd1173` per D-48, alongside the actual output of
    `git -C ../claude-workflow rev-parse HEAD` — if upstream HEAD differs from the pin, state that
    plainly as a recorded fact and confirm the pinned content was used regardless. Upstream drift is
    a follow-up note, never a licence to absorb changes mid-phase. (4) The script's VERBATIM stdout
    in a fenced block — do not summarize, paraphrase, or re-type it. (5) The mutation demonstration
    output from Task 2's acceptance criteria: both the mutated (non-zero) and reverted (zero) runs.

    Then state, as four explicit claim/evidence pairs, what each replay demonstrates: ANCHOR-03 zero
    churn on the real file; ANCHOR-04 above-region on a gitnexus-led file; the naive anchor failing
    ANCHOR-04; and the narrow terminator destroying the region while the widened terminator does
    not. Each claim must cite the evidence line that supports it, so a verifier can confirm the
    replay happened rather than take the claim on faith.

    Close with a one-line gate statement: this evidence exists and shows PASS, therefore plan 09-04
    is unblocked to author 0009's Step 1 Apply (ROADMAP hard ordering 1).
  </action>

  <verify>
    <automated>test -s .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md && grep -q 'ZERO CHURN' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md && grep -q 'ABOVE REGION' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md && grep -q 'NARROW TERMINATOR EATS REGION' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md && grep -q '8520f90' .planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md && echo EVIDENCE-OK</automated>
  </verify>

  <acceptance_criteria>
    - The automated command above prints `EVIDENCE-OK`.
    - The evidence file's fenced output block is byte-consistent with a fresh
      `bash migrations/validate-0009-anchor.sh` run — re-run the script and diff its stdout against
      the recorded block; any divergence means the record is stale and must be regenerated.
    - The file records this repo's SHA, the pin `8520f90`, and the observed
      `git -C ../claude-workflow rev-parse HEAD`.
    - The file contains four claim/evidence pairs, each citing a specific recorded output line.
  </acceptance_criteria>

  <done>Success Criterion 1's evidence exists as a committed, verifier-readable record; plan 09-04 is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay script → repo working tree | The script reads the real AGENTS.md. A write here would corrupt a tracked file for no reason — the boundary is enforced by scratch-dir-only writes. |
| pinned upstream (`claude-workflow @ 8520f90`) → this repo | Ported awk crosses a repo boundary. Upstream HEAD has already moved past the pin; unpinned reads would silently import unreviewed logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `narrow_strip` counter-case (D-24 runaway strip) | mitigate | This plan does not merely avoid the hazard — Task 2 counter-case B REPRODUCES it deliberately in a scratch dir and asserts the widened terminator prevents it. Empirical proof precedes the migration that must not have the bug. |
| T-09-02 | Tampering | `validate-0009-anchor.sh` writing to the real AGENTS.md | mitigate | All writes confined to `mktemp -d`; every task's acceptance criteria assert `git status --porcelain AGENTS.md` is empty. |
| T-09-04 | Tampering | Truncated/partial replay output silently read as PASS | mitigate | Script exits non-zero on any case failure; Task 3's acceptance re-runs the script and diffs stdout against the recorded evidence block, so a stale or truncated record cannot pass. |
| T-09-05 | Tampering | Unpinned upstream awk imported mid-phase | mitigate | `read_first` mandates `git -C ../claude-workflow show 8520f90:<path>`; Task 3 records both the pin and observed upstream HEAD, making drift visible rather than invisible (D-48). |
| T-09-SC | Tampering | Package installs | accept | No `npm`/`pip`/`cargo` installs in this phase — RESEARCH.md's Package Legitimacy Gate scope condition is not met. No new dependency surface; `awk`/`git`/`bash` are already required by the existing harness. |
</threat_model>

<verification>
- `bash migrations/validate-0009-anchor.sh` exits 0 with all five labels present.
- `migrations/run-tests.sh` full suite still reports FAIL: 0 (baseline 278 PASS / 1 SKIP / 0 FAIL).
  This plan adds no assertions to the harness, so the count must be UNCHANGED at 278 — a changed
  count means this plan touched something it should not have.
- `git status --porcelain` shows only the two files in `files_modified`.
- `.planning/phases/09-region-aware-11-placement/09-VALIDATION-EVIDENCE.md` exists and is non-empty.
</verification>

<success_criteria>
- ANCHOR-03 satisfied with recorded evidence: zero churn replaying against the real AGENTS.md.
- ANCHOR-04 satisfied with recorded evidence: above-region anchoring on a gitnexus-led file.
- ANCHOR-01/ANCHOR-02's rule (first `## ` or anchored marker, whichever comes first; EOF fallback)
  is expressed executably and observed behaving on both real and synthesized input.
- Both counter-cases observed failing the wrong rules — no assertion in this plan is dead.
- ROADMAP hard ordering 1 discharged: 09-04 may now author 0009's apply-block.
</success_criteria>

<output>
Create `.planning/phases/09-region-aware-11-placement/09-01-SUMMARY.md` when done.
Record in it: the five replay labels observed, the mutation-demonstration output from Task 2,
this repo's SHA, and the observed upstream HEAD versus the pin.
</output>

