---
phase: 09-region-aware-11-placement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/run-tests.sh
autonomous: true
requirements: [TEST-01, TEST-04]

must_haves:
  truths:
    - "A fence-scoped extractor pulls a named block out of any migration document in this repo — D-35: port the extractor, not claude-workflow's per-fixture-directory layout, adapted from the pinned reference (D-48)"
    - "Every extraction is shape-asserted — an extractor that latched onto the wrong fence fails loudly instead of returning empty-but-unchecked (D-36: non-empty is not the same as correct)"
    - "Each shape guard has been observed FAILING against a deliberately wrong document, including the pre-flight block carrying D-28.1's mirror guards"
    - "test_migration_0001 no longer carries an inlined copy of 0001's anchor awk — it executes 0001's own document (D-37: TEST-04 scope is run-tests.sh:119 only; 0008's Step-3 copy stays deferred)"
    - "The full suite still reports FAIL: 0"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "extract_step_block / extract_preflight_block / assert_extracted_shape helpers, and a document-sourced test_migration_0001"
      contains: "extract_step_block"
  key_links:
    - from: "migrations/run-tests.sh"
      to: "migrations/0001-inject-spec-11-coding-discipline.md"
      via: "extract_step_block reads 0001's Step 1 Apply fence instead of an inlined copy"
      pattern: "0001-inject-spec-11-coding-discipline\\.md"
---

<objective>
Build this repo's first fence-scoped extractor for a migration document's own shell, with
D-36's shape assertions attached, and retire the inlined anchor copy at
`migrations/run-tests.sh:119` by pointing `test_migration_0001` at 0001's own document.

Purpose: TEST-01 requires fixtures to execute the migration's shell **extracted from the
migration document itself, never a transcribed copy**. That extractor does not exist in this
repo yet — `vendor/agenticapps-shared`'s `extract_to()` is a git-show extractor and does not
solve this. Plan 09-03's RED fixture suite depends on this plan's helpers, so this plan builds
them once, against 0001 (a document that already exists), rather than co-developing an
extractor and a fixture suite against a migration that does not exist yet.

Closing `:119` (TEST-04, Success Criterion 5) is the natural first consumer: it proves the
extractor works against a real document before 09-03 bets a whole fixture suite on it.

Output: three reusable helpers plus a document-sourced `test_migration_0001`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-region-aware-11-placement/09-CONTEXT.md
@.planning/phases/09-region-aware-11-placement/09-PATTERNS.md
@.planning/phases/09-region-aware-11-placement/09-VALIDATION.md

**Upstream reference is PINNED (D-48):** `claude-workflow @ 8520f90d235e0c50b0484b170d595ab6f2cd1173`.
Read pinned content with `git -C ../claude-workflow show 8520f90:<path>`.

**Scope fence (D-37) — TEST-04 is `run-tests.sh:119` ONLY.** Do NOT also convert 0008's
Step-3 insert-awk copy near `run-tests.sh:985`. It is a real instance of the same drift class
and it is explicitly DEFERRED (09-CONTEXT.md Deferred Ideas) because reaching into another
migration's tests widens a placement fix into harness refactoring across a 278-assertion suite.

**0001 is IMMUTABLE (fix-forward).** It appears in `read_first` only. Its naive anchor
(`/^## / && !done` at `:91`) is legitimately naive and stays that way. A document-sourced test
against it faithfully asserts 0001's naive behavior — that is a FIDELITY improvement, not a
behavior change, and it does not conflict with TEST-02 (which concerns *0009's* fixtures going
RED). Never edit `migrations/0001-*.md` or `migrations/0004-*.md`.

**Project rule (CLAUDE.md):** GitNexus impact analysis is MANDATORY before editing an existing
symbol. `test_migration_0001` is an existing function being edited — Task 2 runs it. Adding new
functions is symbol creation, not an edit, so Task 1's new helpers do not require it.
</context>

<interfaces>
<!-- The extractor to port. Read it at the pin; do not retype from memory. -->

Source: `git -C ../claude-workflow show 8520f90:migrations/test-fixtures/0029/common-verify.sh`
(168 lines). It extracts THREE blocks per fixture — Step 1 Idempotency (lines 61-95), Apply
(97-131), Rollback (133-168) — each with its own `case` shape guard. 09-PATTERNS.md section
"The extractor to port (D-35)" quotes the Apply extractor verbatim.

The awk shape, per PATTERNS.md: scope on `/^### Step 1/` → `/^### Step 2/`, then
`/^\*\*Apply:\*\*/` sets `want`, then `want && /^```/` opens the block and CLEARS `want` — the
`want=0` is load-bearing: it is why a ```bash→```sh change cannot make the scan skip past and
latch onto the Rollback fence. Then `inb && /^```$/ { exit }`.

**This repo's document conventions differ from upstream's** — verified at plan time:
- This repo uses `### Step N: <title>` (colon). Upstream 0029 uses `### Step N — <title>` (dash).
  The extractor must key on `^### Step N` (prefix only), which matches both.
- This repo's pre-flight heading is `## Pre-flight` (`0001:44`, `0004:38`), fenced ```bash.
- `0001:83` is `**Apply:**` on its own line; `0004:64` is `**Apply:** <prose>` on the same line.
  The `/^\*\*Apply:\*\*/` prefix match handles both. 0009 (plan 09-04) must stay inside this shape.

Existing in-repo precedent for "assert extraction non-empty BEFORE trusting it" (T-08-23):
`migrations/run-tests.sh:967-977`. Cite it; this plan generalizes the same discipline from
template content to migration shell.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the fence-scoped extractor and its shape-assertion guard (TEST-01)</name>

  <files>migrations/run-tests.sh</files>

  <read_first>
    - `migrations/run-tests.sh` lines 1-78 — the file being modified: harness header, `REPO_ROOT`
      resolution, shared-lib sourcing, and the `FILTER` convention. New helpers go near the top,
      above the `test_migration_*` functions, so 09-03 can use them.
    - `migrations/run-tests.sh` lines 967-977 — the in-repo T-08-23 precedent this generalizes.
    - Pinned upstream extractor:
      `git -C ../claude-workflow show 8520f90:migrations/test-fixtures/0029/common-verify.sh`
      — read ALL THREE extractions and their `case` guards, not just Apply's.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` section "The extractor to
      port (D-35)" and "Port all THREE extractions, not just Apply's".
    - `migrations/0001-inject-spec-11-coding-discipline.md` lines 44-118 — a real document to
      extract from; confirms the `## Pre-flight` / `### Step 1:` / `**Apply:**` shape.
    - `vendor/agenticapps-shared/migrations/lib/helpers.sh` lines 47-79 — `run_check` and
      `assert_check`. Note `run_check` already wraps the snippet in a subshell; anything this
      plan's helpers `eval` outside `run_check` must be subshell-wrapped by the caller, because
      an extracted block that calls `exit 3` would otherwise kill the harness mid-suite.
  </read_first>

  <action>
    Add three helpers to `migrations/run-tests.sh`, placed above `test_migration_0001` so every
    later test function can use them. Adapt from the pinned `common-verify.sh` named in read_first —
    read it and adapt deliberately; do not hand-transcribe the awk from memory.

    `extract_step_block <doc_path> <step_number> <label>` — prints the FIRST fenced block following
    a `**<label>:**` line within `### Step <step_number>`, scoped to end at `### Step <step_number+1>`.
    Key the scope on the `^### Step N` prefix, NOT on the full heading text: this repo writes
    `### Step 1: <title>` with a colon while upstream writes a dash, and the prefix match handles
    both. Preserve upstream's `want=0`-on-fence-open behavior exactly — it is the guard that stops
    a bash-to-sh fence-language change from latching onto the Rollback fence. `<label>` is `Apply` or
    `Idempotency check`; escape the label for awk's regex safely rather than interpolating raw.

    `extract_preflight_block <doc_path>` — prints the first fenced block under the `## Pre-flight`
    heading, scoped to end at the next `^## ` heading. Plan 09-03's fixture 10 needs this because
    D-28.1's mirror guards live in pre-flight, not in Step 1.

    `assert_extracted_shape <label> <text> <required_substring>` — D-36's antidote, ported from
    upstream's `case` guards. It must do two things and increment the harness counters for both:
    assert the text is NON-EMPTY, and assert it CONTAINS `required_substring`. On failure it must
    print the extracted text indented (upstream pipes through `sed 's/^/       /'`) and increment
    `FAIL` — never silently return. Non-empty is not the same as correct: an extractor that drifted
    onto the wrong fence returns plenty of text.

    Add a header comment above the helpers recording: that they exist because TEST-01 forbids
    transcribed copies; that `vendor/agenticapps-shared`'s `extract_to()` is a git-show extractor
    and deliberately does not solve this; and the pin `claude-workflow @ 8520f90` these were
    ported from, so a future reader can diff against the source.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0001; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -v '^[[:space:]]*#' migrations/run-tests.sh | grep -c 'extract_step_block\|extract_preflight_block\|assert_extracted_shape'`
      returns at least 3 — the three helpers exist as code, not only as the header comment that
      describes them. (Comments filtered first: an unfiltered count would pass on the comment alone.)
    - Behavior demonstration, extractor works: from a shell, source or invoke the harness such that
      `extract_step_block migrations/0001-inject-spec-11-coding-discipline.md 1 Apply` prints a
      block containing both `/^## / && !done` and `getline line < mirror`, and NOT containing the
      string `Rollback`. Record the output in the task's evidence.
    - **Guard-failure demonstration (Dimension 8 — mandatory, not optional).** Each of the three
      guards must be OBSERVED failing:
      (a) point `extract_step_block` at a document with no `### Step 1` (use
          `migrations/README.md`) and confirm `assert_extracted_shape` reports FAIL for
          empty extraction rather than passing;
      (b) call `assert_extracted_shape` with a correct non-empty block but a
          `required_substring` the block cannot contain (e.g. `gitnexus:start` against 0001's
          Apply, which has no marker alternation) and confirm it reports FAIL and prints the
          extracted text indented;
      (c) confirm the same call with a substring the block DOES contain (`getline line < mirror`)
          reports PASS.
      Capture the stdout of all three demonstrations into the task's SUMMARY evidence. A guard
      never observed failing is not a guard — this is exactly the defect Phase 8 shipped three times.
    - `migrations/run-tests.sh 0001` exits 0.
  </acceptance_criteria>

  <done>Three reusable, shape-asserting extraction helpers exist, and each guard has been observed failing when it should.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the inlined anchor copy at run-tests.sh:119 (TEST-04, Success Criterion 5)</name>

  <files>migrations/run-tests.sh</files>

  <read_first>
    - `migrations/run-tests.sh` lines 79-145 — the file and function being modified.
      `:118-125` is the inlined awk copy of 0001's anchor being retired; `:126-135` is the
      byte-identity assertion that must survive the conversion unchanged in meaning.
    - `migrations/0001-inject-spec-11-coding-discipline.md` lines 79-118 — the document becoming
      the source of truth. Note its Step 1 Apply block is SELF-CONTAINED: it re-declares
      `MIRROR="${CODEX_HOME:-$HOME/.codex}/skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/11-coding-discipline-0.4.0.md"`
      itself and operates on `AGENTS.md` in the current working directory. That is what makes it
      eval-able in a scratch dir.
    - `.planning/phases/09-region-aware-11-placement/09-PATTERNS.md` section
      "`migrations/run-tests.sh:118-134` (retire inlined §11 anchor copy, TEST-04/D-37)".
    - `.planning/phases/09-region-aware-11-placement/09-CONTEXT.md` D-37 — the scope fence.
  </read_first>

  <action>
    FIRST, per CLAUDE.md's mandatory rule, run GitNexus impact analysis on the symbol being edited:
    `gitnexus_impact({target: "test_migration_0001", direction: "upstream"})`. Report the blast
    radius (direct callers, risk level) in the task evidence. If it returns HIGH or CRITICAL, stop
    and surface it before editing. Expected: LOW — the dispatcher at `run-tests.sh:3149` is the only
    caller — but the rule is unconditional, and RESEARCH.md assumption A2 flags that GitNexus may
    not index shell functions at all; if the tool returns nothing, record that as the finding and
    proceed.

    Replace the inlined awk at `run-tests.sh:118-125` with document-sourced execution of 0001's own
    Step 1 Apply block, using Task 1's helpers.

    Build a scratch project root under the existing `$tmp` (the function already has `mktemp -d` +
    `trap ... RETURN`): a directory containing an `AGENTS.md` seeded from the SAME
    `printf '# Title\n\n## Some Section\n\nbody\n'` shape fixture A already uses, plus a fake Codex
    home at `$tmp/codexhome/skills/setup-codex-agenticapps-workflow/templates/spec-mirrors/`
    holding a copy of the repo mirror. Extract 0001's Step 1 Apply with
    `extract_step_block migrations/0001-inject-spec-11-coding-discipline.md 1 Apply`, shape-assert
    it with `assert_extracted_shape` requiring the substring `getline line < mirror` — that
    substring proves the extraction is the mirror-streaming injection block and not some other
    fence — then execute it in a SUBSHELL with `CODEX_HOME` pointed at the fake home and the cwd set
    to the scratch project root. The subshell is mandatory: an extracted block that hits an `exit`
    path would otherwise terminate the whole harness mid-suite.

    Keep the existing byte-identity assertion's MEANING intact: the injected §11 block extracted
    from the resulting AGENTS.md must still `diff` byte-identical against the repo mirror, and it
    must still report a PASS/FAIL line. Retarget it from `$tmp/a-injected.md` to the scratch root's
    AGENTS.md.

    Add one new PASS/FAIL assertion, mirroring the T-08-23 precedent at `:967-977`: extraction from
    0001's REAL document must be non-empty BEFORE the injection is asserted, so a heading-regex
    drift reports as "extraction empty" rather than as a confusing downstream injection failure.

    Add a comment at the conversion site recording that this closes TEST-04 / Success Criterion 5,
    that 0001 is immutable and its naive `/^## / && !done` anchor is asserted here FAITHFULLY and
    on purpose, and that 0008's sibling copy near `:985` is a known, deferred instance of the same
    drift class (D-37) — so the next reader does not assume it was missed.
  </action>

  <verify>
    <automated>migrations/run-tests.sh 0001; echo "exit=$?"; migrations/run-tests.sh 2>&1 | tail -6</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion, the copy is GONE: `awk '/^test_migration_0001\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -v '^[[:space:]]*#' | grep -c '/\^## / && !done'`
      returns 0. The awk scopes the count to `test_migration_0001`'s body and the `grep -v` drops
      comments — a bare repo-wide `grep -c` would match 0001's own document and the new explanatory
      comment, and would report failure for a correct conversion.
    - Source assertion, the replacement is REAL:
      `awk '/^test_migration_0001\(\)/{f=1} f&&/^}/{exit} f' migrations/run-tests.sh | grep -c 'extract_step_block'`
      returns at least 1.
    - `migrations/run-tests.sh 0001` exits 0 and its output still contains the PASS line for
      `injected §11 block is byte-identical to the mirror`.
    - Mutation demonstration (proves the converted assertion is live, not dead): temporarily point
      the extraction at `migrations/0004-revendor-spec-11.md` instead of 0001, re-run
      `migrations/run-tests.sh 0001`, and confirm it FAILS (0004's Apply strips with a content
      sentinel and injects differently, so byte-identity or the shape guard must break). Revert.
      Record both runs' output in the task evidence.
    - `migrations/run-tests.sh` full suite reports `FAIL: 0` and a PASS count of at least 278
      (the pre-plan baseline is 278 PASS / 1 SKIP / 0 FAIL; this plan ADDS assertions, so PASS may
      rise but must never fall, and FAIL must stay 0).
    - `gitnexus_detect_changes()` run before commit, per CLAUDE.md, with the affected scope
      recorded in the task evidence.
  </acceptance_criteria>

  <done>test_migration_0001 executes 0001's own document; the inlined anchor copy at :119 no longer exists; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration document → `eval` in the harness | The harness now executes shell extracted from a markdown document. The document is repo-committed, reviewed content, not user input — but a mis-scoped extractor executes the WRONG committed block, which is the real risk here. |
| extracted block → harness process | An extracted block containing `exit 3` would terminate the whole 278-assertion suite if eval'd in-process. |
| pinned upstream (`8520f90`) → this repo | Ported extractor logic crosses a repo boundary; upstream HEAD has already moved past the pin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | `extract_step_block` latching onto the wrong fence and the harness eval'ing it | mitigate | `assert_extracted_shape` gates every extraction on a required substring before execution, and upstream's `want=0`-on-fence-open behavior is preserved verbatim so a ```bash→```sh change cannot skip into the Rollback fence. Task 1 acceptance OBSERVES the guard failing. |
| T-09-07 | Denial of Service | Extracted block calling `exit` kills the harness mid-suite, hiding all later assertions | mitigate | Every eval of an extracted block runs in an explicit subshell; `read_first` names `run_check`'s existing subshell convention as the precedent. |
| T-09-08 | Tampering | Dead-by-construction assertion reads as coverage while testing nothing (Phase 8's defect, hit 3x) | mitigate | Both tasks carry a mandatory failure/mutation demonstration in acceptance criteria, with output captured to evidence. Source assertions are scoped with `awk` to the function body and comment-filtered so they cannot pass on prose. |
| T-09-09 | Tampering | Silent edit to immutable 0001/0004 | mitigate | 0001/0004 appear in `read_first` only and are absent from `files_modified`; fix-forward is stated in `<context>`. Phase verification asserts `git status --porcelain migrations/0001-*.md migrations/0004-*.md` is empty. |
| T-09-05 | Tampering | Unpinned upstream logic imported mid-phase | mitigate | `read_first` mandates `git -C ../claude-workflow show 8520f90:<path>`; the pin is recorded in the helper header comment (D-48). |
| T-09-SC | Tampering | Package installs | accept | No `npm`/`pip`/`cargo` installs. No new dependency surface; `awk`/`bash`/`git` are already required by the existing harness. |
</threat_model>

<verification>
- `migrations/run-tests.sh 0001` exits 0.
- `migrations/run-tests.sh` full suite: `FAIL: 0`, PASS ≥ 278, SKIP 1.
- `git status --porcelain migrations/0001-inject-spec-11-coding-discipline.md migrations/0004-revendor-spec-11.md`
  prints NOTHING — the immutable migrations were not touched.
- `grep -c 'extract' migrations/run-tests.sh` shows the 0008 copy near `:985` is untouched:
  `awk 'NR>900 && NR<1010' migrations/run-tests.sh | grep -c 'extract_step_block'` returns 0
  (D-37 scope fence — converting it is deferred, not done here).
- All three guard-failure demonstrations and the Task 2 mutation demonstration are recorded in
  the plan's SUMMARY with their captured output.
</verification>

<success_criteria>
- TEST-01's mechanism exists: migration shell is extracted from the migration document, with
  shape assertions, never transcribed.
- TEST-04 satisfied: the inlined anchor copy at `run-tests.sh:119` is gone, replaced by
  document-sourced extraction (ROADMAP Success Criterion 5).
- Every new assertion has been demonstrated to fail when it should (Dimension 8 / D-36).
- The helpers are generic enough for plan 09-03 to consume for 0009's Pre-flight, Step 1
  Idempotency check, and Step 1 Apply.
- No regression: full suite FAIL: 0.
</success_criteria>

<output>
Create `.planning/phases/09-region-aware-11-placement/09-02-SUMMARY.md` when done.
Record in it: the GitNexus impact result for `test_migration_0001` (or the fact that shell
functions are not indexed), the three guard-failure demonstrations' output, the Task 2 mutation
demonstration's output, and the full suite's PASS/FAIL/SKIP counts before and after.
</output>
