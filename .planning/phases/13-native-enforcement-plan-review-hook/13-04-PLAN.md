---
phase: 13-native-enforcement-plan-review-hook
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - docs/decisions/0009-plan-review-gate.md
  - migrations/run-tests.sh
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "ADR-0009 carries a dated ## Correction section recording: decision 9 SUPERSEDED (HOOK-01's unconditional native block), decision 12 REVERSED (references Phase 12's existing inline Reversed marker, does NOT duplicate its content), and the factual correction that native hooks are project-scoped, not global (SC#5)."
    - "The Correction section's presence and required content are grep-asserted by a test, so a silent removal or a missing decision reference would go RED."
  artifacts:
    - path: "docs/decisions/0009-plan-review-gate.md"
      provides: "The dated Correction section closing DOC-03"
      contains: "## Correction"
    - path: "migrations/run-tests.sh"
      provides: "grep-assertion test pinning the Correction section content"
      contains: "0009"
  key_links:
    - from: "migrations/run-tests.sh DOC-03 assertion"
      to: "docs/decisions/0009-plan-review-gate.md ## Correction"
      via: "grep for the Correction heading + the three recorded items"
      pattern: "## Correction"
---

<objective>
Write ADR-0009's dated `## Correction` section (DOC-03, SC#5) and pin it with a
grep-assertion test.

Purpose: ADR-0009 lands its final correction here — DOC-03 was mapped to Phase 13
deliberately (Phase 12 already wrote the in-place `Reversed` marker on decision
12 to avoid two PRs racing the same file region). This section records three
things: decision 9 SUPERSEDED by HOOK-01's unconditional native block, decision
12 REVERSED (by REFERENCE to Phase 12's existing inline marker — never a
duplicate), and the factual correction that native hooks are project-scoped
(`<repo>/.codex/hooks.json`), falsifying decision 9's "global rather than
per-project" claim. RESEARCH.md supplies the exact suggested wording.

Output: the `## Correction` section in `docs/decisions/0009-plan-review-gate.md`
+ a grep-assertion test in `migrations/run-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md
@docs/decisions/0009-plan-review-gate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the dated ## Correction section in ADR-0009</name>
  <files>docs/decisions/0009-plan-review-gate.md</files>
  <read_first>
    - docs/decisions/0009-plan-review-gate.md — decision 9's "global rather than per-project" claim (the false premise), decision 12's EXISTING inline `Reversed (Phase 12, WR-03)` + `Extended (Phase 12 gap-closure, 12-04)` markers (which this section references, NOT duplicates), and the current end of the file (where the new top-level section lands)
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Global-vs-Project-Scoped: The Correction (DOC-03)" (the exact citation + suggested wording), Architectural Responsibility Map (in-place Correction, no new ADR number — REV-04)
  </read_first>
  <action>
    Append a new top-level `## Correction` section to `docs/decisions/0009-plan-review-gate.md`, dated `2026-07-17` (or execution date), stating for DOC-03:
    (1) Decision 9 is SUPERSEDED by HOOK-01 — the plan-review gate now blocks unconditionally on codex-cli's native `PreToolUse` surface, installed project-scoped, retiring decision 9's agent-mediated-only binding. Note this does NOT retroactively validate decision 9's rejection of option B at the time (the trust-ledger and self-scoping concerns it named were real — see Phase 13's spike findings); the specific factual premise "global, not per-project" is what no longer holds.
    (2) The factual correction: `<repo>/.codex/hooks.json` and `<repo>/.codex/config.toml` are documented, discovered, project-scoped layers (developers.openai.com/codex/hooks, .../config-advanced), loaded IN ADDITION to the global `~/.codex/*` layers, project entries winning on conflict — falsifying decision 9's "global rather than per-project" claim as of codex-cli 0.144.4. HOOK-03 uses exactly this project-scoped layer.
    (3) Decision 12 was already REVERSED by Phase 12 (WR-03's real guard) — record that reversal's existence for DOC-03's dated-section requirement, by REFERENCE to the existing inline `Reversed`/`Extended` markers on decision 12, WITHOUT repeating their content.
    Do NOT edit decision 11 (unrelated) and do NOT modify or duplicate the existing inline decision-12 markers. Follow RESEARCH.md's suggested wording as the source of truth for phrasing.
  </action>
  <acceptance_criteria>
    - `grep -c '^## Correction' docs/decisions/0009-plan-review-gate.md` == 1.
    - The section names all three: `grep` finds "decision 9" (or "d.9") with "supersed", "decision 12" (or "d.12") with "revers", and "project-scoped" / "not global" language falsifying the global claim.
    - The decision-12 mention is a REFERENCE to Phase 12's existing marker (the section does not re-explain the symlink-guard mechanics — grep the new section's own line span for absence of a second `_canon_dir`/`_is_contained` walkthrough).
    - The section is DATED (`grep -E '2026-07-(17|1[89]|2[0-9])' ` within the Correction section returns a match).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Grep-assertion test pinning the Correction section (DOC-03)</name>
  <files>migrations/run-tests.sh</files>
  <read_first>
    - migrations/run-tests.sh — the PASS/FAIL counter idiom, the existing `docs/decisions/0009-plan-review-gate.md` file-existence reference (~line 2008 / test_repo_layout ~3666) for placement precedent, and the flat `# Dispatcher` if-chain (~5327, `if [ -z "$FILTER" ] || [ "$FILTER" = "..." ]; then test_...; fi`)
    - docs/decisions/0009-plan-review-gate.md — the `## Correction` section this test pins
  </read_first>
  <action>
    Add a `test_adr_0009_correction()` function to `migrations/run-tests.sh` and register it in the flat `# Dispatcher` if-chain (~line 5327). It greps `docs/decisions/0009-plan-review-gate.md` and asserts, as separate PASS/FAIL counter assertions: (a) exactly one `^## Correction` heading; (b) the section records decision 9 superseded; (c) it records decision 12 reversed (by reference); (d) it records the "not global / project-scoped" factual correction; (e) it is dated. Each assertion follows the harness's existing grep-assert style (a PASS or FAIL line + counter bump) so a silent removal of any one item goes RED. Use portable grep (ubuntu + macOS).
  </action>
  <acceptance_criteria>
    - `bash migrations/run-tests.sh` runs `test_adr_0009_correction` with PASS lines and zero FAIL lines.
    - `grep -c 'test_adr_0009_correction' migrations/run-tests.sh` >= 2 (definition + dispatch registration).
    - The test carries at least 4 distinct content assertions (heading, d.9 superseded, d.12 reversed, global-claim corrected) — deleting any one from the ADR would flip that assertion RED.
    - Full suite exit status is 0: `bash migrations/run-tests.sh; echo "suite rc=$?"` prints `suite rc=0`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- The Correction section records all three DOC-03 items; the decision-12 item is a reference, not a duplicate of Phase 12's inline marker.
- The grep-assertion test would go RED if any of the three records were removed.
</verification>

<success_criteria>
DOC-03 met (SC#5): ADR-0009 carries a dated Correction section recording d.9
superseded, d.12 reversed (referenced), and the global-vs-project factual
correction — pinned by a mutation-detectable grep test.
</success_criteria>

<output>
Create `.planning/phases/13-native-enforcement-plan-review-hook/13-04-SUMMARY.md` when done.
</output>
