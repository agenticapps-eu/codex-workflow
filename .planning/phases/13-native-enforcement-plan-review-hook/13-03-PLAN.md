---
phase: 13-native-enforcement-plan-review-hook
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - migrations/0011-native-plan-review-hook.md
  - skills/agentic-apps-workflow/SKILL.md
  - .codex/workflow-version.txt
  - migrations/run-tests.sh
autonomous: true
requirements: [HOOK-03]
must_haves:
  truths:
    - "Migration 0011 merges ONE PreToolUse entry (pointing at the wrapper) into a project-scoped <repo>/.codex/hooks.json via leaf-level jq array-append — a pre-existing unrelated vendor's PreToolUse entries survive, and re-applying is a no-op (idempotent by command-string match)."
    - "Migration 0011 merges only [features] hooks=true into a project-scoped <repo>/.codex/config.toml (creating it if absent, preserving any other tables), never writing the operator's ~/.codex/* files. Whether this ENABLES the flag project-scoped is gated on 13-01-SPIKE-FINDINGS.md's A1 line: if A1 CONFIRMED, the write enables hooks project-scoped; if A1 FALSIFIED, Step 2 still writes the flag but the migration's Notes instruct the operator to enable hooks globally (RESEARCH.md's named A1 fallback), so the file-content fact holds either way."
    - "SC#4's negative half is automated: a second repo with no .codex/hooks.json carries no PreToolUse entry — asserted by absence."
    - "test_drift stays green: SKILL.md version, .codex/workflow-version.txt, and 0011's to_version all read 0.8.0 in lockstep."
  artifacts:
    - path: "migrations/0011-native-plan-review-hook.md"
      provides: "The forward migration installing the native project-scoped hook (HOOK-03)"
      contains: "PreToolUse"
    - path: "migrations/run-tests.sh"
      provides: "test_migration_0011 (merge-don't-clobber + idempotent re-apply + SC#4-negative)"
      contains: "test_migration_0011"
  key_links:
    - from: "migrations/0011-native-plan-review-hook.md hooks.json command"
      to: "skills/agentic-apps-workflow/scripts/hook-wrapper-plan-review.sh"
      via: "the PreToolUse entry's command string points at the wrapper"
      pattern: "hook-wrapper-plan-review.sh"
    - from: "migrations/0011-native-plan-review-hook.md to_version"
      to: "skills/agentic-apps-workflow/SKILL.md version + .codex/workflow-version.txt"
      via: "drift lockstep at 0.8.0"
      pattern: "0.8.0"
---

<objective>
Author migration 0011 (HOOK-03): install the `PreToolUse` entry into a
project-scoped `<repo>/.codex/hooks.json` (merge-don't-clobber) and enable the
hooks feature flag in a project-scoped `<repo>/.codex/config.toml`, then bump the
version records in lockstep so drift stays green — and mutation-prove the merge.

Purpose: HOOK-03's whole point is the PROJECT-scoped native layer (the layer
ADR-0009 d.9 wrongly believed did not exist), never the operator's global
`~/.codex/*`. The merge must be leaf-level jq array-append onto
`.hooks.PreToolUse` (this machine's own multi-vendor hooks.json proves shared
event arrays are real, not hypothetical), idempotent by the wrapper's command
string. `[features] hooks = true` merges into `<repo>/.codex/config.toml` — a
NEW file type for this repo's migrations — via an awk append-if-absent (no new
TOML dependency). The one-time interactive hook-trust is a documented operator
action (Pitfall 3), never a silent `trusted_hash` pre-seed.

Output: `migrations/0011-native-plan-review-hook.md`, version bumps to 0.8.0, and
`test_migration_0011` in `migrations/run-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md
@migrations/0008-plan-review-gate.md
@migrations/0000-baseline.md

<interfaces>
<!-- Conventions this migration must mirror, extracted from the codebase. -->

Migration file frontmatter (from 0008): id, slug, title, from_version, to_version, applies_to[], requires[], optional_for[]
  → 0011: from_version: 0.7.0, to_version: 0.8.0 (0.7.0 is the current fleet floor = migration 0009's to_version = .codex/workflow-version.txt).
  → applies_to MUST list `.codex/hooks.json` and `.codex/config.toml` WITH the leading dot (Pitfall 1: NOT `.planning/config.codex.json`).

Native hooks.json format (codex-cli): {"hooks": {"PreToolUse": [ {"matcher": "<per 13-01>", "type": "command", "command": "<wrapper path>", ...} ]}}
  (distinct from the DECLARATIVE .planning/config.codex.json shape — Pitfall 1).

Merge precedent (0000 Step 6): idempotency-checked append-if-absent to a shared file.
Leaf-merge precedent (0008 Step 1): jq leaf-level deep merge, NEVER shallow group replacement; rollback removes only our leaf.
Version-seal precedent (0008 Step 4): content steps first, .codex/workflow-version.txt sealed LAST.

test_drift (run-tests.sh:3561): FAILS unless skills/agentic-apps-workflow/SKILL.md `version:` == semver-max migration to_version AND == .codex/workflow-version.txt.
test_repo_layout (run-tests.sh:3626): a file-existence roster — add migrations/0011-*.md and the wrapper script to it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0011 + bump version records to 0.8.0 in lockstep</name>
  <files>migrations/0011-native-plan-review-hook.md, skills/agentic-apps-workflow/SKILL.md, .codex/workflow-version.txt</files>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md — the "Matcher decision" line (the exact matcher value the hooks.json entry must carry); if absent/unresolved, HALT and route to /gsd-plan-phase 13 --gaps.
    - migrations/0008-plan-review-gate.md — frontmatter shape, Step/Apply/Rollback/Idempotency prose structure, leaf-merge discipline, `.codex/workflow-version.txt` seal-last convention, the `CODEX="${CODEX_HOME:-$HOME/.codex}"` template-resolution idiom
    - migrations/0000-baseline.md — Step 6 merge-don't-clobber (idempotent append-if-absent) precedent
    - .planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md — the "A1 (project-scoped feature flag honored): CONFIRMED / FALSIFIED" line (governs Step 2's Notes: A1 CONFIRMED = the flag enables hooks project-scoped; A1 FALSIFIED = Step 2 writes the flag AND the Notes instruct the operator to enable hooks globally, RESEARCH.md's named fallback)
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Migration 0011 Design (HOOK-03)", "Common Pitfalls" 1 & 3, "Project-Scoped Config Layering" + Assumption A1
  </read_first>
  <action>
    Create `migrations/0011-native-plan-review-hook.md` with frontmatter `id: 0011`, `from_version: 0.7.0`, `to_version: 0.8.0`, and `applies_to: [.codex/hooks.json, .codex/config.toml]` (leading dots — Pitfall 1; NEVER `.planning/config.codex.json`).

    Pre-flight: git repo check; `jq` present (mirror 0008's hard requirement); version floor reads `.codex/workflow-version.txt` matching `^0\.(7|8)\.0$` (0.7.0 floor, 0.8.0 for re-apply — reuse 0008's `.codex/workflow-version.txt`-only floor pattern, NEVER a `skills/**` grep, the V-01 defect class); confirm the wrapper ships at `$CODEX/skills/agentic-apps-workflow/scripts/hook-wrapper-plan-review.sh` (a hooks.json pointing at a missing wrapper is a gate that silently never fires).

    Step 1 — merge the PreToolUse entry into `<repo>/.codex/hooks.json`. Idempotency check: the entry is absent (`jq -e` matching the wrapper's command string on `.hooks.PreToolUse[]`). Apply: create `{"hooks":{"PreToolUse":[]}}` if the file is absent; then `jq` array-append ONE entry `{matcher: "<the spike's Matcher decision value>", type: "command", command: "<wrapper path via ${CODEX_HOME:-$HOME/.codex}>"}` onto `.hooks.PreToolUse` using the leaf-level discipline (`.hooks.PreToolUse = ((.hooks.PreToolUse // []) + [$entry])`) — NEVER replace `.hooks` or `.hooks.PreToolUse` wholesale (a pre-existing vendor's entries must survive). Rollback: `jq del` only the entry whose command matches the wrapper, drop `.hooks.PreToolUse`/`.hooks` only if then empty.

    Step 2 — enable the hooks feature flag in `<repo>/.codex/config.toml` at PROJECT scope. Idempotency check: `[features]` with `hooks = true` already present. Apply: if the file is absent, create it with `[features]\nhooks = true`; if present, awk append-if-absent the `[features]` table / `hooks = true` key WITHOUT clobbering any other table (the append-if-absent marker-block idiom, mirroring AGENTS.md section insertion — do NOT introduce a TOML-parsing dependency). NEVER touch `~/.codex/config.toml` (Pitfall 3 — the operator's global file). Rollback: remove only the `hooks = true` line / the `[features]` table if it becomes empty.

    Step 3 — record `0.8.0` in `.codex/workflow-version.txt` (seal LAST, per 0008's convention).

    Notes section: document that the one-time interactive hook-trust (`/hooks` or the startup review prompt) is an EXPECTED operator action, NOT something the migration works around by pre-seeding `trusted_hash` (Pitfall 3); that this migration writes only `<repo>/.codex/*`, never `${CODEX_HOME}/config.toml`; and — gated on 13-01-SPIKE-FINDINGS.md's A1 line — if A1 was FALSIFIED (a project-scoped `[features] hooks=true` is NOT honored), add a Notes line instructing the operator to enable hooks globally (RESEARCH.md's named A1 fallback), since Step 2's project-scoped write alone would not activate the surface; if A1 was CONFIRMED, no such operator instruction is needed. If 13-01-SPIKE-FINDINGS.md lacks an A1 line, HALT and route to /gsd-plan-phase 13 --gaps.

    Then bump the two drift-coupled records to `0.8.0` in this same plan: `skills/agentic-apps-workflow/SKILL.md` `version:` → `0.8.0`, and this repo's own `.codex/workflow-version.txt` → `0.8.0` (both must equal 0011's `to_version` or test_drift goes RED — the 0008 lockstep precedent).
  </action>
  <acceptance_criteria>
    - `migrations/0011-native-plan-review-hook.md` exists; frontmatter `to_version: 0.8.0` and `applies_to` lists `.codex/hooks.json` and `.codex/config.toml` (grep both, with leading dots).
    - `grep -c 'config.codex.json' migrations/0011-native-plan-review-hook.md` == 0 (Pitfall 1 — the migration never touches the declarative advisory file).
    - `grep -c 'CODEX_HOME.*config.toml\|~/.codex/config.toml\|\$HOME/.codex/config.toml' migrations/0011-native-plan-review-hook.md` shows no Apply writing the operator's global config.toml (Pitfall 3); grep for `hook-wrapper-plan-review.sh` returns a match (Step 1 command points at the wrapper).
    - `grep -m1 '^version:' skills/agentic-apps-workflow/SKILL.md` == `version: 0.8.0`; `cat .codex/workflow-version.txt` == `0.8.0`.
    - Step 1's jq uses the `(. // []) + [$entry]` leaf-append form (grep), not a `.hooks.PreToolUse = [...]` wholesale assignment.
    - The migration's Notes reflect 13-01-SPIKE-FINDINGS.md's A1 line: if A1 FALSIFIED, a global-enable operator instruction is present; if A1 CONFIRMED, Step 2's project-scoped write is stated as sufficient (no unhedged "enables the flag" claim independent of the A1 finding).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_migration_0011 — merge-don't-clobber, idempotent re-apply, SC#4-negative; drift stays green</name>
  <files>migrations/run-tests.sh</files>
  <behavior>
    - Merge-don't-clobber: seed a fixture `.codex/hooks.json` carrying a PRE-EXISTING unrelated vendor's PreToolUse entry; after executing 0011's extracted Step 1 Apply, BOTH the vendor entry AND the plan-review wrapper entry are present in `.hooks.PreToolUse`.
    - Config flag: after Step 2 against a fixture with a pre-existing unrelated `[some_other]` table, `[features] hooks = true` is present AND the unrelated table survives.
    - Idempotent re-apply: running Step 1 + Step 2 a second time adds no duplicate entry / no duplicate flag (assert array length and flag count unchanged).
    - SC#4-negative: a second sandbox repo with NO `.codex/hooks.json` has no PreToolUse plan-review entry — asserted by absence.
  </behavior>
  <read_first>
    - migrations/run-tests.sh — `test_migration_0010` (lines ~5131+) as the structural template: `extract_step_block` + `assert_extracted_shape` gating, `mktemp -d` + `trap ... RETURN`, CODEX_HOME pinning, executing extracted Apply blocks against seeded sandboxes; `extract_preflight_block`; the flat `# Dispatcher` if-chain (~5327, `if [ -z "$FILTER" ] || [ "$FILTER" = "..." ]; then test_...; fi`) and `test_repo_layout` roster (~3630)
    - migrations/0011-native-plan-review-hook.md — the Step 1/2/3 Apply blocks this test extracts and executes (never hand-transcribed — 11-02's lesson)
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Validation Architecture" Requirements→Test Map (HOOK-03 + SC#4 rows), Wave 0 Gaps
  </read_first>
  <action>
    Add `test_migration_0011()` to `migrations/run-tests.sh`, register it in the flat `# Dispatcher` if-chain (~line 5327), and add `migrations/0011-native-plan-review-hook.md` + `skills/agentic-apps-workflow/scripts/hook-wrapper-plan-review.sh` to the `test_repo_layout` file roster. Skip gracefully if `jq` is unavailable (mirror test_migration_0010's SKIP idiom). Extract 0011's Pre-flight, applies_to, and each Step Apply via `extract_step_block`/`extract_preflight_block`, each gated by `assert_extracted_shape` (D-36: non-empty is not correct — assert a required substring per block) before execution. In a `mktemp -d` sandbox with `trap 'rm -rf "$tmp"' RETURN`, pin `CODEX_HOME` to a tree where the wrapper resolves: seed a `.codex/hooks.json` with a decoy vendor PreToolUse entry, execute Step 1's Apply, assert BOTH entries present (`jq` length == 2 and both commands found); seed a `.codex/config.toml` with a decoy `[some_other]` table, execute Step 2, assert `hooks = true` present AND the decoy table survives; re-run Step 1 + Step 2 and assert no duplication (array length and flag count unchanged — the idempotency direction that would regress silently if reverted). Add the SC#4-negative assertion: a second sandbox with no `.codex/hooks.json` yields no plan-review PreToolUse entry. Use portable idioms only (ubuntu + macOS CI).
  </action>
  <acceptance_criteria>
    - `bash migrations/run-tests.sh` runs `test_migration_0011` with PASS lines and zero FAIL lines for it.
    - The test proves merge-don't-clobber (decoy vendor entry survives), the config-flag merge (decoy `[some_other]` table survives), idempotent re-apply (no duplicate entry/flag), AND SC#4-negative (absence in a second repo).
    - Every extracted Apply block passes `assert_extracted_shape` before execution (no hand-transcription).
    - `test_drift` is GREEN in the same run (SKILL.md 0.8.0 == semver-max to_version 0.8.0 == .codex/workflow-version.txt 0.8.0).
    - Full suite exit status is 0: `bash migrations/run-tests.sh; echo "suite rc=$?"` prints `suite rc=0`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → `<repo>/.codex/*` | Project-scoped file writes; a merge that clobbers deletes another tool's config |
| migration → operator's `~/.codex/*` | MUST NOT cross — the operator's global trust ledger is out of the migration's authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | Step 1 hooks.json merge clobbering an existing vendor's PreToolUse array | mitigate | leaf-level jq array-append `(. // []) + [$entry]`; idempotent by wrapper command string; test_migration_0011 proves a decoy vendor entry survives |
| T-13-05 | Tampering | Step 2 config.toml merge clobbering an unrelated table | mitigate | awk append-if-absent scoped to `[features]`; decoy `[some_other]` table survival asserted |
| T-13-03 | Elevation of Privilege | pre-seeding trusted_hash / bypass-hook-trust as a silent install mechanism | mitigate | migration writes only `<repo>/.codex/*`, never `${CODEX_HOME}/config.toml`; one-time interactive trust documented in Notes as an expected operator action (Pitfall 3); grep-asserted absence of a global-config write |
| T-13-SC | Tampering | package installs | accept | No npm/pip/cargo installs; `jq`/`git` pre-existing (RESEARCH.md Environment Availability). No legitimacy gate required. |
</threat_model>

<verification>
- Migration touches only `<repo>/.codex/hooks.json`, `<repo>/.codex/config.toml`, `.codex/workflow-version.txt` — never `.planning/config.codex.json` (Pitfall 1), never `~/.codex/*` (Pitfall 3).
- Merge-don't-clobber + idempotent re-apply mutation-proven per 0008's own fixture discipline; Apply blocks extracted, never transcribed.
- Drift lockstep at 0.8.0 keeps test_drift green.
</verification>

<success_criteria>
HOOK-03 delivered: migration 0011 installs the project-scoped PreToolUse entry
(merge-don't-clobber) and enables the hooks feature flag project-scoped; SC#4's
negative half is automated; the version chain and drift test stay coherent at 0.8.0.
</success_criteria>

<output>
Create `.planning/phases/13-native-enforcement-plan-review-hook/13-03-SUMMARY.md` when done.
</output>
