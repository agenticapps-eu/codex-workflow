---
phase: 13-native-enforcement-plan-review-hook
plan: 05
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - .planning/phases/13-native-enforcement-plan-review-hook/13-05-LIVE-SESSION.md
autonomous: false
requirements: [HOOK-01]
must_haves:
  truths:
    - "A disallowed edit, driven through the REAL Codex CLI tool surface in a live human-observed session, is observably prevented end-to-end — not merely a script-level unit test passing (SC#2)."
    - "The binding is verified FIRING in the target repo (SC#4 positive half, folded into the same live session) AND its absence in an unrelated repo is confirmed (SC#4 negative already automated in 13-03)."
    - "The stale project-trust path (Pitfall 4) is verified/fixed BEFORE the session, so an unrelated untrusted-project prompt cannot be misdiagnosed as a hook-install failure."
  artifacts:
    - path: ".planning/phases/13-native-enforcement-plan-review-hook/13-05-LIVE-SESSION.md"
      provides: "Dated operator evidence that the native block fired end-to-end"
      contains: "BLOCKED"
  key_links:
    - from: "the live codex session's disallowed edit"
      to: "the installed PreToolUse wrapper → check-plan-review.sh"
      via: "codex-cli native tool-dispatch interception"
      pattern: "permissionDecision"
---

<objective>
Prove HOOK-01's end-to-end block (SC#2) in a live, human-observed Codex CLI
session, and capture SC#4's positive half (binding fires in the target repo) in
the same session.

Purpose: SC#2 is irreducibly manual — no bash harness can drive an actual
interactive codex session's approval/edit UI. This plan installs migration 0011's
output into a live target repo, has the operator attempt a disallowed edit through
the real tool surface, and observes the native `PreToolUse` wrapper deny it
end-to-end. Per RESEARCH.md, SC#4's positive half is folded into this same
session rather than built as a second automation path. Pitfall 4 (this repo's
stale `[projects.<path>]` trust entry) is fixed as a pre-step so an unrelated
trust prompt cannot masquerade as a hook failure.

Output: `.planning/phases/13-native-enforcement-plan-review-hook/13-05-LIVE-SESSION.md`
— dated operator evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md
@migrations/0011-native-plan-review-hook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — fix stale project-trust path, install 0011 into a live target, stage the disallowed edit</name>
  <files>.planning/phases/13-native-enforcement-plan-review-hook/13-05-LIVE-SESSION.md</files>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Pitfall 4" (stale project-trust path), "Migration 0011 Design" (SC#4 firing-half guidance), Spike Protocol step 1
    - .planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md — the "Environment + Pitfall 4" finding (whether this repo's trust entry is stale) and the default-trust-level answer (does firing require a one-time trust step)
    - migrations/0011-native-plan-review-hook.md — the Apply steps to run against the target repo, and the Notes' one-time-trust operator action
  </read_first>
  <action>
    Prepare the live-session environment and open `13-05-LIVE-SESSION.md` to record it. (1) Pitfall 4 pre-step: run `codex doctor`, confirm the resolved repo root, and check `~/.codex/config.toml` for a `[projects."<current-abs-path>"]` entry; if it is keyed to the stale `/Users/donald/Sourcecode/codex-workflow` path (per the spike finding), re-trust the CURRENT path so `.codex/` loads — record what was done. (2) Apply migration 0011's Steps into a live target (this repo's own `.codex/`, or a fresh clone, per operator preference recorded in the session note): confirm `<repo>/.codex/hooks.json` now carries the plan-review PreToolUse entry pointing at the installed wrapper, and `<repo>/.codex/config.toml` has `[features] hooks = true`. (3) Stage a DISALLOWED edit target: identify (or create) a repo state where the plan-review gate would block — an ordinary file edit that is NOT a plan/review artifact while a phase with `*-PLAN.md` and no valid `*-REVIEWS.md` is active, so `check-plan-review.sh` resolves to a block (per its resolver; a disallowed edit is by definition not a plan/review file). Record the staged conditions. Do NOT drive the interactive codex session here — that is Task 2.
  </action>
  <acceptance_criteria>
    - `13-05-LIVE-SESSION.md` exists and records: the Pitfall-4 check result and any re-trust action; confirmation that `<repo>/.codex/hooks.json` contains the plan-review wrapper PreToolUse entry (`grep` the wrapper path) and `<repo>/.codex/config.toml` contains `hooks = true`.
    - The staged disallowed-edit conditions are recorded (which file, why the gate resolves to a block for it).
    - No write to `~/.codex/hooks.json` beyond the operator's own trust action; the migration wrote only `<repo>/.codex/*`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live human-observed end-to-end block (SC#2 + SC#4-positive)</name>
  <what-built>Task 1 fixed the stale project-trust path, installed migration 0011's native PreToolUse binding into the target repo (wrapper + hooks feature flag), and staged a disallowed-edit condition the plan-review gate resolves to a block. This task is the irreducibly-manual proof that the native block fires through the real Codex CLI — no bash harness can drive codex's interactive tool-approval UI.</what-built>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-05-LIVE-SESSION.md — the staged conditions from Task 1
    - .planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md — the apply_patch coverage answer (which tool codex uses for the edit) and whether the operator SEES the block reason (stderr surfacing)
  </read_first>
  <manual_verification>
    Operator runs, recording each result into `13-05-LIVE-SESSION.md`:
    1. FIRST verify the one-time hook-trust: on first `codex` invocation in the target repo, complete the `/hooks` or startup "Trust all and continue" prompt for the new plan-review hook (the expected operator action documented in 0011's Notes — NOT pre-seeded). Record that the hook is now trusted.
    2. SC#2 — attempt the staged DISALLOWED edit through the real Codex CLI tool surface (the same tool codex naturally chooses for a file edit, per the spike's apply_patch finding). OBSERVE: the edit is denied end-to-end by the native `PreToolUse` wrapper (the gate's block reason, surfaced per the spike's stderr finding). Record the observed denial verbatim — this is the pass condition (an edit that reaches the filesystem is a FAIL).
    3. SC#4-positive — confirm this proves the binding FIRES in the target repo (same observation). Cross-reference that SC#4-negative (absence in an unrelated repo) is already automated in test_migration_0011.
    4. CONTROL — confirm the block is the plan-review gate specifically (the recorded reason names the plan-review gate / missing REVIEWS.md), not an unrelated untrusted-project refusal (Pitfall 4 warning sign).
    RESUME when the session note records an observed end-to-end denial of the disallowed edit with the gate's own reason.
  </manual_verification>
  <resume-signal>Type "block observed" once 13-05-LIVE-SESSION.md records the native PreToolUse denial of the disallowed edit through the real Codex CLI, or describe what was observed instead (e.g. the edit went through — a FAIL to route back to planning).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → codex-cli trust ledger | The one-time hook-trust is a human decision point the migration must not silently bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Repudiation | a "successful" session where the block only fired for Bash-tool edits, never a real file-write tool call (Pitfall 2) | mitigate | the disallowed edit is driven through the tool codex NATURALLY chooses for a file edit (per the spike's apply_patch coverage finding); the control step confirms the reason names the plan-review gate specifically |
| T-13-07 | Spoofing | an unrelated untrusted-project prompt misdiagnosed as the hook firing/failing (Pitfall 4) | mitigate | Pitfall-4 pre-step re-trusts the current path before the session; the control step distinguishes a gate block from a project-trust refusal |
| T-13-SC | Tampering | package installs | accept | No package installs in this plan. No legitimacy gate required. |
</threat_model>

<verification>
- SC#2: a disallowed edit through the real Codex CLI is observably denied end-to-end, recorded with the gate's own reason.
- SC#4-positive: the same observation confirms firing in the target repo; SC#4-negative is automated in 13-03.
- Pitfall 4 handled as a pre-step; the control step rules out a false positive from an unrelated trust prompt.
</verification>

<success_criteria>
HOOK-01 met (SC#2 + SC#4-positive): the native PreToolUse plan-review block is
observed preventing a disallowed edit end-to-end in a live human-observed Codex
CLI session in the target repo, with the stale-trust-path pitfall pre-empted.
</success_criteria>

<output>
Create `.planning/phases/13-native-enforcement-plan-review-hook/13-05-SUMMARY.md` when done.
</output>
