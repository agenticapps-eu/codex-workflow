---
phase: 13-native-enforcement-plan-review-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md
autonomous: false
requirements: [HOOK-01]
must_haves:
  truths:
    - "The two trust-ledger gaps (exact trusted_hash input; whether project-trust and per-hook trust are one operator action or two) are resolved by direct observation, not doc inference, and recorded before any wrapper/migration design is finalized (SC#1)."
    - "Whether codex-cli 0.144.4 fires PreToolUse for apply_patch file edits is settled empirically — with the exact tool_input payload shape captured — resolving the official-vs-third-party contradiction that HOOK-01/HOOK-02's matcher depends on."
    - "The default trust_level for a project with no [projects.<path>] entry is observed (prompt / silent-trusted / silent-untrusted), so HOOK-03's 'verified firing in target repo' criterion is stated accurately, not assumed."
    - "Assumption A1 is tested: a scratch repo with ONLY a project-scoped [features] hooks=true (global forced off) is observed via `codex features list` to report hooks enabled-via-the-project-layer or not — recorded as an explicit A1 CONFIRMED/FALSIFIED line that 13-03 reads."
  artifacts:
    - path: ".planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md"
      provides: "Frozen answers to the 4 SPIKE-REQUIRED questions + the apply_patch matcher decision that downstream plans read"
      contains: "matcher decision"
  key_links:
    - from: "13-02-PLAN.md wrapper matcher/parse logic"
      to: "13-01-SPIKE-FINDINGS.md apply_patch payload + matcher decision"
      via: "executor reads findings before writing matcher"
      pattern: "matcher decision"
    - from: "13-03-PLAN.md migration 0011 hooks.json matcher field"
      to: "13-01-SPIKE-FINDINGS.md matcher decision"
      via: "migration writes the matcher the spike selected"
      pattern: "matcher decision"
---

<objective>
Run RESEARCH.md's 8-step Spike Protocol to freeze the trust-ledger and
apply_patch unknowns BEFORE the wrapper (13-02) and migration (13-03) designs
finalize. This is Success Criterion 1 — its acceptance is OBSERVATIONS
RECORDED, not code shipped.

Purpose: The single highest-leverage unknown in the phase is a direct source
contradiction — official docs say PreToolUse covers apply_patch file edits, one
third-party source says it does not. HOOK-01's entire premise (a disallowed
*edit* is blocked) depends on the answer. The spike settles it, plus the exact
trusted_hash input, the default trust_level for an unlisted project, and whether
hook stderr reaches the operator.

Output: `.planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md`
— a short spike-note (not a RESEARCH.md rewrite) answering the 4 SPIKE-REQUIRED
questions and stating the matcher decision downstream plans consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot environment, scaffold scratch fixtures, resolve Pitfall 4</name>
  <files>.planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Spike Protocol" steps 1-2, "Pitfall 4", "codex-cli Version and Environment" table
  </read_first>
  <action>
    Execute Spike Protocol steps 1-2 without any live `codex exec` invocation (cheap, no quota cost).
    (1) Capture current environment: `codex --version`, `codex features list` (confirm `hooks` stable/true), and the exact `[projects."..."]` and `[hooks.state."..."]` entry shapes from `~/.codex/config.toml` — save a pre-spike copy of `~/.codex/config.toml` to the scratch dir for the later diff. DO NOT modify `~/.codex/hooks.json` — it carries three live vendors' hooks.
    (2) Resolve Pitfall 4 first: run `codex doctor`, record the resolved repo root, and `grep` `~/.codex/config.toml` for a `[projects."<current-abs-path>"]` entry. Record whether this repo's trust entry is keyed to the STALE `/Users/donald/Sourcecode/codex-workflow` path vs the current `/Users/donald/Sourcecode/agenticapps/codex-workflow` path. Do NOT re-trust here — just record the finding for 13-05's pre-step.
    (3) Scaffold two SCRATCH git repos under a temp dir (NOT this repo, NOT `~/.codex/hooks.json`): `spike-repo` with a `.codex/hooks.json` carrying ONE trivial PreToolUse entry whose command logs and passes through (`echo "$(date) fired" >> <scratch>/spike-repo/.codex/fired.log; cat`), and `spike-repo-2` with the SAME hooks.json content but where NO trust action will be taken.
    Open `13-01-SPIKE-FINDINGS.md`, write the header and an "Environment + Pitfall 4" section recording steps 1-2's observations verbatim, and leave clearly-labelled empty sections for the interactive observations (Task 2) and the resolved answers (Task 3).
  </action>
  <acceptance_criteria>
    - `13-01-SPIKE-FINDINGS.md` exists and contains a filled "Environment + Pitfall 4" section stating the codex version, that `hooks` is enabled, and the exact keyed path of this repo's `[projects.*]` entry (stale-vs-current explicitly named).
    - Two scratch repos exist under the temp dir, each with a `.codex/hooks.json` containing one PreToolUse entry; a pre-spike copy of `~/.codex/config.toml` is saved for the Task 2 diff.
    - `~/.codex/hooks.json` is byte-unchanged from before this task (verify: no write to it).
    - grep `13-01-SPIKE-FINDINGS.md` for "Pitfall 4" returns a match.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live interactive protocol (steps 3-7) — human-observed</name>
  <what-built>Task 1 scaffolded two scratch repos, a trivial logging PreToolUse hook, and a saved pre-trust copy of `~/.codex/config.toml`. This task is the irreducibly-interactive core of the spike: it cannot be driven by the bash harness because it observes codex-cli's own trust-approval UI and tool-dispatch behavior.</what-built>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Spike Protocol" steps 3-7, "What is SPIKE-REQUIRED", "PreToolUse stdin JSON Schema", "Exit-Code and JSON Contract"
    - 13-01-SPIKE-FINDINGS.md — the empty interactive-observations section Task 1 left
  </read_first>
  <manual_verification>
    Operator runs, in order, recording each observation into `13-01-SPIKE-FINDINGS.md`:
    1. STEP 3 (default trust): run a trivial `codex` command inside `spike-repo` BEFORE adding any `[projects."<spike-repo>"]` entry. Observe and record: was there an interactive trust prompt? Did the hook fire on first run, or only after an explicit trust action? (Resolves SPIKE-REQUIRED item 3 — default trust_level.)
    2. STEP 4 (trust flow + ledger diff): trust the hook via `/hooks` in an interactive session (or the startup "1 hook is new or changed... Trust all and continue" prompt). Immediately `diff` the saved pre-trust `~/.codex/config.toml` against the post-trust version. Record: the exact `[hooks.state.<key>]` entry format for this hook, AND whether a `[projects."<spike-repo>"]` entry was written at the SAME time (one operator action sets both gates, or two distinct actions?). (Resolves SPIKE-REQUIRED item 2.)
    3. STEP 6 (untrusted-project): in `spike-repo-2` with NO trust action, confirm the hook does NOT fire and record whether codex reports WHY (untrusted project vs untrusted hook — distinguishing Gate A from Gate B).
    4. STEP 7 (apply_patch coverage — THE decisive observation): point the hook's command at `cat >> <scratch>/spike-repo/.codex/payload.log`, then in a human-observed `codex` session perform ONE file-creating edit via whatever tool codex naturally chooses. Inspect the captured payload: record the exact `tool_name` and the full `tool_input` shape (is the patch text under `tool_input.input`, `tool_input.patch`, or elsewhere? does it carry a parseable `*** Update File:`/`*** Add File:` path?). Then change the hook to actually deny and re-run: record whether the operator SEES the block reason text or only a generic denial (resolves the stderr-surfacing question).
    5. A1 (project-scoped feature-flag honored — RESEARCH.md Assumption A1): in a trusted scratch repo, set ONLY a project-scoped `[features] hooks = true` in its `.codex/config.toml` while forcing the machine-wide default OFF for the check (e.g. `codex -c features.hooks=false features list` run from inside the repo, or an equivalent the spike discovers). Record whether `hooks` reports ENABLED via the PROJECT layer specifically — distinct from this machine's already-on global default. This resolves whether HOOK-03's Step 2 (`[features] hooks=true` in `<repo>/.codex/config.toml`) actually enables the flag, or only writes an ignored file.
    RESUME when every observation block in `13-01-SPIKE-FINDINGS.md` (including A1) is filled with real, dated observations.
  </manual_verification>
  <resume-signal>Type "observations recorded" once every interactive step's result is written into 13-01-SPIKE-FINDINGS.md, or describe what could not be observed and why.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Compute trusted_hash offline, freeze the resolved findings + matcher decision</name>
  <files>.planning/phases/13-native-enforcement-plan-review-hook/13-01-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/13-native-enforcement-plan-review-hook/13-RESEARCH.md — "Spike Protocol" step 5 + 8, "Assumptions Log" (A1-A4), "Project-Scoped Config Layering" (A1), Open Questions 1-2
    - 13-01-SPIKE-FINDINGS.md — the interactive observations recorded in Task 2
  </read_first>
  <action>
    Execute Spike Protocol step 5: with the trusted entry's exact hook JSON in hand from `spike-repo/.codex/hooks.json`, compute `sha256` over (a) the `command` string alone, (b) the whole hook object as compact JSON, (c) the whole hook object as normalized TOML, and record which matches the observed `trusted_hash`. Then freeze `13-01-SPIKE-FINDINGS.md`'s "Resolved Answers" section with explicit, one-line-each answers to: SPIKE item 1 (exact hash input), item 2 (pre-seeding viability + one-gate-or-two), item 3 (default trust_level), item 4 (apply_patch coverage + tool_input field name). Also freeze an explicit **"A1 (project-scoped feature flag honored): CONFIRMED / FALSIFIED"** line from Task 2 step 5's observation — 13-03 reads it to decide whether migration 0011 enables the flag project-scoped or must fall back to instructing the operator to enable hooks globally. Add a bold **"Matcher decision"** line stating exactly what `matcher` value(s) 13-03's hooks.json entry must carry and whether 13-02's wrapper needs a `Bash`-command-parsing branch in addition to the apply_patch path (per Open Question 1: do NOT recommend the Bash branch unless step 7 proved apply_patch is NOT covered). Do NOT pre-seed any `trusted_hash` into `~/.codex/config.toml` — the migration must never write the operator's global file (Pitfall 3); record that the one-time interactive trust is an operator action for 13-05.
  </action>
  <acceptance_criteria>
    - `13-01-SPIKE-FINDINGS.md` "Resolved Answers" section answers all four SPIKE-REQUIRED items with a concrete observed value each (not "unknown" / "TBD").
    - grep `13-01-SPIKE-FINDINGS.md` for "Matcher decision" returns exactly one bold decision line naming the matcher value(s) and stating yes/no on the wrapper's Bash-parse branch.
    - The findings explicitly record that no `trusted_hash` was written into `~/.codex/config.toml` (Pitfall 3 honored).
    - grep `13-01-SPIKE-FINDINGS.md` for "sha256" returns the hash-input determination (which of a/b/c matched).
    - grep `13-01-SPIKE-FINDINGS.md` for "A1 (project-scoped feature flag honored)" returns exactly one line ending CONFIRMED or FALSIFIED.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- 13-01-SPIKE-FINDINGS.md answers SC#1's two named gaps plus the apply_patch contradiction, dated, before 13-02/13-03 execute.
- `~/.codex/hooks.json` and (except the operator's own one-time trust action in Task 2) `~/.codex/config.toml` are not mutated by any automated task; no `trusted_hash` pre-seed.
- Scratch repos, never this machine's real multi-vendor hooks file, carried every test hook.
</verification>

<success_criteria>
SC#1 met: the two trust-ledger gaps and the apply_patch coverage question are
resolved by direct observation and frozen in 13-01-SPIKE-FINDINGS.md, with an
explicit Matcher decision line that 13-02 and 13-03 read before finalizing their
matcher/parse logic.
</success_criteria>

<output>
Create `.planning/phases/13-native-enforcement-plan-review-hook/13-01-SUMMARY.md` when done.
</output>
