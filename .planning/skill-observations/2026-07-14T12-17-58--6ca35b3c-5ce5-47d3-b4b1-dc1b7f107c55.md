## Workflow commitment

I am using the agentic-apps-workflow skill for this task.

**Task scope:** Implement the core spec §02 `plan-review` pre-execution gate in codex-workflow — bound skill, programmatic gate with the spec's resolver order + grandfather rule, bindings-table row, and migration — so the host can earn an `implements_spec: 0.5.0` claim.

**Task size:** large (4+ hours)

Sizing rationale: this is not a mechanical port. claude-workflow's `multi-ai-review-gate.sh` slots into an existing `templates/.claude/hooks/` + `settings.json` PreToolUse array. codex-workflow has **no hooks infrastructure at all** under `.codex/`, and there is no Codex analogue of `/gsd-review`. The enforcement mechanism is a genuine open architectural question, so this needs discussion before planning.

**Skills I will invoke, in order:**
1. `superpowers:brainstorming` — the crux is unresolved: how do you enforce a pre-execution gate on a host with no hook runtime? Needs ≥2 written alternatives (native Codex hook surface if one exists in 0.130.0+ / ritual-text-only binding per ADR-0007's "thin binding" stance / wrapper script). This gates everything downstream.
2. `/gsd-discuss-phase 08` — CONTEXT.md missing, architecture decision pending.
3. `/gsd-plan-phase 08` + `superpowers:writing-plans` — no UI hint; new-mechanism work, so ≥2 alternatives recorded in RESEARCH.md.
4. `/gsd-review` — multi-AI plan review → `08-REVIEWS.md`. Pointed, given I'm implementing this very gate; skipping it would be red flag #8 while building the thing that catches red flag #8.
5. `/gsd-execute-phase 08` — `superpowers:test-driven-development` for the `run-tests.sh` guards (resolver order, grandfather rule, migration idempotency): failing test first, RED commit → GREEN commit.
6. `superpowers:verification-before-completion` — before any completion claim.
7. `superpowers:finishing-a-development-branch` — PR composition.

**Post-phase gates:** `/review` (Stage 1, spec compliance) → REVIEW.md; `superpowers:requesting-code-review` (Stage 2, independent reviewer) → separate Stage 2 section, not collapsed; `/cso` → SECURITY.md — the bound skill spawns external AI reviewers with plan/diff content, which is an LLM trust boundary (and per spec/02 at 0.6.0, that path ties directly into the §14 question I flagged earlier). `/qa` is N/A — CLI scaffolder, no dev server.

**Verification evidence I will produce:**
- `migrations/run-tests.sh` full PASS/FAIL/SKIP output including new `test_migration_0008` + resolver/grandfather guards
- grep showing the `plan-review` row present in the `AGENTS.md` bindings table (and the `tdd` duplicate resolved, so the table reads 16 distinct gates)
- grep showing gate resolver implements the spec's four-step order and the `*-SUMMARY.md` grandfather exit
- migration idempotency: second run is a no-op
- drift/version parity check between trigger SKILL.md and `.codex/workflow-version.txt`
- ADR at `docs/decisions/0009-*.md` recording the enforcement-mechanism decision + rejected alternatives
- feature branch + PR to main (never committing to main directly)

Once I have stated this plan, I am committed to it. Deviating without explicit user approval is a protocol violation.

---

Two things I want to flag before I start brainstorming, because they may change scope:

**The `implements_spec` bump is not mine to make unilaterally.** Landing §02 gets you the 0.5.0 *content*, but the field "tracks the last full-conformance audit" per `CHANGELOG.md:88-91`. Claiming 0.5.0 means auditing everything 0.4.0→0.5.0, not just this gate. I'll land the gate and leave the claim bump as an explicit follow-up unless you want the audit in scope too.

**Migration 0008 is the number I'll take** — the chain currently stops at 0007, and this reserves it ahead of any §14 work (which my earlier audit suggested would also want 0008 if it mirrors claude's 0023).

Starting with brainstorming now — I'll come back with the enforcement-mechanism alternatives before writing any code.

Base directory for this skill: /Users/donald/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>
